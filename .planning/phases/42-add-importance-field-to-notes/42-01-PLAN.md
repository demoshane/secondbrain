---
phase: 42-add-importance-field-to-notes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/db.py
  - engine/capture.py
  - engine/mcp_server.py
  - engine/typeclassifier.py
autonomous: true
execution_strategy: direct
requirements: [IMP-01, IMP-02, IMP-03]
must_haves:
  truths:
    - "DB column importance exists on notes table with default 'medium'"
    - "capture_note() accepts importance kwarg and writes it to frontmatter and DB"
    - "sb_capture, sb_capture_batch, sb_edit MCP tools accept importance param"
    - "sb_capture_smart infers importance from content via keyword heuristics"
  artifacts:
    - path: "engine/db.py"
      provides: "migrate_add_importance_column() + init_schema call"
      contains: "migrate_add_importance_column"
    - path: "engine/capture.py"
      provides: "importance in build_post, write_note_atomic, capture_note"
      contains: "importance"
    - path: "engine/mcp_server.py"
      provides: "importance param on sb_capture, sb_capture_batch, sb_edit, sb_capture_smart"
      contains: "importance"
  key_links:
    - from: "engine/capture.py:build_post"
      to: "engine/capture.py:write_note_atomic"
      via: "post['importance'] → INSERT column"
      pattern: "importance"
    - from: "engine/mcp_server.py:sb_capture"
      to: "engine/capture.py:capture_note"
      via: "importance kwarg passthrough"
      pattern: "importance=importance"
---

<objective>
Add the importance field to the data layer: DB migration, capture pipeline (build_post, write_note_atomic, capture_note), and all MCP tools (sb_capture, sb_capture_batch, sb_edit, sb_capture_smart).

Purpose: Foundation for importance across the entire stack. Every note gets an importance value persisted to both frontmatter and DB.
Output: All backend capture paths write importance; MCP tools accept it; smart capture infers it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-add-importance-field-to-notes/42-CONTEXT.md
@.planning/phases/42-add-importance-field-to-notes/42-RESEARCH.md
@.claude/LEARNINGS.md

<interfaces>
<!-- From engine/db.py — migration pattern to copy -->
def migrate_add_status_column(conn: sqlite3.Connection) -> None:
    cols = {row[1] for row in conn.execute("PRAGMA table_info(notes)").fetchall()}
    if "status" not in cols:
        conn.execute("ALTER TABLE notes ADD COLUMN status TEXT NOT NULL DEFAULT 'active'")
        conn.commit()

<!-- From engine/capture.py — current signatures -->
def build_post(note_type, title, body, tags, people, content_sensitivity="public") -> frontmatter.Post
def write_note_atomic(target, post, conn, update=False, *, url=None) -> None
def capture_note(note_type, title, body, tags, people, content_sensitivity, brain_root, conn, *, url=None, source_type=None) -> Path

<!-- From engine/mcp_server.py — current MCP tool signatures -->
def sb_capture(title, body, note_type="note", tags=None, sensitivity="public", confirm_token="") -> dict
def sb_capture_batch(notes: list[dict]) -> dict  # each dict: title, body, note_type, tags, people, sensitivity
def sb_edit(path: str, body: str) -> dict
def sb_capture_smart(content: str) -> dict

<!-- write_note_atomic INSERT column list (line 200-203) -->
INSERT INTO notes (path, type, title, body, tags, people, created_at, updated_at, sensitivity, url, deadline, meeting_date) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DB migration + capture pipeline (build_post, write_note_atomic, capture_note)</name>
  <files>engine/db.py, engine/capture.py, tests/test_capture.py</files>
  <read_first>engine/db.py, engine/capture.py, tests/test_capture.py</read_first>
  <behavior>
    - migrate_add_importance_column() called twice on same DB does not raise (idempotent)
    - After init_schema(), PRAGMA table_info(notes) includes 'importance' column
    - build_post(note_type="note", title="t", body="b", tags=[], people=[], importance="high") produces post["importance"] == "high"
    - build_post() with no importance arg produces post["importance"] == "medium"
    - After capture_note(..., importance="high"), SELECT importance FROM notes WHERE path=? returns "high"
    - After capture_note() with default importance, SELECT importance FROM notes returns "medium"
    - write_note_atomic with update=True preserves importance from post object
  </behavior>
  <action>
    1. **engine/db.py** — Add `migrate_add_importance_column()` after `migrate_add_status_column()` (line ~517):
       ```python
       def migrate_add_importance_column(conn: sqlite3.Connection) -> None:
           """Idempotent: add 'importance' TEXT column to notes if absent."""
           cols = {row[1] for row in conn.execute("PRAGMA table_info(notes)").fetchall()}
           if "importance" not in cols:
               conn.execute("ALTER TABLE notes ADD COLUMN importance TEXT NOT NULL DEFAULT 'medium'")
               conn.commit()
       ```
       Add call in `init_schema()` after `migrate_create_person_insights(conn)` and before `_migrate_fk_cascade(conn)` (line ~683).

    2. **engine/capture.py** — `build_post()`:
       - Add `importance: str = "medium"` parameter after `content_sensitivity` (per D-02, D-05)
       - Add `post["importance"] = importance` after `post["content_sensitivity"] = content_sensitivity` (line ~123)

    3. **engine/capture.py** — `write_note_atomic()`:
       - After line 189 (`meeting_date = post.get("meeting_date") or None`), add:
         `importance = post.get("importance", "medium")`
       - Update the INSERT column list (line ~202) to add `importance` after `meeting_date`:
         `"(path, type, title, body, tags, people, created_at, updated_at, sensitivity, url, deadline, meeting_date, importance)"`
       - Add `importance` to VALUES tuple (13th param): add `importance,` after `meeting_date,`
       - Update placeholder count from 12 to 13: `" VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)"`

    4. **engine/capture.py** — `capture_note()`:
       - Add keyword-only param: `importance: str = "medium"` after `source_type` (per D-07)
       - Thread it to `build_post()` call (line ~520): `post = build_post(note_type, title, body, tags, merged_people, content_sensitivity, importance=importance)`

    5. **tests/test_capture.py** — Add tests:
       - `test_build_post_importance_default`: call build_post without importance, assert post["importance"] == "medium"
       - `test_build_post_importance_high`: call build_post(importance="high"), assert post["importance"] == "high"
       - `test_capture_note_importance_in_db`: capture with importance="high", SELECT importance FROM notes, assert == "high"
       - `test_importance_migration_idempotent`: call migrate_add_importance_column twice, no error
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_capture.py -k importance -x -q</automated>
  </verify>
  <acceptance_criteria>
    - engine/db.py contains `def migrate_add_importance_column(`
    - engine/db.py init_schema() contains `migrate_add_importance_column(conn)`
    - engine/capture.py build_post has `importance: str = "medium"` parameter
    - engine/capture.py build_post contains `post["importance"] = importance`
    - engine/capture.py write_note_atomic INSERT contains `importance` in column list
    - engine/capture.py capture_note has `importance: str = "medium"` keyword param
    - tests/test_capture.py contains `test_build_post_importance_default`
    - `uv run pytest tests/test_capture.py -k importance -x -q` exits 0
  </acceptance_criteria>
  <done>DB migration creates importance column; build_post, write_note_atomic, and capture_note all thread importance to both frontmatter and DB; all new tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MCP tools — sb_capture, sb_capture_batch, sb_edit, sb_capture_smart importance</name>
  <files>engine/mcp_server.py, engine/typeclassifier.py, tests/test_mcp.py</files>
  <read_first>engine/mcp_server.py, engine/typeclassifier.py, tests/test_mcp.py</read_first>
  <behavior>
    - sb_capture(title="t", body="b", importance="high") creates note with importance=high in DB
    - sb_capture(title="t", body="b") creates note with importance=medium (default)
    - sb_capture_batch([{"title":"t","body":"b","importance":"high"}]) writes importance=high
    - sb_edit(path, body, importance="low") updates importance in DB and frontmatter
    - sb_edit(path, body) without importance does NOT change existing importance
    - sb_capture_smart("URGENT: prod is down") infers importance=high
    - sb_capture_smart("random shower thought") infers importance=medium
    - classify_importance("URGENT fix needed") returns "high"
    - classify_importance("routine update") returns "medium"
  </behavior>
  <action>
    1. **engine/typeclassifier.py** — Add `classify_importance(title: str, body: str) -> str` keyword heuristics function (per D-08):
       - HIGH patterns (case-insensitive): urgent, critical, p0, p1, emergency, incident, outage, breaking, blocker, showstopper, asap, immediately, time-sensitive, escalat
       - LOW patterns: fyi, note-to-self, low-priority, someday, maybe, random, shower thought, idle, trivial, minor
       - Default: "medium"
       - Match against combined `(title + " " + body).lower()`
       - Use compiled regex patterns at module level (consistent with existing _URL_PAT, _PRODUCT_WORDS style)

    2. **engine/mcp_server.py** — `sb_capture()` (line ~194):
       - Add param: `importance: str = "medium"` after `sensitivity`
       - Validate: `if importance not in ("low", "medium", "high"): raise ValueError("importance must be low, medium, or high")`
       - Pass to capture_note: `importance=importance`

    3. **engine/mcp_server.py** — `sb_capture_batch()` (line ~279):
       - In the per-note loop (line ~315), extract: `importance = note.get("importance", "medium")`
       - Pass to `_capture_note()` call (line ~341): `importance=importance`

    4. **engine/mcp_server.py** — `sb_edit()` (line ~549):
       - Change signature to: `def sb_edit(path: str, body: str, importance: str | None = None) -> dict`
       - After loading the post (line ~558), if importance is not None:
         - Validate: `if importance not in ("low", "medium", "high"): raise ValueError(...)`
         - Set: `post["importance"] = importance`
       - The existing `write_note_atomic(p, post, conn, update=True)` call will persist it (Task 1 added importance to INSERT)

    5. **engine/mcp_server.py** — `sb_capture_smart()` (line ~819):
       - After type classification (around line ~902 where `note_type = seg["type"]`), add:
         ```python
         from engine.typeclassifier import classify_importance as _classify_importance
         importance = _classify_importance(title, body)
         ```
       - Importance inference uses keyword heuristics via `classify_importance` (per D-08 — no LLM prompt; rule-based only)
       - Pass to `capture_note()` calls within sb_capture_smart: `importance=importance`
       - There are two capture_note calls in sb_capture_smart: the main one (new notes) and the stub creation one (leave stubs as "medium" default)

    6. **tests/test_mcp.py** — Add tests:
       - `test_sb_capture_importance_high`: sb_capture with importance="high", verify DB value
       - `test_sb_capture_importance_default`: sb_capture without importance, verify "medium"
       - `test_sb_edit_importance`: create note, sb_edit with importance="low", verify DB + frontmatter updated
       - `test_sb_edit_importance_none_preserves`: create note with high, sb_edit without importance, verify still high
       - `test_classify_importance_urgent`: classify_importance("URGENT fix", "prod down") == "high"
       - `test_classify_importance_default`: classify_importance("meeting notes", "discussed roadmap") == "medium"
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_mcp.py -k importance -x -q</automated>
  </verify>
  <acceptance_criteria>
    - engine/typeclassifier.py contains `def classify_importance(`
    - engine/mcp_server.py sb_capture contains `importance: str = "medium"` param
    - engine/mcp_server.py sb_capture_batch loop contains `importance = note.get("importance", "medium")`
    - engine/mcp_server.py sb_edit contains `importance: str | None = None` param
    - engine/mcp_server.py sb_capture_smart contains `classify_importance`
    - tests/test_mcp.py contains `test_sb_capture_importance_high`
    - tests/test_mcp.py contains `test_sb_edit_importance`
    - `uv run pytest tests/test_mcp.py -k importance -x -q` exits 0
  </acceptance_criteria>
  <done>All four MCP tools handle importance; sb_capture_smart infers importance via keyword heuristics in classify_importance; tests pass.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_capture.py tests/test_mcp.py -k importance -x -q` — all importance tests green
- `uv run pytest tests/test_capture.py tests/test_mcp.py -x -q` — no regressions in existing tests
</verification>

<success_criteria>
- importance column exists in notes table after init_schema()
- All capture paths (build_post, write_note_atomic, capture_note) write importance to both frontmatter and DB
- sb_capture, sb_capture_batch, sb_edit accept importance parameter
- sb_capture_smart infers importance from content via keyword heuristics
- All tests pass with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/42-add-importance-field-to-notes/42-01-SUMMARY.md`
</output>
