---
phase: 27.9-inbox-page
verified: 2026-03-19T12:00:00Z
status: human_needed
score: 12/12 must-haves verified
re_verification:
  previous_status: gaps_found
  previous_score: 11/12
  gaps_closed:
    - "BacklinkPicker now calls POST /search with JSON body instead of GET /search?q="
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Navigate to Inbox tab. Open a note in Unprocessed Notes. Click 'Add Backlink', type 3 characters."
    expected: "Search results appear in a dropdown within 300ms."
    why_human: "BacklinkPicker POST /search fix needs live UI confirmation that results render correctly."
  - test: "Dismiss all items across all three sections until inbox is empty."
    expected: "'All clear — nothing to triage.' header with last-checked timestamp; all three section headers still render with count (0)."
    why_human: "Inbox-zero visual state and section header persistence at count 0 require live UI observation."
  - test: "Dismiss one item, restart sb-api, navigate back to Inbox."
    expected: "Dismissed item does not reappear after restart."
    why_human: "Persistence across restart requires live server restart to verify."
---

# Phase 27.9: Inbox Page Verification Report

**Phase Goal:** Implement the Inbox page — a triage hub showing recent connections, pending action items, and stale notes — with backend API endpoints, React frontend, Playwright tests, and bug fixes.
**Verified:** 2026-03-19
**Status:** human_needed — all automated checks pass; 3 items need live UI confirmation
**Re-verification:** Yes — after gap closure (BacklinkPicker GET→POST fix)

---

## Re-verification Summary

Previous status: `gaps_found` (11/12)
Current status: `human_needed` (12/12)

**Gap closed:** `InboxPage.tsx` line 66 previously called `GET /search?q=…`. It now calls `POST /search` with `method: 'POST'`, `Content-Type: application/json`, and `body: JSON.stringify({ query })`. The fix matches how `ActionsPage` and other consumers call the search endpoint.

No regressions detected. All 12 truths now pass automated verification.

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | GET /inbox returns `{ unassigned_actions, unprocessed_notes, empty_notes, total_count }` | VERIFIED | `engine/api.py` line 914 — full endpoint with real SQL |
| 2 | Dismissed items do not reappear in GET /inbox after restart | VERIFIED | `dismissed_inbox_items` table + INSERT OR IGNORE; `test_dismiss_persists` passes |
| 3 | Unprocessed query excludes meeting/people types and notes older than 14 days | VERIFIED | Inline SQL type filter + date filter; unit tests cover exclusion |
| 4 | POST /inbox/dismiss returns 200 and persists to `dismissed_inbox_items` table | VERIFIED | `engine/api.py` line 995; INSERT OR IGNORE pattern |
| 5 | POST /relationships creates a relationship and marks note as processed | VERIFIED | `engine/api.py` line 1015; `test_relationships_create` verifies DB row |
| 6 | Inbox tab is visible in the tab bar | VERIFIED | `TabBar.tsx` line 12: `{ id: 'inbox' as const, label: 'Inbox' }` |
| 7 | Three collapsible sections render even when counts are 0 | VERIFIED | All three `<Section>` components unconditionally rendered; empty state shows "Nothing to triage here." |
| 8 | Total count header shows "N items need attention" | VERIFIED | `InboxPage.tsx` line 311; zero state shows "All clear" |
| 9 | Clicking a row opens detail pane on right; list stays left | VERIFIED | Split-view layout; `selectedItem` local state; `data-testid="inbox-detail"` present |
| 10 | Inline dismiss removes item from list after action | VERIFIED | `dismiss()` calls `/inbox/dismiss` then `loadInbox()` — wired for all three sections |
| 11 | Backlink picker searches via POST /search with debounce 300ms, min 2 chars | VERIFIED | `InboxPage.tsx` line 66–69: `method: 'POST'`, `Content-Type: application/json`, `body: JSON.stringify({ query })` |
| 12 | RightPanel does NOT render when currentView === 'inbox' | VERIFIED | `App.tsx` line 87: `{currentView === 'notes' && <RightPanel />}` — inbox excluded |

**Score:** 12/12 truths verified

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `engine/db.py` | `migrate_add_dismissed_inbox_items_table` + `init_schema` call | VERIFIED | CREATE TABLE IF NOT EXISTS with PRIMARY KEY (path, item_type); called from init_schema() |
| `engine/api.py` | `/inbox` GET, `/inbox/dismiss` POST, `/relationships` POST | VERIFIED | Lines 914, 995, 1015 — all three endpoints with real SQL |
| `frontend/src/components/InboxPage.tsx` | Split-view, 3 collapsible sections, inline triage, BacklinkPicker POST | VERIFIED | BacklinkPicker GET→POST gap closed |
| `frontend/src/contexts/UIContext.tsx` | `'inbox'` in View union | VERIFIED | `type View = ... \| 'inbox'` |
| `frontend/src/components/TabBar.tsx` | Inbox tab in TABS array | VERIFIED | Line 12: `{ id: 'inbox' as const, label: 'Inbox' }` |
| `frontend/src/App.tsx` | `currentView === 'inbox'` branch + InboxPage import | VERIFIED | Lines 12, 83–84 |
| `frontend/src/types.ts` | `InboxData`, `NoteSummary` types | VERIFIED | Lines 69–80 |
| `tests/test_inbox.py` | 5 real (non-xfail) unit tests | VERIFIED | 5 passed — no xfail markers |
| `tests/conftest.py` | `gui_brain` fixture with inbox seed data | VERIFIED | Unassigned action, untagged ideas note, empty note seeded |
| `tests/test_gui.py` | 3 Playwright tests for inbox | VERIFIED | `test_inbox_tab_visible`, `test_inbox_items_render`, `test_inbox_split_view` |
| Built JS bundle | InboxPage compiled into deployed asset | VERIFIED | `index-2rq_HeZo.js` contains "inbox-page"/"InboxPage"; `index.html` references bundle |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `engine/api.py get_inbox()` | `dismissed_inbox_items` table | LEFT JOIN filter | WIRED | Both unassigned_actions and empty_notes queries use LEFT JOIN with d.path IS NULL filter |
| `InboxPage.tsx loadInbox()` | `GET /inbox` | `fetch(getAPI() + '/inbox')` | WIRED | Line 192 confirmed |
| `InboxPage.tsx dismiss()` | `POST /inbox/dismiss` | fetch POST with `{path, item_type}` | WIRED | Correct method, headers, body |
| `InboxPage.tsx addBacklink()` | `POST /relationships` | fetch POST with `{source_path, target_path}` | WIRED | Correct method, headers, body |
| `InboxPage.tsx BacklinkPicker` | `POST /search` | fetch POST with `{query}` | WIRED | Fixed: now uses `method: 'POST'` with JSON body — was GET in previous verification |
| `App.tsx` | `InboxPage` | `currentView === 'inbox'` branch | WIRED | Lines 83–84 confirmed |

---

## Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| 27.9-INBOX-01 | 27.9-01, 27.9-03 | Backend `/inbox` endpoint returning all three sections | SATISFIED | Endpoint at line 914; 3 unit tests pass |
| 27.9-INBOX-02 | 27.9-01, 27.9-03 | Dismiss persistence survives restart | SATISFIED | `dismissed_inbox_items` table + `test_dismiss_persists` passing |
| 27.9-INBOX-03 | 27.9-02, 27.9-03 | Frontend InboxPage — split-view, 3 collapsible sections, inline triage | SATISFIED | BacklinkPicker POST gap closed; all triage actions wired |
| 27.9-INBOX-04 | 27.9-02, 27.9-03 | Tab bar wiring + Playwright integration tests | SATISFIED | Tab wired; 3 Playwright tests present |

Note: REQUIREMENTS.md does not contain 27.9-INBOX-* entries — requirements are defined only in plan frontmatter and RESEARCH file. No orphaned requirements found.

---

## Anti-Patterns Found

None. The blocker from the previous verification (GET vs POST on BacklinkPicker) has been resolved.

---

## Human Verification Required

### 1. Backlink picker search results

**Test:** Navigate to Inbox tab. Ensure at least one note is in the Unprocessed Notes section. Click "Add Backlink" on a row. Type 3 characters in the search input.
**Expected:** Search results appear in a dropdown within 300ms.
**Why human:** POST /search fix needs live UI confirmation that results render and the response shape `data.results` is consumed correctly.

### 2. Inbox zero state

**Test:** Dismiss all items across all three sections until `total_count === 0`.
**Expected:** Header changes to "All clear — nothing to triage." with a last-checked timestamp. All three section headers still render with `(0)` counts.
**Why human:** Visual rendering and section persistence at count 0 cannot be confirmed programmatically.

### 3. Dismiss persistence across restart

**Test:** Dismiss one item, then restart sb-api (`launchctl kickstart -k gui/$(id -u)/com.secondbrain.api` or equivalent), navigate back to Inbox.
**Expected:** Dismissed item does not reappear.
**Why human:** Requires live server restart and re-navigation.

---

_Verified: 2026-03-19_
_Verifier: Claude (gsd-verifier)_
