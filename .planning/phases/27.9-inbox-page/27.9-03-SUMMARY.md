---
phase: 27.9-inbox-page
plan: "03"
subsystem: tests
tags: [inbox, playwright, regression-baseline, test-coverage]
dependency_graph:
  requires: [27.9-01, 27.9-02]
  provides: [inbox regression baseline, 3 Playwright tests, gui_brain inbox seed]
  affects: [tests/test_inbox.py, tests/conftest.py, tests/test_gui.py, frontend/src/components/InboxPage.tsx]
tech_stack:
  added: []
  patterns: [playwright split-view test, session-scoped seed fixture extension, data-testid testability]
key_files:
  created: []
  modified:
    - tests/conftest.py
    - tests/test_gui.py
    - frontend/src/components/InboxPage.tsx
decisions:
  - "Task 1 was already complete: test_inbox.py had real assertions (no xfail) from plan 01 Task 2 — verified 5 passed, no changes needed"
  - "data-testid='inbox-detail' added to DetailPane outer div to enable reliable Playwright split-view assertion"
  - "gui_brain inbox seed uses conn7 pattern consistent with prior seeds; notes/ dir created with parents=True"
  - "test_inbox_split_view falls back from li to [class*='cursor-pointer'] selector since InboxPage uses divs not li elements"
  - "Full-suite mixed-session failures in test_gui.py are pre-existing stale-server issue (not caused by this plan); test_gui.py alone passes 30/30"
metrics:
  duration: 20 min
  completed_date: "2026-03-19"
  tasks_completed: 2
  files_changed: 4
---

# Phase 27.9 Plan 03: Inbox Regression Baseline — Summary

**One-liner:** Inbox regression baseline: 5 unit tests verified passing, gui_brain extended with inbox seed data, 3 Playwright tests covering tab visibility, section headers, and split-view detail pane.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Verify xfail test promotion (already done in plan 01) | — (no changes) | tests/test_inbox.py |
| 2 | gui_brain inbox seed + 3 Playwright tests + InboxPage data-testid | bfd449f | conftest.py, test_gui.py, InboxPage.tsx, bundle |

## What Was Built

### Task 1: xfail test promotion

`tests/test_inbox.py` already contained 5 real assertions (no `xfail` markers) from plan 01 Task 2 (GREEN phase). Verified all 5 pass: `test_inbox_endpoint`, `test_unprocessed_excludes_structured_types`, `test_unprocessed_excludes_old_notes`, `test_dismiss_persists`, `test_relationships_create`.

### Task 2: gui_brain fixture + Playwright tests

**`tests/conftest.py`** — conn7 block added to `gui_brain` fixture:
- `ideas/inbox-test-note.md` — recent untagged ideas note (type=ideas, tags=NULL, created_at=now)
- `notes/inbox-empty.md` — empty body note (type=note, body='')
- `action_items` row for inbox-test-note.md with done=0, assignee_path=NULL

**`frontend/src/components/InboxPage.tsx`** — added `data-testid="inbox-detail"` to `DetailPane` outer container for reliable Playwright split-view assertions.

**Frontend rebuild** — bundle rebuilt to `index-2rq_HeZo.js`; `uv tool install . --reinstall` run; `.secrets.baseline` updated.

**`tests/test_gui.py`** — 3 new tests at end of file:

| Test | Covers | Key assertion |
|------|--------|---------------|
| `test_inbox_tab_visible` | 27.9-INBOX-01 | `[data-testid='tab-inbox']` is visible |
| `test_inbox_items_render` | 27.9-INBOX-02 | Three section headers rendered after tab click |
| `test_inbox_split_view` | 27.9-INBOX-03 | `[data-testid='inbox-detail']` visible after row click |

## Verification Results

```
uv run pytest tests/test_inbox.py -q       → 5 passed
uv run pytest tests/test_gui.py -k inbox -q → 3 passed
uv run pytest tests/test_gui.py -q         → 30 passed (full GUI suite)
```

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing] Added data-testid="inbox-detail" to DetailPane**
- **Found during:** Task 2 implementation
- **Issue:** Plan noted that `data-testid="inbox-detail"` may be missing from plan 02; it was absent
- **Fix:** Added attribute to `DetailPane` outer `<div>` in `InboxPage.tsx`
- **Files modified:** frontend/src/components/InboxPage.tsx
- **Commit:** bfd449f

**2. [Rule 3 - Blocking] test_inbox_split_view selector fallback for non-li rows**
- **Found during:** Task 2 implementation
- **Issue:** Plan specified `[data-testid='inbox-page'] li` but InboxPage uses `<div>` elements not `<li>` for rows
- **Fix:** Added fallback selector `[class*='cursor-pointer']` when `li` count is 0
- **Files modified:** tests/test_gui.py
- **Commit:** bfd449f

## Self-Check: PASSED

Files exist:
- tests/conftest.py — FOUND
- tests/test_gui.py — FOUND
- frontend/src/components/InboxPage.tsx — FOUND
- engine/gui/static/assets/index-2rq_HeZo.js — FOUND

Commits:
- bfd449f: test(27.9-03): add inbox Playwright tests + gui_brain inbox seed data — FOUND
