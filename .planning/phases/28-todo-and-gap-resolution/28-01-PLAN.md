---
phase: 28-todo-and-gap-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/capture.py
  - tests/test_capture.py
autonomous: true
requirements: [28-DEDUP-01]

must_haves:
  truths:
    - "sb_capture on a note with body > 2000 chars uses title-only embedding for dedup, not full body"
    - "sb_capture on a note with body <= 2000 chars still uses title+body embedding (unchanged)"
    - "check_capture_dedup never raises; returns [] on timeout or error"
  artifacts:
    - path: engine/capture.py
      provides: "check_capture_dedup() with title-only fast-path"
      contains: "max_body_len"
    - path: tests/test_capture.py
      provides: "test_dedup_title_only_large_body"
  key_links:
    - from: engine/mcp_server.py sb_capture()
      to: engine/capture.py check_capture_dedup()
      via: "called with title + body before capture_note()"
      pattern: "check_capture_dedup"
---

<objective>
Add a title-only fast-path to check_capture_dedup() so large note bodies (>2000 chars) don't embed thousands of tokens, eliminating the 8s timeout risk on sb_capture for long inputs.

Purpose: MCP tool timeouts on large captures are caused by embedding the full title+body. The fix is a single-parameter addition.
Output: Modified engine/capture.py + passing test.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- From engine/capture.py check_capture_dedup() — current signature -->
```python
def check_capture_dedup(
    title: str, body: str, conn: sqlite3.Connection, threshold: float = 0.92
) -> list[dict]:
    """Returns [] on error. Never raises. timeout=8s via ThreadPoolExecutor."""
    # Line 63: blobs = embed_texts([f"{title}\n{body}"])  ← FIX THIS LINE
```

<!-- From engine/mcp_server.py — call site -->
```python
# sb_capture() calls:
dupes = check_capture_dedup(title, body, conn)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add title-only fast-path to check_capture_dedup</name>
  <files>engine/capture.py, tests/test_capture.py</files>
  <behavior>
    - test_dedup_title_only_large_body: when body > 2000 chars, embed_texts is called with [title] only (not f"{title}\n{body}")
    - test_dedup_short_body_uses_full_text: when body <= 2000 chars, embed_texts is called with [f"{title}\n{body}"]
    - test_dedup_returns_empty_on_timeout: existing behavior unchanged (returns [] on timeout)
  </behavior>
  <action>
TDD-inline: Write the failing tests in tests/test_capture.py FIRST (they must fail with the current implementation), then implement the fix to make them pass.

Add `max_body_len: int = 2000` parameter to `check_capture_dedup()` signature.

Inside `_run_dedup()`, change line 63 from:
  `blobs = embed_texts([f"{title}\n{body}"])`
to:
  `text_to_embed = title if len(body) > max_body_len else f"{title}\n{body}"`
  `blobs = embed_texts([text_to_embed])`

No other changes to the function. No changes to callers (default preserves backward compat).

In tests/test_capture.py, add tests after existing dedup tests. Use monkeypatch to patch `engine.capture.embed_texts` and capture what it was called with. Use a 2001-char body to trigger the fast-path. Use a 10-char body to confirm the existing path.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_capture.py -x -q -k dedup</automated>
  </verify>
  <done>All dedup tests pass; check_capture_dedup signature shows max_body_len param; title-only path activated for large bodies.</done>
</task>

</tasks>

<verification>
`uv run pytest tests/test_capture.py -q` — all capture tests green.
</verification>

<success_criteria>
- check_capture_dedup() accepts max_body_len param (default 2000)
- For body > 2000 chars: embed_texts called with title only
- For body <= 2000 chars: embed_texts called with f"{title}\n{body}" (unchanged)
- No callers broken (default param)
</success_criteria>

<output>
After completion, create `.planning/phases/28-todo-and-gap-resolution/28-01-SUMMARY.md`
</output>
