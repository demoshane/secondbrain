---
phase: 28-todo-and-gap-resolution
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/mcp_server.py
  - tests/test_mcp.py
autonomous: true
requirements: [28-SMART-01]

must_haves:
  truths:
    - "sb_capture_smart returns typed note suggestions without saving anything"
    - "suggestions include title, type, body, and cross-links"
    - "a second call to sb_capture_batch with the suggestions saves them"
    - "suggestions classify meeting/person/project/idea by keyword heuristics"
  artifacts:
    - path: engine/mcp_server.py
      provides: "sb_capture_smart() MCP tool"
      contains: "sb_capture_smart"
    - path: tests/test_mcp.py
      provides: "test_sb_capture_smart_returns_suggestions"
  key_links:
    - from: engine/mcp_server.py sb_capture_smart()
      to: engine/mcp_server.py sb_capture_batch()
      via: "user passes suggestions list to sb_capture_batch to commit"
      pattern: "capture_batch"
---

<objective>
Add sb_capture_smart MCP tool: accepts freeform text, classifies it into 1-N typed note suggestions using keyword heuristics, returns suggestions without saving. User calls sb_capture_batch to commit.

Purpose: Enables quick capture of raw text without forcing the user to decide type/title upfront.
Output: New MCP tool in engine/mcp_server.py + passing tests.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- From engine/mcp_server.py — existing MCP tool pattern -->
```python
@mcp.tool()
def sb_capture_batch(notes: list[dict]) -> dict:
    """Each note: {title, note_type, body, tags?, sensitivity?}. Returns {succeeded, failed}."""

@mcp.tool()
def sb_tools() -> list[dict]:
    """Self-documentation. sb_capture_smart must be visible here after addition."""
```

<!-- Heuristic classification signals (from RESEARCH.md) -->
# meeting: "meeting", "discussed", "attendees", date pattern (YYYY-MM-DD or "Jan 5")
# person: "[A-Z][a-z]+ [A-Z][a-z]+" name pattern + role/contact context
# project: "project", "milestone", "deadline", "sprint", "roadmap"
# idea: "idea", "what if", "maybe", "consider"
# default: "note"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement sb_capture_smart with heuristic classifier</name>
  <files>engine/mcp_server.py, tests/test_mcp.py</files>
  <behavior>
    - test_sb_capture_smart_returns_suggestions: raw text containing "meeting" + "discussed" → at least one suggestion with type="meeting"; nothing is saved to DB
    - test_sb_capture_smart_project_hint: text containing "project" + "milestone" → suggestion type="project"
    - test_sb_capture_smart_default_note: plain text with no keywords → suggestion type="note" or type="idea"
    - test_sb_capture_smart_no_auto_save: after calling sb_capture_smart, notes table is unchanged (no INSERT happened)
    - test_sb_capture_smart_returns_confirm_token: response includes a confirm_token string for downstream use
  </behavior>
  <action>
TDD-inline: Write the 5 failing tests in tests/test_mcp.py FIRST (they must fail — sb_capture_smart does not exist yet), then implement to make them pass.

Add `sb_capture_smart(content: str) -> dict` to engine/mcp_server.py after sb_capture_batch.

Implementation:
1. Split content into segments by double-newline (paragraphs). If content is short (<500 chars), treat as single segment.
2. For each segment, classify type via keyword regex (case-insensitive):
   - "meeting|discussed|attendees|agenda" → "meeting"
   - r"[A-Z][a-z]+ [A-Z][a-z]+" in first 200 chars + ("role|contact|email|phone|linkedin") → "person"
   - "project|milestone|deadline|sprint|roadmap" → "project"
   - "idea|what if|maybe|consider|brainstorm" → "idea"
   - else → "note"
3. Derive title: first non-empty line of the segment, stripped of # prefix, truncated to 80 chars.
4. Infer cross-links: if >1 suggestion and types include "meeting" + "person", add each person suggestion path (slugified title) to the meeting suggestion's `links` list.
5. Return: `{"suggestions": [{"title": str, "type": str, "body": str, "links": [str]}, ...], "confirm_token": _make_token(), "hint": "Call sb_capture_batch with suggestions to save."}`

CRITICAL: Do NOT call capture_note() or write to disk. First call is suggestions-only.

Tests in tests/test_mcp.py: use existing `mcp_mod` fixture pattern (direct module-level function calls). Patch `engine.db.DB_PATH` + `engine.paths.DB_PATH` + `engine.paths.BRAIN_ROOT` for isolation. Assert suggestions list is non-empty. Assert notes table count is unchanged after the call.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_mcp.py -x -q -k capture_smart</automated>
  </verify>
  <done>sb_capture_smart registered as MCP tool; returns typed suggestions without saving; all 5 tests pass.</done>
</task>

</tasks>

<verification>
`uv run pytest tests/test_mcp.py -q -k "capture_smart or capture_batch"` — all green.
`uv run pytest tests/ -q --ignore=tests/test_gui.py` — full unit suite green.
</verification>

<success_criteria>
- sb_capture_smart visible in sb_tools() output
- Returns {"suggestions": [...], "confirm_token": "...", "hint": "..."}
- No note written to disk or DB on first call
- Type classification: meeting/project/person/idea/note by keyword heuristics
</success_criteria>

<output>
After completion, create `.planning/phases/28-todo-and-gap-resolution/28-02-SUMMARY.md`
</output>
