---
phase: 28-todo-and-gap-resolution
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/mcp_server.py
  - tests/test_mcp.py
autonomous: true
requirements: [28-PERSON-01]

must_haves:
  truths:
    - "sb_person_context returns the person note body"
    - "sb_person_context returns all meetings where the person's name or path appears"
    - "sb_person_context returns all action items assigned to or mentioning the person"
    - "sb_person_context returns all notes that mention the person by name (body-mention detection)"
    - "all data returned in one call — no follow-up calls needed"
  artifacts:
    - path: engine/mcp_server.py
      provides: "sb_person_context() MCP tool"
      contains: "sb_person_context"
    - path: tests/test_mcp.py
      provides: "test_sb_person_context"
  key_links:
    - from: engine/mcp_server.py sb_person_context()
      to: "engine/api.py note_meta(), list_meetings(), list_actions()"
      via: "direct internal function calls (no HTTP)"
      pattern: "list_meetings|list_actions|note_meta"
---

<objective>
Add sb_person_context MCP tool: one-call full context dump for a person (note body + meetings + actions + body-mention notes). Aggregates existing internal functions — no new DB queries.

Purpose: Agents currently need 4+ MCP calls to get person context. This collapses them to one.
Output: New MCP tool in engine/mcp_server.py + passing tests.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- From engine/api.py — existing functions to call internally -->
```python
def list_meetings():
    # Returns list of meeting dicts: {path, title, meeting_date, people, body_preview}

def list_actions(conn, done=False, assignee=None, note_path=None) -> list[dict]:
    # Returns list of action item dicts

def note_meta(note_path):
    # Returns {backlinks, related, people, ...}
    # people: list of {path, title} resolved from body-mention detection
```

<!-- From engine/db.py get_connection() -->
```python
def get_connection(db_path: str | None = None) -> sqlite3.Connection:
```

<!-- People context aggregation pattern -->
# Given person_path and person_title:
# 1. Read note: conn.execute("SELECT title, body FROM notes WHERE path=?", (person_path,))
# 2. Meetings: query SELECT path, title, body FROM notes WHERE type='meeting'. Filter those where person_title appears (case-insensitive) in body OR path == person_path.
# 3. Actions: conn.execute("SELECT id, text, done, due_date, note_path FROM action_items WHERE assignee_path=?", (path,))
#             + conn.execute("SELECT id, text, done, due_date, note_path FROM action_items WHERE text LIKE ?", (f'%{person_title}%',)) — deduplicate by id
# 4. Mentions: conn.execute("SELECT path, title FROM notes WHERE body LIKE ? AND path != ? AND type NOT IN ('person','people')", (f'%{person_title}%', path))
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement sb_person_context</name>
  <files>engine/mcp_server.py, tests/test_mcp.py</files>
  <behavior>
    - test_sb_person_context_returns_note_body: sb_person_context("people/alice.md") → result["note"]["body"] contains the person note's body text
    - test_sb_person_context_returns_meetings: seed a meeting note with "alice" in body → result["meetings"] contains that meeting
    - test_sb_person_context_returns_actions: seed an action item with assignee_path="people/alice.md" → result["actions"] contains it
    - test_sb_person_context_returns_mentions: seed a regular note with "Alice" in body → result["mentions"] contains that note path
    - test_sb_person_context_unknown_path: returns {"error": "not found"} for non-existent path (not exception)
  </behavior>
  <action>
TDD-inline: Write the 5 failing tests in tests/test_mcp.py FIRST (they must fail — sb_person_context does not exist yet), then implement to make them pass.

Add `sb_person_context(path: str) -> dict` to engine/mcp_server.py after sb_tools.

Import locally: `from engine.paths import BRAIN_ROOT as _BRAIN_ROOT` (inside function body — not module level).

Implementation:
1. Open conn via get_connection().
2. Look up person note: `row = conn.execute("SELECT title, body, type FROM notes WHERE path=?", (path,)).fetchone()`
   - If None: return `{"error": "not found", "path": path}`
3. Extract person_title = row["title"].
4. Meetings: query `SELECT path, title, body FROM notes WHERE type='meeting'`. Filter those where person_title appears (case-insensitive) in body OR path == person_path. Return as list of {path, title}.
5. Actions assigned: `conn.execute("SELECT id, text, done, due_date, note_path FROM action_items WHERE assignee_path=?", (path,))` — returns rows as list of dicts.
6. Actions mentioned: `conn.execute("SELECT id, text, done, due_date, note_path FROM action_items WHERE text LIKE ?", (f'%{person_title}%',))` — deduplicate with assigned set by id.
7. Mentions: `conn.execute("SELECT path, title FROM notes WHERE body LIKE ? AND path != ? AND type NOT IN ('person','people')", (f'%{person_title}%', path))` — returns list of {path, title}.
8. Return:
```python
{
    "path": path,
    "note": {"title": person_title, "body": row["body"]},
    "meetings": [{...}, ...],
    "actions": [{...}, ...],    # combined assigned + mentioned, deduplicated
    "mentions": [{...}, ...]
}
```

Tests: use existing mcp_mod fixture + DB isolation (patch engine.db.DB_PATH + engine.paths.DB_PATH + engine.paths.BRAIN_ROOT). Seed person note, meeting note, action item row, mention note. Call mcp_mod.sb_person_context(path) directly. Assert each section of the response.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_mcp.py -x -q -k person_context</automated>
  </verify>
  <done>All 5 sb_person_context tests pass; tool visible in sb_tools(); returns note+meetings+actions+mentions in one call.</done>
</task>

</tasks>

<verification>
`uv run pytest tests/test_mcp.py -q -k person_context` — green.
`uv run pytest tests/ -q --ignore=tests/test_gui.py` — full unit suite green.
</verification>

<success_criteria>
- sb_person_context registered as MCP tool
- Returns {note, meetings, actions, mentions} in one call
- Uses body-mention detection for meetings and mentions (consistent with note_meta() approach)
- Returns {"error": "not found"} gracefully for unknown paths
- BRAIN_ROOT imported locally (not module-level) for test isolation
</success_criteria>

<output>
After completion, create `.planning/phases/28-todo-and-gap-resolution/28-06-SUMMARY.md`
</output>
