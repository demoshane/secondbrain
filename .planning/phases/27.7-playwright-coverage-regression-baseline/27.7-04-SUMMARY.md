---
phase: 27.7-playwright-coverage-regression-baseline
plan: "04"
subsystem: testing
tags: [playwright, pytest, regression, people-page, brain-health, api]

requires:
  - phase: 27.7-01
    provides: gui_brain fixture with Test Group (type='people') seed
  - phase: 27.7-02
    provides: right panel regression tests baseline
  - phase: 27.7-03
    provides: brain health orphan spec tests
provides:
  - people/person type isolation regression test (test_people_type_isolation)
  - intelligence health score data correctness test (test_intelligence_health_score_shows_value)
  - Fix: list_people() now queries WHERE type IN ('person', 'people')
  - Full Phase 27.7 Playwright regression baseline: 37 passed, 7 xpassed, 0 failed
affects:
  - future people-page changes
  - list_people() callers
  - intelligence page changes

tech-stack:
  added: []
  patterns:
    - "Type isolation test: assert both singular and plural type appear in same table"
    - "Numeric assertion pattern: float(inner_text.replace('%','').strip()) for score elements"

key-files:
  created: []
  modified:
    - tests/test_gui.py
    - engine/api.py

key-decisions:
  - "[27.7-04] list_people() WHERE type IN ('person', 'people') — plural type notes must appear on People page; was silently excluded by type='person' only query"
  - "[27.7-04] test_people_type_isolation uses gui_brain directly (no seed_note_fn) — relies on session-scoped Test Group seed from Plan 01 for deterministic identity"
  - "[27.7-04] health score numeric assertion: float(score_text.replace('%','').strip()) handles both bare integer and percentage-suffix display formats"

patterns-established:
  - "People page type isolation: always test both type='person' and type='people' in any list_people() regression"
  - "Score element assertions: parse inner_text to float, assert 0 <= val <= 100"

requirements-completed: [27.7-QA-03, 27.7-QA-04]

duration: 12min
completed: 2026-03-19
---

# Phase 27.7 Plan 04: People Type Isolation + Health Score Data Assertion Summary

**Playwright regression baseline complete: type='people' isolation bug fixed in list_people(), numeric health-score assertion added, full suite 37 passed + 7 xpassed with zero failures**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-03-19T09:40:00Z
- **Completed:** 2026-03-19T09:52:00Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Added `test_people_type_isolation`: regression guard that asserts both `type='person'` and `type='people'` notes appear on the People page, and meeting notes do not
- Added `test_intelligence_health_score_shows_value`: upgrades existence-only health score test to data correctness check — inner_text must parse as float 0-100
- Auto-fixed `list_people()` in `engine/api.py`: WHERE clause was `type='person'` only; changed to `type IN ('person', 'people')` so group-type notes appear on People page
- Established Phase 27.7 full suite baseline: 37 passed, 7 xpassed (all xfails promoted), zero FAILED, zero ERROR

## Task Commits

1. **Task 1: Add people type isolation test + health score assertion + API bug fix** - `cb5e9af` (feat)
2. **Task 2: Full suite baseline run** — verified via pytest (no separate commit; baseline confirmed by test output)

## Files Created/Modified

- `tests/test_gui.py` — added `test_people_type_isolation` and `test_intelligence_health_score_shows_value`
- `engine/api.py` — fixed `list_people()` WHERE clause: `type='person'` → `type IN ('person', 'people')`

## Decisions Made

- Auto-fixed `list_people()` rather than marking `test_people_type_isolation` as xfail: the fix is a one-line query change, clearly correct, directly caused by the test revealing the regression. Rule 1 applies.
- Used `float(score_text.replace("%", "").strip())` for score assertion to handle both bare integer (`"85"`) and hypothetical percentage suffix display formats gracefully.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed list_people() WHERE clause missing type='people'**
- **Found during:** Task 1 (test_people_type_isolation first run)
- **Issue:** `engine/api.py` `list_people()` queried `WHERE n.type = 'person'` only. Notes with `type='people'` (plural, group-type) were excluded from the People page entirely. Test Group seeded in Plan 01 was invisible.
- **Fix:** Changed to `WHERE n.type IN ('person', 'people')` — one line change, consistent with `note_meta()` body-scan query (line 610) which already used the correct `IN ('person', 'people')` form.
- **Files modified:** `engine/api.py`
- **Verification:** `test_people_type_isolation` went from FAILED to PASSED; full suite confirmed no regressions.
- **Committed in:** `cb5e9af` (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 — bug)
**Impact on plan:** Fix was required for the test to pass. Direct regression in production API surfaced by the new test. No scope creep.

## Issues Encountered

None beyond the auto-fixed bug above.

## Full Suite Baseline Results

```
tests/test_preflight.py    7 passed
tests/test_gui.py         27 passed
tests/test_brain_health.py 3 passed, 7 xpassed
---------------------------------------
Total: 37 passed, 7 xpassed — 0 failed, 0 error
```

The 7 xpassed tests are xfail stubs from earlier phases that now pass (auto-promoted). They are acceptable as xpassed per the baseline spec.

## Next Phase Readiness

- Phase 27.7 regression baseline is fully established
- Any future regression in people page, intelligence page, or brain health will be caught by this suite
- No blockers

---
*Phase: 27.7-playwright-coverage-regression-baseline*
*Completed: 2026-03-19*
