---
phase: 29-add-link-capture
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_link_capture.py
autonomous: true
requirements:
  - LINK-01
  - LINK-02
  - LINK-03
  - LINK-04

must_haves:
  truths:
    - "All Phase 29 behaviors have failing stub tests before implementation begins"
    - "Test file is importable and collected by pytest"
  artifacts:
    - path: "tests/test_link_capture.py"
      provides: "xfail stubs for url column, metadata fetch, sb_capture_link, /links API"
      min_lines: 60
  key_links:
    - from: "tests/test_link_capture.py"
      to: "engine/link_capture.py"
      via: "import at top of each test"
      pattern: "from engine.link_capture import"
---

<objective>
Create xfail test stubs for all Phase 29 link capture behaviors so Wave 1+ tasks have test targets to turn green.

Purpose: Nyquist-compliant TDD baseline; stubs auto-promote to PASS once Wave 1 ships.
Output: tests/test_link_capture.py with 5 xfail stubs (url_column, fetch_metadata, sb_capture_link, links_api, duplicate_url_warn).
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/29-add-link-capture/29-CONTEXT.md
@.planning/phases/29-add-link-capture/29-VALIDATION.md
@.claude/LEARNINGS.md
</context>

<interfaces>
<!-- Pattern: existing xfail stubs use xfail(strict=False) — auto-promote once impl ships -->
<!-- Pattern from tests/test_links.py client fixture: patch engine.db.DB_PATH + engine.paths.DB_PATH -->
<!-- Pattern from tests/test_people.py: patch BRAIN_PATH env var for isolation -->

From engine/db.py (migration pattern):
```python
def migrate_add_url_column(conn: sqlite3.Connection) -> None:
    try:
        conn.execute("ALTER TABLE notes ADD COLUMN url TEXT NULL")
        conn.commit()
    except sqlite3.OperationalError:
        pass  # already exists
```

From engine/mcp_server.py (tool registration):
```python
@mcp.tool()
def sb_capture_link(url: str, tags: list[str] | None = None, people: list[str] | None = None, notes: str = "") -> dict:
    ...
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write xfail stubs for all Phase 29 behaviors</name>
  <files>tests/test_link_capture.py</files>
  <behavior>
    - test_url_column_exists: after init_schema on in-memory DB, `PRAGMA table_info(notes)` includes a column named `url` — xfail strict=False
    - test_fetch_metadata_returns_title: fetch_link_metadata("https://example.com") returns dict with "title" key — xfail strict=False (mock HTTP)
    - test_fetch_metadata_fallback_on_error: fetch_link_metadata("https://unreachable.invalid") returns {"title": "unreachable.invalid", "description": ""} — xfail strict=False
    - test_sb_capture_link_registered: `sb_capture_link` is present in the MCP server's registered tools — xfail strict=False
    - test_links_api_returns_list: GET /links returns JSON {"links": [...]} — xfail strict=False
    - test_capture_link_duplicate_warn: capturing same URL twice returns status="duplicate_url_warning" on second call — xfail strict=False
  </behavior>
  <action>
    Create tests/test_link_capture.py with:
    - Module docstring: "Tests for Phase 29 link capture — url column, metadata fetch, MCP tool, Flask API."
    - Shared `client` fixture (function-scoped): patches engine.db.DB_PATH and engine.paths.DB_PATH to tmp_path DB; calls init_schema; returns Flask test client from engine.api.app.
    - Each test decorated @pytest.mark.xfail(strict=False, reason="Phase 29 implementation not yet shipped").
    - test_url_column_exists: uses _init_conn() (in-memory), checks PRAGMA table_info(notes) for col name "url".
    - test_fetch_metadata_returns_title: imports fetch_link_metadata from engine.link_capture; monkeypatches urllib or requests to return mock HTML with og:title; asserts result["title"] is non-empty string.
    - test_fetch_metadata_fallback_on_error: monkeypatches fetch to raise an exception; asserts result["title"] == "unreachable.invalid" (domain extraction fallback).
    - test_sb_capture_link_registered: imports mcp from engine.mcp_server; checks that "sb_capture_link" appears in the tool registry (use `mcp._local_provider._components` pattern from Phase 27-07 decision).
    - test_links_api_returns_list: uses client fixture; GET /links; asserts response.status_code == 200 and "links" in json.
    - test_capture_link_duplicate_warn: uses client fixture or direct function import; captures URL once, then captures same URL again; second call returns status or response indicating duplicate_url_warning.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_link_capture.py -q 2>&1 | tail -5</automated>
  </verify>
  <done>All 6 tests collected; all xfail (either XFAIL or XPASS — no ERROR or FAILED).</done>
</task>

</tasks>

<verification>
`uv run pytest tests/test_link_capture.py -q` — all tests xfail or xpass, zero errors.
`uv run pytest tests/ -q` — full suite still green (no regressions introduced by new test file).
</verification>

<success_criteria>
- tests/test_link_capture.py exists and is collected without ImportError
- All 6 stub tests are xfail (strict=False) — they fail gracefully because implementation doesn't exist yet
- Full suite still green
</success_criteria>

<output>
After completion, create `.planning/phases/29-add-link-capture/29-01-SUMMARY.md`
</output>
