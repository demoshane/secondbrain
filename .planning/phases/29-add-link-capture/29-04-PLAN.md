---
phase: 29-add-link-capture
plan: 04
type: execute
wave: 3
depends_on:
  - 29-03
files_modified:
  - frontend/src/contexts/UIContext.tsx
  - frontend/src/components/TabBar.tsx
  - frontend/src/components/LinksPage.tsx
  - frontend/src/App.tsx
  - frontend/src/types.ts
autonomous: false
requirements:
  - LINK-04
  - LINK-06
  - LINK-07

must_haves:
  truths:
    - "Links tab is visible in the tab bar alongside existing tabs"
    - "Links page shows a list: title, domain, date, tags, description snippet per link"
    - "Clicking a link opens a detail panel with title, domain, date, body, and a Visit Link button"
    - "Visit Link button opens the URL in the system browser"
    - "In-page search filters the links list by title or description"
    - "Tag filter chip filters by tag"
    - "Empty state message shown when no links saved"
  artifacts:
    - path: "frontend/src/components/LinksPage.tsx"
      provides: "Links page list + detail panel"
      min_lines: 100
    - path: "frontend/src/contexts/UIContext.tsx"
      provides: "View type extended with 'links'"
      contains: "'links'"
    - path: "frontend/src/components/TabBar.tsx"
      provides: "Links tab entry"
      contains: "Links"
    - path: "frontend/src/App.tsx"
      provides: "LinksPage render in view switch"
      contains: "LinksPage"
  key_links:
    - from: "frontend/src/components/TabBar.tsx"
      to: "frontend/src/contexts/UIContext.tsx"
      via: "setCurrentView('links')"
      pattern: "links"
    - from: "frontend/src/components/LinksPage.tsx"
      to: "engine/api.py /links"
      via: "fetch('/links') on mount"
      pattern: "fetch.*links"
    - from: "frontend/src/components/LinksPage.tsx"
      to: "url field"
      via: "window.open(url, '_blank') on Visit Link click"
      pattern: "window.open"
---

<objective>
Add the Links tab and LinksPage React component, wiring it to the /links Flask API with search, tag filter, and detail panel including a Visit Link button.

Purpose: Completes the user-facing feature — link notes are browseable and openable from the GUI.
Output: LinksPage.tsx, View type extended, Tab bar entry, App.tsx render branch.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-add-link-capture/29-CONTEXT.md
@.planning/phases/29-add-link-capture/29-03-SUMMARY.md
@.claude/LEARNINGS.md
@frontend/src/contexts/UIContext.tsx
@frontend/src/components/TabBar.tsx
@frontend/src/App.tsx
@frontend/src/types.ts
</context>

<interfaces>
<!-- View type in UIContext.tsx (line 3): -->
```typescript
type View = 'notes' | 'actions' | 'people' | 'meetings' | 'projects' | 'intelligence' | 'inbox'
// Extend to: ... | 'links'
```

<!-- TabBar.tsx tabs array (lines 6-13): -->
```typescript
const tabs: Tab[] = [
  { id: 'notes' as const, label: 'Notes' },
  { id: 'actions' as const, label: 'Action Items' },
  { id: 'people' as const, label: 'People' },
  { id: 'meetings' as const, label: 'Meetings' },
  { id: 'projects' as const, label: 'Projects' },
  { id: 'intelligence' as const, label: 'Intelligence' },
  { id: 'inbox' as const, label: 'Inbox' },
  // Add: { id: 'links' as const, label: 'Links' },
]
```

<!-- App.tsx render pattern (lines 73-87): -->
```tsx
} : currentView === 'actions' ? (
  <ActionsPage />
) : currentView === 'people' ? (
  <PeoplePage />
...
) : currentView === 'inbox' ? (
  <InboxPage />
) : null}
// Add: : currentView === 'links' ? <LinksPage /> : ...
```

<!-- MeetingsPage.tsx pattern for list + detail panel:
- useState for selectedItem
- fetch on mount to /meetings endpoint
- left panel: list items with onClick setting selected
- right panel: detail view conditional on selectedItem
- search: filter in-memory by title/body
-->

<!-- API response from GET /links: {"links": [{path, title, url, domain, date, tags, description}]} -->
<!-- API response from GET /links/<path>: {path, title, url, domain, body, date, tags} -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: UIContext + TabBar + App.tsx wiring</name>
  <files>frontend/src/contexts/UIContext.tsx, frontend/src/components/TabBar.tsx, frontend/src/App.tsx, frontend/src/components/LinksPage.tsx</files>
  <action>
    FIRST — create a stub LinksPage so the App.tsx import resolves during the build verify in this task. Write the following to frontend/src/components/LinksPage.tsx before making any other changes:
    ```typescript
    export function LinksPage() { return null; }
    ```
    This stub will be replaced with the full implementation in Task 2.

    UIContext.tsx: Add 'links' to the View type union on line 3.

    TabBar.tsx: Add `{ id: 'links' as const, label: 'Links' }` at the end of the tabs array. The Tab type's `id` type will need to accept 'links' — it should be inferred from the array elements, no manual type change needed if id is typed via `as const`.

    App.tsx:
    1. Add import: `import { LinksPage } from './components/LinksPage'` (after existing page imports).
    2. In the view switch chain (the series of ternary expressions), add a new branch before the final `: null`:
       ```tsx
       ) : currentView === 'links' ? (
         <LinksPage />
       ```
    3. The Sidebar and RightPanel are only shown for `currentView === 'notes'` — this is already correct, no change needed.

    After editing, run `npm run build` from the frontend/ directory to confirm TypeScript compiles without errors.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain/frontend && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Build succeeds. LinksPage import resolves. No TypeScript type errors for View type.</done>
</task>

<task type="auto">
  <name>Task 2: LinksPage.tsx component</name>
  <files>frontend/src/components/LinksPage.tsx</files>
  <action>
    Replace the stub from Task 1 with the full implementation of frontend/src/components/LinksPage.tsx, implementing the links list + detail panel layout following the MeetingsPage.tsx pattern:

    State:
    - `links`: array of link objects from /links API (title, url, domain, date, tags, description, path)
    - `selectedLink`: currently selected link for detail panel (null = none)
    - `searchQuery`: string for in-page search filter
    - `tagFilter`: string for tag chip filter (empty = no filter)

    Data fetch: `useEffect(() => { fetch('/links').then(r=>r.json()).then(d => setLinks(d.links)) }, [])`

    Filtering: `const filtered = links.filter(l => l.title.toLowerCase().includes(searchQuery.toLowerCase()) || l.description.toLowerCase().includes(searchQuery.toLowerCase())).filter(l => !tagFilter || JSON.parse(l.tags || '[]').includes(tagFilter))`

    Left panel (list):
    - Search input (shadcn Input component) at top
    - If filtered.length === 0 and no search/filter active: empty state div with message "No links saved yet — use sb_capture_link in Claude"
    - If filtered.length === 0 with active filter: "No links match your filter"
    - List items: each shows title (bold), domain (muted text, smaller), date (muted right), description snippet (1 line truncated), tags as small chips. Clicking item: setSelectedLink(item)
    - Active item highlighted (bg-muted or similar shadcn token)
    - Tag chips in list items: clicking a chip sets tagFilter to that tag

    Right detail panel (conditional on selectedLink !== null):
    - Title (h2)
    - Domain + date row (muted)
    - "Visit Link" button (shadcn Button, primary/default variant) — onClick: `window.open(selectedLink.url, '_blank', 'noopener')`; place this prominently at top, not buried
    - Tags chips row (read-only display)
    - Body text rendered as pre-wrap or markdown (use marked.js via window.marked if available, else pre-wrap)
    - Clear/back button or clicking a different item deselects

    If selectedLink is null and list has items: show "Select a link to view details" placeholder.

    Use shadcn/ui components (Button, Input, Badge) + Tailwind classes throughout. Follow the two-column split layout: left 1/3, right 2/3, same as MeetingsPage/PeoplePage.

    Export: `export function LinksPage() { ... }`
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain/frontend && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Build succeeds with no TypeScript errors. LinksPage.tsx exports LinksPage function. Component compiles cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full Phase 29 implementation:
    - engine/link_capture.py (fetch_link_metadata)
    - DB url column migration
    - TYPE_TO_DIR["link"] = "links" in capture.py
    - sb_capture_link MCP tool
    - GET /links + GET /links/path Flask routes
    - LinksPage React component with list, detail panel, Visit Link button
    - Links tab in tab bar
  </what-built>
  <how-to-verify>
    1. Rebuild and reinstall: `cd /Users/tuomasleppanen/second-brain/frontend && npm run build && cd .. && uv tool install . --reinstall`
    2. Restart sb-api: `launchctl kickstart -k gui/$(id -u)/com.secondbrain.api`
    3. Open sb-gui (or visit http://localhost:5001/ui)
    4. Verify: "Links" tab is visible in the tab bar
    5. Verify: Clicking Links tab shows LinksPage (empty state message if no links captured yet)
    6. Capture a link via MCP in Claude: `sb_capture_link(url="https://react.dev", notes="React docs")`
    7. Verify: Link appears in the Links page list (title, domain react.dev, date, description)
    8. Click the link row — verify detail panel opens with title, domain, and "Visit Link" button
    9. Click "Visit Link" — verify external browser opens react.dev
    10. Run: `uv run pytest tests/ -q` — full suite green
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
`npm run build` succeeds — no TypeScript errors.
`uv run pytest tests/ -q` — full suite green.
Manual: Links tab visible, link list renders, detail panel opens, Visit Link opens browser.
</verification>

<success_criteria>
- Links tab visible in GUI tab bar
- LinksPage renders list of captured links with title/domain/date/description
- Clicking a link shows detail panel with Visit Link button
- Visit Link button opens the URL in the system browser
- In-page search filters the list
- Empty state shown when no links captured
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/29-add-link-capture/29-04-SUMMARY.md`
</output>
