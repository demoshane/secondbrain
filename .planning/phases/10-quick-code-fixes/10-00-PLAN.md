---
phase: 10-quick-code-fixes
plan: "00"
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/ai.py
  - engine/forget.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "engine/ai.py update_memory() docstring accurately describes ModelRouter routing, not direct ClaudeAdapter"
    - "engine/forget.py forget_person() resolves brain_root at function entry — all path ops are symlink-safe"
    - "All forget tests pass after the .resolve() addition"
  artifacts:
    - path: "engine/ai.py"
      provides: "Corrected update_memory() docstring"
      contains: "Routes through ModelRouter with sensitivity='public'"
    - path: "engine/forget.py"
      provides: "Symlink-safe brain_root canonicalization"
      contains: "brain_root = brain_root.resolve()"
  key_links:
    - from: "engine/forget.py:forget_person"
      to: "DB notes.path rows"
      via: "brain_root.resolve() → canonical path construction"
      pattern: "brain_root\\.resolve\\(\\)"
    - from: "engine/ai.py:update_memory docstring"
      to: "_router.get_adapter implementation"
      via: "docstring must match line 141 behaviour"
      pattern: "Routes through ModelRouter"
---

<objective>
Close two tech-debt items from the v1.5 audit: a stale docstring in engine/ai.py and a missing .resolve() call in engine/forget.py.

Purpose: Prevent silent GDPR failure (forget_person DB DELETE matches zero rows on macOS symlink paths) and eliminate misleading documentation that contradicts Phase 8 routing implementation.
Output: engine/ai.py and engine/forget.py with targeted one-line fixes each; all existing tests remain green.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-quick-code-fixes/10-RESEARCH.md
@.planning/phases/10-quick-code-fixes/10-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix update_memory() docstring in engine/ai.py</name>
  <files>engine/ai.py</files>
  <action>
Replace the stale docstring in update_memory() (lines 122-125). The current text says:

    Always uses ClaudeAdapter regardless of sensitivity — summary is safe
    (no PII, just type + controlled summary).

Replace with:

    Routes through ModelRouter with sensitivity='public' — config drives
    adapter selection (AI-05). Summary must not contain PII.

The rest of the docstring (Error handling line, Args block) is unchanged. The implementation at line 141 already uses `_router.get_adapter("public", config_path)` — the docstring must reflect that. This is a documentation-only change; no logic is altered.
  </action>
  <verify>
    <automated>grep -n "Routes through ModelRouter" /Users/tuomasleppanen/second-brain/engine/ai.py</automated>
  </verify>
  <done>engine/ai.py line 123-124 contains "Routes through ModelRouter with sensitivity='public'" and no longer contains "Always uses ClaudeAdapter"</done>
</task>

<task type="auto">
  <name>Task 2: Add .resolve() to brain_root in engine/forget.py</name>
  <files>engine/forget.py</files>
  <action>
In forget_person(), insert `brain_root = brain_root.resolve()` as the first executable line — immediately after the deferred `import frontmatter` on line 22, before the `person_file` construction on line 24.

Result should be:

    import frontmatter  # deferred: available as python-frontmatter dep
    brain_root = brain_root.resolve()  # canonicalize — symlink-safe (Phase 7 pattern)

    person_file = brain_root / "people" / f"{slug}.md"

This follows the established project pattern (Phase 7 decision: "Use .resolve() not .absolute()"). Resolving at entry means all downstream uses — person_file, meetings_dir, brain_root.rglob(), and path strings passed to DELETE — are canonical, matching the absolute paths stored by capture.py.

Do NOT use .absolute() — it does not follow the /var/ → /private/var/ macOS symlink.
  </action>
  <verify>
    <automated>uv run --no-project --with pytest --with python-frontmatter /Users/tuomasleppanen/second-brain/tests/test_forget.py -x -q</automated>
  </verify>
  <done>All test_forget.py tests pass; engine/forget.py line after `import frontmatter` reads `brain_root = brain_root.resolve()`</done>
</task>

<task type="auto">
  <name>Task 3: Full suite regression check</name>
  <files></files>
  <action>
Run the full test suite to confirm neither edit introduced regressions in other modules. Both changes are isolated (one docstring, one path canonicalization at function entry) but validate globally before marking phase complete.
  </action>
  <verify>
    <automated>uv run --no-project --with pytest --with python-frontmatter /Users/tuomasleppanen/second-brain/tests/ -x -q</automated>
  </verify>
  <done>Full test suite exits 0 with no failures or errors</done>
</task>

</tasks>

<verification>
1. `grep "Routes through ModelRouter" engine/ai.py` returns a match
2. `grep "Always uses ClaudeAdapter" engine/ai.py` returns no match
3. `grep "brain_root = brain_root.resolve()" engine/forget.py` returns a match
4. `uv run --no-project --with pytest --with python-frontmatter tests/test_forget.py -x -q` — all green
5. `uv run --no-project --with pytest --with python-frontmatter tests/ -x -q` — all green
</verification>

<success_criteria>
- engine/ai.py docstring for update_memory() matches Phase 8 implementation (ModelRouter, not direct ClaudeAdapter)
- engine/forget.py forget_person() resolves brain_root at entry before any path construction
- All existing tests remain green — zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/10-quick-code-fixes/10-00-SUMMARY.md`
</output>
