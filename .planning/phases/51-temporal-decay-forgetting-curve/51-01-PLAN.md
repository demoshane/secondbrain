---
phase: 51-temporal-decay
plan: 01
type: execute
wave: 1
depends_on: [50-02]
files_modified:
  - engine/search.py
  - tests/test_search.py
autonomous: true
execution_strategy: direct

must_haves:
  truths:
    - "_relevance_decay replaces _recency_multiplier everywhere"
    - "decay rate varies by note type"
    - "access resets decay clock (uses last_accessed_at from Phase 50)"
    - "person notes have no decay (evergreen)"
    - "existing search tests still pass"
  artifacts:
    - path: "engine/search.py"
      provides: "_relevance_decay with per-type half-lives and access refresh"
      exports: ["_relevance_decay", "TYPE_HALF_LIVES"]
---

<objective>
Replace flat recency boost with dual-axis forgetting curve: age-based decay + access refresh.

Purpose: Notes naturally lose relevance unless reinforced by use. Different types decay
at different rates — decisions persist, meeting notes fade.
Output: `_relevance_decay()` replaces `_recency_multiplier()` in all search modes.
</objective>

<context>
@.planning/phases/51-temporal-decay-forgetting-curve/51-CONTEXT.md
@engine/search.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement _relevance_decay with per-type half-lives</name>
  <files>engine/search.py</files>
  <read_first>engine/search.py (full — find all _recency_multiplier call sites)</read_first>
  <behavior>
    - TYPE_HALF_LIVES dict: meeting=30, note=60, project=120, decision=180, person=None (no decay)
    - Default half-life for unknown types: 60 days
    - Formula: base_decay = exp(-age / type_half_life); access_refresh = exp(-access_gap / 60) if accessed; multiplier = 1.0 + 0.15 * max(base_decay, access_refresh)
    - Person type: always returns 1.15 (max boost, no decay)
    - Falls back to 1.0 on parse error (same safety as current function)
    - _access_boost (from Phase 50) is subsumed into _relevance_decay — single unified multiplier
  </behavior>
  <action>
    1. Add TYPE_HALF_LIVES constant
    2. Replace `_recency_multiplier()` with `_relevance_decay(created_at, note_type, last_accessed_at, access_count)`
    3. Update all call sites (3 places in search.py) to pass note_type and access fields
    4. Remove `_access_boost()` — its logic is now part of `_relevance_decay()`
    5. Write tests: per-type decay rates, access refresh, person evergreen, fallback behavior
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_search.py -x -v</automated>
  </verify>
  <done>_relevance_decay replaces both _recency_multiplier and _access_boost with unified scoring</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_search.py -x -v` passes (all existing + new tests)
- `grep "_recency_multiplier" engine/search.py` returns 0 matches
- `grep "_relevance_decay" engine/search.py` returns 3+ matches (definition + call sites)
</verification>

<success_criteria>
Unified decay function with per-type half-lives and access refresh, all search tests passing.
</success_criteria>
