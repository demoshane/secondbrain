---
phase: 51-temporal-decay
plan: 02
type: execute
wave: 2
depends_on: [51-01]
files_modified:
  - engine/intelligence.py
  - tests/test_intelligence.py
autonomous: true
execution_strategy: direct

must_haves:
  truths:
    - "detect_stale_notes returns graduated relevance bands, not binary fresh/stale"
    - "stale detection uses same _relevance_decay formula as search"
    - "three bands: fresh (>0.8), aging (0.4-0.8), stale (<0.4)"
    - "person notes never flagged as stale"
---

<objective>
Upgrade stale detection from binary 90-day threshold to graduated decay bands.

Purpose: Health checks and recap can now distinguish "slightly aging" from "forgotten",
enabling smarter nudges and prioritized review.
Output: `detect_stale_notes()` returns decay scores and band classifications.
</objective>

<context>
@.planning/phases/51-temporal-decay-forgetting-curve/51-CONTEXT.md
@engine/intelligence.py
@engine/search.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upgrade detect_stale_notes to graduated bands</name>
  <files>engine/intelligence.py</files>
  <read_first>engine/intelligence.py (detect_stale_notes function — full implementation), engine/search.py (_relevance_decay for import)</read_first>
  <behavior>
    - Import `_relevance_decay` from `engine.search`
    - For each non-archived, non-evergreen note: compute decay score via _relevance_decay
    - Classify into bands: "fresh" (>0.8), "aging" (0.4-0.8), "stale" (<0.4)
    - Return list of dicts: {path, title, type, decay_score, band, last_accessed_at, days_since_update}
    - Default: return "aging" + "stale" notes only (fresh filtered out)
    - Person notes: excluded (always evergreen)
    - Snooze logic preserved: snoozed notes excluded until snooze expires
  </behavior>
  <action>
    1. Refactor `detect_stale_notes()` to use `_relevance_decay()` for scoring
    2. Add band classification to return values
    3. Update callers if any (check sb_recap, brain_health)
    4. Write tests: fresh note not returned, aging note in "aging" band, old untouched note in "stale" band, person excluded
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_intelligence.py -x -v -k "stale"</automated>
  </verify>
  <done>Stale detection uses graduated decay bands with per-type scoring</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_intelligence.py -x -v -k "stale"` passes
- detect_stale_notes returns objects with band and decay_score fields
</verification>

<success_criteria>
Graduated stale detection using unified decay formula with three relevance bands.
</success_criteria>
