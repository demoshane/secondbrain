---
phase: 35-brain-consolidation
plan: 03
type: execute
wave: 3
depends_on: ["35-02"]
files_modified:
  - engine/db.py
  - engine/brain_health.py
  - engine/mcp_server.py
  - engine/consolidate.py
  - scripts/install_native.py
  - pyproject.toml
  - tests/test_brain_health.py
  - tests/test_consolidate.py
  - tests/test_install_native.py
autonomous: true
execution_strategy: direct
requirements:
  - CONS-04
  - CONS-05

must_haves:
  truths:
    - "health_snapshots table created by init_schema migration"
    - "take_health_snapshot inserts a row with score, counts, and snapped_at date"
    - "Duplicate snapshots for the same day are skipped (one-per-day guard)"
    - "Snapshots older than 90 days are cleaned up"
    - "sb_health_trend MCP tool returns snapshot time series"
    - "sb-consolidate launchd plist fires daily at 03:00"
    - "Consolidation job runs: archive actions, delete dangling rels, take snapshot, clean old snapshots"
  artifacts:
    - path: "engine/db.py"
      provides: "health_snapshots migration"
      contains: "def migrate_add_health_snapshots_table("
    - path: "engine/brain_health.py"
      provides: "take_health_snapshot, cleanup_old_snapshots"
      contains: "def take_health_snapshot("
    - path: "engine/consolidate.py"
      provides: "consolidate_main entry point for sb-consolidate"
      contains: "def consolidate_main("
    - path: "scripts/install_native.py"
      provides: "write_consolidate_plist function"
      contains: "def write_consolidate_plist("
    - path: "pyproject.toml"
      provides: "sb-consolidate entry point"
      contains: "sb-consolidate"
    - path: "tests/test_consolidate.py"
      provides: "consolidation integration tests"
      contains: "def test_consolidate_main_runs_clean"
    - path: "tests/test_install_native.py"
      provides: "plist generation test for consolidate"
      contains: "def test_write_consolidate_plist"
  key_links:
    - from: "engine/consolidate.py"
      to: "engine/brain_health.py"
      via: "import archive_old_action_items, delete_dangling_relationships, take_health_snapshot, cleanup_old_snapshots"
      pattern: "from engine.brain_health import"
    - from: "engine/db.py"
      to: "engine/brain_health.py"
      via: "init_schema calls migrate_add_health_snapshots_table"
      pattern: "migrate_add_health_snapshots_table"
    - from: "scripts/install_native.py"
      to: "engine/consolidate.py"
      via: "plist ProgramArguments references sb-consolidate"
      pattern: "sb-consolidate"
---

<objective>
Implement health trend tracking (health_snapshots table + snapshot/cleanup functions + MCP tool) and scheduled consolidation (new engine/consolidate.py entry point + launchd plist + install script integration).

Purpose: Per D-10 through D-18 — track brain health over time and automate safe daily cleanup.
Output: DB migration, snapshot functions, sb_health_trend MCP tool, sb-consolidate CLI entry point, launchd plist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-brain-consolidation/35-CONTEXT.md
@.planning/phases/35-brain-consolidation/35-RESEARCH.md
@.planning/phases/35-brain-consolidation/35-01-SUMMARY.md
@.planning/phases/35-brain-consolidation/35-02-SUMMARY.md
@.claude/LEARNINGS.md

<interfaces>
<!-- From Plans 01 + 02 -->
From engine/brain_health.py (after Plan 02):
```python
def archive_old_action_items(conn, days=90) -> int:
def delete_dangling_relationships(conn) -> int:
def get_stub_notes(conn, word_limit=50) -> list[dict]:
def get_bidirectional_gaps(conn) -> list[dict]:
def get_duplicate_candidates(conn, threshold=0.92) -> list[dict]:
def get_orphan_notes(conn) -> list[dict]:
def get_missing_file_notes(conn) -> list[dict]:
def compute_health_score(total_notes, orphans, broken, duplicates) -> int:
```

From engine/db.py (migration pattern):
```python
def migrate_add_health_snapshots_table(conn: sqlite3.Connection) -> None:
    # Called from init_schema() — idempotent CREATE TABLE IF NOT EXISTS
```

From scripts/install_native.py:
```python
def write_digest_plist(plist_dir, log_dir, repo_root=REPO_ROOT) -> Path:
    # Model for write_consolidate_plist — same structure, different schedule
```

From pyproject.toml [project.scripts]:
```
sb-digest = "engine.digest:digest_main"
# Pattern: module:function_name
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: health_snapshots migration + snapshot/cleanup functions + tests</name>
  <files>engine/db.py, engine/brain_health.py, tests/test_brain_health.py</files>
  <read_first>
    - engine/db.py (full file — find init_schema() and existing migration functions to follow pattern)
    - engine/brain_health.py (current state after Plan 02 — extend with snapshot functions)
    - tests/test_brain_health.py (existing fixtures — reuse archive_conn pattern)
    - .claude/LEARNINGS.md
  </read_first>
  <behavior>
    - test_health_snapshots_migration: After init_schema, health_snapshots table exists (SELECT name FROM sqlite_master WHERE name='health_snapshots').
    - test_take_health_snapshot: Call take_health_snapshot(). Row inserted with snapped_at = today's date, score is int, counts are ints.
    - test_take_health_snapshot_skips_duplicate_day: Call take_health_snapshot() twice. Only 1 row in health_snapshots (one-per-day guard per Pitfall 6).
    - test_cleanup_old_snapshots: Insert snapshot with snapped_at 100 days ago. cleanup_old_snapshots(conn, days=90) deletes it. Return count = 1.
    - test_cleanup_old_snapshots_keeps_recent: Insert snapshot with snapped_at 30 days ago. cleanup_old_snapshots(conn, days=90) keeps it. Return count = 0.
  </behavior>
  <action>
    **DB migration (engine/db.py):**

    Add function:
    ```python
    def migrate_add_health_snapshots_table(conn: sqlite3.Connection) -> None:
        """Idempotent migration: create health_snapshots table if absent."""
        conn.execute("""
            CREATE TABLE IF NOT EXISTS health_snapshots (
                id              INTEGER PRIMARY KEY,
                snapped_at      TEXT NOT NULL,
                score           INTEGER,
                total_notes     INTEGER,
                orphan_count    INTEGER,
                broken_count    INTEGER,
                duplicate_count INTEGER,
                stub_count      INTEGER
            )
        """)
        conn.commit()
    ```

    Add call to `init_schema()` after the last existing migration call:
    ```python
    migrate_add_health_snapshots_table(conn)
    ```

    **Snapshot functions (engine/brain_health.py):**

    Add `take_health_snapshot(conn: sqlite3.Connection) -> dict`:
    ```python
    def take_health_snapshot(conn: sqlite3.Connection) -> dict:
        """Take a health snapshot for today. Skips if one already exists for today (one-per-day guard)."""
        import datetime
        today = datetime.date.today().isoformat()
        existing = conn.execute(
            "SELECT id FROM health_snapshots WHERE date(snapped_at) = ?", (today,)
        ).fetchone()
        if existing:
            return {"skipped": True, "reason": "snapshot_exists_today"}

        total = conn.execute("SELECT COUNT(*) FROM notes").fetchone()[0]
        orphans = get_orphan_notes(conn)
        broken = get_missing_file_notes(conn)
        duplicates = get_duplicate_candidates(conn)
        stubs = get_stub_notes(conn)
        score = compute_health_score(total, len(orphans), len(broken), len(duplicates))

        conn.execute("""
            INSERT INTO health_snapshots (snapped_at, score, total_notes, orphan_count, broken_count, duplicate_count, stub_count)
            VALUES (?, ?, ?, ?, ?, ?, ?)
        """, (today, score, total, len(orphans), len(broken), len(duplicates), len(stubs)))
        conn.commit()
        return {"skipped": False, "score": score, "total_notes": total,
                "orphan_count": len(orphans), "broken_count": len(broken),
                "duplicate_count": len(duplicates), "stub_count": len(stubs)}
    ```

    Add `cleanup_old_snapshots(conn: sqlite3.Connection, days: int = 90) -> int`:
    ```python
    def cleanup_old_snapshots(conn: sqlite3.Connection, days: int = 90) -> int:
        """Delete health_snapshots older than `days` days. Returns deleted count."""
        result = conn.execute(
            "DELETE FROM health_snapshots WHERE snapped_at < date('now', ?)",
            (f"-{days} days",),
        )
        conn.commit()
        return result.rowcount
    ```

    Tests use the `archive_conn` fixture pattern.
  </action>
  <verify>
    <automated>uv run pytest tests/test_brain_health.py::test_health_snapshots_migration tests/test_brain_health.py::test_take_health_snapshot tests/test_brain_health.py::test_take_health_snapshot_skips_duplicate_day tests/test_brain_health.py::test_cleanup_old_snapshots tests/test_brain_health.py::test_cleanup_old_snapshots_keeps_recent -x -q</automated>
  </verify>
  <acceptance_criteria>
    - engine/db.py contains `def migrate_add_health_snapshots_table(`
    - engine/db.py init_schema() calls `migrate_add_health_snapshots_table(conn)`
    - engine/brain_health.py contains `def take_health_snapshot(conn: sqlite3.Connection) -> dict:`
    - engine/brain_health.py contains `def cleanup_old_snapshots(conn: sqlite3.Connection, days: int = 90) -> int:`
    - engine/brain_health.py take_health_snapshot checks `WHERE date(snapped_at) = ?` before inserting
    - tests/test_brain_health.py contains `def test_health_snapshots_migration`
    - tests/test_brain_health.py contains `def test_take_health_snapshot_skips_duplicate_day`
    - All 5 snapshot tests pass (exit code 0)
  </acceptance_criteria>
  <done>health_snapshots table migrated, take_health_snapshot with one-per-day guard, cleanup_old_snapshots with 90-day retention. All tests green.</done>
</task>

<task type="auto">
  <name>Task 2: sb_health_trend MCP + consolidate entry point + launchd plist + install + tests</name>
  <files>engine/mcp_server.py, engine/consolidate.py, scripts/install_native.py, pyproject.toml, tests/test_consolidate.py, tests/test_install_native.py</files>
  <read_first>
    - engine/mcp_server.py (tool registration pattern, existing imports)
    - scripts/install_native.py (write_digest_plist as model — line 108-130; main() launchd section — line 196-200)
    - pyproject.toml (project.scripts section — line 27-46)
    - engine/brain_health.py (take_health_snapshot, cleanup_old_snapshots, archive_old_action_items, delete_dangling_relationships signatures)
    - tests/test_install_native.py (existing structure — extend with write_consolidate_plist test)
    - .claude/LEARNINGS.md
  </read_first>
  <action>
    **MCP tool (engine/mcp_server.py):**

    Add `sb_health_trend` tool (per D-13):
    ```python
    @mcp.tool()
    def sb_health_trend(days: int = 30) -> dict:
        """Return health snapshots as a time series for the last N days."""
        conn = get_connection()
        try:
            rows = conn.execute("""
                SELECT snapped_at, score, total_notes, orphan_count, broken_count, duplicate_count, stub_count
                FROM health_snapshots
                WHERE snapped_at >= date('now', ?)
                ORDER BY snapped_at ASC
            """, (f"-{days} days",)).fetchall()
            snapshots = [
                {"snapped_at": r[0], "score": r[1], "total_notes": r[2],
                 "orphan_count": r[3], "broken_count": r[4],
                 "duplicate_count": r[5], "stub_count": r[6]}
                for r in rows
            ]
            return {"snapshots": snapshots, "count": len(snapshots), "days": days}
        finally:
            conn.close()
    ```

    **Consolidation entry point (engine/consolidate.py) — NEW FILE:**

    Per D-14 through D-17:
    ```python
    """Scheduled consolidation job for brain hygiene.

    Runs safe, idempotent cleanup operations. Does NOT auto-merge or auto-enrich.
    Entry point: sb-consolidate (launchd daily at 03:00).
    """
    import datetime
    import json
    import logging

    logger = logging.getLogger(__name__)

    def consolidate_main() -> None:
        """Entry point for sb-consolidate launchd job. Per D-16 order."""
        from engine.db import get_connection, init_schema
        from engine.brain_health import (
            archive_old_action_items,
            delete_dangling_relationships,
            take_health_snapshot,
            cleanup_old_snapshots,
        )

        conn = get_connection()
        init_schema(conn)
        results = {}
        try:
            results["archived_actions"] = archive_old_action_items(conn)
            results["deleted_dangling"] = delete_dangling_relationships(conn)
            results["snapshot"] = take_health_snapshot(conn)
            results["cleaned_old_snapshots"] = cleanup_old_snapshots(conn)
        finally:
            conn.close()

        # Log to stdout — captured by launchd StandardOutPath
        print(json.dumps({"at": datetime.datetime.utcnow().isoformat(), **results}))
    ```

    **launchd plist (scripts/install_native.py):**

    Add `write_consolidate_plist` function after `write_digest_plist` (around line 131):
    ```python
    def write_consolidate_plist(plist_dir: Path, log_dir: Path, repo_root: Path = REPO_ROOT) -> Path:
        """Generate and write the launchd plist for sb-consolidate (daily 03:00 trigger).

        Creates com.secondbrain.consolidate.plist in plist_dir using plistlib.dump.
        Returns the path to the written plist file.
        """
        label = "com.secondbrain.consolidate"
        plist = {
            "Label": label,
            "ProgramArguments": ["/usr/bin/env", "uv", "run", "--directory", str(repo_root), "sb-consolidate"],
            "WorkingDirectory": str(Path.home() / "SecondBrain"),
            "EnvironmentVariables": {
                "PATH": f"{Path.home()}/.local/bin:/usr/local/bin:/usr/bin:/bin",
                "HOME": str(Path.home()),
            },
            "StartCalendarInterval": {"Hour": 3, "Minute": 0},
            "StandardOutPath": str(log_dir / "second-brain-consolidate.log"),
            "StandardErrorPath": str(log_dir / "second-brain-consolidate-error.log"),
        }
        out_path = plist_dir / f"{label}.plist"
        with open(out_path, "wb") as f:
            plistlib.dump(plist, f)
        return out_path
    ```

    In `main()`, inside the `if args.launchd or run_all:` block (after digest plist load, around line 198), add:
    ```python
    consolidate_plist = write_consolidate_plist(plist_dir, log_dir)
    load_launchd_agent(consolidate_plist, "com.secondbrain.consolidate")
    ```

    **pyproject.toml entry point:**

    Add to `[project.scripts]` section:
    ```
    sb-consolidate  = "engine.consolidate:consolidate_main"
    ```

    **Consolidation tests (tests/test_consolidate.py) — NEW FILE:**

    ```python
    """Tests for engine/consolidate.py — scheduled consolidation job."""
    import sqlite3
    import pytest
    from engine.db import init_schema

    @pytest.fixture
    def cons_conn(tmp_path):
        import engine.db as _db
        import engine.paths as _paths
        db_path = tmp_path / "consolidate_test.db"
        _db.DB_PATH = db_path
        _paths.DB_PATH = db_path
        conn = sqlite3.connect(str(db_path))
        init_schema(conn)
        yield conn
        conn.close()

    def test_consolidate_main_runs_clean(cons_conn, capsys):
        from engine.consolidate import consolidate_main
        consolidate_main()
        captured = capsys.readouterr()
        import json
        result = json.loads(captured.out)
        assert "archived_actions" in result
        assert "deleted_dangling" in result
        assert "snapshot" in result
        assert "cleaned_old_snapshots" in result

    def test_consolidate_idempotent(cons_conn, capsys):
        from engine.consolidate import consolidate_main
        consolidate_main()
        consolidate_main()  # second run should not error
        # Snapshot should be skipped on second run (one-per-day guard)
        lines = capsys.readouterr().out.strip().split("\n")
        import json
        second = json.loads(lines[1])
        assert second["snapshot"]["skipped"] is True
    ```

    **Plist test (tests/test_install_native.py) — EXTEND existing file:**

    Add `test_write_consolidate_plist` to the existing `tests/test_install_native.py` file:
    ```python
    def test_write_consolidate_plist(tmp_path):
        from scripts.install_native import write_consolidate_plist
        import plistlib
        log_dir = tmp_path / "logs"
        log_dir.mkdir()
        plist_path = write_consolidate_plist(tmp_path, log_dir)
        assert plist_path.exists()
        with open(plist_path, "rb") as f:
            data = plistlib.load(f)
        assert data["Label"] == "com.secondbrain.consolidate"
        assert data["StartCalendarInterval"] == {"Hour": 3, "Minute": 0}
        assert "sb-consolidate" in data["ProgramArguments"]
    ```
  </action>
  <verify>
    <automated>uv run pytest tests/test_consolidate.py tests/test_brain_health.py tests/test_install_native.py::test_write_consolidate_plist -x -q</automated>
  </verify>
  <acceptance_criteria>
    - engine/mcp_server.py contains `def sb_health_trend(`
    - engine/consolidate.py exists and contains `def consolidate_main() -> None:`
    - engine/consolidate.py imports `archive_old_action_items, delete_dangling_relationships, take_health_snapshot, cleanup_old_snapshots`
    - scripts/install_native.py contains `def write_consolidate_plist(`
    - scripts/install_native.py write_consolidate_plist contains `"StartCalendarInterval": {"Hour": 3, "Minute": 0}`
    - scripts/install_native.py main() contains `write_consolidate_plist(`
    - pyproject.toml contains `sb-consolidate`
    - tests/test_consolidate.py exists and contains `def test_consolidate_main_runs_clean`
    - tests/test_install_native.py contains `def test_write_consolidate_plist`
    - All tests pass (exit code 0)
  </acceptance_criteria>
  <done>sb_health_trend returns snapshot time series (D-13). consolidate_main runs D-16 steps in order. launchd plist fires daily at 03:00 (D-14, D-15). Installed via install_native.py (D-18). No auto-merge/enrich (D-17). All tests green.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_consolidate.py tests/test_brain_health.py tests/test_install_native.py::test_write_consolidate_plist -x -q` — all snapshot + consolidation + plist tests green
- `uv run pytest tests/ -q` — full suite green
- `grep "health_snapshots" engine/db.py` returns migration
- `grep "def take_health_snapshot\|def cleanup_old_snapshots" engine/brain_health.py` returns 2 matches
- `grep "sb_health_trend" engine/mcp_server.py` returns match
- `grep "sb-consolidate" pyproject.toml` returns match
- `grep "write_consolidate_plist" scripts/install_native.py` returns match
</verification>

<success_criteria>
- health_snapshots table created by init_schema migration (D-10)
- Snapshots taken only by scheduled job, not sb-health calls (D-11)
- 90-day retention with cleanup (D-12)
- sb_health_trend returns full snapshot objects as time series (D-13)
- com.secondbrain.consolidate plist at 03:00 daily (D-14, D-15)
- Job runs archive → dangling delete → snapshot → cleanup in order (D-16)
- No auto-merge or auto-enrich in job (D-17)
- Installed via install_native.py (D-18)
</success_criteria>

<output>
After completion, create `.planning/phases/35-brain-consolidation/35-03-SUMMARY.md`
</output>
