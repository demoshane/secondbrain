---
phase: 27.6-projects-page
verified: 2026-03-18T00:00:00Z
status: human_needed
score: 9/10 must-haves verified
human_verification:
  - test: "Start sb-gui, click the Projects tab, click a real project row, inspect detail panel"
    expected: "Projects tab appears after Meetings; clicking a row renders note body as formatted HTML, Action Items section visible, Related Notes section visible; layout matches People/Meetings style"
    why_human: "Visual correctness, react-markdown rendering quality, and split-panel layout fidelity cannot be verified programmatically"
---

# Phase 27.6: Projects Page Verification Report

**Phase Goal:** Add a Projects page to the GUI that lists project notes and shows detail view
**Verified:** 2026-03-18
**Status:** human_needed — all automated checks pass; one human visual checkpoint outstanding (Plan 03 Task 2 was not signed off)
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | GET /projects returns a JSON list of project notes with path, title, updated_at, open_actions | VERIFIED | `engine/api.py` line 322-334: `list_projects()` with SQL `WHERE n.type = 'projects'`, returns `{projects: [...]}` |
| 2 | GET /projects/`<path>` returns project body, updated_at, open_actions; 403 on path traversal | VERIFIED | `engine/api.py` line 335-357: `get_project()` calls `_resolve_note_path()`, returns 403 on ValueError, 404 on missing |
| 3 | Both endpoints return correct data when queried against a seeded test database | VERIFIED | `tests/test_projects.py` has 6 tests (empty list, populated, open_actions count, detail, 404, 403/404); Plan 01 SUMMARY reports 6 passed |
| 4 | A Projects tab appears in the tab bar and is clickable | VERIFIED | `TabBar.tsx` line 10: `{ id: 'projects' as const, label: 'Projects' }`; data-testid="tab-projects" follows TABS pattern |
| 5 | Clicking Projects tab renders a list of project notes | VERIFIED | `App.tsx` imports `ProjectsPage` (line 10) and renders it in final else branch (line 78); `ProjectsPage.tsx` fetches `/projects` in useEffect (line 46) |
| 6 | Clicking a project row reveals a detail panel with note body and action items section | VERIFIED | `ProjectsPage.tsx` line 147: `data-testid="project-actions-section"`; `data-testid="project-backlinks-section"` line 169; fetch on select (lines 57-59) |
| 7 | npm run build succeeds with no TypeScript errors | VERIFIED | Plan 02 SUMMARY: "npm run build exits 0, no TypeScript errors, 2115 modules transformed"; bundle `index-CTc5Q8CC.js` + `index-CvadWe7j.css` present in `engine/gui/static/assets/` |
| 8 | Playwright test confirms Projects tab is visible and clickable | VERIFIED | `tests/test_gui.py` line 333: `test_projects_tab` exists and passed (Plan 03 SUMMARY: 352 passed) |
| 9 | Playwright test confirms clicking a project row reveals the detail panel | VERIFIED | `tests/test_gui.py` line 343: `test_projects_page_row_click` waits for `project-actions-section`; conftest.py line 242 seeds `test-project.md` |
| 10 | Human verifies Projects page renders correctly in live GUI | NEEDS HUMAN | Plan 03 Task 2 (human checkpoint) status: "awaiting user approval" — never signed off |

**Score:** 9/10 truths verified (automated); 1 pending human checkpoint

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `tests/test_projects.py` | Unit tests for list and detail endpoints | VERIFIED | Exists; 6 test functions (lines 51, 61, 81, 102, 122, 132) |
| `engine/api.py` | GET /projects and GET /projects/`<path>` routes | VERIFIED | Routes at lines 322 and 335; `def list_projects` and `def get_project` present |
| `frontend/src/components/ProjectsPage.tsx` | Projects list + detail view React component, min 80 lines | VERIFIED | 186 lines; data-testid attributes, fetch calls, action/backlinks sections all present |
| `frontend/src/types.ts` | ProjectSummary interface | VERIFIED | `export interface ProjectSummary` found at line 49 |
| `frontend/src/contexts/UIContext.tsx` | View union extended with 'projects' | VERIFIED | Line 3: `type View = 'notes' \| 'actions' \| 'people' \| 'meetings' \| 'projects'` |
| `frontend/src/components/TabBar.tsx` | Projects tab entry with data-testid="tab-projects" | VERIFIED | Line 10: `{ id: 'projects' as const, label: 'Projects' }` |
| `tests/conftest.py` | gui_brain fixture with project seed note | VERIFIED | Lines 242-253: `test-project.md` seeded with `type=projects` using conn4 pattern |
| `tests/test_gui.py` | test_projects_tab and test_projects_page_row_click | VERIFIED | Lines 333 and 343; both functions present |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `tests/test_projects.py` | `engine/api.py` | Flask test client GET /projects | VERIFIED | test functions use `client.get("/projects")` pattern (Plan 01 SUMMARY confirms 6 passed) |
| `engine/api.py list_projects` | notes table | SQL `WHERE type = 'projects'` | VERIFIED | Line 329: `"FROM notes n WHERE n.type = 'projects' ORDER BY n.updated_at DESC"` |
| `frontend/src/App.tsx` | `ProjectsPage.tsx` | import + currentView === 'projects' branch | VERIFIED | Line 10 import; line 78 renders `<ProjectsPage />` in final else |
| `frontend/src/components/ProjectsPage.tsx` | GET /projects | fetch in useEffect | VERIFIED | Line 46: `fetch(\`${getAPI()}/projects\`)` |
| `frontend/src/components/ProjectsPage.tsx` | GET /projects/`<enc>` | fetch on row select | VERIFIED | Line 57: `fetch(\`${getAPI()}/projects/${enc}\`)` |
| `tests/test_gui.py test_projects_page_row_click` | data-testid='project-actions-section' | Playwright wait_for visible | VERIFIED | Line 343+ waits for `project-actions-section`; ProjectsPage.tsx line 147 has the attribute |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| 27.6-PROJ-01 | 27.6-01 | GET /projects list endpoint | SATISFIED | `list_projects()` route at api.py:322; SQL uses type='projects'; returns path, title, updated_at, open_actions |
| 27.6-PROJ-02 | 27.6-01 | GET /projects/`<path>` detail endpoint with path-traversal guard | SATISFIED | `get_project()` route at api.py:335; `_resolve_note_path()` guard; 403 on traversal, 404 on missing |
| 27.6-PROJ-03 | 27.6-02 | Projects page React component wired into tab bar | SATISFIED | ProjectsPage.tsx (186 lines); TabBar, UIContext, App.tsx all extended; npm build exits 0 |
| 27.6-PROJ-04 | 27.6-03 | Playwright integration tests for Projects page | SATISFIED (automated) | test_projects_tab + test_projects_page_row_click in test_gui.py; gui_brain seed in conftest.py; 352 passed. Human visual sign-off outstanding. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `frontend/src/components/ProjectsPage.tsx` | 88 | `placeholder="Filter by title..."` | Info | HTML input placeholder attribute — not a code stub, no impact |

No blockers or warnings found.

### Human Verification Required

#### 1. Visual correctness of Projects page in live GUI

**Test:** Start the GUI (`cd /Users/tuomasleppanen/second-brain && uv run sb-gui`), navigate to `http://localhost:37491/ui`, click the "Projects" tab, click any project row.

**Expected:**
- Projects tab appears in tab bar after Meetings
- List shows real project notes from ~/SecondBrain/projects/
- Clicking a row opens a right-panel detail view with note body rendered as formatted HTML (not raw markdown text)
- "Action Items" section is visible in the detail panel
- "Related Notes" section is visible in the detail panel
- Layout matches the People/Meetings page split-panel style

**Why human:** React-markdown rendering quality, actual visual layout fidelity, and spacing/typography matching the People/Meetings pattern cannot be verified via code inspection or Playwright DOM assertions alone. This is Plan 03 Task 2 which was left at "awaiting user approval" in the SUMMARY.

### Gaps Summary

No functional gaps. All 10 automated truths are verified. The one outstanding item is the blocking human checkpoint from Plan 03 Task 2 — the executor left it at "awaiting user approval" and the phase was never formally signed off with a human visual review. All code, tests, and build artifacts are in place and verified.

---

_Verified: 2026-03-18_
_Verifier: Claude (gsd-verifier)_
