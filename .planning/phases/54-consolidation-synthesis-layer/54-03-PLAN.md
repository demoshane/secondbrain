---
phase: 54-consolidation-synthesis
plan: 03
type: execute
wave: 3
depends_on: [54-02]
files_modified:
  - engine/mcp_server.py
autonomous: true
execution_strategy: direct

must_haves:
  truths:
    - "sb_insights MCP tool exists and returns recent syntheses"
    - "sb_insights includes contradiction detection results"
    - "tool returns useful data even with zero synthesis notes"
---

<objective>
Expose synthesis insights via MCP so Claude can surface consolidated knowledge.

Purpose: Claude can ask "what patterns emerged recently?" and get synthesized insights
rather than raw note lists.
Output: `sb_insights` MCP tool.
</objective>

<context>
@.planning/phases/54-consolidation-synthesis-layer/54-CONTEXT.md
@engine/mcp_server.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sb_insights MCP tool</name>
  <files>engine/mcp_server.py</files>
  <read_first>engine/mcp_server.py (tool registration pattern)</read_first>
  <action>
    1. Add `sb_insights` tool:
       - Params: `days: int = 7`
       - Query notes WHERE type='synthesis' AND created_at within last N days
       - For each synthesis: return title, summary (first 500 chars), source_notes, tags, people
       - Also query for potential contradictions: notes in same cluster with conflicting
         deadline/status fields (simple heuristic — different dates for same entity tag)
       - Returns: {"syntheses": [...], "contradictions": [...], "period_days": N}
       - If no syntheses exist: return helpful message ("No synthesis notes yet. They are
         generated nightly from clusters of 3+ related notes.")
    2. Docstring: "Surface recent insights from the brain's nightly synthesis process.
       Shows consolidated knowledge, patterns, and contradictions detected across related notes.
       Call this for a high-level view of recent activity patterns."
    3. Log to audit_log with event_type="mcp_insights"
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_api.py -x -v -k "insights"</automated>
  </verify>
  <done>sb_insights tool surfaces synthesis notes and contradictions via MCP</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_api.py -x -v -k "insights"` passes
- sb_insights returns structured data with syntheses and contradictions
</verification>

<success_criteria>
Synthesis insights accessible via MCP for Claude to surface consolidated knowledge.
</success_criteria>
