---
phase: 54-consolidation-synthesis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/consolidate.py
  - engine/intelligence.py
  - tests/test_consolidate.py
autonomous: true
execution_strategy: direct

must_haves:
  truths:
    - "cluster_recent_notes returns groups of 3+ notes sharing person/tag within 7 days"
    - "clustering uses note_people and note_tags junction tables"
    - "clusters are deduplicated (a note appears in at most one cluster)"
  artifacts:
    - path: "engine/intelligence.py"
      provides: "cluster_recent_notes() function"
      exports: ["cluster_recent_notes"]
---

<objective>
Build the clustering engine that identifies groups of related recent notes for synthesis.

Purpose: Foundation for nightly synthesis — first detect which note clusters deserve
a synthesis note before generating anything.
Output: `cluster_recent_notes()` in intelligence.py.
</objective>

<context>
@.planning/phases/54-consolidation-synthesis-layer/54-CONTEXT.md
@engine/intelligence.py
@engine/db.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement cluster_recent_notes</name>
  <files>engine/intelligence.py</files>
  <read_first>engine/intelligence.py (existing clustering/grouping logic if any), engine/db.py (note_people, note_tags junction tables)</read_first>
  <behavior>
    - `cluster_recent_notes(conn, window_days: int = 7, min_cluster_size: int = 3) -> list[dict]`
    - Step 1: Find notes created/updated in last window_days
    - Step 2: Group by shared people (via note_people junction) — notes sharing any person form a candidate cluster
    - Step 3: Group by shared tags (via note_tags junction) — notes sharing any tag form a candidate cluster
    - Step 4: Merge overlapping clusters (if note appears in both person-cluster and tag-cluster with >50% overlap, merge)
    - Step 5: Filter clusters below min_cluster_size
    - Returns: [{"cluster_id", "topic" (inferred), "notes": [paths], "shared_people": [...], "shared_tags": [...]}]
    - Exclude synthesis-type notes from clustering (don't cluster the clusters)
  </behavior>
  <action>
    1. Implement cluster_recent_notes following the 5-step pipeline
    2. Topic inference: use most common shared tag, or person name if person-driven cluster
    3. Write tests: no recent notes → empty, 3 meeting notes about same person → 1 cluster,
       notes sharing tag but not person → tag-based cluster, below threshold → excluded
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_intelligence.py -x -v -k "cluster"</automated>
  </verify>
  <done>Clustering identifies groups of related recent notes using junction tables</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_intelligence.py -x -v -k "cluster"` passes
- Clusters correctly identified from junction table relationships
</verification>

<success_criteria>
Clustering engine identifies note groups by shared people/tags within time window.
</success_criteria>
