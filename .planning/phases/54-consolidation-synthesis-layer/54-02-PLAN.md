---
phase: 54-consolidation-synthesis
plan: 02
type: execute
wave: 2
depends_on: [54-01]
files_modified:
  - engine/consolidate.py
  - engine/capture.py
  - engine/paths.py
autonomous: true
execution_strategy: direct

must_haves:
  truths:
    - "synthesize_clusters generates synthesis notes for each cluster"
    - "synthesis notes are type=synthesis with source_notes in frontmatter"
    - "synthesis notes stored in syntheses/ subfolder"
    - "dedup: skip clusters where synthesis already exists for same source_notes within 7 days"
    - "consolidate_main calls synthesize_clusters after hygiene steps"
  artifacts:
    - path: "engine/consolidate.py"
      provides: "synthesize_clusters() integrated into nightly job"
      exports: ["synthesize_clusters"]
---

<objective>
Generate synthesis notes from clusters and wire into nightly consolidation job.

Purpose: The brain now "sleeps" — nightly processing compresses clusters of related
notes into synthesis notes that capture trajectory, decisions, and open questions.
Output: `synthesize_clusters()` called from `consolidate_main()`.
</objective>

<context>
@.planning/phases/54-consolidation-synthesis-layer/54-CONTEXT.md
@engine/consolidate.py
@engine/capture.py
@engine/paths.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add syntheses/ to brain subdirectories</name>
  <files>engine/paths.py</files>
  <read_first>engine/paths.py (BRAIN_SUBDIRS list)</read_first>
  <action>
    1. Add "syntheses" to BRAIN_SUBDIRS list
    2. Add "synthesis" → "syntheses" mapping to TYPE_TO_DIR if it exists
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run python -c "from engine.paths import BRAIN_SUBDIRS; assert 'syntheses' in BRAIN_SUBDIRS"</automated>
  </verify>
  <done>syntheses/ recognized as valid brain subdirectory</done>
</task>

<task type="auto">
  <name>Task 2: Implement synthesize_clusters</name>
  <files>engine/consolidate.py</files>
  <read_first>engine/consolidate.py (full), engine/intelligence.py (generate_weekly_synthesis, generate_recap_on_demand for AI pattern), engine/capture.py (write_note_atomic or build_post for note creation)</read_first>
  <action>
    1. Add `synthesize_clusters(conn) -> dict` function:
       - Call `cluster_recent_notes(conn)` to get clusters
       - For each cluster, check if synthesis already exists (query notes WHERE type='synthesis'
         AND JSON source_notes overlaps significantly with cluster notes, created in last 7 days)
       - For new clusters: generate synthesis using existing AI adapter pattern (Ollama)
         - Prompt: "Synthesize these {N} notes about {topic}. Include: summary, key decisions,
           open questions, aggregated action items."
         - Input: concatenated note bodies from cluster
       - Write synthesis note via capture pipeline (type="synthesis", tags include "auto-synthesized")
       - Return: {"clusters_found": N, "syntheses_created": N, "skipped_existing": N}
    2. Call `synthesize_clusters(conn)` from `consolidate_main()` after existing hygiene steps
    3. Wrap in try/except — synthesis failure must not break hygiene
    4. Log results to stdout (launchd captures)
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_consolidate.py -x -v -k "synthesize"</automated>
  </verify>
  <done>Nightly consolidation generates synthesis notes for qualifying clusters</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_consolidate.py -x -v -k "synthesize"` passes
- Synthesis notes created with correct frontmatter (type, source_notes, tags)
- Duplicate clusters skipped
</verification>

<success_criteria>
Nightly synthesis generates condensed notes from note clusters, integrated into consolidation pipeline.
</success_criteria>
