---
phase: 48-backend-code-cleanup
plan: 03
type: execute
wave: 3
depends_on: ["48-02"]
files_modified:
  - engine/api.py
  - engine/api_config.py
autonomous: true
execution_strategy: direct
requirements:
  - F-22

must_haves:
  truths:
    - "Config routes served from a Flask Blueprint in engine/api_config.py"
    - "api.py registers the Blueprint via app.register_blueprint()"
    - "All /config/* and /ui/prefs routes work identically after extraction"
  artifacts:
    - path: "engine/api_config.py"
      provides: "Flask Blueprint with config routes"
      contains: "config_bp = Blueprint"
    - path: "engine/api.py"
      provides: "Blueprint registration"
      contains: "register_blueprint"
  key_links:
    - from: "engine/api.py"
      to: "engine/api_config.py"
      via: "register_blueprint"
      pattern: "register_blueprint.*config_bp"
---

<objective>
Extract config routes from api.py into a Flask Blueprint in engine/api_config.py (F-22).

Purpose: Begin api.py partitioning (2792 lines). Config routes are self-contained with no cross-route dependencies — ideal first extraction target.
Output: New engine/api_config.py with config Blueprint; api.py ~50-100 lines shorter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/48-backend-code-cleanup-dead-routes-utcnow-deprecation-brain-path-consolidation-json-loads-helper-api-py-blueprint-partitioning/48-CONTEXT.md
@.planning/phases/48-backend-code-cleanup-dead-routes-utcnow-deprecation-brain-path-consolidation-json-loads-helper-api-py-blueprint-partitioning/48-02-SUMMARY.md
@engine/api.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract config routes into engine/api_config.py Blueprint</name>
  <files>engine/api_config.py, engine/api.py</files>
  <read_first>engine/api.py (full file — identify all /config/* routes and /ui/prefs routes, their imports, and helper functions they use)</read_first>
  <action>
    **Step 1: Identify routes to extract.**
    Search api.py for all route handlers matching:
    - `@app.get("/config/...")` or `@app.post("/config/...")` or `@app.put("/config/...")`
    - `@app.get("/ui/prefs")` and `@app.put("/ui/prefs")`
    Include any helper functions used ONLY by these routes.

    **Step 2: Create engine/api_config.py.**
    ```python
    """Flask Blueprint for configuration routes (/config/*, /ui/prefs)."""

    from flask import Blueprint, request, jsonify
    # ... other imports needed by the extracted routes

    config_bp = Blueprint("config", __name__)

    # Move each route here, changing @app.get/post/put to @config_bp.get/post/put
    # Keep URL paths identical (no url_prefix — routes already have /config/)
    ```

    Move each identified route handler from api.py to api_config.py. Change `@app.` decorator to `@config_bp.`. Move any imports those routes need. If a helper is shared with non-config routes, import it from api.py or move to a shared location (prefer keeping in api.py and importing).

    **Step 3: Register Blueprint in api.py.**
    Near the top of api.py after `app = Flask(...)`:
    ```python
    from engine.api_config import config_bp
    app.register_blueprint(config_bp)
    ```

    **Step 4: Remove extracted routes from api.py.**
    Delete the route handler functions that were moved to api_config.py.

    **Important:** Do NOT use url_prefix on the Blueprint — the routes already include their full paths (/config/..., /ui/prefs). This avoids any URL changes.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && python -c "from engine.api_config import config_bp; print(f'Blueprint: {config_bp.name}, rules: {len(config_bp.deferred_functions)}')" && grep -c "register_blueprint" engine/api.py && uv run pytest tests/test_api.py -x -q --tb=short -k "config or prefs" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `engine/api_config.py` exists
    - `grep -c "config_bp = Blueprint" engine/api_config.py` returns 1
    - `grep -c "register_blueprint.*config_bp" engine/api.py` returns 1
    - `grep -c '@app.*"/config/' engine/api.py` returns 0 (all moved out)
    - `grep -c '@app.*"/ui/prefs"' engine/api.py` returns 0 (moved out)
    - `grep -c '@config_bp' engine/api_config.py` returns at least 3
    - All config/prefs-related tests pass
    - `wc -l engine/api.py` shows reduction from ~2792
  </acceptance_criteria>
  <done>Config routes live in api_config.py Blueprint; api.py is shorter; all tests pass</done>
</task>

</tasks>

<verification>
- `python -c "from engine.api_config import config_bp; print('OK')"` succeeds
- Config API calls work: `curl http://localhost:37491/config/markers` returns 200
- All test_api.py tests pass
</verification>

<success_criteria>
engine/api_config.py exists with config Blueprint. api.py registers it. All routes work identically. api.py line count reduced.
</success_criteria>

<output>
After completion, create `.planning/phases/48-backend-code-cleanup-dead-routes-utcnow-deprecation-brain-path-consolidation-json-loads-helper-api-py-blueprint-partitioning/48-03-SUMMARY.md`
</output>
