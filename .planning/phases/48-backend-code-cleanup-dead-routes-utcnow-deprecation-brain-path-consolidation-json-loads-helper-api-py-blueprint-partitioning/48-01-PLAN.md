---
phase: 48-backend-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/db.py
  - engine/consolidate.py
  - engine/links.py
autonomous: true
execution_strategy: direct
requirements:
  - F-23
  - F-29
  - F-30
  - F-31

must_haves:
  truths:
    - "_json_list helper exists in engine/db.py and returns list from JSON column or empty list"
    - "_now_utc helper exists in engine/db.py and returns UTC timestamp string"
    - "consolidate.py comment accurately describes load-time deferral, not circular import"
    - "ensure_person_profile writes to canonical person/ directory"
  artifacts:
    - path: "engine/db.py"
      provides: "_json_list and _now_utc helper functions"
      exports: ["_json_list", "_now_utc"]
    - path: "engine/consolidate.py"
      provides: "Corrected lazy import comment"
    - path: "engine/links.py"
      provides: "Verified person path usage"
  key_links:
    - from: "engine/db.py"
      to: "all consumers"
      via: "import _json_list, _now_utc"
      pattern: "from engine\\.db import.*_json_list|from engine\\.db import.*_now_utc"
---

<objective>
Create shared helpers (_json_list, _now_utc) in engine/db.py and fix two small findings (F-23 comment, F-30 path verification).

Purpose: Establish helpers that Plan 02 will use to replace 18x json.loads and 11x datetime idioms across the codebase.
Output: Two new functions in db.py, corrected comment in consolidate.py, verified person path in links.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-backend-code-cleanup-dead-routes-utcnow-deprecation-brain-path-consolidation-json-loads-helper-api-py-blueprint-partitioning/48-CONTEXT.md
@engine/db.py
@engine/consolidate.py
@engine/links.py
@engine/paths.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _json_list and _now_utc helpers to engine/db.py</name>
  <files>engine/db.py</files>
  <read_first>engine/db.py, engine/paths.py, engine/api.py (lines 1-50 for datetime import pattern)</read_first>
  <behavior>
    - _json_list(None) returns []
    - _json_list("") returns []
    - _json_list("[]") returns []
    - _json_list('[\"a\",\"b\"]') returns ["a", "b"]
    - _json_list(already_a_list) returns the list as-is (defensive)
    - _now_utc() returns string matching pattern "YYYY-MM-DDTHH:MM:SSZ"
    - _now_utc() returns a string (not datetime object)
  </behavior>
  <action>
    Add two functions to engine/db.py after the existing imports/helpers:

    ```python
    import json
    import datetime

    def _json_list(col):
        """Parse a JSON column value to a list, defaulting to [] for NULL/empty."""
        if isinstance(col, list):
            return col
        return json.loads(col) if col else []

    def _now_utc() -> str:
        """Return current UTC time as ISO-ish string for DB storage."""
        return datetime.datetime.now(datetime.UTC).replace(tzinfo=None).strftime("%Y-%m-%dT%H:%M:%SZ")
    ```

    Ensure `import json` and `import datetime` are present at the top of db.py (add if missing).
    Write tests in tests/test_db_helpers.py for both functions.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_db_helpers.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def _json_list" engine/db.py` returns exactly 1 match
    - `grep -n "def _now_utc" engine/db.py` returns exactly 1 match
    - `grep -n "import json" engine/db.py` returns at least 1 match
    - `grep -n "import datetime" engine/db.py` returns at least 1 match
    - tests/test_db_helpers.py exists and all tests pass
  </acceptance_criteria>
  <done>Both helpers exist in db.py with passing tests covering all edge cases</done>
</task>

<task type="auto">
  <name>Task 2: Fix consolidate.py comment (F-23) and verify links.py person path (F-30)</name>
  <files>engine/consolidate.py, engine/links.py</files>
  <read_first>engine/consolidate.py (full file — find the lazy import block), engine/links.py (line 40-60 for ensure_person_profile), engine/paths.py (BRAIN_SUBDIRS and TYPE_TO_DIR)</read_first>
  <action>
    **F-23 (consolidate.py):** Find the comment block near the lazy imports (likely says "circular import" or similar). Replace with accurate description:
    ```python
    # Lazy import: deferred to avoid slow module-level load of brain_health
    # (heavy dependencies). Not a circular import — just startup performance.
    ```

    **F-30 (links.py):** Read `ensure_person_profile()` in links.py. Verify it writes to `person/` subdirectory. Cross-check with `engine/paths.py` — look for TYPE_TO_DIR mapping for "people" note_type and BRAIN_SUBDIRS. The canonical subdirectory for people notes is `person/` (confirmed by TYPE_TO_DIR fallback and BRAIN_SUBDIRS). If `ensure_person_profile()` already uses `person/`, this finding is a false positive — add a comment confirming canonical path and close. If it uses `people/`, fix to use `person/`.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && grep -n "circular" engine/consolidate.py | wc -l && grep -n "deferred\|startup\|load-time" engine/consolidate.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "circular import" engine/consolidate.py` returns 0
    - `grep -c "deferred\|startup performance\|load-time" engine/consolidate.py` returns at least 1
    - `grep "person" engine/links.py` shows the canonical `person/` directory (not `people/`)
  </acceptance_criteria>
  <done>consolidate.py comment accurately describes lazy load deferral; links.py person path verified as canonical</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_db_helpers.py -x -v` passes
- `grep "def _json_list\|def _now_utc" engine/db.py` shows both helpers
- `grep -c "circular import" engine/consolidate.py` returns 0
</verification>

<success_criteria>
_json_list and _now_utc helpers in db.py with tests. consolidate.py comment fixed. links.py person path verified.
</success_criteria>

<output>
After completion, create `.planning/phases/48-backend-code-cleanup-dead-routes-utcnow-deprecation-brain-path-consolidation-json-loads-helper-api-py-blueprint-partitioning/48-01-SUMMARY.md`
</output>
