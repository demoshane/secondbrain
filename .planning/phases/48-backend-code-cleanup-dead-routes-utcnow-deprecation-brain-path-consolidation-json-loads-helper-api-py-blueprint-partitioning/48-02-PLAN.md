---
phase: 48-backend-code-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - engine/api.py
  - engine/brain_health.py
  - engine/capture.py
  - engine/db.py
  - engine/forget.py
  - engine/mcp_server.py
  - engine/people.py
  - engine/search.py
  - engine/watcher.py
autonomous: true
execution_strategy: direct
requirements:
  - F-27
  - F-28
  - F-29
  - F-31

must_haves:
  truths:
    - "No /people route aliases remain in api.py"
    - "No inline os.environ.get BRAIN_PATH calls remain in api.py"
    - "No json.loads(col or '[]') inline pattern remains in any engine file"
    - "No verbose datetime.now(UTC).replace(...).strftime(...) pattern remains in api.py"
  artifacts:
    - path: "engine/api.py"
      provides: "Cleaned routes using helpers"
  key_links:
    - from: "engine/api.py"
      to: "engine/db.py"
      via: "import _json_list, _now_utc"
      pattern: "from engine\\.db import.*_json_list"
---

<objective>
Apply the helpers from Plan 01 across the codebase: remove /people aliases (F-27), consolidate BRAIN_PATH (F-28), replace json.loads pattern (F-29), replace datetime idiom (F-31).

Purpose: Eliminate 4 categories of code duplication/deprecation across engine files.
Output: Cleaner api.py and engine files using shared helpers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/48-backend-code-cleanup-dead-routes-utcnow-deprecation-brain-path-consolidation-json-loads-helper-api-py-blueprint-partitioning/48-CONTEXT.md
@.planning/phases/48-backend-code-cleanup-dead-routes-utcnow-deprecation-brain-path-consolidation-json-loads-helper-api-py-blueprint-partitioning/48-01-SUMMARY.md
@engine/api.py
@engine/db.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove /people aliases and consolidate BRAIN_PATH in api.py (F-27, F-28)</name>
  <files>engine/api.py, engine/watcher.py</files>
  <read_first>engine/api.py (full file), engine/watcher.py (around line 164), engine/paths.py (BRAIN_ROOT definition)</read_first>
  <action>
    **F-27 — Delete 4 deprecated /people route aliases:**
    Delete the complete route handler functions at these locations (line numbers approximate — read file first):
    - `@app.get("/people")` — the alias that redirects/duplicates `/persons` list
    - `@app.post("/people")` — the alias that duplicates `/persons` create
    - `@app.get("/people/<path:note_path>/links")` — the alias for person links
    - `@app.delete("/people/<path:note_path>")` — the alias for person delete

    Each is a complete route handler (decorator + function). Delete the entire block for each.

    **F-28 — Replace os.environ.get("BRAIN_PATH") with BRAIN_ROOT:**
    In api.py, find all `os.environ.get("BRAIN_PATH"` occurrences (13 expected). Replace each with `BRAIN_ROOT` (already imported from engine.paths at line 27). The `BRAIN_ROOT` is computed once at import time from `os.environ.get("BRAIN_PATH", ...)` in paths.py, so this is equivalent.

    For any occurrence inside a function where dynamic/test-time resolution might matter (e.g., used in test fixtures that set BRAIN_PATH after import), verify the call site. If `BRAIN_ROOT` is already used elsewhere in the same function, it's safe. If it's the only env read in a hot path, keep it and add comment: `# Read at call time for test isolation`.

    In engine/watcher.py, check line ~164 for same pattern and replace if present (watcher.py likely already imports from paths — verify).

    Remove `import os` from api.py if no other os.* calls remain (unlikely — check first).
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && grep -c '"/people"' engine/api.py && grep -c 'os.environ.get.*BRAIN_PATH' engine/api.py && uv run pytest tests/test_api.py -x -q --tb=short 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '@app.*"/people"' engine/api.py` returns 0
    - `grep -c '@app.*"/people/' engine/api.py` returns 0
    - `grep -c 'os.environ.get.*BRAIN_PATH' engine/api.py` returns 0
    - `grep 'BRAIN_ROOT' engine/api.py | head -3` shows usage of the imported constant
    - test_api.py tests pass (no regressions from route removal)
  </acceptance_criteria>
  <done>Zero /people aliases in api.py; zero inline BRAIN_PATH env reads in api.py</done>
</task>

<task type="auto">
  <name>Task 2: Replace json.loads and datetime idioms across all engine files (F-29, F-31)</name>
  <files>engine/api.py, engine/brain_health.py, engine/capture.py, engine/forget.py, engine/mcp_server.py, engine/people.py, engine/search.py</files>
  <read_first>engine/db.py (verify _json_list and _now_utc from Plan 01), engine/api.py, engine/brain_health.py, engine/capture.py, engine/forget.py, engine/mcp_server.py, engine/people.py, engine/search.py</read_first>
  <action>
    **F-29 — Replace `json.loads(col or "[]")` with `_json_list(col)`:**

    In each file, add `from engine.db import _json_list` (or add to existing import line). Then find and replace:
    - `json.loads(row["tags"] or "[]")` -> `_json_list(row["tags"])`
    - `json.loads(row["people"] or "[]")` -> `_json_list(row["people"])`
    - `json.loads(col or "[]")` (any column name) -> `_json_list(col)`
    - Also catch variants like `json.loads(r["tags"] or "[]")`, `json.loads(note["people"] or "[]")`

    Files with occurrences (18 total across ~6-8 files):
    - engine/api.py (most occurrences)
    - engine/brain_health.py
    - engine/capture.py
    - engine/db.py (if any — check)
    - engine/forget.py
    - engine/mcp_server.py
    - engine/people.py
    - engine/search.py

    After replacement, if `json.loads` is no longer used in a file AND no other json.* calls exist, remove `import json`. Check carefully before removing.

    **F-31 — Replace verbose datetime idiom with `_now_utc()`:**

    In api.py, add `from engine.db import _now_utc` (or add to existing import). Find all occurrences of:
    `datetime.datetime.now(datetime.UTC).replace(tzinfo=None).strftime("%Y-%m-%dT%H:%M:%SZ")`
    Replace with: `_now_utc()`

    There are 11 occurrences in api.py. Also check other engine files for the same pattern.
    After replacement, if `datetime.datetime.now` is no longer used in api.py, the `import datetime` can stay (likely used for other datetime operations).
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && grep -rn 'json.loads.*or.*\[\]' engine/*.py | wc -l && grep -rn 'datetime.UTC.*replace.*strftime' engine/*.py | wc -l && uv run pytest tests/test_api.py tests/test_capture.py tests/test_search.py -x -q --tb=short 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc 'json.loads.*or.*\[\]' engine/*.py` returns 0 for every file
    - `grep -rc 'datetime.UTC.*replace.*strftime' engine/*.py` returns 0 for every file
    - `grep -c '_json_list' engine/api.py` returns at least 5 (was 18 total, api.py has most)
    - `grep -c '_now_utc' engine/api.py` returns at least 5 (was 11)
    - All existing tests pass
  </acceptance_criteria>
  <done>Zero inline json.loads(or []) patterns; zero verbose datetime idioms; all tests green</done>
</task>

</tasks>

<verification>
- `grep -rn 'json.loads.*or.*\[\]' engine/*.py` returns no matches
- `grep -rn 'datetime.UTC.*replace.*strftime' engine/*.py` returns no matches
- `grep -c '"/people"' engine/api.py` returns 0
- `grep -c 'os.environ.get.*BRAIN_PATH' engine/api.py` returns 0
- Focused test runs pass
</verification>

<success_criteria>
All 4 findings (F-27, F-28, F-29, F-31) resolved. No inline patterns remain. Tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/48-backend-code-cleanup-dead-routes-utcnow-deprecation-brain-path-consolidation-json-loads-helper-api-py-blueprint-partitioning/48-02-SUMMARY.md`
</output>
