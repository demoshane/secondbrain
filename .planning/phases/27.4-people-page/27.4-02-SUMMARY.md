---
phase: 27.4-people-page
plan: "02"
subsystem: api
tags: [flask, sqlite, people, endpoints]

requires:
  - phase: 27.4-01
    provides: test scaffold for /people endpoint (xfail stubs in test_people.py)

provides:
  - GET /people endpoint in engine/api.py returning people notes with open_actions count

affects: [27.4-03, 27.4-04]

tech-stack:
  added: []
  patterns:
    - "correlated subquery COUNT(*) in SELECT for per-row aggregation without GROUP BY"
    - "BRAIN_PATH env var resolved at request time via os.environ.get() inside route handler"

key-files:
  created: []
  modified:
    - engine/api.py

key-decisions:
  - "[27.4-02] list_people() resolves BRAIN_PATH inside route handler (not at module load) so test monkeypatching of BRAIN_PATH env var takes effect correctly"
  - "[27.4-02] Correlated subquery used for open_actions count — avoids GROUP BY complexity and is correct for single-pass fetch pattern"

patterns-established:
  - "People prefix filter: str(brain_root / 'people') + '/' used as LIKE prefix — trailing slash ensures only people/ folder, not e.g. people-old/"

requirements-completed: [27.4-PP-02]

duration: 5min
completed: 2026-03-18
---

# Phase 27.4 Plan 02: GET /people Backend Endpoint Summary

**Flask GET /people route returning people-folder notes with correlated open_actions count via SQLite subquery**

## Performance

- **Duration:** 5 min
- **Started:** 2026-03-18T15:10:00Z
- **Completed:** 2026-03-18T15:15:00Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments

- Added `list_people()` route to `engine/api.py` — filters notes by `people/` folder path prefix
- Correlated subquery counts `action_items` rows where `assignee_path=n.path AND done=0` per person
- All 3 xfail tests promoted to XPASSED (test_list_people_endpoint, test_list_people_empty, test_list_people_stats)
- Full non-GUI test suite: 330 passed, 0 failed

## Task Commits

1. **Task 1: Add GET /people endpoint** - `73fb80d` (feat)

## Files Created/Modified

- `engine/api.py` — added `list_people()` route (16 lines), placed adjacent to `/actions` route

## Decisions Made

- BRAIN_PATH resolved inside route handler body (not at module load time) so test fixtures' `monkeypatch.setenv("BRAIN_PATH", ...)` takes effect at call time
- Correlated subquery chosen over JOIN+GROUP BY — simpler, no risk of row multiplication, consistent with existing query patterns in api.py

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- GET /people endpoint live and tested — unblocks 27.4-03 (frontend People page) and 27.4-04
- Existing GET /actions?assignee=<path> was already correct (list_actions() in intelligence.py already accepted assignee param from Phase 27.2-02)

---
*Phase: 27.4-people-page*
*Completed: 2026-03-18*
