---
phase: 50-retrieval-reinforcement
plan: 03
type: execute
wave: 3
depends_on: [50-01]
files_modified:
  - engine/mcp_server.py
  - engine/api.py
autonomous: true
execution_strategy: direct

must_haves:
  truths:
    - "sb_read increments access_count and updates last_accessed_at"
    - "sb_person_context increments access on the person note"
    - "API /notes/<path> endpoint increments access on read"
    - "Access tracking is fire-and-forget (never blocks primary response)"
---

<objective>
Wire access tracking into all note-reading code paths.

Purpose: Every time a note is read (MCP, API, GUI), its access metadata updates.
Output: touch_note_access called from sb_read, sb_person_context, and API endpoints.
</objective>

<context>
@.planning/phases/50-retrieval-reinforcement-learning-from-use/50-CONTEXT.md
@engine/mcp_server.py
@engine/api.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire touch_note_access into MCP read paths</name>
  <files>engine/mcp_server.py</files>
  <read_first>engine/mcp_server.py (sb_read function, sb_person_context function)</read_first>
  <action>
    1. Import `touch_note_access` from `engine.db`
    2. In `sb_read()`: after successful note read, call `touch_note_access(conn, path)`
    3. In `sb_person_context()`: after resolving person note, call `touch_note_access(conn, person_path)`
    4. Wrap in try/except — access tracking must never break the primary operation
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_api.py -x -v -k "read"</automated>
  </verify>
  <done>MCP read paths update access metadata silently</done>
</task>

<task type="auto">
  <name>Task 2: Wire touch_note_access into API read paths</name>
  <files>engine/api.py</files>
  <read_first>engine/api.py (find note-serving endpoints: GET /notes/<path>, GET /notes/<path>/meta)</read_first>
  <action>
    1. Import `touch_note_access` from `engine.db`
    2. In the note detail endpoint (GET /notes/<path>): call `touch_note_access(conn, path)` after successful read
    3. Wrap in try/except — fire-and-forget
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_api.py -x -v -k "note"</automated>
  </verify>
  <done>API note-reading endpoints update access metadata silently</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -x -v -k "read or note"` passes
- Manual: sb_read a note, check notes table — access_count > 0, last_accessed_at set
</verification>

<success_criteria>
All note-reading code paths (MCP + API) silently update access tracking.
</success_criteria>
