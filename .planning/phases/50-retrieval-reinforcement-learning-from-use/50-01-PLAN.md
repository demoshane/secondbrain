---
phase: 50-retrieval-reinforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/db.py
  - tests/test_db_helpers.py
autonomous: true
execution_strategy: direct

must_haves:
  truths:
    - "notes table has last_accessed_at TEXT column (nullable)"
    - "notes table has access_count INTEGER DEFAULT 0 column"
    - "migration runs idempotently on existing databases"
  artifacts:
    - path: "engine/db.py"
      provides: "Schema migration + touch_note_access() helper"
      exports: ["touch_note_access"]
---

<objective>
Add access tracking columns to notes table and a helper function to update them.

Purpose: Foundation for retrieval reinforcement — every downstream consumer calls
`touch_note_access(conn, path)` to record that a note was used.
Output: Two new columns, one new function, migration tested.
</objective>

<context>
@.planning/phases/50-retrieval-reinforcement-learning-from-use/50-CONTEXT.md
@engine/db.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add access tracking columns + touch_note_access helper</name>
  <files>engine/db.py</files>
  <read_first>engine/db.py (full — understand migration pattern and existing helpers)</read_first>
  <behavior>
    - `last_accessed_at` column: TEXT, nullable, no default (NULL = never accessed)
    - `access_count` column: INTEGER, NOT NULL, DEFAULT 0
    - Migration follows existing add-column pattern (try ALTER TABLE, ignore if exists)
    - `touch_note_access(conn, path)` atomically: UPDATE notes SET access_count = access_count + 1, last_accessed_at = _now_utc() WHERE path = ?
    - Returns True if row updated, False if path not found
  </behavior>
  <action>
    1. Add migration function `_add_access_tracking_columns(conn)` following existing pattern
    2. Call it from `init_schema()` alongside other migrations
    3. Add `touch_note_access(conn, path) -> bool` function
    4. Write tests: migration idempotency, touch_note_access increments, touch on missing path
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_db_helpers.py -x -v -k "access"</automated>
  </verify>
  <done>Columns exist in schema, touch_note_access works, tests pass</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_db_helpers.py -x -v -k "access"` passes
- Fresh DB via init_schema has both columns
- touch_note_access increments count and sets timestamp
</verification>

<success_criteria>
Access tracking schema + helper in db.py with passing tests.
</success_criteria>
