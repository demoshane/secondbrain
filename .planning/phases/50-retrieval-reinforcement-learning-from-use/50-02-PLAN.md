---
phase: 50-retrieval-reinforcement
plan: 02
type: execute
wave: 2
depends_on: [50-01]
files_modified:
  - engine/search.py
  - tests/test_search.py
autonomous: true
execution_strategy: direct

must_haves:
  truths:
    - "_access_boost() returns 1.0 for notes with no access history"
    - "_access_boost() returns >1.0 for recently accessed notes"
    - "access boost is applied post-RRF merge in hybrid search"
    - "access boost caps at ~15% maximum influence"
  artifacts:
    - path: "engine/search.py"
      provides: "_access_boost function integrated into ranking pipeline"
      exports: ["_access_boost"]
---

<objective>
Add access-based ranking boost to the search pipeline.

Purpose: Notes that have been read/used more get a ranking advantage in future searches.
Output: `_access_boost()` function applied post-merge in hybrid search.
</objective>

<context>
@.planning/phases/50-retrieval-reinforcement-learning-from-use/50-CONTEXT.md
@engine/search.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _access_boost() and integrate into search ranking</name>
  <files>engine/search.py</files>
  <read_first>engine/search.py (full — understand _recency_multiplier pattern, _rrf_merge, all search modes)</read_first>
  <behavior>
    - `_access_boost(last_accessed_at: str | None, access_count: int) -> float`
    - Returns 1.0 when last_accessed_at is None or access_count is 0
    - Returns up to ~1.15 for heavily accessed, recently used notes
    - Formula: `1.0 + 0.15 * min(access_count, 20) / 20 * exp(-days_since_access / 60)`
    - Applied as multiplier on final RRF scores (post-merge, before sort)
    - BM25-only and semantic-only modes also apply the boost
  </behavior>
  <action>
    1. Add `_access_boost()` function near `_recency_multiplier()`
    2. In each search mode (BM25, semantic, hybrid), fetch `last_accessed_at` and
       `access_count` from the notes row and apply boost to final score
    3. For hybrid/RRF: apply after merge, before final sort
    4. Write tests: no-access returns 1.0, recent+frequent returns >1.0, old access decays
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_search.py -x -v -k "access_boost"</automated>
  </verify>
  <done>_access_boost integrated into all search modes with passing tests</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_search.py -x -v -k "access_boost"` passes
- Existing search tests still pass (no regression)
</verification>

<success_criteria>
Access boost multiplier integrated into search ranking pipeline.
</success_criteria>
