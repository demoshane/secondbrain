---
phase: 08-fix-update-memory-routing
plan: "01"
type: execute
wave: 2
depends_on:
  - "08-00"
files_modified:
  - engine/ai.py
autonomous: true
requirements:
  - AI-05

must_haves:
  truths:
    - "update_memory() calls _router.get_adapter('public', config_path) — no hardcoded ClaudeAdapter()"
    - "config_path parameter is now active: changing public_model in config.toml changes the adapter used"
    - "All existing test_ai.py tests remain GREEN"
  artifacts:
    - path: "engine/ai.py"
      provides: "Fixed update_memory() routing through ModelRouter"
      contains: "_router.get_adapter"
  key_links:
    - from: "engine/ai.py::update_memory"
      to: "engine.router.get_adapter"
      via: "_router.get_adapter('public', config_path)"
      pattern: "_router\\.get_adapter\\(.*public.*config_path"
---

<objective>
Wire config_path through _router.get_adapter() inside update_memory() — Option A from research.

Purpose: Closes AI-05 gap — config.toml now controls adapter selection for memory updates, not just captures.
Output: engine/ai.py with update_memory() routing through ModelRouter; ClaudeAdapter direct import removed.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Current broken implementation in engine/ai.py -->
<!-- Line 8: from engine.adapters.claude_adapter import ClaudeAdapter  <-- remove if unused after fix -->
<!-- Line 7: import engine.router as _router  <-- already present, use this -->

Current update_memory signature (ai.py ~line 122):
```python
def update_memory(note_type: str, summary: str, config_path: Path) -> None:
    """...config_path: Path to config.toml (unused for memory, ClaudeAdapter is always used)."""
    try:
        ...
        adapter = ClaudeAdapter()   # <-- replace this line
        ...
```

Fixed form (Option A):
```python
adapter = _router.get_adapter("public", config_path)  # AI-05: config drives adapter
```

Call site in capture.py is already correct — no changes to capture.py needed.

Existing test (must remain GREEN):
    tests/test_ai.py::test_cap06_memory_update_uses_write_tool
    — patches subprocess.run and shutil.which; passes tmp_config_toml; asserts --allowedTools Write in call
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix update_memory() to route through ModelRouter</name>
  <files>engine/ai.py</files>
  <behavior>
    - test_update_memory_routing_uses_config: GREEN after fix (was RED in Wave 0)
    - test_cap06_memory_update_uses_write_tool: still GREEN (subprocess.run still called)
    - All other tests/test_ai.py tests: GREEN
    - Full suite: GREEN
  </behavior>
  <action>
    In engine/ai.py, make exactly these two changes:

    1. Replace the hardcoded adapter line inside update_memory():
       BEFORE: `adapter = ClaudeAdapter()`
       AFTER:  `adapter = _router.get_adapter("public", config_path)`

    2. Update the docstring for config_path parameter:
       BEFORE: "config_path: Path to config.toml (unused for memory, ClaudeAdapter is always used)."
       AFTER:  "config_path: Path to config.toml — passed to ModelRouter for public-sensitivity routing (AI-05)."

    3. After making the change, grep engine/ai.py for ClaudeAdapter. If no other reference exists
       (currently only update_memory() used it), remove the import line:
       `from engine.adapters.claude_adapter import ClaudeAdapter`

    No other files need changes. capture.py call site is already correct.

    IMPORTANT: Do NOT add a sensitivity parameter to update_memory() — use the hardcoded string "public"
    internally. The call site guard `if sensitivity != "pii"` in capture.py already ensures PII is
    filtered before this function is called.
  </action>
  <verify>
    <automated>uv run --no-project --with pytest tests/test_ai.py -x</automated>
  </verify>
  <done>
    - test_update_memory_routing_uses_config: GREEN (get_adapter called with ("public", config_path))
    - test_cap06_memory_update_uses_write_tool: GREEN
    - Full AI test suite: GREEN
    - engine/ai.py contains no reference to ClaudeAdapter (import removed)
  </done>
</task>

<task type="auto">
  <name>Task 2: Full suite smoke check</name>
  <files></files>
  <action>
    Run the full test suite to confirm no regressions introduced by removing the ClaudeAdapter import
    or changing the routing path.
  </action>
  <verify>
    <automated>uv run --no-project --with pytest tests/ -x</automated>
  </verify>
  <done>All tests GREEN. Zero failures, zero errors.</done>
</task>

</tasks>

<verification>
uv run --no-project --with pytest tests/ -x
All tests pass. No ClaudeAdapter reference in engine/ai.py. _router.get_adapter("public", config_path) call present in update_memory().
</verification>

<success_criteria>
- engine/ai.py: update_memory() calls _router.get_adapter("public", config_path)
- engine/ai.py: ClaudeAdapter import removed (no longer used)
- tests/test_ai.py::test_update_memory_routing_uses_config: GREEN
- Full suite: GREEN
- AI-05 closed: config.toml public_model now controls adapter used for memory updates
</success_criteria>

<output>
After completion, create .planning/phases/08-fix-update-memory-routing/08-01-SUMMARY.md
</output>
