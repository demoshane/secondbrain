---
phase: 04-automation
plan: "09"
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/ai.py
  - engine/capture.py
  - tests/test_rag.py
autonomous: true
gap_closure: true
requirements: [SEARCH-04]

must_haves:
  truths:
    - "ask_followup_questions() receives a conn parameter and passes it to augment_prompt()"
    - "augment_prompt(title, conn) is called instead of passing raw title to the AI adapter"
    - "conn is created before ask_followup_questions() is called in capture.py main()"
    - "RAG context block appears in AI prompt when relevant notes exist (visible via debug=True)"
  artifacts:
    - path: "engine/ai.py"
      provides: "ask_followup_questions(note_type, title, sensitivity, config_path, conn) — conn parameter added"
      exports: ["ask_followup_questions"]
    - path: "engine/capture.py"
      provides: "conn created before ask_followup_questions() call; conn passed as argument"
  key_links:
    - from: "engine/ai.py:ask_followup_questions"
      to: "engine/rag.py:augment_prompt"
      via: "from engine.rag import augment_prompt; user_content=augment_prompt(title, conn)"
      pattern: "augment_prompt"
    - from: "engine/capture.py:main"
      to: "engine/ai.py:ask_followup_questions"
      via: "conn passed as keyword arg after being created earlier in main()"
      pattern: "ask_followup_questions.*conn"
---

<objective>
Wire engine/rag.py into the capture pipeline: engine/rag.py is fully implemented but dead code. Three changes needed — conn parameter on ask_followup_questions(), augment_prompt() call instead of raw title, and conn creation moved earlier in capture.py.

Purpose: Test 4 (RAG context injection) fails because rag.py is never imported or called from ai.py or capture.py. Root cause is three missing wiring steps identified in UAT diagnosis.
Output: engine/ai.py with conn param and augment_prompt() call; engine/capture.py with conn created before ask_followup_questions(); updated test_rag.py integration test.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-automation/04-02-SUMMARY.md
</context>

<interfaces>
<!-- Key contracts the executor needs. -->

From engine/rag.py:
```python
def augment_prompt(query: str, conn: sqlite3.Connection) -> str:
    """Prepend RETRIEVED CONTEXT block to query. Returns query unchanged if no results."""

def retrieve_context(query: str, conn: sqlite3.Connection, limit: int = 5, debug: bool = False) -> str:
    """Return formatted context block string, or '' if no results."""
```

From engine/ai.py (current signature — to be changed):
```python
def ask_followup_questions(
    note_type: str,
    title: str,
    sensitivity: str,
    config_path: Path,
) -> list[str]:
```

From engine/capture.py (current call site — capture.py:178):
```python
questions = ask_followup_questions(args.note_type, args.title, sensitivity, CONFIG_PATH)
# conn created at line 195 — AFTER this call
conn = get_connection()
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add conn parameter to ask_followup_questions() and call augment_prompt()</name>
  <files>engine/ai.py, tests/test_rag.py</files>
  <behavior>
    - ask_followup_questions(note_type, title, sensitivity, config_path, conn=None) — conn is optional for backwards compat
    - When conn is provided: user_content = augment_prompt(title, conn) from engine.rag
    - When conn is None: user_content = title (unchanged behaviour)
    - augment_prompt returns title unchanged if no FTS5 results, so no empty-context block is ever injected
    - Test: mock conn with search returning 1 result, verify augment_prompt output contains RETRIEVED CONTEXT header
    - Test: conn=None falls back to raw title (no import error)
  </behavior>
  <action>
    In engine/ai.py:

    1. Add `import sqlite3` to imports if not present.
    2. Change signature: `def ask_followup_questions(note_type, title, sensitivity, config_path, conn=None)`
    3. Inside the try block, before `adapter.generate(...)`:
       ```python
       from engine.rag import augment_prompt
       user_content = augment_prompt(title, conn) if conn is not None else title
       ```
    4. Change `adapter.generate(user_content=title, ...)` to `adapter.generate(user_content=user_content, ...)`

    In tests/test_rag.py: add integration test for the wiring:
    - Use in-memory SQLite, insert a note row, call ask_followup_questions with conn, mock the adapter, assert user_content passed to adapter.generate contains RETRIEVED CONTEXT (or at minimum the note title when no FTS5 match).
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run --no-project --with pytest tests/test_rag.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>test_rag.py passes; grep shows augment_prompt called inside ask_followup_questions in ai.py</done>
</task>

<task type="auto">
  <name>Task 2: Move conn creation before ask_followup_questions() call in capture.py</name>
  <files>engine/capture.py</files>
  <action>
    In engine/capture.py main():

    Current order (lines ~177-199):
    1. classify sensitivity
    2. ask_followup_questions(args.note_type, args.title, sensitivity, CONFIG_PATH)  ← line 178
    3. ...process answers...
    4. conn = get_connection()   ← line 195
    5. init_schema(conn)
    6. migrate_add_people_column(conn)
    7. capture_note(...)

    Required order:
    1. classify sensitivity
    2. conn = get_connection()
    3. init_schema(conn)
    4. migrate_add_people_column(conn)
    5. ask_followup_questions(args.note_type, args.title, sensitivity, CONFIG_PATH, conn)
    6. ...process answers...
    7. capture_note(...)
    8. conn.close()

    Move lines 195-197 (conn = get_connection(); init_schema; migrate) to before the ask_followup_questions() call, and add conn as the 5th argument to ask_followup_questions(). Ensure conn.close() remains at the end, after capture_note().

    Do not change the try/except around ask_followup_questions — it must remain best-effort (never blocks capture).
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && python -c "import ast, pathlib; ast.parse(pathlib.Path('engine/capture.py').read_text()); print('syntax OK')"</automated>
  </verify>
  <done>capture.py parses without syntax error; conn created before ask_followup_questions call (verify with grep -n "get_connection\|ask_followup" engine/capture.py)</done>
</task>

</tasks>

<verification>
Run full test suite: `cd /Users/tuomasleppanen/second-brain && uv run --no-project --with pytest tests/ -x -q 2>&1 | tail -10`

Wiring check: `grep -n "augment_prompt\|ask_followup_questions\|get_connection" engine/ai.py engine/capture.py`
</verification>

<success_criteria>
- ask_followup_questions() has conn=None parameter in ai.py
- augment_prompt() called with conn when conn is not None
- conn created before ask_followup_questions() call in capture.py main()
- All test_rag.py + full suite tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-automation/04-09-SUMMARY.md`
</output>
