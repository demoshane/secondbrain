---
phase: 04-automation
plan: "08"
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/paths.py
  - engine/init_brain.py
  - brain/.meta/templates/people.md
  - brain/.meta/templates/meeting.md
  - brain/.meta/templates/projects.md
  - brain/.meta/templates/coding.md
  - brain/.meta/templates/strategy.md
autonomous: true
gap_closure: true
requirements: [PEOPLE-01, PEOPLE-02, WORK-01, WORK-02, WORK-03, WORK-04]

must_haves:
  truths:
    - "Running sb-init (or create_brain_structure) creates ~/SecondBrain/.meta/templates/ directory"
    - "All 5 template files (people.md, meeting.md, projects.md, coding.md, strategy.md) exist in ~/SecondBrain/.meta/templates/ after init"
    - "Each template has a meaningful skeleton — not empty"
  artifacts:
    - path: "engine/paths.py"
      provides: "'.meta/templates' in BRAIN_SUBDIRS or explicit mkdir in init path"
    - path: "engine/init_brain.py"
      provides: "seed_templates() copies repo templates to brain templates dir"
    - path: "brain/.meta/templates/people.md"
      provides: "People profile template skeleton"
    - path: "brain/.meta/templates/meeting.md"
      provides: "Meeting note template skeleton"
    - path: "brain/.meta/templates/projects.md"
      provides: "Projects note template skeleton"
    - path: "brain/.meta/templates/coding.md"
      provides: "Coding note template skeleton"
    - path: "brain/.meta/templates/strategy.md"
      provides: "Strategy note template skeleton"
  key_links:
    - from: "engine/init_brain.py:create_brain_structure"
      to: "brain/.meta/templates/*.md"
      via: "seed_templates() copies files from repo to brain_root/.meta/templates/"
      pattern: "seed_templates"
---

<objective>
Create the templates directory and seed template files during brain init so ~/SecondBrain/.meta/templates/ exists with 5 meaningful skeletons.

Purpose: Test 5 fails because engine/paths.py omits '.meta/templates' from BRAIN_SUBDIRS, and init_brain.py has no template-seeding step (despite config.toml seeding existing as a model at lines 92-109). engine/templates.py silently falls back to inline defaults, masking the gap.
Output: '.meta/templates' added to BRAIN_SUBDIRS, seed_templates() in init_brain.py, 5 skeleton template files in brain/.meta/templates/.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-automation/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add .meta/templates to BRAIN_SUBDIRS and create seed_templates()</name>
  <files>engine/paths.py, engine/init_brain.py</files>
  <action>
    In engine/paths.py:
    - Add ".meta/templates" to BRAIN_SUBDIRS list (after ".meta")

    In engine/init_brain.py:
    - Add function seed_templates(brain_root: Path) -> dict:
      - Source dir: Path(__file__).parent.parent / "brain" / ".meta" / "templates"
      - Dest dir: brain_root / ".meta" / "templates"
      - For each .md file in source dir: if dest_dir/{filename} does not exist, copy it (use shutil.copy2)
      - Return {"seeded": [...], "existed": [...]}
    - Call seed_templates(brain_root) from create_brain_structure() after the subdirs loop (mirror how config.toml seeding works at lines 92-109)
    - Add `import shutil` at top of init_brain.py if not already present

    The copy is idempotent: existing files are never overwritten (matches config.toml seeding pattern).
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && python -c "from engine.paths import BRAIN_SUBDIRS; assert '.meta/templates' in BRAIN_SUBDIRS, 'missing'; print('OK')"</automated>
  </verify>
  <done>'.meta/templates' in BRAIN_SUBDIRS; seed_templates() present in init_brain.py; shutil.copy2 used for idempotent copy</done>
</task>

<task type="auto">
  <name>Task 2: Create 5 skeleton template files in brain/.meta/templates/</name>
  <files>brain/.meta/templates/people.md, brain/.meta/templates/meeting.md, brain/.meta/templates/projects.md, brain/.meta/templates/coding.md, brain/.meta/templates/strategy.md</files>
  <action>
    Create the repo-side template files at brain/.meta/templates/ (seed source). Each must have a meaningful skeleton appropriate to its type. Use these skeletons:

    people.md:
    ```
    # {{name}}

    **Role:**
    **Company:**
    **Contact:**

    ## Notes

    ## Backlinks
    ```

    meeting.md:
    ```
    # {{title}}

    **Date:** {{date}}
    **Attendees:** {{people}}
    **Project:**

    ## Agenda

    ## Notes

    ## Action Items

    ## Backlinks
    ```

    projects.md:
    ```
    # {{title}}

    **Status:** active
    **Started:** {{date}}
    **Goal:**

    ## Context

    ## Progress

    ## Next Steps

    ## Backlinks
    ```

    coding.md:
    ```
    # {{title}}

    **Date:** {{date}}
    **Language/Stack:**
    **Tags:** {{tags}}

    ## Problem

    ## Solution

    ## Code

    ## References

    ## Backlinks
    ```

    strategy.md:
    ```
    # {{title}}

    **Date:** {{date}}
    **Horizon:**

    ## Situation

    ## Options

    ## Decision

    ## Rationale

    ## Backlinks
    ```

    Also ensure brain/.meta/templates/ directory exists in the repo (create a .gitkeep if the directory does not exist yet — the template files themselves will make .gitkeep unnecessary).
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && ls brain/.meta/templates/*.md | wc -l | tr -d ' '</automated>
  </verify>
  <done>5 .md files present in brain/.meta/templates/; each has at least 10 non-empty lines</done>
</task>

</tasks>

<verification>
Run: `cd /Users/tuomasleppanen/second-brain && uv run --no-project --with pytest tests/ -x -q 2>&1 | tail -10`

Manual check: `ls brain/.meta/templates/` shows 5 .md files; `cat brain/.meta/templates/meeting.md` shows skeleton with headings.
</verification>

<success_criteria>
- '.meta/templates' in engine/paths.py BRAIN_SUBDIRS
- seed_templates() in engine/init_brain.py, called from create_brain_structure()
- 5 skeleton .md files exist in brain/.meta/templates/
- Full test suite green (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/04-automation/04-08-SUMMARY.md`
</output>
