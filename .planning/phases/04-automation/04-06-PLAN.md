---
phase: 04-automation
plan: "06"
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03", "04-04", "04-05"]
files_modified: []
autonomous: false
requirements: [CAP-04, CAP-05, PEOPLE-01, PEOPLE-02, PEOPLE-03, PEOPLE-04, PEOPLE-05, WORK-01, WORK-02, WORK-03, WORK-04, SEARCH-03, SEARCH-04]

must_haves:
  truths:
    - "Full automated test suite passes green with watchdog installed"
    - "Dropping a file into brain/files/ while sb-watch is running triggers a categorization prompt"
    - "Meeting note with attendees auto-creates backlinks in people profiles"
    - "sb-check-links reports zero orphans after properly linked meeting capture"
    - "sb-search --type people returns cross-type results for a known person"
    - "RAG context appears in AI debug output when querying with a term that matches a note"
  artifacts:
    - path: "tests/"
      provides: "Full green test suite"
  key_links:
    - from: "Phase 4 success criteria"
      to: "ROADMAP.md"
      via: "Human verification against 5 success criteria"
      pattern: "Phase 4"
---

<objective>
Phase 4 manual verification gate — confirm all five phase success criteria against live system behavior.

Purpose: Code is correct on paper; this gate confirms it works end-to-end with real files, real brain state, and real CLI interaction.
Output: Human approval of phase completion; ROADMAP.md marked complete.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-automation/04-01-SUMMARY.md
@.planning/phases/04-automation/04-02-SUMMARY.md
@.planning/phases/04-automation/04-03-SUMMARY.md
@.planning/phases/04-automation/04-04-SUMMARY.md
@.planning/phases/04-automation/04-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated suite</name>
  <files></files>
  <action>
Run the complete test suite with watchdog installed. All tests must pass (no failures, xfail is acceptable).

Command: `uv run --no-project --with pytest --with watchdog python -m pytest tests/ -v`

If any tests are failing (not xfail), diagnose and fix before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>uv run --no-project --with pytest --with watchdog python -m pytest tests/ -q 2>&1 | tail -5</automated>
  </verify>
  <done>Exit code 0. No test failures (xfail/xpass acceptable).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Phase 4 success criteria</name>
  <action>Human verification of all 5 Phase 4 success criteria against the live system.</action>
  <what-built>
    Phase 4 automation features:
    - CAP-04: sb-watch daemon watches brain/files/ for dropped files (debounce 5s, rate-limited)
    - CAP-05: .githooks/post-commit summarizes commits and offers brain linking
    - PEOPLE-01/02: people and meeting templates with proper sections
    - PEOPLE-03: meeting capture auto-backlinks person profiles
    - PEOPLE-04 / SEARCH-03: sb-check-links reports orphaned bidirectional links
    - PEOPLE-05: sb-search --type people returns cross-type results
    - WORK-01–04: strategy/projects/coding/ideas templates usable via --type
    - SEARCH-04: RAG-lite prepends FTS5 context to AI prompts (visible with --debug or in code)
  </what-built>
  <how-to-verify>
**Success Criteria from ROADMAP.md — verify each:**

1. **CAP-04 (file watcher)**
   - Open a terminal in the DevContainer
   - Run: `sb-watch` (or `uv run --no-project python -m engine.watcher`)
   - In another terminal, copy any file into `~/SecondBrain/files/`
   - Confirm: categorization prompt appears within 10 seconds
   - Bulk test: copy 5 files rapidly; confirm no more than 1 prompt per 5-second window

2. **CAP-05 (git hook)**
   - In a project directory (not the brain repo), run: `git config core.hooksPath /workspace/.githooks`
   - Make a trivial commit (edit any file, `git commit -am "test commit"`)
   - Confirm: AI summary is printed and brain-linking prompt appears
   - (Optional) Accept the prompt and confirm brain entry is created

3. **PEOPLE-03 + PEOPLE-04 + SEARCH-03 (backlinks + orphan check)**
   - Run: `sb-capture --type people --title "Alice Test"` → creates `brain/people/alice-test.md`
   - Run: `sb-capture --type meeting --title "Q1 Review" --people "Alice Test"` → creates meeting note
   - Run: `sb-check-links`
   - Confirm: no orphans reported; check `brain/people/alice-test.md` to see backlink appended

4. **PEOPLE-05 (cross-type people search)**
   - Run: `sb-search --type people "Alice Test"`
   - Confirm: Alice's profile and the Q1 Review meeting both appear in results

5. **SEARCH-04 (RAG-lite context in AI)**
   - Capture a note with distinctive content (e.g., "brain architecture decisions" in body)
   - Look at the code: `engine/rag.py augment_prompt()` prepends FTS5 results to user_content
   - Verify by code inspection: `retrieve_context()` calls `search_notes()` and returns a block starting with `=== RETRIEVED CONTEXT`
   - (Optional) Add a debug print or run a quick Python snippet to confirm context is built

**If any criterion fails:** Describe what you observed and type "issues: [description]"
  </how-to-verify>
  <verify>
    <automated>MISSING — human gate; no automated command applies</automated>
  </verify>
  <done>Human types "approved" confirming all 5 success criteria pass.</done>
  <resume-signal>Type "approved" if all criteria pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
All 5 Phase 4 success criteria from ROADMAP.md confirmed by human verification.
</verification>

<success_criteria>
- Full automated test suite passes green
- Human confirms all 5 ROADMAP.md Phase 4 success criteria pass
- Phase 4 marked complete in ROADMAP.md
</success_criteria>

<output>
After completion, create `.planning/phases/04-automation/04-06-SUMMARY.md`

Update ROADMAP.md: mark Phase 4 as complete with today's date.
Update STATE.md: increment completed_phases to 4, update last_activity.
</output>
