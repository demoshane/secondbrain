---
phase: 04-automation
plan: "07"
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/links.py
  - engine/capture.py
  - tests/test_links.py
autonomous: true
gap_closure: true
requirements: [PEOPLE-03, PEOPLE-04, PEOPLE-05, SEARCH-03]

must_haves:
  truths:
    - "sb-capture --people 'Alice Smith' creates people/alice-smith.md when no file exists"
    - "The newly created profile has a meaningful skeleton (name heading + Backlinks section)"
    - "The backlink to the meeting note is appended to the profile"
    - "sb-search --type people 'Alice' returns alice-smith.md after capture"
  artifacts:
    - path: "engine/links.py"
      provides: "ensure_person_profile() + updated add_backlinks() that calls it"
      exports: ["ensure_person_profile", "add_backlinks", "check_links", "main_check_links"]
    - path: "engine/capture.py"
      provides: "capture pipeline calls add_backlinks after note write"
  key_links:
    - from: "engine/capture.py"
      to: "engine/links.py:add_backlinks"
      via: "deferred import inside capture_note()"
      pattern: "add_backlinks"
    - from: "engine/links.py:add_backlinks"
      to: "brain_root/people/{slug}.md"
      via: "ensure_person_profile() creates file when missing"
      pattern: "ensure_person_profile"
---

<objective>
Fix silent skip in add_backlinks() so capturing a note with --people creates the person profile when none exists.

Purpose: Test 1 (backlinks on capture) and Test 3 (people search) both fail because engine/links.py:24 does `if not matches: continue` — there is no profile-creation path. Gap 3 (people search) resolves automatically once alice-smith.md exists.
Output: engine/links.py with ensure_person_profile(), updated add_backlinks() that calls it, updated test_links.py.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-automation/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ensure_person_profile() and fix add_backlinks() silent skip</name>
  <files>engine/links.py, tests/test_links.py</files>
  <behavior>
    - ensure_person_profile(slug, brain_root) creates brain_root/people/{slug}.md with skeleton "# {display_name}\n\n## Backlinks\n" when file does not exist; returns the Path
    - ensure_person_profile is idempotent: if file already exists, returns path without modifying
    - add_backlinks() calls ensure_person_profile(slug, brain_root) instead of the `if not matches: continue` guard
    - backlink is still appended to the file after ensure_person_profile returns
    - display_name in skeleton derived from slug: hyphens to spaces, title-case (e.g. "alice-smith" -> "Alice Smith")
  </behavior>
  <action>
    In engine/links.py:

    1. Add function ensure_person_profile(slug: str, brain_root: Path) -> Path:
       - Construct path: brain_root / "people" / f"{slug}.md"
       - If path does not exist:
         - display_name = slug.replace("-", " ").title()
         - Write skeleton: f"# {display_name}\n\n## Backlinks\n"
       - Return path

    2. Replace the `if not matches: continue` block in add_backlinks() with a call to ensure_person_profile(slug, brain_root). Use the returned path as person_file instead of matches[0].

    Keep all existing logic below (backlink append + relationships INSERT) unchanged.

    In tests/test_links.py: add tests for:
    - ensure_person_profile creates file with correct skeleton when absent
    - ensure_person_profile is idempotent (existing file unchanged)
    - add_backlinks creates person profile when none exists, then appends backlink
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run --no-project --with pytest pytest tests/test_links.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>All test_links.py tests pass; ensure_person_profile and updated add_backlinks present in engine/links.py</done>
</task>

<task type="auto">
  <name>Task 2: Verify add_backlinks is called from capture_note()</name>
  <files>engine/capture.py</files>
  <action>
    Read engine/capture.py and confirm add_backlinks() is called from capture_note() after the note is written to disk. It should already be wired (per 04-01-SUMMARY.md decision: deferred import inside function body). If the call is present, no change needed — just verify. If missing, add:

        if people:
            from engine.links import add_backlinks
            add_backlinks(path, people, brain_root, conn)

    Place this call after `conn.commit()` and before `return path` in capture_note().

    Do NOT move or modify the conn creation / migration lines in main().
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && grep -n "add_backlinks" engine/capture.py</automated>
  </verify>
  <done>grep shows add_backlinks called in capture_note(); line number confirms it is after the note write</done>
</task>

</tasks>

<verification>
Run full test suite: `cd /Users/tuomasleppanen/second-brain && uv run --no-project --with pytest tests/ -x -q 2>&1 | tail -10`

Smoke test: create a temp directory simulating brain_root, call add_backlinks with a new person slug, confirm the people/{slug}.md file is created with correct skeleton and backlink appended.
</verification>

<success_criteria>
- ensure_person_profile() exists in engine/links.py
- add_backlinks() calls ensure_person_profile() — no more silent skip for missing files
- All existing + new test_links.py tests pass
- Full test suite green (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/04-automation/04-07-SUMMARY.md`
</output>
