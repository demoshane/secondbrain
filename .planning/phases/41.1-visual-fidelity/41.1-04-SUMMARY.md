---
phase: 41.1-visual-fidelity
plan: 04
subsystem: frontend
tags: [ui, people, projects, links, avatar, stat-tiles]
dependency_graph:
  requires: []
  provides: [Gap7-closed, Gap8-closed, Gap9-closed]
  affects: [PeoplePage, ProjectsPage, LinksPage]
tech_stack:
  added: []
  patterns: [avatar-initials, stat-tiles, context-navigation]
key_files:
  created: []
  modified:
    - frontend/src/components/PeoplePage.tsx
    - frontend/src/components/ProjectsPage.tsx
    - frontend/src/components/LinksPage.tsx
decisions:
  - AvatarInitials is inline in PeoplePage (not a separate shared component) — keeps scope minimal; can be extracted later if needed by other pages
  - timeAgo helper is inline in ProjectsPage — simple utility, no shared dependency needed
  - stat-tiles render after the title/status row inside the header border-b block — avoids visual break between header and tiles
metrics:
  duration: 96
  completed_date: "2026-03-28"
  tasks_completed: 2
  files_modified: 3
---

# Phase 41.1 Plan 04: People/Projects/Links Visual Gaps Summary

People get circular avatar initials in both detail header and list rows; Projects detail gains 4-tile stats at a glance; Links detail gains "Open as Note" navigation.

## Tasks Completed

| # | Name | Commit | Files |
|---|------|--------|-------|
| 1 | Add avatar initials and Profile & Context to PeoplePage | 48f99ae | frontend/src/components/PeoplePage.tsx |
| 2 | Add stat tiles to ProjectsPage and Open as Note to LinksPage | 37eaec2 | frontend/src/components/ProjectsPage.tsx, frontend/src/components/LinksPage.tsx |

## Decisions Made

- `AvatarInitials` inlined in PeoplePage — single consumer, no cross-component dependency needed yet
- `timeAgo` helper inlined in ProjectsPage — trivial date utility, same rationale
- Stat tiles placed inside the `px-6 py-4 border-b` header block with `mt-4` — tiles feel attached to the header rather than a separate section

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None — all stat tile values wire to real fetched data (`backlinks.length`, `actions.filter(a => !a.done).length`, `projectDetail?.linked_meetings.length`, `projectDetail?.updated_at`).

## Self-Check: PASSED

- FOUND: 48f99ae (Task 1 commit)
- FOUND: 37eaec2 (Task 2 commit)
- FOUND: PeoplePage.tsx, ProjectsPage.tsx, LinksPage.tsx
