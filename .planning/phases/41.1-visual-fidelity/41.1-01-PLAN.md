---
phase: 41.1-visual-fidelity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/ui/health-score-gauge.tsx
  - frontend/src/components/Sidebar.tsx
autonomous: true
execution_strategy: multi-agent
requirements: []
must_haves:
  truths:
    - "Health score renders as a circular SVG arc gauge with score number centered inside"
    - "Sidebar groups notes first by folder (extracted from note.path), then by type within each folder"
  artifacts:
    - path: "frontend/src/components/ui/health-score-gauge.tsx"
      provides: "SVG circular arc gauge component"
      contains: "<svg"
    - path: "frontend/src/components/Sidebar.tsx"
      provides: "Two-level folder+type grouping"
      contains: "groupByFolder"
  key_links:
    - from: "frontend/src/components/ui/health-score-gauge.tsx"
      to: "IntelligencePage"
      via: "import"
      pattern: "HealthScoreGauge"
    - from: "frontend/src/components/Sidebar.tsx"
      to: "note.path"
      via: "folder extraction"
      pattern: "split.*/"
---

<objective>
Close Gap 1 (HealthScoreGauge) and Gap 2 (Sidebar folder grouping).

Purpose: Replace plain number health score with SVG arc gauge per Visily mockup. Add two-level folder+type grouping to sidebar per ui-descriptions/sidebar.md spec.
Output: Updated health-score-gauge.tsx and Sidebar.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/LEARNINGS.md
@frontend/src/components/ui/health-score-gauge.tsx
@frontend/src/components/Sidebar.tsx
@frontend/src/types.ts
@ui-descriptions/sidebar.md
</context>

<interfaces>
<!-- Types used by these components -->

From frontend/src/types.ts:
```typescript
export interface Note {
  path: string
  title: string
  type: string
  body: string
  tags: string[]
  people: string[]
  folder: string
  created_at: string
  updated_at: string
}

export interface BrainHealth {
  score: number
  total_notes: number
  orphan_count: number
  // ...
}
```

From frontend/src/components/ui/health-score-gauge.tsx (current):
```typescript
interface HealthScoreGaugeProps {
  score: number
  className?: string
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace HealthScoreGauge with SVG circular arc</name>
  <files>frontend/src/components/ui/health-score-gauge.tsx</files>
  <read_first>
    - frontend/src/components/ui/health-score-gauge.tsx
    - frontend/src/components/IntelligencePage.tsx
  </read_first>
  <action>
Replace the entire HealthScoreGauge component with an SVG-based circular arc gauge.

Implementation:
- Keep the same exported interface: `HealthScoreGaugeProps { score: number; className?: string }`
- SVG viewBox `0 0 120 120`, centered circle at (60, 60), radius 50
- Background track circle: `stroke` = `hsl(var(--border))`, `stroke-width` = 8, `fill` = none
- Foreground arc: `stroke` color based on score (green-500 for >=80, amber-400 for >=50, red-500 for <50), `stroke-width` = 8, `fill` = none
- Arc length: use `stroke-dasharray` = circumference (2 * pi * 50 = ~314.16), `stroke-dashoffset` = circumference * (1 - score/100)
- Apply `stroke-linecap="round"` to the foreground arc
- Rotate the arc start to 12 o'clock: `transform="rotate(-90 60 60)"` on the foreground circle
- Center the score number inside: `<text>` element at (60, 60) with `text-anchor="middle"` `dominant-baseline="central"` `font-size="28"` `font-weight="600"` `fill="currentColor"`
- Below the SVG: keep the "Brain Health" label span with `text-xs text-muted-foreground`
- The `<text>` fill color should match the arc color (green/amber/red)
- Wrap in a `<div>` with `cn("flex flex-col items-center gap-1", className)`
- SVG size: `w-24 h-24` (96px) via className on the `<svg>` element

Do NOT change the export name or props interface.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain/frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File contains `<svg` element with `viewBox="0 0 120 120"`
    - File contains `stroke-dasharray` and `stroke-dashoffset` for arc rendering
    - File contains `<text` element for centered score number
    - File contains `rotate(-90` for 12 o'clock start position
    - File exports `HealthScoreGauge` with unchanged props interface
    - File contains color logic: `green-500` for >=80, `amber-400` for >=50, `red-500` for <50
  </acceptance_criteria>
  <done>HealthScoreGauge renders as SVG circular arc with score centered, colored by threshold</done>
</task>

<task type="auto">
  <name>Task 2: Add folder+type two-level grouping to Sidebar</name>
  <files>frontend/src/components/Sidebar.tsx</files>
  <read_first>
    - frontend/src/components/Sidebar.tsx
    - frontend/src/types.ts
    - ui-descriptions/sidebar.md
  </read_first>
  <action>
Replace the `groupByType()` function with a two-level grouping: folder first, then type within each folder.

Implementation:
- Add `groupByFolderThenType(notes: Note[]): Map&lt;string, Map&lt;string, Note[]&gt;&gt;` function
- Extract folder from `note.path`: take the first path segment before the first `/`. E.g. `meetings/2026-03-28-standup.md` -> `meetings`, `notes/ideas/cool-thing.md` -> `notes`. If no `/` in path, use `other` as folder name.
- Within each folder, group by `note.type` (same logic as current `groupByType`)
- Folder ordering: use a `FOLDER_ORDER` array: `['meetings', 'people', 'person', 'projects', 'ideas', 'notes', 'strategy', 'coding', 'personal', 'links', 'files']`. Folders not in this list appear at the end alphabetically.
- Type ordering within folder: reuse existing `TYPE_ORDER` array
- Render structure: outer `CollapsibleSection` per folder (capitalized folder name, count = total notes in folder, sectionId = `sidebar-folder-${folder}`), inner `CollapsibleSection` per type (capitalized type name, count = notes of that type, sectionId = `sidebar-${folder}-${type}`, defaultOpen = true)
- Inner type sections: indent by adding `className="pl-2"` wrapper div around each inner CollapsibleSection
- NoteRow component: unchanged
- Keep all existing functionality: tag filter banner, search results mode, active note highlighting
- Remove the old `groupByType()` function

Do NOT touch the `NoteRow` component, the tag filter banner, or any import/context usage.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain/frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File contains `groupByFolderThenType` function
    - File contains `FOLDER_ORDER` array
    - File contains nested `CollapsibleSection` components (two levels)
    - File contains folder extraction logic using `split('/')` on note.path
    - File does NOT contain standalone `groupByType` function (replaced by the nested version)
    - Outer CollapsibleSection sectionId pattern: `sidebar-folder-`
    - Inner CollapsibleSection sectionId pattern: `sidebar-${folder}-${type}`
  </acceptance_criteria>
  <done>Sidebar shows two-level folder then type grouping with collapsible sections at both levels</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes in frontend directory
- HealthScoreGauge contains SVG arc rendering with stroke-dasharray
- Sidebar contains two-level nested CollapsibleSection structure
</verification>

<success_criteria>
- Gap 1 closed: HealthScoreGauge is SVG circular arc, not plain number
- Gap 2 closed: Sidebar groups by folder first, then type within folder
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/41.1-visual-fidelity/41.1-01-SUMMARY.md`
</output>
