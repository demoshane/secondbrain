---
phase: 41.1-visual-fidelity
plan: "03"
subsystem: frontend
tags: [intelligence-page, priority-actions, quick-capture, stale-notes, ui]
dependency_graph:
  requires: []
  provides: [intelligence-page-right-column-sections, stale-notes-badges]
  affects: [IntelligencePage.tsx]
tech_stack:
  added: []
  patterns: [fetch-to-api, useCallback-state-pattern, urgency-badge-helper]
key_files:
  modified:
    - frontend/src/components/IntelligencePage.tsx
decisions:
  - "Priority Actions card placed between Weekly Recap and Quick Capture for visual priority hierarchy"
  - "urgency computed client-side from due_date: overdue=High(red), <=7d=Med(amber), >7d=Low(green)"
  - "daysSince uses Date.now() - new Date(dateStr).getTime() / 86400000 floor division"
  - "handleQuickCapture POSTs to /capture with note_type: note matching existing capture API contract"
  - "stale notes days badge replaces raw updated_at string — shows e.g. 45d in amber chip"
metrics:
  duration: 115s
  completed_date: "2026-03-28"
  tasks_completed: 2
  files_modified: 1
---

# Phase 41.1 Plan 03: Intelligence Page Right Column Improvements Summary

Three Visily gap closures for the Intelligence page: Priority Actions with urgency badges (Gap 4), Quick Capture shortcut textarea (Gap 5), and days-stale badges on stale notes (Gap 6).

## Tasks Completed

| Task | Description | Commit |
|------|-------------|--------|
| 1 | Add Priority Actions section with urgency badges to right column | c2eafb2 |
| 2 | Add Quick Capture card and days-stale badges on stale notes | 357774b |

## What Was Built

**Task 1 — Priority Actions**
- Imports `ActionItemRow` and `useUIContext` (for navigation)
- `fetchPriorityActions` calls `GET /actions?done=0&limit=5`
- `togglePriorityAction` / `deletePriorityAction` PUT/DELETE then re-fetch
- `getUrgency(dueDate)` returns `{ label, className } | null` — High/Med/Low based on due date proximity
- Priority Actions card renders in right column between Weekly Recap and Quick Capture
- "Go to Actions View" button calls `setCurrentView('actions')`

**Task 2 — Quick Capture + stale badges**
- `daysSince(dateStr)` computes floor of millisecond diff / 86400000
- `handleQuickCapture` POSTs to `/capture` with `{ body, note_type: 'note' }`, shows `toast.success/error`
- Quick Capture card: textarea (3 rows, placeholder) + "Save to Inbox" Button (disabled when empty or saving)
- Stale notes list now shows `{N}d` amber badge (bg-amber-500/15 text-amber-400) instead of raw date string

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None — all three features are fully wired to real API endpoints (`/actions`, `/capture`, `/intelligence`).

## Self-Check: PASSED

- `/Users/tuomasleppanen/second-brain/frontend/src/components/IntelligencePage.tsx` — exists and compiles
- Commits `c2eafb2` and `357774b` — confirmed in git log
- `npx tsc --noEmit` — zero errors
