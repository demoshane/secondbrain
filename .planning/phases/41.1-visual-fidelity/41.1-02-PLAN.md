---
phase: 41.1-visual-fidelity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/ActionsPage.tsx
  - frontend/src/components/MeetingsPage.tsx
autonomous: true
execution_strategy: multi-agent
requirements: []
must_haves:
  truths:
    - "Actions page groups action items by source note with collapsible sections"
    - "Actions page has a Collapse All / Expand All toggle button"
    - "Meetings list items show colored date chips (e.g. MAR 28) inline with title"
  artifacts:
    - path: "frontend/src/components/ActionsPage.tsx"
      provides: "Source-note grouped action items with collapse toggle"
      contains: "Collapse All"
    - path: "frontend/src/components/MeetingsPage.tsx"
      provides: "Date chip in meeting list rows"
      contains: "toUpperCase"
  key_links:
    - from: "frontend/src/components/ActionsPage.tsx"
      to: "ActionItemRow"
      via: "import"
      pattern: "ActionItemRow"
    - from: "frontend/src/components/ActionsPage.tsx"
      to: "note_path"
      via: "grouping key"
      pattern: "note_path"
---

<objective>
Close Gap 3 (ActionsPage source-note grouping) and Gap 10 (MeetingsPage date chips).

Purpose: Group action items by their source note for better organization. Add colored date chips to meeting list items for visual scanning.
Output: Updated ActionsPage.tsx and MeetingsPage.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/LEARNINGS.md
@frontend/src/components/ActionsPage.tsx
@frontend/src/components/MeetingsPage.tsx
@frontend/src/components/ui/action-item-row.tsx
@frontend/src/types.ts
@ui-descriptions/actions-page.md
</context>

<interfaces>
From frontend/src/types.ts:
```typescript
export interface ActionItem {
  id: number
  note_path: string
  text: string
  done: boolean
  due_date: string | null
  assignee_path: string | null
}

export interface MeetingSummary {
  path: string
  title: string
  meeting_date: string
  participant_count: number
  open_actions: number
}
```

From frontend/src/components/ui/action-item-row.tsx:
```typescript
interface ActionItemRowProps {
  item: ActionItem
  onToggle: (id: number) => void
  onDelete: (id: number) => void
  onAssign?: (id: number, path: string | null) => void
  showSource?: boolean
  className?: string
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add source-note grouping to ActionsPage</name>
  <files>frontend/src/components/ActionsPage.tsx</files>
  <read_first>
    - frontend/src/components/ActionsPage.tsx
    - frontend/src/components/ui/action-item-row.tsx
    - frontend/src/types.ts
  </read_first>
  <action>
Replace the flat action item list with source-note grouped sections.

Implementation:
- Add state: `const [allCollapsed, setAllCollapsed] = useState(false)`
- Add grouping function `groupBySource(items: ActionItem[]): Map&lt;string, ActionItem[]&gt;` that groups by `item.note_path`. Key = note_path, value = array of ActionItems.
- Extract display title from note_path: take last segment, remove `.md` extension, replace dashes/underscores with spaces, capitalize first letter. E.g. `meetings/2026-03-28-standup.md` -> `2026 03 28 standup` (keep it simple — just filename sans extension).
- Add "Sort by: Source Note" label and "Collapse All" / "Expand All" toggle button in the header area between the filter bar and the content area. Button text toggles: when `allCollapsed` is true show "Expand All", otherwise "Collapse All". Use `ChevronDown` / `ChevronRight` icon from lucide-react.
- Render each group as a `div` with:
  - Header row: `button` element with note title (extracted), action count badge (e.g. "3 items"), click toggles group collapse. Use local state `Map&lt;string, boolean&gt;` for per-group collapse, initialized from `allCollapsed`.
  - When `allCollapsed` changes, reset all groups to match.
  - Body: conditionally rendered `ActionItemRow` components for each item in the group
- Keep existing filter bar (status filter buttons + assignee select) unchanged
- Keep existing empty states (filtered empty, all done) unchanged
- Keep existing ConfirmDialog for delete unchanged
- Import `ChevronDown`, `ChevronRight` from lucide-react (add to existing import)

Do NOT change the loadActions, toggleDone, handleDelete, confirmDelete functions.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain/frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File contains `groupBySource` function that groups by `note_path`
    - File contains "Collapse All" and "Expand All" text strings
    - File contains `allCollapsed` state variable
    - File contains per-group collapse state (Map or Record)
    - File renders ActionItemRow inside grouped sections
    - File imports `ChevronDown` or `ChevronRight` from lucide-react
  </acceptance_criteria>
  <done>Actions page groups items by source note with collapsible sections and Collapse All toggle</done>
</task>

<task type="auto">
  <name>Task 2: Add colored date chips to MeetingsPage list items</name>
  <files>frontend/src/components/MeetingsPage.tsx</files>
  <read_first>
    - frontend/src/components/MeetingsPage.tsx
    - frontend/src/types.ts
  </read_first>
  <action>
Add a colored date chip to each meeting list row item.

Implementation:
- Add a helper function `formatDateChip(dateStr: string): { month: string; day: string } | null` that:
  - Parses the meeting_date string (format varies: ISO date or "YYYY-MM-DD")
  - Returns `{ month: "MAR", day: "28" }` — 3-letter uppercase month abbreviation + day number
  - Returns null if dateStr is falsy or unparseable
- In each meeting list row (the `filtered.map(m => ...)` block), before the title `<span>`:
  - Call `formatDateChip(m.meeting_date)`
  - If result is non-null, render a date chip: `<span className="inline-flex flex-col items-center justify-center w-10 h-10 rounded-md bg-primary/15 text-primary shrink-0"><span className="text-[10px] font-semibold leading-none">{month}</span><span className="text-sm font-bold leading-none">{day}</span></span>`
  - The chip sits to the left of the title in a flex row
- Adjust the meeting row layout: change inner div to `flex items-center gap-2.5` to accommodate the chip before the title
- Remove the existing `{m.meeting_date && (<div className="text-xs text-muted-foreground mt-0.5">...` line since the date is now shown in the chip

Do NOT change the detail column, action handlers, or modal logic.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain/frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File contains `formatDateChip` function
    - File contains `toUpperCase()` call for month abbreviation
    - File contains `bg-primary/15 text-primary` classes for chip styling
    - File contains `w-10 h-10` chip dimensions
    - The old plain-text date line (`text-xs text-muted-foreground mt-0.5` with meeting_date) is removed from list rows
  </acceptance_criteria>
  <done>Meeting list rows show colored date chips (e.g. "MAR 28") inline with title</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes in frontend directory
- ActionsPage contains source-note grouping with collapse toggle
- MeetingsPage list rows contain date chip elements
</verification>

<success_criteria>
- Gap 3 closed: Actions grouped by source note with collapsible sections
- Gap 10 closed: Meetings list items show colored date chips
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/41.1-visual-fidelity/41.1-02-SUMMARY.md`
</output>
