---
phase: 41.1-visual-fidelity
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/PeoplePage.tsx
  - frontend/src/components/ProjectsPage.tsx
  - frontend/src/components/LinksPage.tsx
autonomous: true
execution_strategy: multi-agent
requirements: []
must_haves:
  truths:
    - "People detail header shows circular avatar with initials left of the person name"
    - "People detail has a Profile & Context section prominently displaying the person note body"
    - "Projects detail header shows 4 stat tiles: Related Notes, Open Actions, Linked Meetings, Last Updated"
    - "Links detail header has an Open as Note button that navigates to the Notes view"
  artifacts:
    - path: "frontend/src/components/PeoplePage.tsx"
      provides: "Avatar initials + Profile & Context section"
      contains: "AvatarInitials"
    - path: "frontend/src/components/ProjectsPage.tsx"
      provides: "Stat tiles row in detail header"
      contains: "stat-tiles"
    - path: "frontend/src/components/LinksPage.tsx"
      provides: "Open as Note button"
      contains: "Open as Note"
  key_links:
    - from: "frontend/src/components/LinksPage.tsx"
      to: "NoteContext"
      via: "openNote + setCurrentView"
      pattern: "openNote.*setCurrentView"
---

<objective>
Close Gap 7 (PeoplePage avatar + profile), Gap 8 (ProjectsPage stat tiles), and Gap 9 (LinksPage "Open as Note").

Purpose: Add visual identity to people (avatar initials), surface project stats at a glance, and add note navigation from links detail.
Output: Updated PeoplePage.tsx, ProjectsPage.tsx, LinksPage.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/LEARNINGS.md
@frontend/src/components/PeoplePage.tsx
@frontend/src/components/ProjectsPage.tsx
@frontend/src/components/LinksPage.tsx
@frontend/src/types.ts
@ui-descriptions/people-page.md
@ui-descriptions/projects-page.md
</context>

<interfaces>
From frontend/src/types.ts:
```typescript
export interface PersonSummary {
  path: string; title: string; updated_at: string;
  open_actions: number; org: string; last_interaction: string | null; mention_count: number;
}
export interface ProjectSummary {
  path: string; title: string; updated_at: string; open_actions: number;
}
export interface ActionItem {
  id: number; note_path: string; text: string; done: boolean;
  due_date: string | null; assignee_path: string | null;
}
```

From LinksPage.tsx (existing imports):
```typescript
// Currently does NOT import useUIContext or useNoteContext — needs to add them
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add avatar initials and Profile & Context to PeoplePage</name>
  <files>frontend/src/components/PeoplePage.tsx</files>
  <read_first>
    - frontend/src/components/PeoplePage.tsx
    - frontend/src/types.ts
    - ui-descriptions/people-page.md
  </read_first>
  <action>
Add avatar initials component and restructure the detail header to prominently show profile context.

AvatarInitials implementation (inline in PeoplePage, not a separate file):
- Add a function component: `function AvatarInitials({ name, size = 'lg' }: { name: string; size?: 'sm' | 'lg' })`
- Extract initials: split name by spaces, take first char of first and last word, uppercase. E.g. "Sarah Jenkins" -> "SJ", "Tuomas" -> "TU" (first two chars if single word).
- Size `lg`: `w-16 h-16 text-xl` (64px, for detail header)
- Size `sm`: `w-8 h-8 text-xs` (32px, for list rows)
- Render: `<div className={cn("rounded-full bg-primary/20 text-primary font-semibold flex items-center justify-center shrink-0", sizeClass)}>{initials}</div>`

Detail header changes:
- In the detail header `<div className="px-6 py-4 border-b ...">`:
  - Add `AvatarInitials` with size="lg" to the LEFT of the name heading
  - Layout: `<div className="flex items-center gap-4">` wrapping avatar + name/org block
  - Keep existing "Open in Notes" button on the right

Profile & Context section:
- The existing "Brain Insight" CollapsibleSection already renders `personNote.body` as markdown
- Rename it from "Brain Insight" to "Profile & Context" — change the `title` prop
- This is the person note body, which IS the profile context — just relabeling for clarity

List row enhancement (optional, small):
- In each person list row, add `AvatarInitials` with size="sm" before the person name
- Adjust list row: wrap content in `<div className="flex items-center gap-2.5">`
- Avatar sits left of name

Do NOT change action handlers, delete logic, or modal components.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain/frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File contains `AvatarInitials` function component
    - File contains initials extraction logic using `split` and `toUpperCase`
    - File contains `w-16 h-16` (large avatar) and `w-8 h-8` (small avatar) size classes
    - File contains `bg-primary/20 text-primary` avatar styling
    - File contains "Profile & Context" string (renamed from "Brain Insight")
    - Detail header has avatar left of name (flex items-center gap-4 pattern)
  </acceptance_criteria>
  <done>People detail shows circular avatar with initials; profile body prominently displayed as "Profile & Context"</done>
</task>

<task type="auto">
  <name>Task 2: Add stat tiles to ProjectsPage and Open as Note to LinksPage</name>
  <files>frontend/src/components/ProjectsPage.tsx, frontend/src/components/LinksPage.tsx</files>
  <read_first>
    - frontend/src/components/ProjectsPage.tsx
    - frontend/src/components/LinksPage.tsx
    - frontend/src/types.ts
  </read_first>
  <action>
ProjectsPage stat tiles:
- In the detail header (the `<div className="px-6 py-4 border-b ...">` block), after the title+status row and before the collapsible sections:
- Add a stat tiles row: `<div className="flex items-center gap-4 mt-4" data-testid="stat-tiles">`
- 4 tiles, each: `<div className="flex-1 rounded-lg border border-border bg-card p-3 text-center"><div className="text-lg font-semibold text-foreground">{value}</div><div className="text-xs text-muted-foreground">{label}</div></div>`
- Tile values (all data already fetched):
  1. Related Notes: `backlinks.length`
  2. Open Actions: `actions.filter(a => !a.done).length`
  3. Linked Meetings: `projectDetail?.linked_meetings.length ?? 0`
  4. Last Updated: compute relative time from `projectDetail?.updated_at` using a simple helper `function timeAgo(dateStr: string): string` — returns "Xd ago", "Xh ago", "Xm ago", "just now"
- The `timeAgo` helper: compute diff in ms, format as days/hours/minutes ago

LinksPage "Open as Note" button:
- Import `useUIContext` from `@/contexts/UIContext` and `useNoteContext` from `@/contexts/NoteContext`
- Destructure `{ setCurrentView }` from `useUIContext()` and `{ openNote }` from `useNoteContext()` at component top
- In the detail header buttons area (the `<div className="flex items-center gap-2 flex-shrink-0">` block), add a secondary button BEFORE the "Visit Link" button:
  ```jsx
  <Button
    size="sm"
    variant="outline"
    onClick={async () => {
      if (!selectedLink) return
      await openNote(selectedLink.path)
      setCurrentView('notes')
    }}
  >
    <FileText className="h-4 w-4 mr-1" />
    Open as Note
  </Button>
  ```
- Import `FileText` from lucide-react (add to existing import)

Do NOT change delete logic, filter logic, or modal components in either file.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain/frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - ProjectsPage contains `data-testid="stat-tiles"` div
    - ProjectsPage contains 4 stat labels: "Related Notes", "Open Actions", "Linked Meetings", "Last Updated"
    - ProjectsPage contains `timeAgo` helper function
    - LinksPage contains "Open as Note" button text
    - LinksPage imports `useUIContext` and `useNoteContext`
    - LinksPage imports `FileText` from lucide-react
    - LinksPage contains `openNote` and `setCurrentView` calls in the button handler
  </acceptance_criteria>
  <done>Projects detail shows 4 stat tiles; Links detail has "Open as Note" button navigating to Notes view</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes in frontend directory
- PeoplePage detail has avatar initials + Profile & Context section
- ProjectsPage detail has 4 stat tiles
- LinksPage detail has "Open as Note" button
</verification>

<success_criteria>
- Gap 7 closed: People have avatar initials and prominent profile context display
- Gap 8 closed: Projects detail shows stat tiles for Related Notes, Open Actions, Linked Meetings, Last Updated
- Gap 9 closed: Links detail has "Open as Note" button wired to openNote + setCurrentView
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/41.1-visual-fidelity/41.1-04-SUMMARY.md`
</output>
