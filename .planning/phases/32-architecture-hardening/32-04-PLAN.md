---
phase: 32-architecture-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/db.py
  - engine/brain_health.py
  - engine/export.py
  - tests/test_brain_health.py
autonomous: true
requirements: [ARCH-06]
must_haves:
  truths:
    - "Completed action items older than 90 days are moved to action_items_archive"
    - "Archive table has archived_at and archived_reason columns for GDPR audit trail"
    - "sb-health reports archived item count"
    - "Archived items are NOT visible in GUI, CLI, or MCP list endpoints"
    - "sb-export includes archived items for GDPR data portability"
    - "audit_log has index on (created_at, note_path)"
  artifacts:
    - path: "engine/db.py"
      provides: "action_items_archive table DDL + audit_log index"
      contains: "action_items_archive"
    - path: "engine/brain_health.py"
      provides: "archive_old_action_items() function + archive count in health report"
      contains: "archive_old_action_items"
  key_links:
    - from: "engine/brain_health.py"
      to: "action_items_archive table"
      via: "INSERT INTO archive, DELETE FROM action_items"
      pattern: "action_items_archive"
---

<objective>
Create action_items_archive table for completed items >90 days old, add archival function triggered by sb-health, add audit_log index, include archived items in sb-export.

Purpose: Prevent action_items table from growing unbounded; maintain GDPR audit trail and data portability.
Output: Archive table, archival function in brain_health.py, audit_log index, export inclusion.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-architecture-hardening/32-CONTEXT.md
@.planning/phases/32-architecture-hardening/32-RESEARCH.md
@.claude/LEARNINGS.md
@engine/db.py
@engine/brain_health.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Archive table DDL + audit_log index</name>
  <files>engine/db.py, tests/test_db.py</files>
  <behavior>
    - action_items_archive table exists after init_schema()
    - Columns: id, note_path, text, done_at, created_at, archived_at (DEFAULT CURRENT_TIMESTAMP), archived_reason
    - audit_log has composite index on (created_at, note_path)
    - Both are idempotent (IF NOT EXISTS)
  </behavior>
  <action>
    Add `migrate_add_action_items_archive_table(conn)` to engine/db.py:
    ```sql
    CREATE TABLE IF NOT EXISTS action_items_archive (
        id              INTEGER PRIMARY KEY,
        note_path       TEXT NOT NULL,
        text            TEXT NOT NULL,
        done_at         TEXT,
        created_at      TEXT NOT NULL,
        archived_at     TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%SZ', 'now')),
        archived_reason TEXT NOT NULL DEFAULT 'auto_90day'
    )
    ```
    Add audit_log index:
    ```sql
    CREATE INDEX IF NOT EXISTS idx_audit_log_created_path ON audit_log(created_at, note_path)
    ```
    Register in init_schema().

    Tests: verify table and index exist after init_schema().
  </action>
  <verify>
    <automated>uv run pytest tests/test_db.py -x -q -k "archive or audit_log"</automated>
  </verify>
  <done>Archive table and audit_log index created idempotently in init_schema()</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Archival function + health report + export inclusion</name>
  <files>engine/brain_health.py, engine/export.py, tests/test_brain_health.py</files>
  <behavior>
    - archive_old_action_items(conn, days=90) moves done items older than 90 days to archive
    - Returns count of archived items
    - sb-health report includes "archived_action_items" count
    - sb-export includes archived items in export output
    - Archival does not affect non-done items or items less than 90 days old
  </behavior>
  <action>
    Add `archive_old_action_items(conn, days=90)` to engine/brain_health.py:
    1. SELECT from action_items WHERE done=1 AND done_at < datetime('now', '-{days} days')
    2. INSERT each into action_items_archive (note_path, text, done_at, created_at, archived_reason='auto_90day')
    3. DELETE the archived rows from action_items
    4. Return count
    5. All in a single transaction

    Call `archive_old_action_items(conn)` from the main health check function (wherever sb-health triggers compute_health_score or similar).

    Add archived count to health report output (e.g., in the dict returned by the health endpoint).

    In engine/export.py (or wherever sb-export builds its JSON), add a query for action_items_archive and include in export.

    Tests in tests/test_brain_health.py:
    - Insert done items with done_at > 90 days ago -> verify archived
    - Insert done items with done_at < 90 days ago -> verify NOT archived
    - Insert non-done items -> verify NOT archived
    - Verify count returned matches archived items
    - If tests/test_brain_health.py doesn't exist, create it
  </action>
  <verify>
    <automated>uv run pytest tests/test_brain_health.py -x -q</automated>
  </verify>
  <done>Archival moves old done items to archive; health report includes count; export includes archived items; non-done and recent items untouched</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_db.py tests/test_brain_health.py -x -q` -- all pass
- `uv run pytest tests/ -q` -- full suite green
</verification>

<success_criteria>
- action_items_archive table exists with correct schema
- audit_log has (created_at, note_path) index
- 90-day archival works correctly
- Health report shows archived count
- Export includes archived items
</success_criteria>

<output>
After completion, create `.planning/phases/32-architecture-hardening/32-04-SUMMARY.md`
</output>
