# Plan 32-04: Action item archiving + audit_log index + sign-off

<objective>
Keep the DB lean long-term: archive old completed actions and index audit_log
for fast queries. Full Phase 32 sign-off.
</objective>

<context>
@.planning/PROJECT.md
@.claude/LEARNINGS.md
@engine/db.py
@engine/api.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: action_items_archive table + auto-archiving job</name>
  <files>engine/db.py, engine/intelligence.py</files>
  <behavior>
    - Add to SCHEMA_SQL: CREATE TABLE IF NOT EXISTS action_items_archive (
        same columns as action_items, plus archived_at TEXT DEFAULT (strftime(...))
      )
    - Add archive_old_actions(conn, days: int = 90):
        INSERT OR IGNORE INTO action_items_archive SELECT *, strftime(...) FROM action_items
        WHERE done=1 AND created_at < date('now', '-{days} days')
        DELETE FROM action_items WHERE id IN (archived)
    - Call archive_old_actions() from init_schema() (runs at startup — fast, idempotent)
    - sb-health output: add "archived_actions": N count
  </behavior>
  <action>
    Read engine/db.py and engine/intelligence.py; implement archiving; run suite
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/ -q 2>&1 | tail -10</automated>
  </verify>
  <done>Archive runs; old done actions moved; suite green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: audit_log index on (created_at, note_path)</name>
  <files>engine/db.py</files>
  <behavior>
    - Add to SCHEMA_SQL: CREATE INDEX IF NOT EXISTS idx_audit_log_created_at
        ON audit_log(created_at DESC, note_path)
    - Verify index exists after init_schema() via PRAGMA index_list(audit_log)
    - No data changes; index is non-destructive
  </behavior>
  <action>
    Read engine/db.py SCHEMA_SQL; add index; run suite
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/ -q 2>&1 | tail -10</automated>
  </verify>
  <done>Index added; suite green</done>
</task>

<task type="auto">
  <name>Task 3: Full Phase 32 sign-off</name>
  <files>none</files>
  <behavior>
    Run complete test suite. Confirm:
    - ARCH-01 (relative paths): no absolute paths in DB after migration
    - ARCH-02 (FK cascade): PRAGMA foreign_keys = ON confirmed in get_connection()
    - ARCH-03 (connection safety): no bare conn.close() without finally in api.py
    - ARCH-04 (upload cap + suppress_next_delete): upload > 50 MB returns 413
    - ARCH-05 (tags junction): note_tags table exists and populated
    - ARCH-06 (archiving + audit index): archive table exists; index in place
  </behavior>
  <action>
    uv run pytest tests/ -q 2>&1 | tail -15
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/ -q 2>&1 | tail -15</automated>
  </verify>
  <done>All requirements met; suite exits 0</done>
</task>

</tasks>

<verification>
cd /Users/tuomasleppanen/second-brain && uv run pytest tests/ -q 2>&1 | tail -10
</verification>

<success_criteria>
- action_items_archive table; done actions > 90 days moved on startup
- audit_log indexed on (created_at, note_path)
- All 6 ARCH requirements verifiably met
- Full suite green
</success_criteria>

<output>After completion create `.planning/phases/32-architecture-hardening/32-04-SUMMARY.md`</output>
