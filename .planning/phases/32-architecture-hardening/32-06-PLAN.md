---
phase: 32-architecture-hardening
plan: 06
type: execute
wave: 3
depends_on: [32-03]
files_modified:
  - engine/people.py
  - engine/api.py
  - engine/mcp_server.py
  - engine/capture.py
  - engine/reindex.py
  - tests/test_people.py
  - tests/test_reindex.py
  - tests/test_mcp.py
autonomous: true
requirements: [ARCH-10, ARCH-11, ARCH-12, ARCH-13]
must_haves:
  truths:
    - "list_people_with_metrics() is a shared function in engine/people.py"
    - "/people API and sb_list_people MCP both call list_people_with_metrics()"
    - "People column entries matched by both path AND exact title"
    - "sb-reindex --entities merges extracted people with existing frontmatter, not overwrite"
    - "sb_edit and update_note() re-extract entities on body change and merge into people+entities columns"
  artifacts:
    - path: "engine/people.py"
      provides: "list_people_with_metrics(conn) shared service function"
      exports: ["list_people_with_metrics"]
    - path: "tests/test_people.py"
      provides: "Tests for shared people service"
    - path: "tests/test_reindex.py"
      provides: "Tests for entity merge on reindex"
  key_links:
    - from: "engine/api.py"
      to: "engine/people.py"
      via: "from engine.people import list_people_with_metrics"
      pattern: "from engine\\.people import"
    - from: "engine/mcp_server.py"
      to: "engine/people.py"
      via: "from engine.people import list_people_with_metrics"
      pattern: "from engine\\.people import"
    - from: "engine/capture.py"
      to: "engine/entities.py"
      via: "Re-extract entities on update_note and merge with existing"
      pattern: "extract_entities"
---

<objective>
Extract shared people service function, fix path+title matching, implement entity merge on reindex (not overwrite), and re-extract entities on edit.

Purpose: Eliminate duplicate people query code between API and MCP, fix zero-metrics bug where name-string entries in people column miss matches, prevent reindex from destroying user-curated people fields, keep entities fresh after edits.
Output: New engine/people.py, updated API and MCP to use it, merge logic in reindex, re-extraction on edit.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-architecture-hardening/32-CONTEXT.md
@.planning/phases/32-architecture-hardening/32-RESEARCH.md
@.planning/phases/32-architecture-hardening/32-03-SUMMARY.md
@.claude/LEARNINGS.md
@engine/api.py
@engine/mcp_server.py
@engine/capture.py
@engine/reindex.py
@engine/entities.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared people service + path+title matching</name>
  <files>engine/people.py, engine/api.py, engine/mcp_server.py, tests/test_people.py</files>
  <behavior>
    - list_people_with_metrics(conn) returns list of dicts with: path, title, updated_at, open_actions, org, last_interaction, mention_count
    - People matched by BOTH path AND exact title (case-insensitive) — fixes ARCH-11 zero-metrics bug
    - /people API endpoint calls list_people_with_metrics() and returns its output
    - sb_list_people MCP tool calls list_people_with_metrics() and returns its output
    - Both return identical field structure
  </behavior>
  <action>
    **ARCH-10 — Create engine/people.py:**
    New file with `list_people_with_metrics(conn)` function. The query matches people by BOTH `pe.value = n.path` OR `LOWER(pe.value) = LOWER(n.title)` in the json_each subqueries (or note_people junction table if available from Plan 03).

    Use the note_people junction table for the main query if Plan 03 is complete (check if table exists), with fallback to json_each for backward compat.

    Prefer using the junction table: `JOIN note_people np ON np.note_path = m.path WHERE (np.person = n.path OR LOWER(np.person) = LOWER(n.title))`.

    Import PERSON_TYPES from engine.db (from Plan 05, or hardcode if Plan 05 hasn't run yet — check import availability).

    **ARCH-10 — Wire up:**
    In engine/api.py /people endpoint: replace inline SQL with `from engine.people import list_people_with_metrics; result = list_people_with_metrics(conn)`.
    In engine/mcp_server.py sb_list_people: replace duplicate SQL with same import.

    **ARCH-11 — Path+title matching:** Already handled by the query design above — matching on `pe.value = n.path OR LOWER(pe.value) = LOWER(n.title)`.

    **Tests in tests/test_people.py:**
    - Create person note, create meeting note with person's PATH in people column → verify mention_count > 0
    - Create person note, create meeting note with person's TITLE (name string) in people column → verify mention_count > 0 (this is the ARCH-11 fix)
    - Verify /people and sb_list_people return same structure
  </action>
  <verify>
    <automated>uv run pytest tests/test_people.py -x -q</automated>
  </verify>
  <done>Shared list_people_with_metrics in people.py; /people and sb_list_people use it; path+title matching works</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Entity merge on reindex + re-extract on edit</name>
  <files>engine/reindex.py, engine/capture.py, tests/test_reindex.py, tests/test_mcp.py</files>
  <behavior>
    - sb-reindex --entities merges extracted people with existing frontmatter people field (union, not overwrite)
    - User-curated entries in frontmatter people field are preserved after reindex
    - update_note() re-runs extract_entities() on new body
    - update_note() merges extracted people with existing people (union)
    - update_note() updates both people and entities DB columns
    - update_note() also updates note_people junction table
  </behavior>
  <action>
    **ARCH-12 — Reindex merge:**
    In engine/reindex.py, find where entities are written during reindex. Change from overwrite to merge:
    1. Read existing frontmatter people field from the .md file
    2. Run extract_entities() on body
    3. Merge: `merged_people = list(dict.fromkeys(existing_people + extracted_people))` (preserves order, deduplicates)
    4. Write merged list back to people column (and note_people junction table)

    **ARCH-13 — Re-extract on edit:**
    In engine/capture.py `update_note()`:
    1. After updating body, call `extract_entities(title, new_body)`
    2. Get existing people from DB or function params
    3. Merge extracted people with existing: `merged = list(dict.fromkeys(existing + extracted))`
    4. UPDATE notes SET people=?, entities=? with merged values
    5. Refresh note_people junction table (DELETE + INSERT for this note_path)

    **Tests:**
    Create tests/test_reindex.py:
    - Create note with frontmatter people=["Alice"], body mentions "Bob" → reindex → verify people=["Alice", "Bob"] (not just ["Bob"])
    - Verify user-curated entry preserved after reindex

    In tests/test_mcp.py or tests/test_capture.py:
    - Edit note body to mention new person → verify entities and people columns updated
    - Verify note_people junction table updated on edit
  </action>
  <verify>
    <automated>uv run pytest tests/test_reindex.py tests/test_capture.py -x -q</automated>
  </verify>
  <done>Reindex merges people (not overwrites); edit re-extracts and merges entities; junction table updated on edit</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_people.py tests/test_reindex.py tests/test_capture.py tests/test_mcp.py -x -q` — all pass
- `uv run pytest tests/ -q` — full suite green
</verification>

<success_criteria>
- engine/people.py exists with shared service function
- /people and sb_list_people produce identical output via shared function
- Path+title matching works (ARCH-11 fix verified)
- Reindex merges, not overwrites, people field
- Edit re-extracts entities and updates all columns + junction table
</success_criteria>

<output>
After completion, create `.planning/phases/32-architecture-hardening/32-06-SUMMARY.md`
</output>
