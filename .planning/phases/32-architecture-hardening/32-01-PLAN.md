---
phase: 32-architecture-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/paths.py
  - engine/db.py
  - engine/capture.py
  - tests/test_paths.py
  - tests/test_db.py
  - tests/test_capture.py
autonomous: true
requirements: [ARCH-01]
must_haves:
  truths:
    - "All new notes are stored with relative paths in DB"
    - "Existing absolute paths are migrated to relative on init_schema()"
    - "resolve_path() returns absolute path from relative; store_path() returns relative from absolute"
    - "Paths outside BRAIN_ROOT are skipped with warning during migration"
    - "capture_note() stores relative path in DB via store_path()"
    - "Note read paths (note_meta, sb_read, list endpoints) resolve via resolve_path()"
  artifacts:
    - path: "engine/paths.py"
      provides: "resolve_path() and store_path() helpers"
      exports: ["resolve_path", "store_path"]
    - path: "engine/db.py"
      provides: "migrate_paths_to_relative() called from init_schema()"
      contains: "migrate_paths_to_relative"
    - path: "engine/capture.py"
      provides: "store_path() called before DB insert"
      contains: "store_path"
  key_links:
    - from: "engine/db.py"
      to: "engine/paths.py"
      via: "import resolve_path, store_path, BRAIN_ROOT"
      pattern: "from engine\\.paths import.*resolve_path"
    - from: "engine/capture.py"
      to: "engine/paths.py"
      via: "import store_path for DB write boundary"
      pattern: "from engine\\.paths import.*store_path"
---

<objective>
Add resolve_path/store_path helpers to paths.py, implement the idempotent migration that converts all absolute paths in the notes table (and child tables) to relative paths, and wire store_path/resolve_path at DB read/write boundaries so new notes use relative paths.

Purpose: Moving ~/SecondBrain currently orphans the entire DB index. Relative paths make the brain directory portable.
Output: Path helpers in paths.py, migration in db.py, call-site wiring in capture.py and read paths, tests proving round-trip, migration, and end-to-end correctness.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-architecture-hardening/32-CONTEXT.md
@.planning/phases/32-architecture-hardening/32-RESEARCH.md
@.claude/LEARNINGS.md
@engine/paths.py
@engine/db.py
@engine/capture.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Path helpers + tests</name>
  <files>engine/paths.py, tests/test_paths.py</files>
  <behavior>
    - store_path("/Users/x/SecondBrain/coding/note.md") with BRAIN_ROOT=/Users/x/SecondBrain returns "coding/note.md"
    - resolve_path("coding/note.md") with BRAIN_ROOT=/Users/x/SecondBrain returns Path("/Users/x/SecondBrain/coding/note.md")
    - resolve_path("/Users/x/SecondBrain/coding/note.md") returns it unchanged (already absolute — backward compat)
    - store_path raises ValueError if path is outside BRAIN_ROOT
    - store_path on already-relative path returns it unchanged
  </behavior>
  <action>
    Add to engine/paths.py:
    - `store_path(abs_path: str | Path) -> str` — converts absolute path to relative (relative_to BRAIN_ROOT). If already relative, return as-is. If outside BRAIN_ROOT, raise ValueError.
    - `resolve_path(rel_path: str | Path) -> Path` — if relative, return BRAIN_ROOT / rel_path. If already absolute, return as Path (backward compat during migration window).

    Write tests in tests/test_paths.py covering the behavior above. Tests must monkeypatch engine.paths.BRAIN_ROOT to a tmp_path fixture.
  </action>
  <verify>
    <automated>uv run pytest tests/test_paths.py -x -q</automated>
  </verify>
  <done>store_path and resolve_path pass all tests; handle edge cases (already relative, already absolute, outside BRAIN_ROOT)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Relative path migration in init_schema()</name>
  <files>engine/db.py, tests/test_db.py</files>
  <behavior>
    - After init_schema(), all rows in notes where path starts with "/" are converted to relative paths
    - Child tables (relationships.source_path, relationships.target_path, action_items.note_path, note_embeddings.note_path, audit_log.note_path, attachments.note_path, action_items.assignee_path) are also migrated
    - Already-relative paths are untouched
    - Paths outside BRAIN_ROOT are skipped with logging.warning()
    - Migration is idempotent — running twice has no effect
    - Migration runs in a single transaction
  </behavior>
  <action>
    Add `migrate_paths_to_relative(conn, brain_root)` in engine/db.py:
    1. Query all notes WHERE path LIKE '/%' (absolute paths)
    2. If none found, return early (idempotent)
    3. In a single transaction (`with conn:`):
       - For each absolute path, compute relative via Path.relative_to(brain_root)
       - UPDATE notes SET path=? WHERE path=?
       - UPDATE each child table's path columns similarly
    4. Log count via logging.info()
    5. Guard with try/except ValueError for paths outside BRAIN_ROOT — log warning, skip

    Register in init_schema() BEFORE any junction table or FK migrations (per locked decision).
    Pass BRAIN_ROOT from engine.paths: `from engine.paths import BRAIN_ROOT`.

    Add tests in tests/test_db.py:
    - Insert notes with absolute paths, call migrate, verify they become relative
    - Insert notes with relative paths, call migrate, verify untouched
    - Verify child tables also migrated
    - Verify idempotency (call twice, same result)
  </action>
  <verify>
    <automated>uv run pytest tests/test_db.py -x -q -k "relative or migrate_path"</automated>
  </verify>
  <done>Migration converts absolute paths to relative in all tables; idempotent; logged; registered in init_schema()</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire store_path/resolve_path at DB read/write boundaries</name>
  <files>engine/capture.py, tests/test_capture.py</files>
  <behavior>
    - capture_note() stores a relative path in the DB (not absolute)
    - update_note() stores a relative path in the DB (not absolute)
    - Reading a note path from DB and resolving it returns the correct absolute path
    - Existing tests still pass (backward compat via resolve_path accepting absolute)
  </behavior>
  <action>
    **Write boundary (capture.py):**
    In `capture_note()`, before the INSERT INTO notes statement, call `store_path()` on the file path to convert it to relative before DB storage. Same for `update_note()` if it writes paths.

    Import: `from engine.paths import store_path`

    Grep for any other DB INSERT/UPDATE that writes a file path and apply store_path() there too (e.g., relationships, attachments inserts in capture.py).

    **Read boundary — defer to Plan 05:**
    Note: Plan 05 already touches api.py and mcp_server.py for other ARCH fixes. The read-side resolve_path() wiring for note_meta(), sb_read, and list endpoints should be handled there since Plan 05 already modifies those files. However, the critical write-side wiring MUST happen here to prevent new absolute paths from being stored.

    **Tests (tests/test_capture.py):**
    - Add test: call capture_note() with a note, query DB directly, verify path column is relative (not starting with "/")
    - Add test: capture a note, then read it back using resolve_path() on the stored path, verify file exists at resolved location

    Use monkeypatching for BRAIN_ROOT as in Task 1.
  </action>
  <verify>
    <automated>uv run pytest tests/test_capture.py -x -q</automated>
  </verify>
  <done>capture_note() and update_note() store relative paths in DB; verified by test querying DB directly; no new absolute paths can enter the DB through the capture code path</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_paths.py tests/test_db.py tests/test_capture.py -x -q` — all pass
- `uv run pytest tests/ -q` — full suite still green (no regressions)
</verification>

<success_criteria>
- resolve_path/store_path helpers exist and are tested
- init_schema() runs migration that converts absolute paths to relative
- Migration is idempotent, transaction-wrapped, and logs progress
- capture_note() stores relative paths — verified by DB inspection test
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/32-architecture-hardening/32-01-SUMMARY.md`
</output>
