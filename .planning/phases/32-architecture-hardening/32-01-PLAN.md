# Plan 32-01: Relative path storage + migration

<objective>
Store paths in the DB relative to BRAIN_ROOT. Moving or renaming ~/SecondBrain
must not orphan the index. A migration script converts all existing rows.
</objective>

<context>
@.planning/PROJECT.md
@.claude/LEARNINGS.md
@engine/db.py
@engine/paths.py
@engine/capture.py
@engine/reindex.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add path helpers to engine/paths.py</name>
  <files>engine/paths.py</files>
  <behavior>
    - Add to_relative(abs_path: str | Path, brain_root: Path) -> str
      Returns posix relative path string, e.g. "meetings/2026-03-standup.md"
    - Add to_absolute(rel_path: str, brain_root: Path) -> Path
      Resolves relative path against brain_root; raises ValueError if result escapes root
    - Both functions used everywhere DB rows are read/written
  </behavior>
  <action>
    Read engine/paths.py; add helpers; run pytest tests/ -q to confirm no breakage
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run python -c "from engine.paths import to_relative, to_absolute; print('OK')" 2>&1</automated>
  </verify>
  <done>Helpers importable; tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migration — convert all absolute paths to relative in DB</name>
  <files>engine/db.py</files>
  <behavior>
    - Add migrate_relative_paths(conn, brain_root: Path) in db.py
    - Idempotent: skip rows where path does not start with brain_root str (already relative)
    - Updates: notes.path, note_embeddings.note_path, action_items.note_path,
      relationships.source_path, relationships.target_path, attachments.note_path,
      audit_log.note_path
    - All in one transaction; rolls back on any error
    - Call from init_schema() after existing migrations
  </behavior>
  <action>
    Read engine/db.py; implement migration; run full test suite
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/ -q 2>&1 | tail -10</automated>
  </verify>
  <done>Migration runs; suite green; no absolute paths remain in DB</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update all read/write paths to use helpers</name>
  <files>engine/capture.py, engine/reindex.py, engine/forget.py, engine/api.py</files>
  <behavior>
    - capture_note(): store to_relative(target, brain_root) in DB, return absolute path to caller
    - reindex_brain(): store relative paths; _resolve_note_path() in api.py calls to_absolute()
    - forget.py: resolve relative path before file deletion
    - All tests must pass — conftest fixtures already patch BRAIN_ROOT so relative paths
      resolve correctly within tmp dirs
  </behavior>
  <action>
    Update each file; run full suite; fix any test failures from path format changes
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/ -q 2>&1 | tail -15</automated>
  </verify>
  <done>Suite green; DB stores relative paths throughout</done>
</task>

</tasks>

<verification>
cd /Users/tuomasleppanen/second-brain && uv run pytest tests/ -q 2>&1 | tail -10
</verification>

<success_criteria>
- DB stores relative paths (no absolute paths in any table)
- to_relative / to_absolute helpers in paths.py
- Migration runs on startup and is idempotent
- Full suite green
</success_criteria>

<output>After completion create `.planning/phases/32-architecture-hardening/32-01-SUMMARY.md`</output>
