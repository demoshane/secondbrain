# Plan 32-03: Tags junction table + remove _SlashNormMiddleware

<objective>
Replace the JSON-text tags column with a proper indexed junction table so tag
filtering is fast and correct at scale. Remove _SlashNormMiddleware by fixing
its root cause in test fixture path construction.
</objective>

<context>
@.planning/PROJECT.md
@.claude/LEARNINGS.md
@engine/db.py
@engine/api.py
@engine/capture.py
@tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add note_tags junction table and migration</name>
  <files>engine/db.py</files>
  <behavior>
    - Add to SCHEMA_SQL: CREATE TABLE IF NOT EXISTS note_tags (
        note_path TEXT NOT NULL REFERENCES notes(path) ON DELETE CASCADE,
        tag TEXT NOT NULL,
        PRIMARY KEY (note_path, tag)
      )
    - Add migrate_note_tags_from_json(conn, brain_root): parse existing tags JSON column
      for all notes; INSERT OR IGNORE into note_tags; idempotent
    - Call from init_schema() after other migrations
    - Keep notes.tags TEXT column for now (removal is a future step after all readers updated)
  </behavior>
  <action>
    Read engine/db.py; add table + migration; run suite
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/ -q 2>&1 | tail -10</automated>
  </verify>
  <done>note_tags table created; migration populates it; suite green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update tag read/write to use junction table</name>
  <files>engine/capture.py, engine/api.py</files>
  <behavior>
    capture_note(): after writing note to DB, INSERT OR IGNORE tags into note_tags
    api.py tag save (PUT /notes/:path): update note_tags — DELETE old, INSERT new
    api.py /notes list: JOIN note_tags for tag filter instead of LIKE '%"tag"%' scan
    api.py /notes/:path GET: read tags from note_tags rather than JSON column
    Keep notes.tags column in sync (write both) for backwards compat during transition
  </behavior>
  <action>
    Read capture.py and api.py; update tag operations to use junction table; run suite
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_capture.py tests/test_gui.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>Tag operations use junction table; filter uses indexed query; tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Remove _SlashNormMiddleware by fixing test fixture paths</name>
  <files>engine/api.py, tests/conftest.py, tests/test_gui.py, tests/test_delete.py</files>
  <behavior>
    Root cause: tests encode absolute paths like /tmp/xxx/note.md into URL as
    /notes//tmp/xxx/note.md (double slash). Fix: test fixtures must URL-encode the
    path properly using urllib.parse.quote(path, safe='') so /tmp becomes %2Ftmp.
    - Audit all test files for /notes/ + abs path construction
    - Replace with: f"/notes/{quote(path, safe='')}"
    - After fixing, remove _SlashNormMiddleware class and app.wsgi_app assignment from api.py
    - Run full suite to confirm no double-slash routes remain
  </behavior>
  <action>
    Read engine/api.py, tests/conftest.py, tests/test_gui.py, tests/test_delete.py;
    fix URL construction in tests; remove middleware; run suite
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/ -q 2>&1 | tail -10</automated>
  </verify>
  <done>Middleware removed; all tests still pass</done>
</task>

</tasks>

<verification>
cd /Users/tuomasleppanen/second-brain && uv run pytest tests/ -q 2>&1 | tail -10
</verification>

<success_criteria>
- note_tags junction table created and populated
- Tag filtering uses indexed JOIN not LIKE scan
- _SlashNormMiddleware removed from production code
- Full suite green
</success_criteria>

<output>After completion create `.planning/phases/32-architecture-hardening/32-03-SUMMARY.md`</output>
