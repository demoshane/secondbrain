---
phase: 32-architecture-hardening
plan: 05
type: execute
wave: 3
depends_on: [32-01, 32-02]
files_modified:
  - engine/api.py
  - engine/forget.py
  - engine/capture.py
  - engine/intelligence.py
  - engine/search.py
  - engine/db.py
  - engine/mcp_server.py
  - tests/test_api.py
  - tests/test_forget.py
autonomous: true
requirements: [ARCH-07, ARCH-08, ARCH-09, ARCH-14, ARCH-16]
must_haves:
  truths:
    - "move_file() validates src and dst are within BRAIN_ROOT"
    - "note_meta() does not redundantly re-resolve path"
    - "forget_person() commits DB transaction first, then deletes files"
    - "forget_person() cleans person from people JSON in surviving notes + frontmatter"
    - "Entity extraction logs failures via logging.warning(), not bare except:pass"
    - "search_semantic() uses logging.warning(), not print()"
    - "All LIKE patterns with user input escape % and _ via _escape_like()"
    - "sb_person_context uses exact title match, not LIKE"
    - "PERSON_TYPES constant in db.py replaces all hardcoded type IN ('person','people')"
  artifacts:
    - path: "engine/db.py"
      provides: "PERSON_TYPES constant and _escape_like() helper"
      exports: ["PERSON_TYPES", "_escape_like"]
    - path: "engine/forget.py"
      provides: "Reversed cascade order: DB first, files second, with transaction"
      contains: "forget_person"
    - path: "engine/api.py"
      provides: "Path traversal guard on move_file, cleaned note_meta"
      contains: "is_relative_to"
  key_links:
    - from: "engine/api.py"
      to: "engine/db.py"
      via: "import PERSON_TYPES, _escape_like"
      pattern: "from engine\\.db import.*PERSON_TYPES"
    - from: "engine/forget.py"
      to: "notes table"
      via: "Transaction commit before file deletion"
      pattern: "conn\\.commit.*os\\.remove|unlink"
---

<objective>
Fix security and consistency issues: path traversal guard on move_file, reverse forget_person cascade order (DB first), replace bare except/print with logging, add LIKE escape helper, add PERSON_TYPES constant.

Purpose: Close security gaps (path traversal, LIKE injection), prevent data loss (forget order), improve debuggability (logging), reduce magic strings (PERSON_TYPES).
Output: Hardened api.py, safe forget.py, consistent logging, PERSON_TYPES constant used everywhere.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-architecture-hardening/32-CONTEXT.md
@.planning/phases/32-architecture-hardening/32-RESEARCH.md
@.planning/phases/32-architecture-hardening/32-01-SUMMARY.md
@.planning/phases/32-architecture-hardening/32-02-SUMMARY.md
@.claude/LEARNINGS.md
@engine/api.py
@engine/forget.py
@engine/capture.py
@engine/intelligence.py
@engine/search.py
@engine/db.py
@engine/mcp_server.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: PERSON_TYPES constant + _escape_like helper + LIKE/path security fixes</name>
  <files>engine/db.py, engine/api.py, engine/mcp_server.py, engine/capture.py, tests/test_api.py</files>
  <action>
    **ARCH-16 — PERSON_TYPES constant:**
    Add to engine/db.py (near top, after imports):
    ```python
    PERSON_TYPES = ("person", "people")
    ```
    Then grep all engine files for `type IN ('person'` or `type='person'` or `"person"` in type checks. Replace each with `PERSON_TYPES` usage:
    - SQL: `WHERE n.type IN (?, ?)` with `*PERSON_TYPES` as params
    - Python: `if note_type in PERSON_TYPES`
    Import from engine.db in api.py, mcp_server.py, capture.py.

    **ARCH-14 — _escape_like helper:**
    Add to engine/db.py:
    ```python
    def _escape_like(s: str) -> str:
        return s.replace("\\", "\\\\").replace("%", "\\%").replace("_", "\\_")
    ```
    Find all LIKE patterns in engine files with user-supplied input (especially note_meta() in api.py). Apply _escape_like() and add `ESCAPE '\\'` to the SQL.

    For sb_person_context: replace LIKE with exact title match (`WHERE LOWER(n.title) = LOWER(?)`).

    **ARCH-07 — Path traversal guard on move_file():**
    In api.py `move_file()`, validate both source and destination paths are within BRAIN_ROOT:
    ```python
    from engine.paths import BRAIN_ROOT, resolve_path
    src = resolve_path(src_path)
    dst = resolve_path(dst_path)
    if not src.resolve().is_relative_to(BRAIN_ROOT) or not dst.resolve().is_relative_to(BRAIN_ROOT):
        return jsonify({"error": "Path outside brain directory"}), 403
    ```

    **ARCH-07 — note_meta() cleanup:**
    In note_meta(), remove the redundant `_resolve_note_path` call that re-resolves an already-resolved path.

    **Tests:** Add test for move_file path traversal rejection (403). Add test for LIKE with % in title not causing SQL injection.
  </action>
  <verify>
    <automated>uv run pytest tests/test_api.py tests/test_mcp.py -x -q</automated>
  </verify>
  <done>PERSON_TYPES used everywhere; _escape_like applied to all LIKE patterns; move_file blocks traversal; note_meta cleaned up</done>
</task>

<task type="auto">
  <name>Task 2: forget_person cascade reversal + logging cleanup</name>
  <files>engine/forget.py, engine/capture.py, engine/intelligence.py, engine/search.py, tests/test_forget.py</files>
  <action>
    **ARCH-08 — forget_person cascade reversal:**
    In engine/forget.py, restructure forget_person (or the equivalent person-deletion function):
    1. Dry-run mode: query affected notes, return list for confirmation (same two-step token pattern as sb_forget)
    2. Execute mode (with valid token):
       a. Open DB transaction
       b. DELETE person note from notes table (FK cascade handles junction tables)
       c. For each surviving note that references this person in people JSON:
          - Remove person from the JSON array
          - UPDATE the notes.people column
          - Also remove from note_people junction table row
       d. COMMIT transaction
       e. AFTER commit: delete the person's .md file from disk
       f. If file deletion fails, log warning (sb-reindex will fix eventually)
    3. Clean person from frontmatter: for each surviving note file on disk whose people field contained the person, read frontmatter, remove person from people list, write back

    Per locked decision: structured fields only (frontmatter people/entities fields and DB columns); body text untouched.
    Per locked decision: DB first, files second.

    **ARCH-09 — Logging cleanup:**
    Sweep engine files for:
    - `except:` or `except Exception:` followed by `pass` → replace with `logging.warning("...", exc_info=True)`
    - `print(` in non-CLI code → replace with `logging.warning()` or `logging.info()`
    Focus on: capture.py (entity extraction), intelligence.py (check_connections budget), search.py (search_semantic error path).

    **Tests:** Update test_forget.py to verify:
    - DB rows deleted before file removal
    - People JSON cleaned in surviving notes
    - Frontmatter people field cleaned in surviving note files
  </action>
  <verify>
    <automated>uv run pytest tests/test_forget.py tests/test_capture.py tests/test_search.py -x -q</automated>
  </verify>
  <done>forget_person does DB-first cascade with people field cleanup; bare except/print replaced with logging; tests pass</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_api.py tests/test_mcp.py tests/test_forget.py tests/test_capture.py tests/test_search.py -x -q` — all pass
- `uv run pytest tests/ -q` — full suite green
- Grep confirms no remaining hardcoded 'person' type checks: `grep -rn "type.*'person'" engine/` should only show PERSON_TYPES definition
</verification>

<success_criteria>
- Path traversal blocked on move_file
- forget_person does DB commit before file deletion
- People fields cleaned in surviving notes
- No bare except:pass or print() in engine code
- _escape_like used on all LIKE with user input
- PERSON_TYPES constant used everywhere
</success_criteria>

<output>
After completion, create `.planning/phases/32-architecture-hardening/32-05-SUMMARY.md`
</output>
