---
phase: 12-micro-code-fixes
plan: "04"
type: execute
wave: 2
depends_on: ["12-01", "12-02", "12-03"]
files_modified: []
autonomous: false
requirements: [GDPR-03, GDPR-01, GDPR-05, CAP-02, AI-06]

must_haves:
  truths:
    - "sb-anonymize --help exits 0 in a real terminal"
    - "sb-update-memory --help exits 0 in a real terminal"
    - "sb-export completes without OperationalError on a fresh DB"
    - "After sb-reindex then sb-forget <person>, the DELETE matches at least one row"
    - "After sb-reindex, a note's people field is non-empty when frontmatter has people"
  artifacts: []
  key_links: []
---

<objective>
Manual end-to-end verification of all five v1.5 audit gap fixes in a live DevContainer environment.

Purpose: Automated tests run against fixtures; this checkpoint confirms the fixes work in the actual installed CLI environment (uv tool install --editable, real SQLite volume, real brain root).
Output: User confirms all 5 success criteria from the Phase 12 roadmap entry.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-micro-code-fixes/12-01-SUMMARY.md
@.planning/phases/12-micro-code-fixes/12-02-SUMMARY.md
@.planning/phases/12-micro-code-fixes/12-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and confirm all 6 new tests GREEN</name>
  <files></files>
  <action>
    Run the full test suite and confirm zero failures:
    ```
    cd /Users/tuomasleppanen/second-brain && uv run pytest -q
    ```

    Also run the targeted subset to see the new tests explicitly:
    ```
    uv run pytest tests/test_anonymize.py::test_sb_anonymize_entry_point_registered \
                  tests/test_ai.py::test_sb_update_memory_entry_point_registered \
                  tests/test_ai.py::test_update_memory_main_argparse \
                  tests/test_export.py::test_export_initialises_schema_on_fresh_db \
                  tests/test_reindex.py::test_reindex_stores_absolute_paths \
                  tests/test_reindex.py::test_reindex_preserves_people_column \
                  -v
    ```

    All 6 must show PASSED.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest -q 2>&1 | tail -3</automated>
  </verify>
  <done>Full suite exits 0. The 6 targeted tests show PASSED individually.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end verification of all 5 CLI fixes</name>
  <action>Human verifies all 5 success criteria in a live terminal.</action>
  <what-built>
    Five micro-code fixes:
    1. pyproject.toml: sb-anonymize and sb-update-memory entry points added
    2. engine/ai.py: main() wrapper added with argparse (--note-type, --summary, --config-path)
    3. engine/export.py: init_schema(conn) called in main() before export_brain()
    4. engine/reindex.py: str(md_path.resolve()) replaces str(md_path)
    5. engine/reindex.py: people column added to INSERT/DO UPDATE SET
  </what-built>
  <how-to-verify>
    Run each check in order. All five must pass.

    **Check 1 — sb-anonymize entry point:**
    ```
    sb-anonymize --help
    ```
    Expected: Usage message showing --note-path and --tokens, exits 0. No "command not found".

    **Check 2 — sb-update-memory entry point:**
    ```
    sb-update-memory --help
    ```
    Expected: Usage message showing --note-type, --summary, --config-path, exits 0. No "command not found".

    **Check 3 — sb-export fresh install:**
    ```
    # Rename DB to simulate fresh install
    mv ~/.local/share/second-brain/brain.db ~/.local/share/second-brain/brain.db.bak 2>/dev/null || true
    sb-export --output /tmp/test-export.json
    # Restore
    mv ~/.local/share/second-brain/brain.db.bak ~/.local/share/second-brain/brain.db 2>/dev/null || true
    ```
    Expected: Completes without OperationalError. /tmp/test-export.json created.
    (Adjust DB path if your install differs.)

    **Check 4 — sb-reindex + sb-forget path match:**
    ```
    sb-reindex
    sb-forget <any-person-in-brain>
    ```
    Expected: sb-forget reports deleted rows > 0. If brain is empty, first run `sb-capture --type people --title "Test Person"`, then reindex, then forget.

    **Check 5 — sb-reindex preserves people field:**
    ```
    sb-reindex
    sqlite3 $BRAIN_DB_PATH "SELECT people FROM notes WHERE people != '[]' LIMIT 3;"
    ```
    Expected: At least one row shows a non-empty JSON array matching frontmatter. If all notes lack people fields, create one with `people: [someone]` and reindex.
  </how-to-verify>
  <resume-signal>Type "verified" if all 5 checks pass, or describe which check(s) failed with error output.</resume-signal>
</task>

</tasks>

<verification>
All 5 Phase 12 success criteria from ROADMAP.md confirmed:
1. sb-anonymize --help exits 0
2. sb-update-memory --help exits 0
3. sb-export completes without OperationalError on fresh DB
4. After sb-reindex + sb-forget, DELETE matches > 0 rows
5. After sb-reindex, notes retain people field values
</verification>

<success_criteria>
- uv run pytest -q exits 0 (full suite green)
- All 5 manual checks pass in live environment
- User types "verified"
- Phase 12 complete — all GDPR-03, GDPR-01, GDPR-05, CAP-02, AI-06 gaps closed
</success_criteria>

<output>
After completion, create `.planning/phases/12-micro-code-fixes/12-04-SUMMARY.md`
</output>
