---
phase: 12-micro-code-fixes
plan: "02"
type: execute
wave: 1
depends_on: ["12-00"]
files_modified:
  - engine/export.py
autonomous: true
requirements: [GDPR-05]

must_haves:
  truths:
    - "sb-export on a fresh DB (no schema) completes without OperationalError"
    - "export_brain() is unmodified — schema init remains the caller's responsibility"
    - "init_schema() is called in main() before export_brain()"
  artifacts:
    - path: "engine/export.py"
      provides: "init_schema(conn) call inside main() after get_connection()"
      contains: "init_schema"
  key_links:
    - from: "engine/export.py main()"
      to: "engine.db.init_schema"
      via: "lazy import inside main(); called after get_connection()"
    - from: "engine/export.py main()"
      to: "export_brain(brain_root, conn, output_path)"
      via: "conn already has schema when export_brain() is called"
---

<objective>
Add `init_schema(conn)` call inside `engine/export.py:main()` immediately after `get_connection()`, so `sb-export` on a fresh install does not raise OperationalError.

Purpose: Closes GDPR-05 — prevents OperationalError stack traces that could expose connection state in error output on fresh installs.
Output: One-line addition to engine/export.py main(); init_schema imported in the same lazy-import block.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-micro-code-fixes/12-RESEARCH.md
@.planning/phases/12-micro-code-fixes/12-00-SUMMARY.md

<interfaces>
<!-- engine/export.py main() — current state (lines 53-82) -->
def main() -> None:
    # ... argparse setup ...
    from engine.paths import BRAIN_ROOT
    from engine.db import get_connection          # <-- import target line
    # ...
    conn = get_connection()
    try:
        count = export_brain(brain_root, conn, output_path)   # <-- OperationalError here on fresh DB
    # ...

<!-- engine/db.py -->
def get_connection() -> sqlite3.Connection: ...
def init_schema(conn: sqlite3.Connection, reset: bool = False) -> None: ...
    # init_schema uses IF NOT EXISTS throughout — safe to call on already-initialised DB
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add init_schema call in export.py main()</name>
  <files>engine/export.py</files>
  <action>
    In engine/export.py, find the lazy import line inside main():
    ```python
    from engine.db import get_connection
    ```
    Change it to:
    ```python
    from engine.db import get_connection, init_schema
    ```

    Then find the line:
    ```python
    conn = get_connection()
    ```
    Insert `init_schema(conn)` on the very next line, before the `try:` block:
    ```python
    conn = get_connection()
    init_schema(conn)
    try:
        count = export_brain(brain_root, conn, output_path)
    ```

    Do NOT modify export_brain() itself — schema init belongs at the call site (main()), not inside the library function. This preserves the single-responsibility pattern: export_brain receives a ready conn.

    init_schema() is idempotent (uses IF NOT EXISTS throughout) — calling it on an already-initialised DB is safe and produces no side effects.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_export.py -q 2>&1 | tail -5</automated>
  </verify>
  <done>test_export_initialises_schema_on_fresh_db passes GREEN. All pre-existing test_export.py tests still pass. uv run pytest -q exits 0.</done>
</task>

</tasks>

<verification>
uv run pytest tests/test_export.py -q

Expected: all tests GREEN including test_export_initialises_schema_on_fresh_db. Then uv run pytest -q — zero failures.
</verification>

<success_criteria>
- engine/export.py main() calls init_schema(conn) after get_connection()
- init_schema is imported in the same lazy-import block as get_connection
- export_brain() function body is unmodified
- test_export_initialises_schema_on_fresh_db passes
- uv run pytest -q exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/12-micro-code-fixes/12-02-SUMMARY.md`
</output>
