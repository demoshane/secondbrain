---
phase: 31-smart-capture-multi-context-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_smart_capture.py
  - engine/segmenter.py
  - engine/smart_classifier.py
autonomous: true
requirements: [CAP-01]

must_haves:
  truths:
    - "sb_capture_smart accepts raw freeform text and returns N typed note suggestions"
    - "Each suggestion has title, type, body, and links fields"
    - "Two-pass segmentation splits on structural markers then name-cluster shifts"
    - "Short segments (<50 chars or <2 lines) merge into previous"
    - "Maximum 20 notes per call; excess segments merged"
    - "URLs in blob produce type='link' suggestions"
    - "Code blocks and tables stay inline in parent segment"
  artifacts:
    - path: "engine/segmenter.py"
      provides: "Two-pass segmentation logic"
      exports: ["segment_blob"]
    - path: "engine/smart_classifier.py"
      provides: "Entity-based PII classifier"
      exports: ["classify_smart"]
    - path: "tests/test_smart_capture.py"
      provides: "xfail stubs for all CAP requirements + segmenter unit tests"
    - path: "engine/mcp_server.py"
      provides: "Rewritten sb_capture_smart with auto-save, no confirm token"
  key_links:
    - from: "engine/mcp_server.py:sb_capture_smart"
      to: "engine/segmenter.py:segment_blob"
      via: "direct import"
      pattern: "from engine.segmenter import segment_blob"
    - from: "engine/segmenter.py:segment_blob"
      to: "engine/entities.py:extract_entities"
      via: "per-segment entity extraction"
      pattern: "extract_entities"
---

<objective>
Implement `sb_capture_smart` as a real two-pass segmentation engine that accepts freeform text and returns typed note suggestions, replacing the existing stub. Create `engine/segmenter.py` with the segmentation logic and `engine/smart_classifier.py` with entity-based PII detection. Set up xfail test stubs for all Phase 31 requirements.

Purpose: This is the foundation of Phase 31 -- the segmenter is the core algorithm that all subsequent plans depend on.
Output: `engine/segmenter.py`, `engine/smart_classifier.py`, rewritten `sb_capture_smart` in `mcp_server.py`, `tests/test_smart_capture.py` with xfail stubs.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-smart-capture-multi-context-intelligence/31-CONTEXT.md
@.planning/phases/31-smart-capture-multi-context-intelligence/31-RESEARCH.md

<interfaces>
<!-- Key existing functions the executor needs -->

From engine/capture.py:
```python
def capture_note(
    note_type: str, title: str, body: str, tags: list, people: list,
    content_sensitivity: str, brain_root: Path, conn: sqlite3.Connection,
    *, url: str | None = None,
) -> Path:
```

From engine/entities.py:
```python
def extract_entities(title: str, body: str) -> dict:
    # Returns {"people": [...], "places": [...], "topics": [...], "orgs": [...]}
```

From engine/classifier.py:
```python
def classify(content_sensitivity: str, body: str) -> str:
    # Returns "pii" | "private" | "public"
```

From engine/mcp_server.py (EXISTING STUB to replace):
```python
def sb_capture_smart(content: str) -> dict:
    # Lines 540-614 — basic double-newline split + regex classify
    # Has _issue_token() which must be REMOVED (auto-save, no confirm round-trip)
```

From engine/link_capture.py:
```python
def fetch_link_metadata(url: str) -> dict:
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create xfail test stubs + segmenter module + smart classifier</name>
  <files>tests/test_smart_capture.py, engine/segmenter.py, engine/smart_classifier.py</files>
  <behavior>
    - test_segment_structural_markers: heading/---/date splits produce separate segments
    - test_segment_short_merge: segments <50 chars merge into previous
    - test_segment_max_20_cap: >20 segments merges smallest until <=20
    - test_segment_url_detection: segment with https:// URL gets type='link'
    - test_segment_code_block_inline: ``` fenced code stays in parent segment
    - test_segment_table_inline: |---| tables stay in parent segment
    - test_classify_smart_phone: phone number -> 'pii'
    - test_classify_smart_email: email address -> 'pii'
    - test_classify_smart_hetu: Finnish hetu -> 'pii'
    - test_classify_smart_ssn: US SSN -> 'pii'
    - test_classify_smart_clean: no PII entities -> 'public'
    - test_classify_smart_never_downgrade: pii beats user-supplied public
    - xfail stubs for CAP-01 through CAP-11 + performance test (12 stubs from RESEARCH test map)
  </behavior>
  <action>
    1. Create `tests/test_smart_capture.py`:
       - Import fixtures following existing pattern: monkeypatch `engine.db.DB_PATH` + `engine.paths.DB_PATH` + `BRAIN_PATH` env var
       - Write passing unit tests for `segment_blob()` testing all structural marker splits (headings `# `, `## `, `### `; horizontal rules `---`; date/time stamps like `2026-03-20`; `RE:` and `Subject:` patterns; bullet list starts)
       - Write passing unit tests for `classify_smart(body, user_sensitivity)` testing all entity-based patterns
       - Write xfail(strict=False) stubs for all 12 CAP requirements from RESEARCH test map (test_capture_smart_returns_suggestions, test_multi_context_atomic_save, test_dedup_three_path, test_dormant_resurfacing, test_similar_relationship_created, test_async_hooks_nonblocking, test_bidirectional_relationships, test_entity_resolution_links_existing, test_batch_links_field, test_sensitivity_classify_smart, test_batch_dedup_warnings, test_smart_capture_performance)

    2. Create `engine/segmenter.py`:
       - `segment_blob(content: str) -> list[dict]` — main entry point
       - Pass 1 (structural): split on `re.split(r'(?m)^(?=#{1,3}\s|---\s*$|\d{4}-\d{2}-\d{2}|RE:\s|Subject:\s|^\s*[-*]\s)', content)`
       - Detect code blocks (``` fences) and tables (|---|) — mark regions as non-splittable before Pass 1
       - Pass 2 (name-cluster): for segments >500 chars without structural markers, detect topic shifts by checking if extracted people names change between paragraphs (use `extract_entities` on paragraph chunks)
       - Short segment merge: if `len(seg.strip()) < 50 or seg.count('\n') < 2`, append to previous
       - Max 20 cap: sort by len ascending, merge smallest pair repeatedly until <=20
       - URL detection: `re.search(r'https?://\S+', seg)` -> set segment type to 'link'
       - Per-segment type classification: reuse existing `_classify_segment` logic from mcp_server.py but improved — check for meeting/person/project/idea/link keywords
       - Per-segment title derivation: first non-empty line, stripped of `#` prefix, max 80 chars
       - Return: `[{"title": str, "type": str, "body": str, "links": [], "entities": dict}, ...]`

    3. Create `engine/smart_classifier.py`:
       - `classify_smart(body: str, user_sensitivity: str = "public") -> tuple[str, str | None]`
       - Returns `(level, reason)` where reason is None if no upgrade, else e.g. "detected: phone number"
       - Entity-based PII patterns (NO keyword scanning):
         - SSN: `r'\b\d{3}-\d{2}-\d{4}\b'`
         - Finnish hetu: `r'\b\d{6}[+-A]\d{3}[A-Z0-9]\b'`
         - Credit card (Visa/MC): `r'\b4\d{15}\b|\b5[1-5]\d{14}\b'`
         - Email: `r'\b[\w.+-]+@[\w-]+\.[a-z]{2,}\b'`
         - Phone: `r'\+?[\d\s\-()]{10,15}'` (but require at least 7 digits to avoid false positives)
       - Never-downgrade rule: `max(detected_level, user_level)` using ordering pii > private > public
  </action>
  <verify>
    <automated>cd /workspace && uv run pytest tests/test_smart_capture.py -x -q --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>All segmenter unit tests pass green. All classify_smart tests pass green. 12 xfail stubs collected and marked xfail. No import errors.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite sb_capture_smart in mcp_server.py with auto-save</name>
  <files>engine/mcp_server.py</files>
  <action>
    Rewrite `sb_capture_smart` (lines 540-614) in `engine/mcp_server.py`:

    1. Remove `_issue_token()` call and `confirm_token` from return value (CONTEXT.md: auto-save, no confirm round-trip)
    2. New signature: `sb_capture_smart(content: str) -> dict`
    3. New implementation flow:
       a. `from engine.segmenter import segment_blob`
       b. `segments = segment_blob(content)`
       c. Generate `capture_session = str(uuid.uuid4())`
       d. Open connection: `conn = get_connection()`
       e. For each segment:
          - Run entity resolution (CAP-08, placeholder — actual resolution in plan 31-02)
          - Build suggestion dict with title, type, body, links, entities
       f. Save all segments atomically via `capture_note()` per segment (auto-save per CONTEXT.md)
       g. Create `co-captured` relationships between all saved note paths using `itertools.combinations`
       h. Add `capture_session` to each note's frontmatter
       i. Commit and close connection
       j. Return `{"status": "created", "notes": [...], "capture_session": session_id, "count": N}`

    4. Infer cross-links: if meeting + person segments both present, add person slugs to meeting's links field

    Keep the `@mcp.tool()` decorator. Keep the docstring updated to reflect auto-save behavior.

    IMPORTANT: Do NOT modify `capture_note()` signature. Do NOT modify `sb_capture()` or `sb_capture_batch()` — those are modified in later plans.
  </action>
  <verify>
    <automated>cd /workspace && uv run pytest tests/test_smart_capture.py tests/test_mcp.py -x -q --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>sb_capture_smart accepts freeform text, segments it, saves atomically, returns created notes with capture_session UUID. No confirm_token in response. Existing MCP tests still pass.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_smart_capture.py -x -q` -- segmenter + classifier tests pass, xfail stubs collected
- `uv run pytest tests/test_mcp.py -x -q` -- existing MCP tests unbroken
- `uv run pytest tests/ -q` -- full suite green
</verification>

<success_criteria>
1. `segment_blob("# Meeting\n...\n---\n# Person\n...")` returns 2+ segments with correct types
2. `classify_smart("Call me at +358 50 123 4567", "public")` returns `("pii", "detected: phone number")`
3. `sb_capture_smart` with multi-section input saves N notes and returns capture_session UUID
4. No `confirm_token` in `sb_capture_smart` response
5. All 12 xfail stubs collected by pytest
</success_criteria>

<output>
After completion, create `.planning/phases/31-smart-capture-multi-context-intelligence/31-01-SUMMARY.md`
</output>
