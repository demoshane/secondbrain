---
phase: 31-smart-capture-multi-context-intelligence
plan: 06
type: execute
wave: 4
depends_on: ["31-02", "31-03", "31-04", "31-05"]
files_modified:
  - tests/test_smart_capture.py
  - engine/mcp_server.py
autonomous: true
requirements: [CAP-01, CAP-02, CAP-03, CAP-04, CAP-05, CAP-06, CAP-07, CAP-08, CAP-09, CAP-10, CAP-11]

must_haves:
  truths:
    - "All 12 xfail stubs now pass green (promoted from xfail)"
    - "Full test suite passes with no regressions"
    - "sb_capture_smart end-to-end: freeform text -> segmented -> entity-resolved -> dedup-checked -> saved -> dormant returned"
    - "Performance: sb_capture_smart completes in <5s with synthetic 500-note brain"
    - "Overdue actions appear in recap output"
  artifacts:
    - path: "tests/test_smart_capture.py"
      provides: "All stubs promoted to passing tests"
  key_links:
    - from: "tests/test_smart_capture.py"
      to: "engine/mcp_server.py"
      via: "integration test calling sb_capture_smart end-to-end"
      pattern: "sb_capture_smart"
---

<objective>
Final integration testing and sign-off for Phase 31. Promote all remaining xfail stubs to real passing tests, run the golden-path end-to-end test (freeform meeting notes -> N linked notes), run performance regression, and add overdue action surfacing to recap.

Purpose: Verify all CAP requirements are met end-to-end before marking the phase complete.
Output: All tests green, performance verified, VERIFY-HOST.md for host-side UAT.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31-smart-capture-multi-context-intelligence/31-CONTEXT.md
@.planning/phases/31-smart-capture-multi-context-intelligence/31-RESEARCH.md
@.planning/phases/31-smart-capture-multi-context-intelligence/31-01-SUMMARY.md
@.planning/phases/31-smart-capture-multi-context-intelligence/31-02-SUMMARY.md
@.planning/phases/31-smart-capture-multi-context-intelligence/31-03-SUMMARY.md
@.planning/phases/31-smart-capture-multi-context-intelligence/31-04-SUMMARY.md
@.planning/phases/31-smart-capture-multi-context-intelligence/31-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Golden-path integration test + promote remaining xfails + overdue in recap</name>
  <files>tests/test_smart_capture.py, engine/mcp_server.py</files>
  <action>
    1. Write golden-path end-to-end test `test_smart_capture_golden_path` in test_smart_capture.py:
       - Input: realistic meeting notes blob with 2+ sections, person names, action items, a URL
       - Flexible assertions (per CONTEXT.md testing policy):
         - 3-5 notes created (not exact count)
         - At least 1 `co-captured` relationship in DB
         - At least 1 action item extracted (may be async — poll with short timeout)
         - `capture_session` in response
         - Each note has title, type, path
       - Uses tmp brain with DB isolation (monkeypatch pattern)

    2. Write performance regression test `test_smart_capture_performance`:
       - Create synthetic brain: 500 notes with embeddings in tmp DB
       - Generate embeddings: use `embed_texts()` with dummy content for speed, or mock if model not available
       - Call `sb_capture_smart` with a ~2000 char multi-section input
       - Assert total time < 5 seconds
       - Use `pytest.mark.slow` marker so it can be skipped in quick runs

    3. Promote ALL remaining xfail stubs to real tests:
       - Review each xfail stub in test_smart_capture.py
       - If the feature was implemented in plans 01-04, convert to a real test with proper assertions
       - If any stub still can't pass (unexpected gap), document why and keep as xfail with a comment

    4. Add overdue action surfacing to recap:
       - In `engine/mcp_server.py:sb_recap` (or wherever recap is generated), check if `get_overdue_actions()` from `engine/intelligence.py` is already called
       - If not: import and call it, prepend overdue section to recap output
       - This was partially done in Phase 28-08 — verify it's working and add a test

    5. Create `.planning/phases/31-smart-capture-multi-context-intelligence/VERIFY-HOST.md` with:
       - All pytest commands to run on host
       - Manual GUI verification: open http://localhost:37491/ui, click Smart Capture, paste test content, verify notes created
       - MCP tool verification: call sb_capture_smart via Claude Desktop, verify response
       - Performance check: full suite timing
  </action>
  <verify>
    <automated>cd /workspace && uv run pytest tests/test_smart_capture.py -x -q --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>All xfail stubs promoted to passing tests. Golden-path integration test passes. Performance regression test passes. Full test suite green. VERIFY-HOST.md written.</done>
</task>

<task type="auto">
  <name>Task 2: Full suite regression + VERIFY-HOST.md</name>
  <files>tests/test_smart_capture.py</files>
  <action>
    1. Run full test suite: `uv run pytest tests/ -q --tb=short`
    2. Fix any regressions introduced by Phase 31 changes:
       - Check test_mcp.py for sb_capture_smart signature changes
       - Check test_capture.py for capture_note changes
       - Check test_links.py for relationship query changes (bidirectional)
    3. Verify no xfail(strict=False) stubs remain in test_smart_capture.py — all should be real tests now
    4. Write VERIFY-HOST.md if not already done in Task 1
  </action>
  <verify>
    <automated>cd /workspace && uv run pytest tests/ -q --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>Full test suite passes. No xfail stubs remain. VERIFY-HOST.md written with all host-side verification steps.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_smart_capture.py -x -q` -- all Phase 31 tests pass (no xfails remaining)
- `uv run pytest tests/ -q` -- full suite green, no regressions
- `ls .planning/phases/31-smart-capture-multi-context-intelligence/VERIFY-HOST.md` -- exists
</verification>

<success_criteria>
1. Golden-path test: meeting notes blob -> 3-5 notes + relationships + action items
2. Performance: sb_capture_smart < 5s with 500-note brain
3. Zero xfail stubs remaining in test_smart_capture.py
4. Full suite (`uv run pytest tests/ -q`) passes green
5. VERIFY-HOST.md written with GUI and MCP verification steps
</success_criteria>

<output>
After completion, create `.planning/phases/31-smart-capture-multi-context-intelligence/31-06-SUMMARY.md`
</output>
