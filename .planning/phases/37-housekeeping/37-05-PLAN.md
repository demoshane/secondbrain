---
phase: 37-housekeeping
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: [tests/test_install_subagent.py]
autonomous: true
execution_strategy: direct
requirements: [HK-05]

must_haves:
  truths:
    - "install_subagent.py has unit test coverage"
    - "Tests verify file copy, idempotency, and error handling"
  artifacts:
    - path: "tests/test_install_subagent.py"
      provides: "Unit tests for scripts/install_subagent.py"
      contains: "test_install"
  key_links:
    - from: "tests/test_install_subagent.py"
      to: "scripts/install_subagent.py"
      via: "import and test"
      pattern: "install_subagent"
---

<objective>
Write unit tests for scripts/install_subagent.py to close the test coverage gap.

Purpose: install_subagent.py is the only script without test coverage — a gap surfaced during v4.0 development.
Output: tests/test_install_subagent.py with full coverage of the script's behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-housekeeping/37-CONTEXT.md
@.claude/LEARNINGS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write unit tests for install_subagent.py</name>
  <files>tests/test_install_subagent.py</files>
  <read_first>scripts/install_subagent.py, tests/test_install_subagent.py</read_first>
  <behavior>
    - Test: script copies subagent file to target directory when target does not exist
    - Test: script is idempotent — running twice does not error and file is unchanged
    - Test: script handles missing source file gracefully (error message, not crash)
    - Test: script creates target directory if it doesn't exist
  </behavior>
  <action>
1. Read `scripts/install_subagent.py` to understand its full behavior (it's ~20 lines). Identify:
   - What file(s) it copies and from where to where
   - What happens on first run vs repeat run
   - Error handling behavior

2. Create `tests/test_install_subagent.py` (or extend if it already exists with stubs). Use `tmp_path` fixture for isolation:
   - Patch the source and destination paths to use tmp_path
   - Test happy path: source exists, target dir exists, file gets copied
   - Test idempotency: run twice, no error, file content identical
   - Test target dir creation: target dir doesn't exist, script creates it
   - Test missing source: source file doesn't exist, script handles gracefully
   - Import the script's main function (or subprocess.run it if it's CLI-only)

3. All tests must be self-contained (no filesystem side effects outside tmp_path).
  </action>
  <verify>
    <automated>uv run pytest tests/test_install_subagent.py -q -x</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_install_subagent.py exists
    - tests/test_install_subagent.py contains at least 3 test functions
    - `uv run pytest tests/test_install_subagent.py -q -x` exits 0
  </acceptance_criteria>
  <done>install_subagent.py has unit tests covering copy, idempotency, dir creation, and error handling</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_install_subagent.py -q -x` — all tests pass
- `uv run pytest tests/ -q -x` — no regressions
</verification>

<success_criteria>
install_subagent.py test coverage gap closed with passing unit tests
</success_criteria>

<output>
After completion, create `.planning/phases/37-housekeeping/37-05-SUMMARY.md`
</output>
