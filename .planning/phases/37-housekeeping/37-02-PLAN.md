---
phase: 37-housekeeping
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [frontend/src/components/PeoplePage.tsx, frontend/src/components/ActionItemList.tsx]
autonomous: true
execution_strategy: direct
requirements: [HK-02]

must_haves:
  truths:
    - "User can create an action item from the person detail view"
    - "Created action item is automatically assigned to that person"
  artifacts:
    - path: "frontend/src/components/PeoplePage.tsx"
      provides: "Person detail view with action item creation"
      contains: "ActionItemList"
  key_links:
    - from: "frontend/src/components/PeoplePage.tsx"
      to: "frontend/src/components/ActionItemList.tsx"
      via: "ActionItemList component with create prop"
      pattern: "ActionItemList"
---

<objective>
Enable action item creation from the person note detail view in PeoplePage.

Purpose: Users viewing a person's profile should be able to create action items directly assigned to that person — currently they have to navigate to the Actions page.
Output: PeoplePage person detail view with working action item creation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-housekeeping/37-CONTEXT.md
@.claude/LEARNINGS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add action item creation affordance in PeoplePage person detail view</name>
  <files>frontend/src/components/PeoplePage.tsx, frontend/src/components/ActionItemList.tsx</files>
  <read_first>frontend/src/components/PeoplePage.tsx, frontend/src/components/ActionItemList.tsx</read_first>
  <action>
1. Read `ActionItemList.tsx` to understand its creation props/pattern. Look for:
   - Does it accept an `onCreate` callback or `showCreate` boolean?
   - Does it have an inline form or modal for creating items?
   - What props control the creation UX (e.g., `assigneePath`, `notePath`)?

2. In `PeoplePage.tsx`, find where `ActionItemList` is rendered in the person detail view.

3. Pass the appropriate creation-enabling props to `ActionItemList`:
   - If ActionItemList already has a create button/form controlled by a prop, enable it
   - If ActionItemList does NOT have creation built in, add a simple inline form above or below the list:
     - Text input + "Add" button
     - On submit: `POST /action-items` with `{ text, assignee_path: selectedPerson.path }`
     - After success: refresh the action items list (re-fetch)

4. Ensure the created action item has `assignee_path` set to the current person's note path so it appears in their "My Actions" section immediately.

5. If `ActionItemList` needs a new prop (e.g., `defaultAssigneePath`), add it to the component's props interface and wire it in PeoplePage.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && source "$HOME/.nvm/nvm.sh" && cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - PeoplePage.tsx passes `assignee` or creation-related prop to ActionItemList
    - ActionItemList.tsx has creation UI (button, form, or modal) accessible from person view
    - TypeScript compiles without errors (`npx tsc --noEmit` exits 0)
  </acceptance_criteria>
  <done>User can create action items from person detail view; items auto-assigned to that person; TypeScript compiles clean</done>
</task>

</tasks>

<verification>
- TypeScript compiles: `cd frontend && npx tsc --noEmit`
- `make dev` on host, navigate to People page, open a person, create an action item
</verification>

<success_criteria>
Action item creation form visible in person detail view; created items assigned to current person
</success_criteria>

<output>
After completion, create `.planning/phases/37-housekeeping/37-02-SUMMARY.md`
</output>
