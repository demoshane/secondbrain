---
phase: 37-housekeeping
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/adapters/claude_adapter.py
  - engine/intelligence.py
  - engine/mcp_server.py
autonomous: true
gap_closure: true
requirements: []
must_haves:
  truths:
    - "sb_recap MCP tool returns real recap content within 60 seconds"
  artifacts:
    - path: "engine/adapters/claude_adapter.py"
      provides: "30s subprocess timeout (down from 60s)"
    - path: "engine/intelligence.py"
      provides: "Time-budgeted recap generation that skips PII call if over budget"
    - path: "engine/mcp_server.py"
      provides: "Direct _do_recap() call without retry wrapper"
  key_links:
    - from: "engine/mcp_server.py"
      to: "engine/intelligence.py"
      via: "generate_recap_on_demand()"
      pattern: "generate_recap_on_demand"
    - from: "engine/intelligence.py"
      to: "engine/adapters/claude_adapter.py"
      via: "adapter.generate()"
      pattern: "adapter\\.generate"
execution_strategy: direct
---

<objective>
Fix sb_recap MCP timeout by reducing subprocess timeout, adding time budget, and removing retry wrapper.

Purpose: UAT gap 3 — three compounding timeout issues: 60s subprocess timeout x 2 adapter calls x 4 retries = 240s worst case, far exceeding 60s MCP timeout.
Output: Recap completes within ~35s worst case (30s public + skip PII if over budget, no retries).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/LEARNINGS.md
@engine/adapters/claude_adapter.py
@engine/intelligence.py
@engine/mcp_server.py

<interfaces>
From engine/adapters/claude_adapter.py line 45-50:
```python
result = subprocess.run(
    ["claude", "-p", full_prompt, "--allowedTools", ""],
    capture_output=True, text=True, timeout=60,  # <-- change to 30
)
```

From engine/intelligence.py lines 583-596 (generate_recap_on_demand):
```python
# Two sequential adapter.generate() calls: public_parts then pii_parts
# Each can take up to timeout seconds
```

From engine/mcp_server.py line 600:
```python
result = _retry_call(_do_recap)  # <-- replace with _do_recap()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reduce adapter timeout, add time budget, remove retry</name>
  <files>engine/adapters/claude_adapter.py, engine/intelligence.py, engine/mcp_server.py</files>
  <action>
1. **engine/adapters/claude_adapter.py line 49**: Change `timeout=60` to `timeout=30`.

2. **engine/intelligence.py** in `generate_recap_on_demand()` (around line 583):
   - Add `import time` at top of function (or use module-level import if already present).
   - Before the public adapter.generate() call (line 587), record `t0 = time.monotonic()`.
   - After the public adapter.generate() call completes, check elapsed time: `elapsed = time.monotonic() - t0`.
   - Before the PII adapter.generate() call (line 591-594), add a budget check:
     ```python
     if pii_parts and (time.monotonic() - t0) < 25:
         # Only attempt PII recap if we have time budget remaining
         adapter = _router.get_adapter("pii", CONFIG_PATH)
         ...
     ```
     If elapsed >= 25s, skip the PII call entirely (the public recap is enough).

3. **engine/mcp_server.py line 600**: Replace `result = _retry_call(_do_recap)` with `result = _do_recap()`. Retry semantics are wrong for AI inference — a timeout is not a transient SQLite error.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run python -c "
from engine.adapters.claude_adapter import ClaudeAdapter
import inspect
src = inspect.getsource(ClaudeAdapter.generate)
assert 'timeout=30' in src, 'timeout not updated'
print('adapter timeout: OK')

from engine import intelligence
src2 = inspect.getsource(intelligence.generate_recap_on_demand)
assert 'monotonic' in src2, 'time budget not added'
print('time budget: OK')

# Check mcp_server no longer wraps _do_recap in _retry_call
import engine.mcp_server as mcp
src3 = inspect.getsource(mcp)
# Find the sb_recap function area
assert '_retry_call(_do_recap)' not in src3 or '_retry_call( _do_recap)' not in src3, 'retry still wrapping recap'
print('retry removed: OK')
"</automated>
  </verify>
  <done>ClaudeAdapter uses 30s timeout; generate_recap_on_demand skips PII if public took >25s; mcp_server calls _do_recap directly without retry. Worst case ~35s, well within 60s MCP timeout.</done>
</task>

</tasks>

<verification>
- Adapter timeout is 30s (inspect source)
- Time budget logic present in generate_recap_on_demand
- _retry_call no longer wraps _do_recap in mcp_server.py
- `uv run pytest tests/ -q -x` passes (no regressions)
</verification>

<success_criteria>
sb_recap MCP tool completes within 60s MCP timeout under all conditions.
</success_criteria>

<output>
After completion, create `.planning/phases/37-housekeeping/37-11-SUMMARY.md`
</output>
