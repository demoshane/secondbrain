---
phase: 37-housekeeping
plan: 06
type: execute
wave: 3
depends_on: [03, 04]
files_modified: [tests/test_gui_playwright.py, frontend/src/components/NoteViewer.tsx, frontend/src/components/DeleteNoteModal.tsx, frontend/src/components/RightPanel.tsx]
autonomous: true
execution_strategy: direct
requirements: [HK-06]

must_haves:
  truths:
    - "test_title_sync passes (SSE title propagation to sidebar)"
    - "test_delete_flow passes (sidebar re-render after delete)"
    - "test_right_panel_people_mention passes (people badge detection in right panel)"
  artifacts:
    - path: "tests/test_gui_playwright.py"
      provides: "3 previously-failing Playwright tests now green"
  key_links:
    - from: "tests/test_gui_playwright.py"
      to: "frontend/src/components/NoteViewer.tsx"
      via: "Playwright selectors matching data-testid attributes"
      pattern: "data-testid"
---

<objective>
Fix 3 failing Playwright tests: test_title_sync, test_delete_flow, test_right_panel_people_mention.

Purpose: These E2E tests have been failing since Phase 34 UI work — likely due to moved/renamed DOM elements or SSE timing issues. Fixing them restores the Playwright regression baseline.
Output: All 3 tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-housekeeping/37-CONTEXT.md
@.planning/phases/37-housekeeping/37-RESEARCH.md
@.claude/LEARNINGS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose and fix 3 failing Playwright tests</name>
  <files>tests/test_gui_playwright.py, frontend/src/components/NoteViewer.tsx, frontend/src/components/DeleteNoteModal.tsx, frontend/src/components/RightPanel.tsx</files>
  <read_first>tests/test_gui_playwright.py, tests/conftest.py, frontend/src/components/NoteViewer.tsx, frontend/src/components/DeleteNoteModal.tsx, frontend/src/components/RightPanel.tsx</read_first>
  <action>
**Step 1: Run the failing tests to capture actual error messages:**
```
uv run pytest tests/test_gui_playwright.py::test_title_sync tests/test_gui_playwright.py::test_delete_flow tests/test_gui_playwright.py::test_right_panel_people_mention -v --timeout=30 2>&1 | head -100
```
(If test file is named differently, grep for the test function names across test files first.)

**Step 2: For each test, diagnose based on error output:**

**test_title_sync (SC-4):**
- Expected behavior: PUT to update title → POST /notes/refresh → SSE event → sidebar shows new title
- Likely causes: `/notes/refresh` endpoint missing, SSE event type mismatch, or sidebar selector changed
- Check: Does `api.py` have a `/notes/refresh` POST endpoint? Does the SSE broadcast use the right event type? Does the sidebar use the same CSS class/data-testid the test expects?
- Fix: Update the test selectors to match current DOM structure, or fix the endpoint/SSE if missing

**test_delete_flow (SC-6):**
- Expected behavior: Click delete-btn → modal appears → confirm → note disappears from sidebar
- Likely causes: `data-testid="delete-btn"` moved from NoteViewer to RightPanel, or modal testid changed
- Check: `grep -r "delete-btn" frontend/src/` to find current location
- Fix: Update test to click the correct element, or add missing data-testid attributes back

**test_right_panel_people_mention (QA-02):**
- Expected behavior: Open note with person mention in body → RightPanel shows people-badge
- Likely causes: `gui_brain` fixture doesn't seed the right note/person, or `people-badge` testid changed
- Check: `conftest.py` gui_brain fixture for "Test Mention Note" / "Test Person" setup; `grep -r "people-badge" frontend/src/`
- Fix: Fix fixture data if incomplete, or update test selectors to match current DOM

**Step 3: Apply fixes:**
- If DOM selectors changed: update test expectations to match current component structure
- If data-testid attributes are missing: add them back to the appropriate React components
- If fixture data is incomplete: update conftest.py gui_brain fixture
- If SSE timing is the issue: add appropriate `page.wait_for_selector()` or `expect(locator).to_have_text()` with timeout

**Step 4: Re-run all 3 tests to confirm green:**
```
uv run pytest tests/test_gui_playwright.py::test_title_sync tests/test_gui_playwright.py::test_delete_flow tests/test_gui_playwright.py::test_right_panel_people_mention -v
```
  </action>
  <verify>
    <automated>uv run pytest tests/test_gui_playwright.py::test_title_sync tests/test_gui_playwright.py::test_delete_flow tests/test_gui_playwright.py::test_right_panel_people_mention -v</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_gui_playwright.py::test_title_sync -v` exits 0
    - `uv run pytest tests/test_gui_playwright.py::test_delete_flow -v` exits 0
    - `uv run pytest tests/test_gui_playwright.py::test_right_panel_people_mention -v` exits 0
  </acceptance_criteria>
  <done>All 3 previously-failing Playwright tests pass</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_gui_playwright.py::test_title_sync tests/test_gui_playwright.py::test_delete_flow tests/test_gui_playwright.py::test_right_panel_people_mention -v` — all 3 green
- `uv run pytest tests/ -q -x` — no regressions
</verification>

<success_criteria>
3 previously-failing Playwright tests all pass consistently
</success_criteria>

<output>
After completion, create `.planning/phases/37-housekeeping/37-06-SUMMARY.md`
</output>
