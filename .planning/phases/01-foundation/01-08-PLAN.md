---
phase: 01-foundation
plan: "08"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - scripts/bootstrap.py
autonomous: true
gap_closure: true
requirements:
  - FOUND-07

must_haves:
  truths:
    - "bootstrap.py --dev reports all checks green when invoked via uv run"
    - "README.md instructs users to invoke via uv run"
    - "bootstrap.py warns when run outside venv"
  artifacts:
    - path: "README.md"
      provides: "Correct uv run invocation instructions"
      contains: "uv run python scripts/bootstrap.py"
    - path: "scripts/bootstrap.py"
      provides: "Venv detection guard in main()"
      contains: "sys.prefix == sys.base_prefix"
  key_links:
    - from: "README.md"
      to: "scripts/bootstrap.py"
      via: "user follows docs to invoke script"
      pattern: "uv run python scripts/bootstrap.py --dev"
---

<objective>
Fix the bootstrap.py invocation documentation and add a venv detection guard so users who accidentally invoke bare `python` get a clear warning.

Purpose: UAT test 2 failed — bootstrap.py checks python-frontmatter but the package is only in the uv venv; bare `python` uses system Python. README.md and the script docstring both say bare `python` which is incorrect.
Output: README.md updated, bootstrap.py docstring updated, venv guard added near top of main().
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md invocation line</name>
  <files>README.md</files>
  <action>
    Read README.md. Find the line (around line 73) that says `python scripts/bootstrap.py --dev` and change it to `uv run python scripts/bootstrap.py --dev`. Update any other occurrences of bare `python scripts/bootstrap.py` in the README to use `uv run python scripts/bootstrap.py`. Do not change other content.
  </action>
  <verify>
    <automated>grep -n "python scripts/bootstrap.py" /Users/tuomasleppanen/second-brain/README.md</automated>
  </verify>
  <done>Every occurrence of the bootstrap invocation in README.md uses `uv run python scripts/bootstrap.py`; no bare `python scripts/bootstrap.py` remains.</done>
</task>

<task type="auto">
  <name>Task 2: Fix bootstrap.py docstring and add venv guard</name>
  <files>scripts/bootstrap.py</files>
  <action>
    Read scripts/bootstrap.py. Make two changes:

    1. Docstring (line 7 area): Change the invocation example from `python scripts/bootstrap.py --dev` to `uv run python scripts/bootstrap.py --dev`.

    2. Venv detection guard: Near the top of main() (before any checks run), add:

    ```python
    import sys
    if sys.prefix == sys.base_prefix:
        print("[WARN] Not running inside a virtual environment.")
        print("[WARN] Run via: uv run python scripts/bootstrap.py --dev")
        print("[WARN] python-frontmatter and other deps may be missing.")
        print()
    ```

    The sys import may already exist — do not duplicate it. Place the guard block at the start of main(), before the first check runs, so users see the warning immediately.
  </action>
  <verify>
    <automated>grep -n "sys.prefix\|uv run python scripts/bootstrap" /Users/tuomasleppanen/second-brain/scripts/bootstrap.py</automated>
  </verify>
  <done>scripts/bootstrap.py docstring uses `uv run python`; venv guard prints warning when sys.prefix == sys.base_prefix; existing functionality unchanged.</done>
</task>

</tasks>

<verification>
`grep -rn "python scripts/bootstrap.py" README.md` — all results show `uv run python scripts/bootstrap.py`.
`grep -n "sys.prefix" scripts/bootstrap.py` — guard present in main().
</verification>

<success_criteria>
README.md and bootstrap.py docstring both say `uv run python scripts/bootstrap.py --dev`. Venv guard in main() prints visible warning if invoked with bare python.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-08-SUMMARY.md`
</output>
