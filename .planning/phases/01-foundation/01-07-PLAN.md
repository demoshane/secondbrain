---
phase: 01-foundation
plan: "07"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_precommit.py
autonomous: true
gap_closure: true
requirements:
  - FOUND-08

must_haves:
  truths:
    - "pytest suite exits 0 with zero failures"
    - "detect-secrets limitation for Anthropic keys is explicitly documented in test suite"
  artifacts:
    - path: "tests/test_precommit.py"
      provides: "Fixed pre-commit tests using detectable secret types"
      contains: "test_anthropic_key_not_detected"
  key_links:
    - from: "tests/test_precommit.py"
      to: "detect-secrets binary"
      via: "subprocess call with AWS key fixture"
      pattern: "AKIA[0-9A-Z]{16}"
---

<objective>
Fix test_precommit.py so the pytest suite passes with zero failures.

Purpose: The UAT revealed test_blocks_api_key asserts detect-secrets catches sk-ant-api03-* keys — it cannot. The test must use a known-detectable secret type (e.g., AWS access key) and a separate test must document the Anthropic key limitation.
Output: tests/test_precommit.py with passing tests and documented limitation.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix test_blocks_api_key and add test_anthropic_key_not_detected</name>
  <files>tests/test_precommit.py</files>
  <behavior>
    - test_blocks_api_key: writes a temp file containing a real AWS access key pattern (AKIA followed by 16 uppercase alphanumeric chars), runs `detect-secrets scan` on it, asserts the result JSON contains at least one finding. Skips if detect-secrets not installed.
    - test_anthropic_key_not_detected: writes a temp file with `sk-ant-api03-FAKEFAKEFAKEFAKEFAKEFAKE`, runs `detect-secrets scan`, asserts zero findings. Documents via docstring: "detect-secrets has no Anthropic API key plugin — sk-ant-api03-* keys are not detected by pattern matching; protection relies on baseline diff for high-entropy values only." Skips if detect-secrets not installed.
    - All previously passing tests remain unchanged.
  </behavior>
  <action>
    Read the current tests/test_precommit.py first. Replace the existing test_blocks_api_key function body:
    - Old: uses sk-ant-api03-* key (undetectable by detect-secrets)
    - New: uses AWS key AKIAIOSFODNN7EXAMPLE (HmacKeyDetector or KeywordDetector catches it); assert len(findings) > 0

    Add new test_anthropic_key_not_detected after test_blocks_api_key:
    - Write file with sk-ant-api03-FAKEFAKEFAKEFAKEFAKEFAKEFAKEFAKE
    - Run detect-secrets scan on that file
    - Assert findings is empty ({}  or all empty arrays)
    - Docstring explaining limitation

    Both tests decorated with @pytest.mark.skipif(shutil.which("detect-secrets") is None, reason="detect-secrets not installed").

    Use subprocess.run(["detect-secrets", "scan", str(tmp_path)], capture_output=True, text=True) + json.loads(result.stdout) to get findings dict. findings = {k: v for k, v in data.get("results", {}).items() if v} to get non-empty results.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run --no-project --with pytest pytest tests/test_precommit.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>pytest tests/test_precommit.py passes with 0 failures; test_anthropic_key_not_detected exists and is either passing or skipped.</done>
</task>

</tasks>

<verification>
Run full suite: `uv run --no-project --with pytest tests/ -v` — 0 failures (some tests may skip if outside container).
</verification>

<success_criteria>
pytest exits 0; test_blocks_api_key uses AWS key pattern; test_anthropic_key_not_detected documents the known limitation.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-07-SUMMARY.md`
</output>
