---
phase: 41.2-interactive-gaps
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/NoteEditor.tsx
autonomous: true
execution_strategy: multi-agent
requirements: []
must_haves:
  truths:
    - "NoteEditor has a Preview toggle button that switches between textarea and rendered markdown"
    - "Preview mode renders the markdown body using ReactMarkdown"
    - "Toggle label says 'Preview' when in edit mode and 'Edit' when in preview mode"
  artifacts:
    - path: "frontend/src/components/NoteEditor.tsx"
      provides: "Preview/Edit toggle in note editor"
      contains: "preview"
---

<objective>
Add a markdown preview toggle to NoteEditor.

Phase 41 replaced EasyMDE with a plain textarea (Visily spec). User noted the "markdown editor seems
to be missing" — meaning they want some way to see the rendered output without switching out of edit mode.

Solution: add a "Preview" button in the editor toolbar that toggles between:
- Edit mode: plain textarea (current behavior)
- Preview mode: ReactMarkdown rendered view of the current content

This is a minimal addition — no toolbar, no split-view, just a single toggle.

Output: NoteEditor.tsx with preview toggle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.claude/LEARNINGS.md
@frontend/src/components/NoteEditor.tsx
</context>

<interfaces>
NoteEditor currently:
- Has title `<input>` and body `<textarea>`
- Has Save/Cancel buttons at bottom
- Uses `useState` for title, content, saving

ReactMarkdown is already installed (used in NoteViewer.tsx, MeetingsPage.tsx).
remarkGfm is also installed.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add preview toggle to NoteEditor</name>
  <files>frontend/src/components/NoteEditor.tsx</files>
  <read_first>
    - frontend/src/components/NoteEditor.tsx
  </read_first>
  <action>
Add a preview toggle to NoteEditor.tsx.

**Imports to add:**
```ts
import ReactMarkdown from 'react-markdown'
import remarkGfm from 'remark-gfm'
import { Eye, EyeOff } from 'lucide-react'
```

**State addition:**
```ts
const [previewing, setPreviewing] = useState(false)
```

**In the JSX**, after the title input and before/above the Save/Cancel buttons area:
1. Add a "Preview" / "Edit" toggle button in the action bar area (above the textarea/preview):
```tsx
<div className="flex items-center justify-between mb-2">
  <span className="text-xs text-muted-foreground">{previewing ? 'Preview' : 'Editing'}</span>
  <Button
    type="button"
    variant="ghost"
    size="sm"
    onClick={() => setPreviewing(p => !p)}
  >
    {previewing ? <EyeOff className="h-4 w-4 mr-1" /> : <Eye className="h-4 w-4 mr-1" />}
    {previewing ? 'Edit' : 'Preview'}
  </Button>
</div>
```

2. Replace the textarea section with a conditional render:
```tsx
{previewing ? (
  <div
    className="flex-1 w-full bg-card border border-border rounded p-3 text-sm text-foreground min-h-[300px] overflow-y-auto [&_h1]:text-lg [&_h1]:font-semibold [&_h1]:mt-4 [&_h1]:mb-2 [&_h2]:text-base [&_h2]:font-semibold [&_h2]:mt-3 [&_h2]:mb-1 [&_a]:text-primary [&_a]:underline [&_code]:bg-muted [&_code]:px-1 [&_code]:rounded [&_pre]:bg-muted [&_pre]:p-3 [&_pre]:rounded [&_ul]:list-disc [&_ul]:ml-4 [&_ol]:list-decimal [&_ol]:ml-4 [&_blockquote]:border-l-2 [&_blockquote]:border-primary [&_blockquote]:pl-3 [&_blockquote]:text-muted-foreground"
  >
    <ReactMarkdown remarkPlugins={[remarkGfm]}>{content || '*Nothing to preview*'}</ReactMarkdown>
  </div>
) : (
  <textarea
    className="flex-1 w-full bg-input border border-border rounded p-3 text-sm text-foreground font-mono resize-none min-h-[300px]"
    value={content}
    onChange={e => { setContent(e.target.value); setIsDirty(true) }}
    placeholder="Write your note in Markdown..."
    data-testid="editor-body"
  />
)}
```

Keep Save/Cancel buttons unchanged. Do NOT change save logic.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain/frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File imports `ReactMarkdown` and `remarkGfm`
    - File imports `Eye` and `EyeOff` from lucide-react
    - File contains `previewing` state
    - File contains conditional render between textarea and ReactMarkdown
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>NoteEditor has Preview/Edit toggle button; preview shows rendered markdown</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Clicking "Edit Note" opens editor with Preview button visible
- Clicking Preview renders the markdown body
- Clicking Edit returns to textarea
</verification>

<success_criteria>
- Preview toggle works — switches between textarea and rendered markdown
- Save still works from edit mode (preview mode is read-only within the editor)
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/41.2-interactive-gaps/41.2-05-SUMMARY.md`
</output>
