---
phase: 41.2-interactive-gaps
verified: 2026-03-28T15:30:00Z
status: passed
score: 14/14 must-haves verified
re_verification: false
---

# Phase 41.2: Interactive Gaps Verification Report

**Phase Goal:** Fix critical bugs discovered post-Phase 41 and restore missing interactive features: note save destroys frontmatter (data loss), meetings participants render as [object Object], missing Tags/People sections, no way to add actions or participants from meeting detail, no way to add connections from person detail, no markdown preview in editor, and link-meeting failure.
**Verified:** 2026-03-28T15:30:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #  | Truth                                                                            | Status     | Evidence                                                                                       |
|----|----------------------------------------------------------------------------------|------------|-----------------------------------------------------------------------------------------------|
| 1  | Note save preserves frontmatter (tags, type, people, dates)                      | VERIFIED   | `title_and_body_*` branch in api.py line 1139; `useNoteActions.ts` never sends `{content}`    |
| 2  | Title changes in NoteEditor are saved                                            | VERIFIED   | `handleSave` calls `saveNote(note.path, content, title)` at NoteEditor.tsx line 26             |
| 3  | Meetings participants render as names, not [object Object]                       | VERIFIED   | MeetingsPage.tsx line 89: `.map(p => typeof p === 'string' ? p : (p.name ?? ''))`             |
| 4  | Meetings detail has Add Action inline form that POSTs to /actions                | VERIFIED   | `showAddAction`, `handleAddAction`, `note_path: selectedPath` at lines 54, 133, 141           |
| 5  | Meetings detail has Add Participant inline form that PUTs people array           | VERIFIED   | `showAddParticipant`, `handleAddParticipant` with PUT `/notes/${enc}` at lines 57, 155        |
| 6  | Meetings detail shows Tags section                                               | VERIFIED   | `meetings-tags-${selectedPath}` sectionId at MeetingsPage.tsx line 389                       |
| 7  | Person detail has Link Note button that creates relationships                    | VERIFIED   | `showLinkNote`, `handleLinkNote`, POSTs to `/relationships` at PeoplePage.tsx lines 54, 150   |
| 8  | Link Note backlinks refresh after successful link                                | VERIFIED   | `handleLinkNote` refetches `/notes/${enc}/meta` and calls `setBacklinks` on success           |
| 9  | NoteEditor has Preview/Edit toggle rendering markdown                            | VERIFIED   | `previewing` state, ReactMarkdown conditional render at NoteEditor.tsx lines 20, 61-65        |
| 10 | ProjectsPage detail shows Tags section                                           | VERIFIED   | `tags` state + `projects-tags-${selectedPath}` sectionId at ProjectsPage.tsx lines 55, 407   |
| 11 | ProjectsPage detail shows People section                                         | VERIFIED   | `detailPeople` state + `projects-people-${selectedPath}` sectionId at lines 56, 428           |
| 12 | link_meeting_to_project returns descriptive error messages                       | VERIFIED   | api.py lines 567-570, 594-597: returns type found vs expected in error body                   |
| 13 | link_meeting_to_project handles absolute-path DBs (pre-Phase-32)                | VERIFIED   | Two-pass lookup with `meeting_sp != meeting_path` guard at api.py lines 582-588               |
| 14 | TypeScript and Python compile cleanly after all changes                          | VERIFIED   | `npx tsc --noEmit` produces no output; `from engine.api import app` succeeds                  |

**Score:** 14/14 truths verified

### Required Artifacts

| Artifact                                         | Expected                                      | Status     | Details                                                              |
|--------------------------------------------------|-----------------------------------------------|------------|----------------------------------------------------------------------|
| `frontend/src/components/ProjectsPage.tsx`       | Tags + People CollapsibleSections             | VERIFIED   | Both sections wired to live data from /notes and /meta endpoints     |
| `frontend/src/components/MeetingsPage.tsx`       | Participants fix + Add Action/Participant/Tags | VERIFIED   | All 4 changes wired to live API endpoints                            |
| `frontend/src/components/PeoplePage.tsx`         | Link Note inline form in Connections section  | VERIFIED   | Full select+search form wired to POST /relationships                 |
| `frontend/src/components/NoteEditor.tsx`         | Preview/Edit toggle with ReactMarkdown        | VERIFIED   | Conditional render wired to `previewing` state                       |
| `frontend/src/hooks/useNoteActions.ts`           | Frontmatter-safe save (body not content key)  | VERIFIED   | Conditional payload: combined or body-only, never destructive content|
| `engine/api.py`                                  | title+body combined save branch               | VERIFIED   | Branch at lines 1139-1165 with `_fm.loads/dumps` preserving metadata|

### Key Link Verification

| From                    | To                             | Via                          | Status     | Details                                                            |
|-------------------------|--------------------------------|------------------------------|------------|--------------------------------------------------------------------|
| NoteEditor.tsx          | api.py save_note               | `{title_and_body_*}` payload | WIRED      | useNoteActions sends combined payload; api.py branch guards it     |
| MeetingsPage.tsx        | POST /actions                  | `handleAddAction`            | WIRED      | POSTs `{text, note_path}`, calls `reloadActions()` on success      |
| MeetingsPage.tsx        | PUT /notes/<path>              | `handleAddParticipant`       | WIRED      | PUTs `{people: updatedPeople}`, optimistic state update on success |
| PeoplePage.tsx          | POST /relationships            | `handleLinkNote`             | WIRED      | POSTs `{source_path, target_path, rel_type:'manual'}`, refreshes backlinks |
| PeoplePage.tsx          | GET /notes?limit=100           | `handleShowLinkNote`         | WIRED      | Fetches notes list to populate selector                            |
| MeetingsPage.tsx        | GET /notes/<path>              | third Promise.all leg        | WIRED      | Tags fetched, stored in `tags` state, rendered in CollapsibleSection |
| ProjectsPage.tsx        | GET /notes/<path>              | fourth Promise.all leg       | WIRED      | Tags fetched; meta.people extracted for detailPeople               |

### Data-Flow Trace (Level 4)

| Artifact              | Data Variable  | Source                  | Produces Real Data | Status    |
|-----------------------|----------------|-------------------------|--------------------|-----------|
| ProjectsPage.tsx      | `tags`         | GET /notes/<path>       | Yes (noteData.tags)| FLOWING   |
| ProjectsPage.tsx      | `detailPeople` | GET /notes/<path>/meta  | Yes (meta.people)  | FLOWING   |
| MeetingsPage.tsx      | `participants` | GET /meetings/<path>    | Yes, normalized    | FLOWING   |
| MeetingsPage.tsx      | `tags`         | GET /notes/<path>       | Yes (noteData.tags)| FLOWING   |
| PeoplePage.tsx        | `allNotes`     | GET /notes?limit=100    | Yes (d.results)    | FLOWING   |
| NoteEditor.tsx        | `content`      | `note.body` prop        | Yes (passed in)    | FLOWING   |

### Behavioral Spot-Checks

| Behavior                                   | Command                                                                         | Result         | Status  |
|--------------------------------------------|---------------------------------------------------------------------------------|----------------|---------|
| Python engine imports                      | `uv run python -c "from engine.api import app; print('import ok')"`             | "import ok"    | PASS    |
| TypeScript compiles without errors         | `cd frontend && npx tsc --noEmit --pretty`                                      | (no output)    | PASS    |
| All 7 phase commits exist in git log       | `git log --oneline <hashes>`                                                    | All confirmed  | PASS    |

### Requirements Coverage

No requirement IDs mapped to this phase (requirements: null in phase goal). No orphaned requirements found.

### Anti-Patterns Found

None found. Scanned all 5 modified frontend files and engine/api.py for TODOs, FIXMEs, placeholder returns, and empty handlers.

**Minor observation (non-blocking):** `POST /relationships` in api.py (line 1832) hardcodes `rel_type='link'` regardless of what the frontend sends (`rel_type:'manual'`). The relationship is still created and functional. No code queries specifically for `rel_type='manual'`, so this causes no behavioral difference. Severity: Info.

### Human Verification Required

The following functional behaviors require runtime verification (server must be running):

#### 1. Note Save Frontmatter Preservation

**Test:** Edit a note that has tags and type set. Change title and body. Save. Reload and inspect the file.
**Expected:** Title and body updated; tags, type, people frontmatter fields unchanged.
**Why human:** Requires running sb-api + GUI; file write path can't be tested statically.

#### 2. Meeting Participants Display

**Test:** Open a meeting with known participants. Verify they display as names.
**Expected:** Names shown (not [object Object]).
**Why human:** Requires running GUI with real meeting data.

#### 3. Add Action from Meeting Detail

**Test:** Open a meeting, click Add Action, type text, press Enter.
**Expected:** Action appears in the list; toast confirmation shown.
**Why human:** Requires running GUI + sb-api.

#### 4. Add Participant from Meeting Detail

**Test:** Open a meeting, click Add Participant, type a name, click Add.
**Expected:** Participant appears in the list; file updated with new people entry.
**Why human:** Requires running GUI + file verification.

#### 5. Link Note from Person Detail

**Test:** Open a person, go to Related Notes, click Link Note, select a note, click Link.
**Expected:** Note appears in Related Notes; toast confirmation shown.
**Why human:** Requires running GUI.

#### 6. Link Meeting to Project

**Test:** Open a project, use the Link Meeting feature to link an existing meeting.
**Expected:** Meeting appears in Linked Meetings section; no failure toast.
**Why human:** Requires running GUI with pre-existing meeting in DB.

#### 7. NoteEditor Markdown Preview

**Test:** Edit a note with markdown (headers, bold, lists). Click Preview.
**Expected:** Rendered HTML output with correct formatting.
**Why human:** Requires visual rendering check.

### Gaps Summary

No gaps. All 14 must-have truths are verified against the actual codebase. All artifacts exist, are substantive, are wired to live data sources, and TypeScript/Python compile cleanly.

The one minor discrepancy (rel_type stored as 'link' not 'manual') has no functional impact.

---

_Verified: 2026-03-28T15:30:00Z_
_Verifier: Claude (gsd-verifier)_
