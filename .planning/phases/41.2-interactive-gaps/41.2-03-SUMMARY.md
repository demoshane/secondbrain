---
phase: 41.2-interactive-gaps
plan: "03"
subsystem: api
tags: [flask, sqlite, relationships, projects, meetings]

requires:
  - phase: 41-visual-redesign
    provides: ProjectsPage link meeting UI wired to POST /projects/<path>/meetings

provides:
  - Fixed link_meeting_to_project endpoint with descriptive error messages
  - Absolute-path fallback for pre-Phase-32 meeting rows in DB

affects:
  - ProjectsPage link meeting feature
  - Any consumer of POST /projects/<path>/meetings

tech-stack:
  added: []
  patterns:
    - "After store_path lookup fails, fall back to raw path for absolute-path DB compat"
    - "Add row_factory=sqlite3.Row when diagnostic dict access is needed"

key-files:
  created: []
  modified:
    - engine/api.py

key-decisions:
  - "link_meeting_to_project: add absolute-path fallback — if store_path(meeting) not in DB, try raw meeting_path (handles pre-Phase-32 un-migrated rows)"
  - "link_meeting_to_project: set conn.row_factory=sqlite3.Row for descriptive error dict access"
  - "Error messages now include the actual path and type checked, making failures diagnosable from toast alone"

patterns-established:
  - "Two-pass DB lookup pattern: try relative path first, fall back to raw value for absolute-path compat"

requirements-completed: []

duration: 5min
completed: 2026-03-28
---

# Phase 41.2 Plan 03: Fix link_meeting_to_project endpoint Summary

**Fixed `link_meeting_to_project` with descriptive errors and absolute-path fallback for pre-Phase-32 meeting rows**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-03-28T14:50:00Z
- **Completed:** 2026-03-28T14:55:00Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Added `conn.row_factory = sqlite3.Row` so diagnostic queries can access `type` column by name
- Project lookup now returns specific error: path + actual type found vs expected 'projects'
- Meeting lookup adds a second-pass: if `store_path`-converted path not found, tries raw `meeting_path` value — this handles DBs that were never migrated from absolute to relative paths
- Both not-found and type-mismatch cases return actionable error messages visible in toast

## Task Commits

1. **Task 1: Diagnose type values and fix link_meeting_to_project** - `919ff23` (fix)

## Files Created/Modified
- `engine/api.py` - Fixed `link_meeting_to_project` with better errors and absolute-path fallback

## Decisions Made
- Two-pass lookup pattern: try `store_path(abs_mtg)` first (relative), fall back to raw `meeting_path` string for absolute-path-in-DB compat. Guard with `meeting_sp != meeting_path` to avoid redundant query.
- `row_factory=sqlite3.Row` added only to this connection instance (not globally) to enable diagnostic `row['type']` access without touching other endpoint behaviour.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Backend fix is in place; user should `make restart` to pick up the change and retest the Link Meeting button in ProjectsPage
- If meetings still fail to link after restart, the descriptive toast error will now identify whether it's a path mismatch or type mismatch

## Self-Check: PASSED

- SUMMARY.md: found
- engine/api.py: found
- Commit 919ff23: found

---
*Phase: 41.2-interactive-gaps*
*Completed: 2026-03-28*
