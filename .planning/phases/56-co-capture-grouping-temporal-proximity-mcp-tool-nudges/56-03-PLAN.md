---
phase: 56-co-capture-grouping
plan: 03
type: execute
wave: 3
depends_on: ["56-02"]
files_modified:
  - engine/mcp_server.py
  - engine/api.py
  - tests/test_capture_nudges.py
autonomous: true
execution_strategy: direct

must_haves:
  truths:
    - "sb_capture returns recent_context with recently captured notes"
    - "sb_capture_smart returns recent_context with temporal neighbors outside the batch"
    - "nudge text is included when co-captured relationships were auto-created"
    - "API /capture-session/<id> endpoint returns all notes in a capture session"
  artifacts:
    - path: "engine/mcp_server.py"
      provides: "recent_context + nudge in capture responses"
    - path: "engine/api.py"
      provides: "/capture-session/<id> retrieval endpoint"
---

<objective>
Surface recent capture context as nudges in MCP tool responses and add a capture session
retrieval endpoint. Claude sees what was recently captured and can inform the user or
use the context for smarter follow-up captures.
</objective>

<context>
@.planning/phases/56-co-capture-grouping-temporal-proximity-mcp-tool-nudges/56-CONTEXT.md
@engine/mcp_server.py (sb_capture, sb_capture_smart, sb_capture_batch response formats)
@engine/api.py (existing endpoint patterns)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: recent_context in sb_capture response</name>
  <files>engine/mcp_server.py</files>
  <read_first>engine/mcp_server.py (sb_capture return dict structure; find_temporal_neighbors return format)</read_first>
  <behavior>
    - After capture + auto-linking (from 56-02), build recent_context list:
      `[{"path": str, "title": str, "type": str, "minutes_ago": int}]`
    - Add to response dict: `"recent_context": [...]`
    - Add `"nudge": str` with human-readable message when co-captured links were created:
      - 1 link: "Auto-linked with recent capture: {title}"
      - 2+ links: "Auto-linked with {N} recent captures from this session."
      - 0 links: omit nudge field entirely
    - Apply same pattern to sb_capture_batch: aggregate recent_context across all batch notes
  </behavior>
  <action>
    1. Build recent_context from find_temporal_neighbors() results
    2. Format nudge string based on count
    3. Include both in response dicts for sb_capture and sb_capture_batch
    4. Test: capture with neighbors → nudge present; capture alone → no nudge
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_capture_nudges.py -x -v -k "sb_capture"</automated>
  </verify>
  <done>sb_capture and sb_capture_batch include recent_context and nudge in responses</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: recent_context in sb_capture_smart response</name>
  <files>engine/mcp_server.py</files>
  <read_first>engine/mcp_server.py (sb_capture_smart response structure, lines 1200-1225)</read_first>
  <behavior>
    - sb_capture_smart already creates intra-batch co-captured links. Extend:
      1. After batch save, find temporal neighbors OUTSIDE the batch
      2. Include external neighbors as recent_context in response
      3. Add nudge if external links were created: "Also linked with {N} notes captured
         earlier in this session."
    - Do NOT include intra-batch notes in recent_context (they're already in the "notes" list)
  </behavior>
  <action>
    1. After existing co-captured logic: find temporal neighbors excluding batch paths
    2. Create co-captured relationships with external neighbors
    3. Add recent_context (external only) and nudge to response
    4. Test: smart capture after prior sb_capture → external neighbor in recent_context
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_capture_nudges.py -x -v -k "smart"</automated>
  </verify>
  <done>sb_capture_smart surfaces external temporal neighbors as recent_context</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Capture session retrieval endpoint</name>
  <files>engine/api.py</files>
  <read_first>engine/api.py (existing GET endpoint patterns, JSON response format)</read_first>
  <behavior>
    - GET `/capture-session/<session_id>` → returns all notes with that capture_session
    - Response: `{"session_id": str, "notes": [{"path", "title", "type", "created_at"}], "count": int}`
    - 404 if no notes found with that session_id
    - Also include co-captured relationships between the session's notes
  </behavior>
  <action>
    1. Add GET endpoint at /capture-session/<session_id>
    2. Query notes table WHERE capture_session = ?
    3. Include co-captured relationships for context
    4. Test: valid session → notes returned; unknown session → 404
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_capture_nudges.py -x -v -k "session_endpoint"</automated>
  </verify>
  <done>Capture sessions retrievable via API for later reference</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: End-to-end integration tests</name>
  <files>tests/test_capture_nudges.py</files>
  <read_first>tests/test_smart_capture.py (fixtures, client patterns), tests/test_co_capture_auto.py (from 56-02)</read_first>
  <behavior>
    Full lifecycle tests:
    - Simulate conversation: sb_capture("meeting") → sb_capture("action item") → sb_capture("person")
      All within 5 minutes. Verify: all 3 co-captured, nudges escalate (0 → 1 → 2 neighbors),
      capture_session retrievable.
    - Simulate mixed tools: sb_capture_smart(blob) → sb_capture(followup)
      Verify: followup linked to smart-capture notes, recent_context shows them.
    - Simulate timeout: sb_capture at T=0, sb_capture at T=20min
      Verify: no auto-linking, no nudge.
    - Session-based grouping: 3 sb_capture calls with same session_id, 30 min apart
      Verify: all linked via session_id despite exceeding temporal window.
    - Graph integrity: after all tests, verify no orphaned relationships, no FK violations,
      all co-captured paths exist in notes table.
  </behavior>
  <action>
    1. Create test fixtures with controllable timestamps
    2. Write the 5 scenarios above
    3. Assert on response fields (recent_context, nudge, co_captured_with)
    4. Assert on DB state (relationships count, paths validity)
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_capture_nudges.py -x -v</automated>
  </verify>
  <done>Full lifecycle coverage for temporal grouping + nudges across all capture tools</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_capture_nudges.py tests/test_co_capture_auto.py -x -v` all pass
- Nudges appear in MCP responses when temporal neighbors exist
- Capture sessions retrievable via API
- No regressions in existing capture tests: `uv run pytest tests/test_capture.py tests/test_smart_capture.py -x -v`
</verification>

<success_criteria>
All capture tools surface recent context as nudges. Capture sessions persistent and
retrievable. Full test coverage for temporal grouping lifecycle.
</success_criteria>
