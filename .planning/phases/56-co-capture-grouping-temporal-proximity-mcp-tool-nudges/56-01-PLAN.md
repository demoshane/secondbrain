---
phase: 56-co-capture-grouping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/db.py
  - engine/capture.py
  - engine/intelligence.py
  - tests/test_temporal_neighbors.py
autonomous: true
execution_strategy: direct

must_haves:
  truths:
    - "notes table has capture_session TEXT column (nullable)"
    - "capture_note() accepts optional capture_session param and stores it in DB"
    - "find_temporal_neighbors() returns notes created within N minutes of a reference time"
    - "temporal neighbors exclude the reference note itself"
  artifacts:
    - path: "engine/intelligence.py"
      provides: "find_temporal_neighbors() function"
      exports: ["find_temporal_neighbors"]
    - path: "engine/db.py"
      provides: "capture_session column migration"
    - path: "engine/capture.py"
      provides: "capture_note() with capture_session support"
---

<objective>
Add capture_session persistence to the notes table and a temporal neighbor detection
function. Foundation for auto co-capture linking in subsequent plans.
</objective>

<context>
@.planning/phases/56-co-capture-grouping-temporal-proximity-mcp-tool-nudges/56-CONTEXT.md
@engine/db.py
@engine/capture.py
@engine/intelligence.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DB migration — capture_session column</name>
  <files>engine/db.py</files>
  <read_first>engine/db.py (existing migration patterns: migrate_add_* functions, init_schema call chain)</read_first>
  <behavior>
    - Add `migrate_add_capture_session()` function following existing add-column pattern
    - ALTER TABLE notes ADD COLUMN capture_session TEXT (nullable, no default)
    - Call from `init_schema()` after existing migrations
    - Idempotent: skip if column already exists
  </behavior>
  <action>
    1. Add migration function
    2. Wire into init_schema()
    3. Test: migration runs without error on fresh DB, re-running is no-op
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_db.py -x -v -k "capture_session or migration"</automated>
  </verify>
  <done>notes table has capture_session column after init_schema()</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: capture_note() accepts capture_session</name>
  <files>engine/capture.py</files>
  <read_first>engine/capture.py (capture_note() signature, INSERT statement, return value)</read_first>
  <behavior>
    - Add `capture_session: str | None = None` param to capture_note()
    - Include capture_session in the INSERT INTO notes statement
    - Include capture_session in the return dict
    - If None, column stores NULL — no auto-generation here (callers decide)
  </behavior>
  <action>
    1. Add param to capture_note()
    2. Update INSERT statement to include capture_session column
    3. Update return dict
    4. Test: capture with session ID → stored in DB; capture without → NULL
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_capture.py -x -v -k "capture_session or session"</automated>
  </verify>
  <done>capture_note() persists capture_session to DB when provided</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: find_temporal_neighbors() function</name>
  <files>engine/intelligence.py, tests/test_temporal_neighbors.py</files>
  <read_first>engine/intelligence.py (find_similar for pattern reference), engine/db.py (notes table columns, datetime format)</read_first>
  <behavior>
    - `find_temporal_neighbors(conn, reference_time: str, window_minutes: int = 15, exclude_path: str | None = None) -> list[dict]`
    - Query notes where created_at is within +/- window_minutes of reference_time
    - Exclude the note at exclude_path (the note that just triggered the check)
    - Return: [{"path", "title", "type", "created_at", "delta_seconds"}]
    - Sort by delta_seconds ascending (closest first)
    - Limit to 10 results (avoid unbounded returns in high-capture sessions)
  </behavior>
  <action>
    1. Implement function in intelligence.py
    2. Create tests/test_temporal_neighbors.py:
       - No notes in window → empty list
       - 3 notes within 5 min → all returned, sorted by proximity
       - Notes outside window → excluded
       - exclude_path → self not in results
       - Boundary: note exactly at window edge → included
       - Window of 0 → only exact timestamp matches
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_temporal_neighbors.py -x -v</automated>
  </verify>
  <done>find_temporal_neighbors() detects notes created within configurable time window</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_db.py tests/test_capture.py tests/test_temporal_neighbors.py -x -v` all pass
- capture_session column exists and persists through capture_note()
- Temporal neighbor detection works with various time windows
</verification>

<success_criteria>
Foundation in place: capture_session stored in DB, temporal neighbors detectable. Ready for
auto-linking in Plan 56-02.
</success_criteria>
