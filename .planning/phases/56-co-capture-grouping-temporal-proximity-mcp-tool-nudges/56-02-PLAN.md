---
phase: 56-co-capture-grouping
plan: 02
type: execute
wave: 2
depends_on: ["56-01"]
files_modified:
  - engine/mcp_server.py
  - engine/api.py
  - tests/test_co_capture_auto.py
autonomous: true
execution_strategy: direct

must_haves:
  truths:
    - "sb_capture auto-creates co-captured relationships with temporal neighbors"
    - "sb_capture_batch auto-creates co-captured relationships between batch notes and temporal neighbors"
    - "/smart-capture/confirm uses store_path() for co-captured relationship inserts"
    - "co-captured relationships use store_path-normalized paths (no FK violations)"
  artifacts:
    - path: "engine/mcp_server.py"
      provides: "auto co-capture linking in sb_capture and sb_capture_batch"
    - path: "engine/api.py"
      provides: "fixed /smart-capture/confirm handler + temporal linking in API routes"
---

<objective>
Wire automatic co-captured relationship creation into all capture paths. When a note
is captured via any tool, check for temporal neighbors and auto-link them. Fix the
known /smart-capture/confirm path bug.
</objective>

<context>
@.planning/phases/56-co-capture-grouping-temporal-proximity-mcp-tool-nudges/56-CONTEXT.md
@engine/mcp_server.py (sb_capture line 248, sb_capture_batch line 339)
@engine/api.py (/smart-capture/confirm handler)
@engine/intelligence.py (find_temporal_neighbors)
@engine/paths.py (store_path)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Auto co-capture in sb_capture</name>
  <files>engine/mcp_server.py</files>
  <read_first>engine/mcp_server.py (sb_capture function, lines 248-337; sb_capture_smart co-captured pattern, lines 1180-1197)</read_first>
  <behavior>
    - After successful capture in sb_capture():
      1. Call find_temporal_neighbors() with the new note's created_at
      2. For each neighbor: INSERT OR IGNORE co-captured relationship (both src→tgt and tgt→src)
      3. Use store_path() normalization on all paths
      4. Swallow FK errors gracefully (debug log, not user-visible)
    - Add optional `session_id: str = ""` parameter to sb_capture
    - If session_id provided: store as capture_session, also find notes with same session
      and create co-captured relationships with those
    - Include co-captured neighbor info in return dict: `"co_captured_with": [paths]`
  </behavior>
  <action>
    1. Add session_id param to sb_capture signature
    2. Pass capture_session to capture_note()
    3. After capture: find temporal neighbors + same-session notes
    4. Insert co-captured relationships
    5. Include co_captured_with in response
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_co_capture_auto.py -x -v -k "sb_capture"</automated>
  </verify>
  <done>sb_capture auto-links with temporal neighbors and same-session notes</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Auto co-capture in sb_capture_batch</name>
  <files>engine/mcp_server.py</files>
  <read_first>engine/mcp_server.py (sb_capture_batch, lines 339-478; co-captured pattern from sb_capture_smart)</read_first>
  <behavior>
    - sb_capture_batch already creates intra-batch relationships. Extend:
      1. After batch save, find temporal neighbors for the batch's time window
      2. Create co-captured relationships between batch notes AND temporal neighbors
      3. Pass capture_session UUID through to each capture_note() call
    - Use store_path() for all relationship inserts
  </behavior>
  <action>
    1. Pass capture_session to each capture_note() call in the batch
    2. After batch: find temporal neighbors outside the batch
    3. Create co-captured links with external neighbors
    4. Tests: batch of 2 notes + 1 pre-existing note within 5 min → all 3 linked
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_co_capture_auto.py -x -v -k "batch"</automated>
  </verify>
  <done>sb_capture_batch links batch notes with each other AND temporal neighbors</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fix /smart-capture/confirm path bug + API temporal linking</name>
  <files>engine/api.py</files>
  <read_first>engine/api.py (/smart-capture/confirm handler ~line 2481; store_path usage in /smart-capture ~line 2449)</read_first>
  <behavior>
    - /smart-capture/confirm: apply store_path() normalization to co-captured INSERT paths
      (same pattern as the main /smart-capture handler fix)
    - /capture API route: after saving, call find_temporal_neighbors() and create
      co-captured relationships; pass capture_session if provided in request body
    - All relationship inserts use store_path() normalization
  </behavior>
  <action>
    1. Fix /smart-capture/confirm: normalize paths with store_path() before INSERT
    2. Add temporal neighbor linking to /capture route
    3. Accept optional capture_session in /capture request body
    4. Tests: confirm handler creates valid co-captured rows; /capture auto-links neighbors
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_co_capture_auto.py -x -v -k "api or confirm"</automated>
  </verify>
  <done>All capture paths (MCP + API) create valid co-captured relationships with temporal neighbors</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Integration test — full co-capture lifecycle</name>
  <files>tests/test_co_capture_auto.py</files>
  <read_first>tests/test_smart_capture.py (existing co-capture test patterns, fixtures)</read_first>
  <behavior>
    - Test: 3 separate sb_capture calls within 5 minutes → 3 co-captured relationships created
    - Test: sb_capture with session_id → all same-session notes linked
    - Test: sb_capture outside time window → no auto-linking
    - Test: sb_capture_batch + sb_capture within window → cross-linked
    - Test: graph integrity — all co-captured paths exist in notes table (no FK violations)
    - Test: co-captured relationships are bidirectional (both directions present or
      deduplicated via INSERT OR IGNORE on reversed pair)
  </behavior>
  <action>
    1. Create test fixtures: DB with notes at known timestamps
    2. Write integration tests covering all scenarios above
    3. Assert relationship counts and path validity after each operation
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_co_capture_auto.py -x -v</automated>
  </verify>
  <done>Full integration coverage for temporal auto-linking across all capture paths</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_co_capture_auto.py -x -v` all pass
- No FK violations in any capture path
- Co-captured relationships created automatically for temporal neighbors
- /smart-capture/confirm path bug fixed
</verification>

<success_criteria>
All capture paths (sb_capture, sb_capture_batch, sb_capture_smart, API routes) auto-create
co-captured relationships with temporal neighbors. No FK violations.
</success_criteria>
