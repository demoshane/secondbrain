---
phase: 11-gdpr-scope-expansion
plan: "01"
type: execute
wave: 2
depends_on:
  - "11-00"
files_modified:
  - engine/export.py
autonomous: true
requirements:
  - GDPR-02

must_haves:
  truths:
    - "export_brain() writes a JSON file containing all notes rows"
    - "PII-sensitivity notes are included in the export (not filtered out)"
    - "An 'export' row appears in audit_log after every call"
    - "export_brain() returns the integer count of exported notes"
    - "sb-export CLI exits 0 and prints output path"
  artifacts:
    - path: "engine/export.py"
      provides: "export_brain() implementation + sb-export CLI main()"
      exports: ["export_brain", "main"]
  key_links:
    - from: "engine/export.py"
      to: "notes table"
      via: "conn.execute SELECT path,type,title,body,tags,people,created_at,updated_at,sensitivity FROM notes"
      pattern: "SELECT.*FROM notes"
    - from: "engine/export.py"
      to: "audit_log table"
      via: "INSERT INTO audit_log event_type='export'"
      pattern: "INSERT INTO audit_log.*export"
---

<objective>
Wave 1a: implement engine/export.py — GDPR Article 20 data portability CLI.

Purpose: Users can export all their brain notes to a portable JSON file, satisfying the right to data portability.
Output: Fully implemented export_brain() and sb-export main() with 4 passing tests.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-gdpr-scope-expansion/11-RESEARCH.md
@.planning/phases/11-gdpr-scope-expansion/11-00-SUMMARY.md

<interfaces>
<!-- Exact patterns from codebase. Executor uses these directly. -->

From engine/forget.py (audit log pattern to replicate):
```python
now = datetime.datetime.utcnow().strftime("%Y-%m-%dT%H:%M:%SZ")
conn.execute(
    "INSERT INTO audit_log (event_type, note_path, detail, created_at) VALUES (?, ?, ?, ?)",
    ("forget", None, f"slug:{slug}", now),
)
conn.commit()
```

From engine/db.py — notes columns available for SELECT:
  path TEXT, type TEXT, title TEXT, body TEXT, tags TEXT, people TEXT,
  created_at TEXT, updated_at TEXT, sensitivity TEXT

From tests/conftest.py:
```python
@pytest.fixture
def db_conn() -> sqlite3.Connection:
    """In-memory SQLite with full schema (notes, audit_log, FTS5 triggers)"""

@pytest.fixture
def seeded_db(db_conn):
    """db_conn with one row: type='coding', title='Test Note', sensitivity='public'"""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement export_brain() and sb-export CLI main()</name>
  <files>engine/export.py</files>
  <behavior>
    - export_brain(brain_root, conn, output_path, fmt="json") returns int count of rows exported
    - JSON output: {"exported_at": "<ISO>", "notes": [{path, type, title, body, tags, people, created_at, updated_at, content_sensitivity}, ...]}
    - notes with sensitivity='pii' ARE included (Article 20 covers all personal data)
    - audit_log row inserted: event_type='export', note_path=NULL, detail='format:json count:N'
    - output_path.parent created if absent (parents=True, exist_ok=True)
    - JSON written with indent=2, utf-8 encoding
    - main() uses argparse: --output PATH (default: ./sb-export-<YYYYMMDDTHHMMSS>.json), --brain-root PATH
    - main() prints "Exported N notes to <path>" on success
  </behavior>
  <action>
    Replace the NotImplementedError stub in engine/export.py with full implementation.

    Key constraints from research:
    - Default filename must include time (not just date) to prevent collision on double-run: `sb-export-{datetime.datetime.utcnow().strftime('%Y%m%dT%H%M%S')}.json`
    - Audit uses note_path=None (operation-level event, consistent with forget pattern)
    - Export scope: notes table ONLY — no audit_log rows exported (see Pitfall 5 in RESEARCH.md)
    - No passphrase gate — user owns their own data

    main() pattern: argparse → get BRAIN_ROOT from engine.paths or --brain-root arg → get_connection() → call export_brain() → print result → sys.exit(0)

    Import from engine.paths import BRAIN_ROOT, from engine.db import get_connection.
  </action>
  <verify>
    <automated>uv run --no-project --with pytest pytest tests/test_export.py -x -q</automated>
  </verify>
  <done>All 4 tests in test_export.py pass (not xfail). export_brain() returns correct count, JSON valid, audit logged, PII notes present.</done>
</task>

</tasks>

<verification>
uv run --no-project --with pytest pytest tests/test_export.py tests/ -q
Expected: test_export.py 4 passed; full suite no regressions.
</verification>

<success_criteria>
- test_export_returns_note_count: PASS
- test_export_json_contains_all_fields: PASS
- test_export_includes_pii_notes: PASS
- test_export_audit_logged: PASS
- Full suite: uv run --no-project --with pytest pytest tests/ -q — all green
</success_criteria>

<output>
After completion, create `.planning/phases/11-gdpr-scope-expansion/11-01-SUMMARY.md`
</output>
