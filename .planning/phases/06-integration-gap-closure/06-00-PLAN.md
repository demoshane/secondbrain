---
phase: 06-integration-gap-closure
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_capture.py
  - tests/test_watcher.py
  - tests/test_reindex.py
  - tests/test_subagent.py
autonomous: true
requirements:
  - CAP-06
  - AI-02
  - SEARCH-01

must_haves:
  truths:
    - "All 6 new test stubs exist and fail with xfail or ImportError (not pass)"
    - "3 existing test assertions updated to not break after code fixes"
    - "Full test suite still collectable (pytest --collect-only succeeds)"
  artifacts:
    - path: "tests/test_capture.py"
      provides: "test_cap06_update_memory_called_after_capture, test_cap06_update_memory_skipped_for_pii"
    - path: "tests/test_watcher.py"
      provides: "test_watcher_pii_routes_to_ollama, test_watcher_binary_fallback_to_private; updated on_new_file closure shape"
    - path: "tests/test_reindex.py"
      provides: "test_reindex_stores_absolute_paths; updated path assertions in test_reindex_parses_frontmatter_fields and test_reindex_idempotent"
    - path: "tests/test_subagent.py"
      provides: "test_subagent_documents_all_commands"
  key_links:
    - from: "tests/test_capture.py"
      to: "engine.capture.main"
      via: "mock update_memory via unittest.mock.patch"
      pattern: "patch.*update_memory"
    - from: "tests/test_watcher.py"
      to: "engine.router.get_adapter"
      via: "patch engine.router.get_adapter"
      pattern: "patch.*router.*get_adapter"
---

<objective>
Wave 0: Add failing test stubs for all 6 new behaviors (CAP-06, AI-02, SEARCH-01/AI-08) and fix 3 existing test assertions that will break after the code fixes land.

Purpose: Nyquist compliance — every Wave 1 code change must have a failing test before implementation begins. Also prevents regressions in existing tests that currently assert relative paths.
Output: 4 updated test files with xfail stubs; full suite still collectable.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-integration-gap-closure/06-CONTEXT.md
@.planning/phases/06-integration-gap-closure/06-RESEARCH.md

<interfaces>
<!-- Established patterns from existing tests — executor should match these exactly. -->

From tests/test_capture.py (existing pattern):
  - Uses `conftest.py` fixtures: `tmp_brain`, `initialized_db`
  - Patches via `unittest.mock.patch` context managers
  - `from engine.capture import capture_note, main` (deferred inside test body)

From tests/test_watcher.py (existing pattern):
  - `test_main_on_new_file_no_input_on_ai_failure` manually reconstructs the `on_new_file` closure
  - Patches `engine.router.get_adapter` (module ref, not `engine.watcher.get_adapter`)
  - Uses `tmp_path` fixture for file creation

From tests/test_reindex.py (existing pattern):
  - `seeded_db` fixture provides an in-memory conn with notes
  - Asserts `cur.execute("SELECT path FROM notes WHERE path='typed.md'")` — this exact-match will break

From tests/test_subagent.py (existing pattern):
  - Reads `.claude/agents/second-brain.md` from repo root via `Path(__file__).parent.parent`
  - `test_subagent_frontmatter_valid` checks YAML keys: `name`, `description`, `tools`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add capture and subagent test stubs</name>
  <files>tests/test_capture.py, tests/test_subagent.py</files>
  <behavior>
    - test_cap06_update_memory_called_after_capture: patch `engine.ai.update_memory` and `engine.capture.capture_note`; invoke `main()` with non-PII args; assert `update_memory` was called once
    - test_cap06_update_memory_skipped_for_pii: same setup but `--sensitivity pii`; assert `update_memory` was NOT called
    - test_subagent_documents_all_commands: read `.claude/agents/second-brain.md`; assert each of `sb-capture`, `sb-search`, `sb-forget`, `sb-read`, `sb-check-links` appears in the file body
  </behavior>
  <action>
    Append to tests/test_capture.py (after existing tests):

    Two new tests decorated with `@pytest.mark.xfail(strict=True, reason="CAP-06 not wired yet")`:
    - `test_cap06_update_memory_called_after_capture`: patch `engine.ai.update_memory` to a Mock; patch `engine.capture.capture_note` to return a `tmp_path / "note.md"` dummy; call `main()` with `["--type", "note", "--title", "T", "--body", "B", "--sensitivity", "public"]`; assert mock was called once.
    - `test_cap06_update_memory_skipped_for_pii`: same but `"--sensitivity", "pii"`; assert mock call_count == 0.

    Both tests must import deferred inside the test body (not at module top) to preserve --collect-only.

    Append to tests/test_subagent.py (after existing test):
    - `test_subagent_documents_all_commands`: no xfail (spec expansion is a doc task); read the file; assert all 5 command strings present. Mark `@pytest.mark.xfail(strict=True, reason="CAP-08 not expanded yet")`.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run --no-project --with pytest pytest tests/test_capture.py::test_cap06_update_memory_called_after_capture tests/test_capture.py::test_cap06_update_memory_skipped_for_pii tests/test_subagent.py::test_subagent_documents_all_commands -v 2>&1 | tail -20</automated>
  </verify>
  <done>3 tests collected; all report xfail (expected failure). No PASSED or ERROR.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add watcher and reindex test stubs; fix existing path assertions</name>
  <files>tests/test_watcher.py, tests/test_reindex.py</files>
  <behavior>
    - test_watcher_pii_routes_to_ollama: patch `engine.classifier.classify` to return `"pii"`; patch `engine.router.get_adapter`; call `on_new_file` (constructed as in main()); assert get_adapter called with `"pii"` (not `"private"`)
    - test_watcher_binary_fallback_to_private: make `path.read_text` raise `UnicodeDecodeError`; assert get_adapter called with `"private"`
    - test_reindex_stores_absolute_paths: reindex a tmp brain; query `SELECT path FROM notes LIMIT 1`; assert `os.path.isabs(row["path"])`
    - Fix test_reindex_parses_frontmatter_fields: change `WHERE path='typed.md'` to `WHERE path LIKE '%typed.md'`
    - Fix test_reindex_idempotent: same — update exact-path WHERE clause to LIKE pattern
    - Update test_main_on_new_file_no_input_on_ai_failure closure: the on_new_file closure must resolve adapter per-file (not capture from outer scope); update the manually-constructed closure to match the new shape described in 06-RESEARCH.md Pattern 2
  </behavior>
  <action>
    In tests/test_watcher.py:
    - Update `test_main_on_new_file_no_input_on_ai_failure`: the closure currently captures an `adapter` from outer scope. Update it to resolve adapter per-file by calling `router_mod.get_adapter(sensitivity, CONFIG_PATH)` inside the closure body. This makes it match the new watcher.py shape. The test should still pass (not xfail) after the watcher fix lands.
    - Add `test_watcher_pii_routes_to_ollama` with `@pytest.mark.xfail(strict=True, reason="AI-02 watcher fix not applied yet")`.
    - Add `test_watcher_binary_fallback_to_private` with `@pytest.mark.xfail(strict=True, reason="AI-02 watcher fix not applied yet")`.

    In tests/test_reindex.py:
    - Change all `WHERE path='typed.md'` exact-match queries to `WHERE path LIKE '%typed.md'` (affects test_reindex_parses_frontmatter_fields and test_reindex_idempotent). Do NOT add xfail — these tests should pass now (relative paths still work with LIKE before fix) and pass after fix (absolute paths still match LIKE).
    - Add `test_reindex_stores_absolute_paths` with `@pytest.mark.xfail(strict=True, reason="SEARCH-01 path fix not applied yet")`: reindex tmp brain dir; query first note path; assert `Path(path).is_absolute()`.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run --no-project --with pytest tests/test_reindex.py tests/test_watcher.py -v 2>&1 | tail -30</automated>
  </verify>
  <done>test_reindex_parses_frontmatter_fields and test_reindex_idempotent still pass (GREEN). test_reindex_stores_absolute_paths is xfail. test_watcher_pii_routes_to_ollama and test_watcher_binary_fallback_to_private are xfail. test_main_on_new_file_no_input_on_ai_failure still passes.</done>
</task>

</tasks>

<verification>
uv run --no-project --with pytest pytest tests/ --collect-only -q 2>&1 | tail -5
— must show 0 errors, all tests collectable.

uv run --no-project --with pytest tests/test_capture.py tests/test_watcher.py tests/test_reindex.py tests/test_subagent.py -v 2>&1 | tail -30
— existing tests GREEN, new stubs xfail, no ERROR.
</verification>

<success_criteria>
- 6 new test stubs exist (4 xfail, plus test_subagent_documents_all_commands xfail)
- 3 existing test assertions updated (2 reindex path assertions + 1 watcher closure)
- pytest --collect-only shows 0 collection errors
- No previously-passing tests are broken by this Wave 0 work
</success_criteria>

<output>
After completion, create `.planning/phases/06-integration-gap-closure/06-00-SUMMARY.md`
</output>
