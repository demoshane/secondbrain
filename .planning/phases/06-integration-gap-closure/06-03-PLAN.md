---
phase: 06-integration-gap-closure
plan: "03"
type: execute
wave: 1
depends_on:
  - "06-00"
files_modified:
  - .claude/agents/second-brain.md
  - ~/.claude/CLAUDE.md
autonomous: false
requirements:
  - CAP-08
  - CAP-09

must_haves:
  truths:
    - "second-brain.md documents all 5 sb-* commands with args and a usage example each"
    - "second-brain.md has a Claude Cowork equivalence section"
    - "~/.claude/CLAUDE.md has a Second Brain proactive capture section (verified manually)"
  artifacts:
    - path: ".claude/agents/second-brain.md"
      provides: "sb-capture, sb-search, sb-forget, sb-read, sb-check-links documented; Cowork section"
      contains: "sb-check-links"
    - path: "~/.claude/CLAUDE.md"
      provides: "## Second Brain proactive capture instructions block"
  key_links:
    - from: "~/.claude/CLAUDE.md"
      to: "second-brain subagent"
      via: "reference to second-brain subagent in capture instructions"
      pattern: "second-brain"
---

<objective>
Documentation-only plan: expand the second-brain subagent spec to cover all 5 sb-* commands, and append proactive capture instructions to ~/.claude/CLAUDE.md.

Purpose: CAP-08 (Claude Code invocability) and CAP-09 (proactive capture behavior) are the human-facing interface layer. No code changes.
Output: .claude/agents/second-brain.md fully expanded; ~/.claude/CLAUDE.md has Second Brain section.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-integration-gap-closure/06-CONTEXT.md
@.planning/phases/06-integration-gap-closure/06-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand second-brain subagent spec (CAP-08)</name>
  <files>.claude/agents/second-brain.md</files>
  <behavior>
    - test_subagent_documents_all_commands: each of sb-capture, sb-search, sb-forget, sb-read, sb-check-links appears in the file body
    - test_subagent_frontmatter_valid: still passes (YAML keys name, description, tools unchanged)
  </behavior>
  <action>
    Read .claude/agents/second-brain.md. Keep the existing YAML frontmatter block (name, description, tools) unchanged.

    Expand the body to document all 5 commands using this structure for each:

    ## sb-capture

    **What it does:** Captures a note into the brain — prompts AI follow-up questions, classifies PII, routes to correct AI model, writes atomic markdown, indexes in SQLite.
    **Arguments:**
      --type &lt;type&gt;         Note type: note, meeting, people, project, idea, coding, strategy, personal
      --title &lt;title&gt;       Note title (required)
      --body &lt;body&gt;         Note body content (required)
      --tags &lt;tags&gt;         Comma-separated tags (optional)
      --people &lt;people&gt;     Comma-separated people references (optional)
      --sensitivity &lt;level&gt; public | private | pii (default: inferred from content)
    **Content types:** all
    **Example:**
      sb-capture --type meeting --title "Q1 planning with Alice" --body "Discussed OKRs..." --people alice --sensitivity private

    ## sb-search

    **What it does:** Full-text search across all notes using FTS5 BM25 ranking; supports type scoping.
    **Arguments:**
      &lt;query&gt;               Search query (required)
      --type &lt;type&gt;         Scope to a single content type folder (optional)
    **Content types:** all
    **Example:**
      sb-search "Alice OKR"
      sb-search --type people "Alice"

    ## sb-forget

    **What it does:** GDPR right-to-erasure — deletes person's markdown file, all meeting notes referencing only them, FTS5 shadow entries, audit log rows, and backlinks. Rebuilds FTS5 index after deletion.
    **Arguments:**
      &lt;person&gt;              Person slug or name (required)
    **Content types:** people, meetings
    **Example:**
      sb-forget alice

    ## sb-read

    **What it does:** Displays a note's content. Notes with content_sensitivity: pii require passphrase confirmation before content is shown.
    **Arguments:**
      &lt;path&gt;                Path to note file (required)
    **Content types:** all
    **Example:**
      sb-read brain/people/alice.md

    ## sb-check-links

    **What it does:** Validates all bidirectional links across people/meetings/projects and reports orphans.
    **Arguments:**
      (none)
    **Content types:** people, meetings, projects
    **Example:**
      sb-check-links

    ---

    ## Claude Cowork Equivalence

    All sb-* commands are available identically in Claude Cowork sessions via the Bash tool. Invoke them the same way — no special wrapper needed. The second-brain subagent spec applies equally to both Claude Code and Claude Cowork interfaces.

    Pitfall: do NOT modify the YAML frontmatter. The existing test_subagent_frontmatter_valid asserts specific keys.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run --no-project --with pytest tests/test_subagent.py -v 2>&1 | tail -15</automated>
  </verify>
  <done>test_subagent_documents_all_commands: PASSED. test_subagent_frontmatter_valid: still PASSED.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Append Second Brain section to ~/.claude/CLAUDE.md (CAP-09)</name>
  <files>/Users/tuomasleppanen/.claude/CLAUDE.md</files>
  <action>
    Read /Users/tuomasleppanen/.claude/CLAUDE.md. Check if "## Second Brain" already exists (idempotency guard). If the section is absent, append exactly this block at the end of the file:

    ## Second Brain

    When you notice high-value content in a session — a decision made, a person discussed, a meeting described, or project context established — offer once: "I noticed a [decision/meeting/person/project context] — capture it?"

    If the user says yes, use the `second-brain` subagent or run directly:
      sb-capture --type &lt;type&gt; --title "&lt;title&gt;" --body "&lt;body&gt;" --sensitivity &lt;level&gt;

    Content types that warrant an offer: decisions, people discussions, meeting notes, project context.
    Content types that do NOT warrant an offer: casual chat, code snippets, debugging sessions, transient questions.

    Re-offer policy: offer once per topic. Re-offer a second time only if significantly more detail about the same topic emerges in the same session.

    If "## Second Brain" is already present, skip the append (do not create a duplicate section).
  </action>
  <verify>
    <automated>MISSING — CAP-09 target file lives outside repo on host filesystem; manual inspection required</automated>
  </verify>
  <done>~/.claude/CLAUDE.md contains exactly one "## Second Brain" section with proactive offer phrasing, sb-capture usage, and re-offer policy.</done>
  <what-built>
    Executor reads ~/.claude/CLAUDE.md, checks for "## Second Brain", and appends the proactive capture block if absent.
  </what-built>
  <how-to-verify>
    1. Open /Users/tuomasleppanen/.claude/CLAUDE.md
    2. Confirm "## Second Brain" section is present at the end
    3. Confirm the section contains: "I noticed a", "capture it?", "sb-capture --type", "Re-offer policy"
    4. Confirm no duplicate "## Second Brain" headings exist
  </how-to-verify>
  <resume-signal>Type "approved" if section is present and correct, or describe what's missing/wrong</resume-signal>
</task>

</tasks>

<verification>
uv run --no-project --with pytest tests/test_subagent.py -v 2>&1 | tail -10
— both tests GREEN (frontmatter_valid + documents_all_commands).

Manual: grep "Second Brain" /Users/tuomasleppanen/.claude/CLAUDE.md
— returns exactly one match.
</verification>

<success_criteria>
- test_subagent_documents_all_commands: PASSED
- test_subagent_frontmatter_valid: still PASSED
- .claude/agents/second-brain.md documents all 5 sb-* commands with args and examples
- ~/.claude/CLAUDE.md contains exactly one "## Second Brain" section with proactive capture instructions
- User confirms CLAUDE.md update at checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/06-integration-gap-closure/06-03-SUMMARY.md`
</output>
