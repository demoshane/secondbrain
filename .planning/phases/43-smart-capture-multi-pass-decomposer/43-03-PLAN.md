---
phase: 43-smart-capture-multi-pass-decomposer
plan: 03
type: execute
wave: 3
depends_on: ["43-02"]
files_modified:
  - engine/api.py
  - engine/mcp_server.py
  - engine/segmenter.py
  - tests/test_smart_capture.py
autonomous: true
execution_strategy: direct
requirements:
  - D-02
  - D-08
  - D-12
  - D-13

must_haves:
  truths:
    - "POST /smart-capture calls decompose() instead of segment_blob()"
    - "POST /smart-capture creates person stubs (parity with MCP)"
    - "POST /smart-capture response includes link_notes and person_stubs"
    - "sb_capture_smart calls decompose() instead of segment_blob()"
    - "Both callers produce identical capture behaviour"
    - "segment_blob() is deleted from segmenter.py"
    - "Action items extracted by keyword are persisted to action_items table at capture time"
  artifacts:
    - path: "engine/api.py"
      provides: "Updated POST /smart-capture using decompose()"
      contains: "from engine.passes import decompose"
    - path: "engine/mcp_server.py"
      provides: "Updated sb_capture_smart using decompose()"
      contains: "from engine.passes import decompose"
    - path: "engine/segmenter.py"
      provides: "Only resolve_entities() and dedup_segment() remain"
  key_links:
    - from: "engine/api.py"
      to: "engine/passes/__init__.py"
      via: "from engine.passes import decompose, CONFIDENCE_THRESHOLD"
      pattern: "from engine.passes import decompose"
    - from: "engine/mcp_server.py"
      to: "engine/passes/__init__.py"
      via: "from engine.passes import decompose, CONFIDENCE_THRESHOLD"
      pattern: "from engine.passes import decompose"
    - from: "engine/api.py"
      to: "action_items table"
      via: "INSERT for keyword-extracted action items from decompose result"
      pattern: "INSERT.*action_items"
---

<objective>
Wire api.py and mcp_server.py to use decompose() instead of segment_blob(). Delete segment_blob() from segmenter.py. Achieve full GUI/MCP parity for person stub creation. Persist keyword-extracted action items at capture time. Migrate existing tests.

Purpose: This is the critical wiring plan — after this, the old pipeline is gone and the new pass architecture is live.
Output: Updated callers, deleted segment_blob(), migrated tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-smart-capture-multi-pass-decomposer/43-CONTEXT.md
@.planning/phases/43-smart-capture-multi-pass-decomposer/43-RESEARCH.md
@.planning/phases/43-smart-capture-multi-pass-decomposer/43-01-SUMMARY.md
@.planning/phases/43-smart-capture-multi-pass-decomposer/43-02-SUMMARY.md
@.claude/LEARNINGS.md

@engine/api.py
@engine/mcp_server.py
@engine/segmenter.py
@engine/passes/__init__.py
@tests/test_smart_capture.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire api.py POST /smart-capture to use decompose(), add person stub creation and action item persistence</name>
  <files>engine/api.py</files>
  <read_first>
    - engine/api.py (lines 2195-2270: current POST /smart-capture implementation — understand segment_blob call, saved/pending_review logic, co-captured relationships, source_url/source_type handling)
    - engine/api.py (lines 2272-2330: POST /smart-capture/confirm — understand if this also needs updating)
    - engine/passes/__init__.py (decompose() signature, DecomposedResult fields — especially link_notes, action_items, person_stubs, existing_people)
    - engine/mcp_server.py (lines 880-910: person stub creation loop in sb_capture_smart — this is the pattern to replicate in api.py for parity per D-12)
    - .planning/phases/43-smart-capture-multi-pass-decomposer/43-CONTEXT.md (D-02, D-08, D-12, D-13)
    - .planning/phases/43-smart-capture-multi-pass-decomposer/43-RESEARCH.md (Target caller pattern for api.py, Pitfall 2: MCP has more logic than API)
  </read_first>
  <action>
    **Replace POST /smart-capture implementation (lines 2195-2269):**

    1. Change imports: replace `from engine.segmenter import segment_blob` with `from engine.passes import decompose, CONFIDENCE_THRESHOLD`. Remove `from engine.typeclassifier import CONFIDENCE_THRESHOLD`.

    2. Replace `segments = segment_blob(content)` with:
    ```python
    conn = get_connection()
    results = decompose(content, conn=conn, brain_root=_engine_paths.BRAIN_ROOT)
    ```

    3. For each result in results:
       - If `result.confidence < CONFIDENCE_THRESHOLD`: add to `pending_review` list with fields: title=result.primary_title, body=result.primary_body, suggested_type=result.primary_type, confidence=round(result.confidence, 2), people=result.entities.get("people", [])
       - Otherwise, save in this order:
         a. **Link notes first** (per D-04): for each `link` in `result.link_notes`, call `capture_note(note_type="link", title=link.title, body=link.body, tags=[], people=[], content_sensitivity="public", brain_root=_engine_paths.BRAIN_ROOT, conn=conn, url=link.url, source_type=source_type or None)`. Append to `saved` list with type="link".
         b. **Person stubs** (per D-12): for each `stub` in `result.person_stubs`, call `capture_note(note_type=stub["type"], title=stub["name"], body="", tags=[], people=[], content_sensitivity="public", brain_root=_engine_paths.BRAIN_ROOT, conn=conn)`. Track created stub paths. Wrap in try/except (non-fatal).
         c. **Primary note**: call `capture_note(note_type=result.primary_type, title=result.primary_title, body=result.primary_body, tags=[], people=result.entities.get("people", []), content_sensitivity="public", brain_root=_engine_paths.BRAIN_ROOT, conn=conn, url=source_url or None, source_type=source_type or None)`. Append to `saved`.
         d. **Keyword action items** (per D-08): for each `ai` in `result.action_items`, INSERT into action_items table: `conn.execute("INSERT OR IGNORE INTO action_items (note_path, item_text, status, created_at) VALUES (?, ?, 'open', datetime('now'))", (str(path), ai.text))`. Only if primary note save succeeded (path is available).
         e. **Intent-based action items**: call `extract_action_items(path, result.primary_body, "public", conn)` post-save — same as today's pattern.

    4. Co-captured relationships: keep existing `itertools.combinations(paths, 2)` pattern, but include link note paths and stub paths in the paths list.

    5. Return response: add `person_stubs` field listing created stub names, and include link notes in the `notes` array (they are regular saved notes).

    **POST /smart-capture/confirm** (lines 2272+): This endpoint takes pre-classified segments from pending_review and saves them. It currently calls capture_note() directly — it does NOT need decompose(). However, update it to also create person stubs for confirmed segments' entities (same stub creation pattern as above). Read the full implementation first to confirm.
  </action>
  <verify>
    <automated>uv run pytest tests/test_decomposer.py tests/test_typeclassifier.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `engine/api.py` smart_capture function contains `from engine.passes import decompose`
    - `engine/api.py` smart_capture function does NOT contain `from engine.segmenter import segment_blob`
    - `engine/api.py` smart_capture function contains `result.person_stubs` (person stub creation loop)
    - `engine/api.py` smart_capture function contains `result.link_notes` (link note saving loop)
    - `engine/api.py` smart_capture function contains `result.action_items` (action item persistence)
    - `engine/api.py` smart_capture function contains `extract_action_items(` (intent-based post-save)
  </acceptance_criteria>
  <done>POST /smart-capture uses decompose(), creates person stubs (GUI/MCP parity), saves link notes, persists keyword action items</done>
</task>

<task type="auto">
  <name>Task 2: Wire mcp_server.py sb_capture_smart to use decompose(), delete segment_blob() from segmenter.py, migrate tests</name>
  <files>engine/mcp_server.py, engine/segmenter.py, tests/test_smart_capture.py</files>
  <read_first>
    - engine/mcp_server.py (lines 825-960: full sb_capture_smart implementation — segment_blob call, resolve_entities stub creation loop, dedup_segment calls, classify_smart, importance classification, co-captured relationships, dormant resurfacing)
    - engine/segmenter.py (full file — identify segment_blob and all its private helpers that will be deleted vs resolve_entities/dedup_segment that stay)
    - tests/test_smart_capture.py (full file — TestSegmentStructuralMarkers imports segment_blob, understand all test classes and what needs migration)
    - engine/passes/__init__.py (decompose() signature and what it already handles vs what callers still need to do)
  </read_first>
  <action>
    **Update `engine/mcp_server.py` sb_capture_smart (per D-02, D-13):**

    1. Change imports: replace `from engine.segmenter import dedup_segment, resolve_entities, segment_blob` with `from engine.segmenter import dedup_segment` and `from engine.passes import decompose, CONFIDENCE_THRESHOLD`. Remove `from engine.typeclassifier import CONFIDENCE_THRESHOLD`.

    2. Replace `segments = segment_blob(content)` with:
    ```python
    results = decompose(content, conn=conn, brain_root=BRAIN_ROOT)
    ```

    3. **Remove the existing stub creation loop** (lines 882-908 where it calls resolve_entities and creates stubs). This logic is now in Pass 5 — the `results` already have `person_stubs` populated.

    4. Rewrite the main loop to iterate over `results` (list[DecomposedResult]) instead of `segments` (list[dict]):
       - Map fields: `result.primary_title` → title, `result.primary_type` → note_type, `result.confidence` → confidence, `result.primary_body` → body, `result.entities` → entities
       - Person stubs: iterate `result.person_stubs` and create via `capture_note()` — same pattern as before but data comes from DecomposedResult
       - Link notes: iterate `result.link_notes` and save each via `capture_note(note_type="link", ...)`
       - Keyword action items: iterate `result.action_items` and INSERT into action_items table (same pattern as api.py Task 1)
       - Dedup: call `dedup_segment(title, body, conn, BRAIN_ROOT)` per result — keep existing dedup logic
       - Sensitivity, importance: keep existing `classify_smart(body)` and `classify_importance(title, body)` calls
       - Co-captured relationships: keep existing pattern
       - Dormant resurfacing: keep existing post-save logic

    5. Low-confidence results: keep existing `pending_review` handling

    **Delete segment_blob() from `engine/segmenter.py` (per D-02):**
    - Remove `segment_blob()` function and ALL its private helpers: `_mask_protected_regions`, `_split_at_safe_positions`, `_classify_segment`, `_derive_title`, `_merge_short_segments`, `_enforce_max_cap`, `_pass2_name_cluster`, `_extract_original_parts`
    - Remove constants used only by segment_blob: `_MIN_SEGMENT_LEN`, `_MIN_SEGMENT_LINES`, `_MAX_SEGMENTS`, `_STRUCTURAL_SPLIT`, `_URL_PAT`, `_TABLE_ROW`
    - Keep: `resolve_entities()`, `dedup_segment()`, and their imports (`difflib`, `hashlib`, `re`, `sqlite3`, etc.)
    - Keep: module docstring but update it to reflect the remaining functions

    **Migrate `tests/test_smart_capture.py`:**
    - `TestSegmentStructuralMarkers`: change `from engine.segmenter import segment_blob` to `from engine.passes import decompose`. Update test assertions to work with DecomposedResult objects instead of dicts: `result[0].primary_title` instead of `result[0]["title"]`, `result[0].primary_type` instead of `result[0]["type"]`, etc.
    - Find any other imports of `segment_blob` in the test file and update them
    - `test_smart_capture_golden_path`: this is a pre-existing failure (Phase 45). Mark with `@pytest.mark.xfail(reason="Pre-existing failure — Phase 45 tracks fix")` if not already marked. Update its imports from segment_blob to decompose.
    - Add `TestGuiMcpParity` test class: test that POST /smart-capture response includes `person_stubs` field when content mentions unknown people names. This is an integration test requiring the Flask test client.
  </action>
  <verify>
    <automated>uv run pytest tests/test_smart_capture.py tests/test_decomposer.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `engine/mcp_server.py` sb_capture_smart contains `from engine.passes import decompose`
    - `engine/mcp_server.py` sb_capture_smart does NOT contain `segment_blob`
    - `engine/segmenter.py` does NOT contain `def segment_blob(`
    - `engine/segmenter.py` still contains `def resolve_entities(` and `def dedup_segment(`
    - `tests/test_smart_capture.py` does NOT contain `from engine.segmenter import segment_blob`
    - `tests/test_smart_capture.py` contains `from engine.passes import decompose`
    - `tests/test_smart_capture.py` contains `TestGuiMcpParity`
    - `uv run pytest tests/test_smart_capture.py tests/test_decomposer.py -x -q` exits 0
  </acceptance_criteria>
  <done>Both callers use decompose(), segment_blob() deleted, tests migrated, GUI/MCP parity verified</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_smart_capture.py tests/test_decomposer.py tests/test_typeclassifier.py -x -q` — all pass
- `grep -r "segment_blob" engine/` returns no hits (deleted everywhere)
- `grep "from engine.passes import decompose" engine/api.py engine/mcp_server.py` — both files hit
- `grep "person_stubs" engine/api.py` — confirms stub creation in GUI path
</verification>

<success_criteria>
- segment_blob() fully removed from codebase
- Both api.py and mcp_server.py call decompose()
- POST /smart-capture creates person stubs (GUI/MCP parity)
- Keyword action items persisted to action_items table at capture time
- Intent-based action items still extracted post-save via extract_action_items()
- All migrated and new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/43-smart-capture-multi-pass-decomposer/43-03-SUMMARY.md`
</output>
