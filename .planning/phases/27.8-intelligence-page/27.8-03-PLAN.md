---
phase: 27.8-intelligence-page
plan: 03
type: execute
wave: 3
depends_on:
  - 27.8-02
files_modified:
  - tests/test_gui.py
autonomous: false
requirements:
  - 27.8-INTEL-04

must_haves:
  truths:
    - "test_intelligence_tab navigates to Intelligence page and passes"
    - "test_intelligence_recap_button finds generate-recap-btn and passes"
    - "test_intelligence_health_score finds health-score element and passes"
    - "User confirms Intelligence page layout is correct in live GUI"
  artifacts:
    - path: "tests/test_gui.py"
      provides: "3 Playwright tests for Intelligence page"
      contains: "test_intelligence_tab"
  key_links:
    - from: "tests/test_gui.py"
      to: "http://localhost:37491/ui"
      via: "page.goto('/ui') pattern"
      pattern: "test_intelligence_tab"
---

<objective>
Add three Playwright smoke tests for the Intelligence page to `tests/test_gui.py`, run them headless to confirm pass, then present a human-verify checkpoint for visual layout confirmation.

Purpose: Nyquist compliance — every requirement has automated test coverage before sign-off.
Output: 3 green Playwright tests + human sign-off on visual layout.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27.8-intelligence-page/27.8-RESEARCH.md
@.planning/phases/27.8-intelligence-page/27.8-02-SUMMARY.md
</context>

<interfaces>
<!-- Playwright test pattern from test_gui.py (existing pages for reference) -->
From tests/test_gui.py (projects test pattern — lines ~333-352):
```python
def test_projects_tab(page, live_server_url):
    page.goto("/ui")
    page.locator('[data-testid="tab-bar"]').wait_for(state='visible')
    page.locator('[data-testid="tab-projects"]').wait_for(state='visible')
    page.locator('[data-testid="tab-projects"]').click()
    page.locator('[data-testid="projects-page"]').wait_for(state='visible')
```

<!-- No new fixture needed -->
Note: Intelligence page works on empty DB (score=100, nudges=[], recap=placeholder text).
No gui_brain fixture needed for these tests — use the same `page` + `live_server_url` fixtures as existing smoke tests.

<!-- data-testids to target -->
- `[data-testid="tab-bar"]` — existing tab bar container
- `[data-testid="tab-intelligence"]` — Intelligence tab (added in Plan 02)
- `[data-testid="intelligence-page"]` — page root div
- `[data-testid="generate-recap-btn"]` — Generate Recap button
- `[data-testid="health-score"]` — brain health score element
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Playwright tests for Intelligence page</name>
  <files>tests/test_gui.py</files>
  <action>
Append three tests to `tests/test_gui.py` (after the existing projects tests, before the file ends):

```python
def test_intelligence_tab(page, live_server_url):
    page.goto("/ui")
    page.locator('[data-testid="tab-bar"]').wait_for(state='visible')
    page.locator('[data-testid="tab-intelligence"]').wait_for(state='visible')
    page.locator('[data-testid="tab-intelligence"]').click()
    page.locator('[data-testid="intelligence-page"]').wait_for(state='visible')


def test_intelligence_recap_button(page, live_server_url):
    page.goto("/ui")
    page.locator('[data-testid="tab-intelligence"]').wait_for(state='visible')
    page.locator('[data-testid="tab-intelligence"]').click()
    page.locator('[data-testid="intelligence-page"]').wait_for(state='visible')
    page.locator('[data-testid="generate-recap-btn"]').wait_for(state='visible')


def test_intelligence_health_score(page, live_server_url):
    page.goto("/ui")
    page.locator('[data-testid="tab-intelligence"]').wait_for(state='visible')
    page.locator('[data-testid="tab-intelligence"]').click()
    page.locator('[data-testid="intelligence-page"]').wait_for(state='visible')
    page.locator('[data-testid="health-score"]').wait_for(state='visible')
```

Run tests headless to confirm all three pass before presenting checkpoint. If any test fails, diagnose and fix before proceeding (do not present human-verify with broken tests — per LEARNINGS.md Playwright rule).
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_gui.py -k "intelligence" -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>All 3 intelligence tests pass (not xfail — real PASSED); full suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Intelligence page promoted from sidebar panel to full dedicated tab. Three sections: Recap (with Generate Recap button), Brain Health (with score and breakdown), Stale Notes. RightPanel no longer shows recap/health content (backlinks and people sections remain).
  </what-built>
  <how-to-verify>
1. Open `http://localhost:37491/ui`
2. Confirm "Intelligence" tab is visible in the tab bar
3. Click the Intelligence tab
4. Verify three sections are visible: Recap, Brain Health, Stale Notes
5. Confirm Brain Health shows a numeric score (e.g. "100/100" on empty DB)
6. Click "Generate Recap" — confirm button animates (spinner), then shows recap text or placeholder
7. Open any note, confirm right panel shows Backlinks and People but NO Recap or Brain Health sections
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. `uv run pytest tests/test_gui.py -k "intelligence" -x -q` — 3 tests PASSED
2. `uv run pytest tests/ -q` — full suite green
3. Human confirms visual layout in live GUI
</verification>

<success_criteria>
- 3 Playwright intelligence tests pass (not xfail)
- Full test suite green
- Human approves visual layout
- Phase 27.8 complete — Intelligence page is a first-class tab
</success_criteria>

<output>
After completion, create `.planning/phases/27.8-intelligence-page/27.8-03-SUMMARY.md`
</output>
