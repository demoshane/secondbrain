---
phase: 27.8-intelligence-page
plan: "01"
subsystem: backend
tags: [brain-health, testing, xfail-stubs, wave-0]
dependency_graph:
  requires: []
  provides: [engine/brain_health.get_empty_notes, tests/test_intelligence_page.py]
  affects: [engine/api.py brain_health_endpoint]
tech_stack:
  added: []
  patterns: [xfail(strict=False) stub pattern, local client fixture with BRAIN_PATH monkeypatch]
key_files:
  created:
    - tests/test_intelligence_page.py
  modified:
    - engine/brain_health.py
decisions:
  - "get_empty_notes queries TRIM(body)='' to catch whitespace-only notes; LIMIT 20 consistent with orphan cap"
  - "xfail(strict=False) stubs auto-promote to XPASS once Wave 2 ships intelligence endpoints"
  - "test_get_brain_health remains XFAIL until /brain-health response includes empty_count key (Wave 2)"
metrics:
  duration: "7 min"
  completed_date: "2026-03-18"
  tasks_completed: 2
  files_changed: 2
requirements:
  - 27.8-INTEL-01
  - 27.8-INTEL-02
  - 27.8-INTEL-03
  - 27.8-INTEL-04
---

# Phase 27.8 Plan 01: Brain Health Fix + Intelligence Test Stubs Summary

**One-liner:** Fixed missing `get_empty_notes` function that caused `/brain-health` ImportError, and added three xfail stubs scaffolding Intelligence page backend tests.

## What Was Built

### Task 1: get_empty_notes added to brain_health.py (commit a4fdd51)

Added `get_empty_notes(conn: sqlite3.Connection) -> list[dict]` after `get_orphan_notes`. Queries notes where `body IS NULL OR TRIM(body) = ''`, returns up to 20 results as `[{"path": ..., "title": ...}]` dicts. This fixes the `ImportError` that was causing the `/brain-health` endpoint to return 500 on every call.

### Task 2: xfail test stubs for intelligence page backend (commit 8fb892a)

Created `tests/test_intelligence_page.py` with a local `client` fixture (matches `test_meetings.py` / `test_people.py` pattern — monkeypatches BRAIN_PATH, engine.db.DB_PATH, engine.paths.DB_PATH, inits schema). Three stubs:

- `test_get_intelligence` — GET /intelligence returns 200 with "nudges" key (XPASS — route exists)
- `test_post_recap` — POST /intelligence/recap returns 200 with "recap" string (XPASS — route exists)
- `test_get_brain_health` — GET /brain-health returns 200 with all 5 health keys (XFAIL — empty_count not yet in response)

All 3 collected; no ERROR or FAILED states. Full suite: 352 passed, 1 skipped, 12 xfailed, 32 xpassed.

## Verification Results

1. `from engine.brain_health import get_empty_notes` — OK
2. `uv run pytest tests/test_intelligence_page.py -v` — 3 collected, 1 xfailed, 2 xpassed
3. `uv run pytest tests/ -q` — 352 passed, no failures, no regressions

## Deviations from Plan

None — plan executed exactly as written.

## Commits

| Task | Commit | Message |
|------|--------|---------|
| 1 | a4fdd51 | feat(27.8-01): add get_empty_notes to brain_health.py |
| 2 | 8fb892a | test(27.8-01): add xfail stubs for intelligence page backend |

## Self-Check: PASSED

- engine/brain_health.py: FOUND
- tests/test_intelligence_page.py: FOUND
- commit a4fdd51: FOUND
- commit 8fb892a: FOUND
