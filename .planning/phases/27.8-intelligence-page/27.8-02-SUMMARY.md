---
phase: 27.8-intelligence-page
plan: "02"
subsystem: frontend
tags: [intelligence, react, tab-bar, right-panel, brain-health, recap]
dependency_graph:
  requires: [27.8-01]
  provides: [intelligence-page-ui]
  affects: [frontend/src/components, engine/gui/static]
tech_stack:
  added: []
  patterns: [Section collapsible pattern from ProjectsPage, deploy pipeline]
key_files:
  created:
    - frontend/src/components/IntelligencePage.tsx
  modified:
    - frontend/src/types.ts
    - frontend/src/components/TabBar.tsx
    - frontend/src/contexts/UIContext.tsx
    - frontend/src/App.tsx
    - frontend/src/components/RightPanel.tsx
    - engine/gui/static/index.html
    - engine/gui/static/assets/index-2GCm87e-.js
    - engine/gui/static/assets/index-BnM_DPuc.css
    - .secrets.baseline
decisions:
  - "[27.8-02] .secrets.baseline updated to new vite asset filename (index-2GCm87e-.js); hashed_secret unchanged (same minified keyword pattern)"
  - "[27.8-02] Tasks 1+2 committed together in a single commit — App.tsx imports IntelligencePage so both must exist before build verifies"
  - "[27.8-02] RightPanel retains only Backlinks + People sections; all intelligence content promoted to full page"
metrics:
  duration: 8 min
  completed: "2026-03-18"
  tasks_completed: 2
  files_changed: 9
---

# Phase 27.8 Plan 02: IntelligencePage UI — Summary

Promotes Recap, Brain Health, and Stale Notes from the cramped 256px RightPanel sidebar into a dedicated full-page Intelligence tab.

## What Was Built

**IntelligencePage component** (`frontend/src/components/IntelligencePage.tsx`):
- Recap section with Generate Recap button (`data-testid="generate-recap-btn"`), animated RefreshCw spinner, Markdown rendering via `react-markdown` + `remark-gfm`
- Brain Health section with score display (`data-testid="health-score"`) and expandable detail tables for orphans, empty notes, broken links, and duplicates
- Stale Notes section listing nudges by title with "No stale notes." fallback
- `data-testid="intelligence-page"` on root div

**Routing wiring:**
- `UIContext.tsx` View union extended with `'intelligence'`
- `TabBar.tsx` TABS array appended with `{ id: 'intelligence', label: 'Intelligence' }` (renders `data-testid="tab-intelligence"`)
- `App.tsx` final routing branch made explicit: `projects` → `ProjectsPage`, else → `IntelligencePage`
- `BrainHealth` interface added to `types.ts`

**RightPanel cleanup:** Removed recap, brain health, digest, connections, and stale nudge sections — keeping only Backlinks and People (note-context-specific content). Removed all associated state variables and effects (`recap`, `healthScore`, `generatingRecap`, `connections`, `digest`, second `useEffect` for `/brain-health` and `/intelligence`).

**Deploy pipeline:** Bundle rebuilt → `uv tool install . --reinstall` → sb-api restarted on port 37491.

## Verification

- `npm run build` — exit 0, no TypeScript errors
- `pytest tests/test_intelligence_page.py -x -q` — 3 stubs collected, 2 xfailed, 1 xpassed
- `pytest tests/ -q` — 352 passed, 1 skipped, 12 xfailed, 32 xpassed, 0 failures

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] detect-secrets false positive on new bundle hash**
- **Found during:** Task 1 commit
- **Issue:** `index-2GCm87e-.js` triggered detect-secrets hook (Secret Keyword in minified JS)
- **Fix:** Ran `uv run detect-secrets scan > .secrets.baseline` to update baseline with new filename
- **Files modified:** `.secrets.baseline`
- **Commit:** 51e6f72 (included in same commit)

## Self-Check: PASSED

- IntelligencePage.tsx: FOUND
- Commit 51e6f72: FOUND
- sb-api on :37491: FOUND
- test suite: 352 passed, 0 failed
