---
phase: 07-fix-path-format-split
plan: "01"
type: execute
wave: 2
depends_on:
  - "07-00"
files_modified:
  - engine/capture.py
autonomous: true
requirements:
  - SEARCH-01
  - SEARCH-04
  - GDPR-01
must_haves:
  truths:
    - "All 4 phase-7 tests are GREEN after the fix"
    - "Full test suite passes with no regressions"
    - "DB path column stores the canonical resolved form — no symlinks in stored paths"
    - "RAG reads file content directly without '[note file not readable]' fallback"
    - "forget_person deletes the DB row for a note captured without an intervening reindex"
  artifacts:
    - path: "engine/capture.py"
      provides: "write_note_atomic with resolved path storage"
      contains: "resolved_path = str(target.resolve())"
  key_links:
    - from: "engine/capture.py"
      to: "sqlite notes table"
      via: "resolved_path local variable used for both INSERT and log_audit"
      pattern: "resolved_path = str\\(target\\.resolve\\(\\)\\)"
    - from: "engine/capture.py"
      to: "engine/rag.py"
      via: "Path(r['path']).read_text() — works when path is fully resolved"
      pattern: "read_text"
    - from: "engine/capture.py"
      to: "engine/forget.py"
      via: "exact-match DELETE WHERE path = ? — matches only when both sides are resolved"
      pattern: "WHERE path ="
---

<objective>
Apply the one-line fix in engine/capture.py and turn all 4 RED tests GREEN.

Purpose: Eliminate the path-format split — capture now stores the canonical resolved path, making RAG reads and forget deletions reliable without requiring sb-reindex.
Output: Single production change in write_note_atomic(); all 4 phase-7 tests pass; full suite green.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-fix-path-format-split/07-RESEARCH.md
@.planning/phases/07-fix-path-format-split/07-00-SUMMARY.md

<interfaces>
<!-- From engine/capture.py — confirmed from source read -->
<!-- write_note_atomic inserts into notes table at line ~110 -->
<!-- Two lines to change: the INSERT value and the log_audit call -->

Relevant section of write_note_atomic (current broken code):
```python
conn.execute(
    "INSERT INTO notes (path, type, title, body, tags, people, created_at, updated_at, sensitivity)"
    " VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)",
    (
        str(target),          # BUG: unresolved — may be symlink on macOS
        ...
    ),
)
log_audit(conn, "create", str(target))  # BUG: same unresolved path
```

Fixed pattern (extract local variable, use for both):
```python
resolved_path = str(target.resolve())
conn.execute(
    "INSERT INTO notes (path, ...) VALUES (?, ...)",
    (
        resolved_path,        # FIX: canonical absolute path
        ...
    ),
)
log_audit(conn, "create", resolved_path)  # FIX: consistent with notes table
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix write_note_atomic() — str(target) → str(target.resolve())</name>
  <files>engine/capture.py</files>
  <behavior>
    - After fix: SELECT path FROM notes WHERE title=? returns str(target.resolve()) — canonical form
    - After fix: stored path starts with "/" on all platforms
    - After fix: RAG Path(r["path"]).read_text() succeeds without OSError
    - After fix: forget_person exact-match DELETE finds the row when brain_root is resolved
  </behavior>
  <action>
    Read engine/capture.py to find the exact lines. The INSERT into notes uses str(target) as the first positional arg in the VALUES tuple. The log_audit call below it also uses str(target).

    Make this change:
    1. Before the conn.execute("INSERT INTO notes ...") call, extract: `resolved_path = str(target.resolve())`
    2. Replace str(target) in the INSERT VALUES tuple with resolved_path
    3. Replace str(target) in the log_audit call with resolved_path

    No other changes. Do not touch rag.py or forget.py — research confirms they need no production changes.

    Commit message: "fix(capture): store str(target.resolve()) in DB — canonical path, symlinks resolved"

    Decision note: Using .resolve() not .absolute() — .absolute() does not follow symlinks, only .resolve() guarantees canonical form. macOS tmp_path is /var/folders/... (symlink to /private/var/folders/...) — .resolve() is required.
  </action>
  <verify>
    <automated>uv run --no-project --with pytest tests/test_capture.py::test_write_note_atomic_stores_absolute_path tests/test_capture.py::test_write_note_atomic_path_is_absolute tests/test_rag.py::test_retrieve_context_reads_captured_note tests/test_forget.py::test_forget_removes_row_stored_by_capture -v 2>&1 | tail -15</automated>
  </verify>
  <done>All 4 phase-7 tests GREEN. engine/capture.py contains `resolved_path = str(target.resolve())` and uses it for both INSERT and log_audit.</done>
</task>

<task type="auto">
  <name>Task 2: Full suite regression check</name>
  <files></files>
  <action>
    Run the full test suite to confirm no regressions. The only change was in write_note_atomic() — all existing capture tests use `target.exists()` and frontmatter content checks, not DB path values, so they are unaffected.

    If any pre-existing test fails that was passing before this phase, investigate and fix before declaring success. Do not skip or xfail existing tests.
  </action>
  <verify>
    <automated>uv run --no-project --with pytest -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>Full suite green. Zero regressions. All 4 phase-7 tests passing.</done>
</task>

</tasks>

<verification>
Run the targeted 4-test check plus full suite:

```
uv run --no-project --with pytest tests/test_capture.py::test_write_note_atomic_stores_absolute_path tests/test_capture.py::test_write_note_atomic_path_is_absolute tests/test_rag.py::test_retrieve_context_reads_captured_note tests/test_forget.py::test_forget_removes_row_stored_by_capture -v
uv run --no-project --with pytest -x -q
```

Both must exit 0.
</verification>

<success_criteria>
- engine/capture.py write_note_atomic() stores resolved_path (not str(target)) for both INSERT and log_audit
- 4 phase-7 tests: GREEN
- Full suite: GREEN (zero regressions)
- Phase goal achieved: DB rows from capture now use canonical absolute paths — RAG and forget work without sb-reindex
</success_criteria>

<output>
After completion, create `.planning/phases/07-fix-path-format-split/07-01-SUMMARY.md`
</output>
