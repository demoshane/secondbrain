---
phase: 27-search-quality-tuning
plan: 07
type: execute
wave: 2
depends_on:
  - 27-02
  - 27-03
  - 27-04
  - 27-05
  - 27-06
files_modified: []
autonomous: false
requirements:
  - ENGL-02

must_haves:
  truths:
    - "Full pytest suite passes with zero failures"
    - "All 10 regression queries pass (precision + recall)"
    - "sb_edit preserves frontmatter in test"
    - "sb-recap returns results in live brain"
    - "Person chips render in sidebar for a note with people frontmatter"
    - "CI workflow file exists and is valid"
  artifacts:
    - path: tests/test_search_regression.py
      provides: "10 passing regression tests (promoted from xfail)"
    - path: .github/workflows/ci.yml
      provides: "Valid CI workflow"
  key_links:
    - from: tests/test_search_regression.py
      to: engine/search.py
      via: "Regression tests confirm BM25 weights produce correct ranking"
      pattern: "search_notes.*precision"
---

<objective>
Phase sign-off: run the full test suite, verify all regression tests pass, and confirm real-world behavior via human checkpoint.

Purpose: Gate check before closing Phase 27 — automated coverage plus human confirmation of sb-recap and person chips.
Output: Passing suite + human approval
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and regression suite</name>
  <files></files>
  <action>
    Run the full pytest suite:
    ```
    uv run pytest tests/ -q --tb=short
    ```
    Expected: all tests pass, 0 failures, 0 errors.

    If any failures exist:
    - Diagnose each failure
    - Fix the root cause (do not skip or xfail to hide real failures)
    - Re-run until green

    Also specifically confirm regression suite results:
    ```
    uv run pytest tests/test_search_regression.py -v
    ```
    Expected: all 10 tests PASSED (promoted from xfail), none still xfail.

    If some recall tests still fail after BM25 weighting (recall is harder than precision):
    - Recall tests use xfail(strict=False) — they can remain xfail without blocking
    - But ALL 5 precision tests must pass
    - Document any still-xfail recall tests in the SUMMARY
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/ -q --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>Full suite exits 0; test_search_regression.py shows at least 5 precision tests PASSED</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - BM25 title weighting (10.0 title / 1.0 body) in search_notes()
    - Recency multiplier for tiebreaking
    - sb_edit frontmatter preservation fix
    - sb-recap fallback to recent notes
    - Person chips in sidebar
    - CI workflow
    - Adapter + health check tests
  </what-built>
  <how-to-verify>
    1. Open the GUI: cd /Users/tuomasleppanen/second-brain && uv run sb-gui
    2. Search for a note by its exact title → verify it appears as the first result
    3. Run sb-recap in terminal: uv run sb-recap → verify results appear (not empty)
    4. Open any note that has people in its frontmatter → check sidebar for person chips
    5. If person note exists for that name, click the chip → verify navigation works
    6. Optional: edit a note via MCP/CLI with sb_edit → verify frontmatter is preserved after edit
  </how-to-verify>
  <resume-signal>Type "approved" or describe any remaining issues</resume-signal>
</task>

</tasks>

<verification>
uv run pytest tests/ -q 2>&1 | tail -3
Expected: N passed, 0 failed, 0 error
</verification>

<success_criteria>
- uv run pytest tests/ -q exits 0
- At least 5 precision regression tests PASSED
- Human approved: search ranking, sb-recap, person chips all working in live GUI
</success_criteria>

<output>
After completion, create `.planning/phases/27-this-phase-should-resolve-all-open-todos-and-gaps-that-can-be-identified-when-the-phase-starts/27-07-SUMMARY.md`
</output>
