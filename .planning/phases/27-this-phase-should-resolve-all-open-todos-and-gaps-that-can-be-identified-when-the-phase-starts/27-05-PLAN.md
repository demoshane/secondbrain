---
phase: 27-search-quality-tuning
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_adapters.py
  - tests/test_health.py
autonomous: true
requirements:
  - ENGL-02

must_haves:
  truths:
    - "LLM adapter routing is tested: correct adapter selected based on config, mock responses returned"
    - "engine/health.py check functions are tested: each returns expected structure"
    - "New test files pass as part of full suite"
  artifacts:
    - path: tests/test_adapters.py
      provides: "Unit tests for claude_adapter, ollama_adapter, and adapter selection logic"
    - path: tests/test_health.py
      provides: "Unit tests for engine/health.py check functions"
  key_links:
    - from: tests/test_adapters.py
      to: engine/adapters/
      via: "imports adapter classes, patches API calls with unittest.mock"
      pattern: "from engine.adapters"
    - from: tests/test_health.py
      to: engine/health.py
      via: "imports check functions directly, calls with isolated DB fixture"
      pattern: "from engine.health import"
---

<objective>
Add missing test coverage for LLM adapters and health checks — both identified in TODOS.md as test coverage gaps.

Purpose: Prevents silent regressions in the adapter routing layer and health check functions, which have no automated tests currently.
Output: tests/test_adapters.py (new), tests/test_health.py (new)
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/27-this-phase-should-resolve-all-open-todos-and-gaps-that-can-be-identified-when-the-phase-starts/TODOS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write tests/test_adapters.py for LLM adapter routing</name>
  <files>tests/test_adapters.py</files>
  <action>
    First, read engine/adapters/ directory to understand:
    - base.py: base adapter interface
    - claude_adapter.py: Claude implementation
    - ollama_adapter.py: Ollama implementation
    - Any router/selector module

    Then read any config module that controls adapter selection (e.g. engine/config.py or similar).

    Write tests/test_adapters.py covering:
    1. **Adapter interface** — base adapter has expected methods (generate, or equivalent)
    2. **Claude adapter** — mock the Claude API call (requests.post or anthropic SDK); verify:
       - Calls the correct endpoint/method
       - Returns a string response
       - Handles API error gracefully (raises or returns fallback)
    3. **Ollama adapter** — mock requests.post to Ollama endpoint; verify:
       - Calls localhost:11434 (or configured host)
       - Returns a string response
    4. **Adapter selection/routing** — if a router exists, test that:
       - Config set to "claude" → returns ClaudeAdapter instance (or equivalent)
       - Config set to "ollama" → returns OllamaAdapter instance
       - Unknown config → raises ValueError or returns default
    5. Use unittest.mock.patch for all external HTTP calls — no real API calls
    6. Use existing DB_PATH patching pattern if any adapter reads from DB
    7. Keep tests simple: each test function covers one behavior, < 20 lines

    If the adapter module structure differs significantly from what's described above,
    adapt the tests to match actual structure. Do not invent APIs that don't exist.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_adapters.py -v 2>&1 | tail -15</automated>
  </verify>
  <done>tests/test_adapters.py passes with at least 4 tests; no real API calls made</done>
</task>

<task type="auto">
  <name>Task 2: Write tests/test_health.py for engine/health.py check functions</name>
  <files>tests/test_health.py</files>
  <action>
    First, read engine/health.py to understand all check functions and their signatures.
    The TODOS.md mentions ~8 check functions.

    Write tests/test_health.py:
    1. Use the standard DB isolation fixture (tmp_path + get_connection + init_schema + monkeypatch DB_PATH)
    2. For each check function, write one test that:
       - Sets up minimal DB state (insert notes/relationships as needed)
       - Calls the check function
       - Asserts return type and key fields (not brittle value assertions)
    3. Priority checks to test:
       - Orphan detection: note with no backlinks/relationships → appears in orphan list
       - Broken link detection: relationship pointing to nonexistent note → appears in broken list
       - Duplicate detection: two notes with identical titles → appears in duplicate list
       - Health score: returns int 0-100
    4. Do not test the CLI entry point (sb-health) — only the Python functions
    5. If health.py functions require specific schema tables (e.g. relationships), ensure init_schema creates them (it should — established pattern)

    Use the same fixture structure as tests/test_brain_health.py (which already exists — read it first).
    If test_brain_health.py already covers some functions, skip those and focus on uncovered ones.
    If health.py and brain_health.py overlap significantly, add tests to existing test_brain_health.py
    instead of creating a new file (adjust files_modified accordingly).
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_health.py -v 2>&1 | tail -15</automated>
  </verify>
  <done>tests/test_health.py passes with at least 3 tests covering orphan, broken link, and health score checks</done>
</task>

</tasks>

<verification>
uv run pytest tests/test_adapters.py tests/test_health.py -q 2>&1 | tail -10
Expected: all tests pass, no external API calls
</verification>

<success_criteria>
- tests/test_adapters.py tests adapter creation, routing, and mocked response handling
- tests/test_health.py tests at least orphan detection, broken link detection, health score
- Both files pass as part of full suite: uv run pytest tests/ -q
- No real Claude or Ollama API calls in tests (all mocked)
</success_criteria>

<output>
After completion, create `.planning/phases/27-this-phase-should-resolve-all-open-todos-and-gaps-that-can-be-identified-when-the-phase-starts/27-05-SUMMARY.md`
</output>
