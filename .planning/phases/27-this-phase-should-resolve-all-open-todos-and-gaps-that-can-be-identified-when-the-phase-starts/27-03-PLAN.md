---
phase: 27-search-quality-tuning
plan: 03
type: execute
wave: 1
depends_on:
  - 27-01
files_modified:
  - engine/capture.py
  - engine/mcp_server.py
  - engine/intelligence.py
autonomous: true
requirements:
  - ENGL-02

must_haves:
  truths:
    - "sb_edit preserves all YAML frontmatter fields when editing a note's body"
    - "sb-recap returns results when recent notes exist (no longer returns empty)"
    - "capture_note() in CLI mode suggests note_type based on title heuristics"
  artifacts:
    - path: engine/capture.py
      provides: "write_note_atomic with update=True parameter supporting INSERT OR REPLACE"
      contains: "INSERT OR REPLACE INTO notes"
    - path: engine/mcp_server.py
      provides: "sb_edit passes update=True to write_note_atomic"
  key_links:
    - from: engine/mcp_server.py
      to: engine/capture.py
      via: "sb_edit calls write_note_atomic(p, post, conn, update=True)"
      pattern: "write_note_atomic.*update=True"
    - from: engine/intelligence.py
      to: engine/db.py
      via: "fallback query: SELECT * FROM notes ORDER BY updated_at DESC LIMIT 5"
      pattern: "ORDER BY updated_at DESC LIMIT"
---

<objective>
Fix three accumulated bugs: sb_edit wipes frontmatter (write_note_atomic INSERT→UPSERT), sb-recap returns empty (add fallback to recent notes), and capture context detection improvements.

Purpose: These are logged pending TODOs in STATE.md that block daily usability.
Output: engine/capture.py (UPSERT + context heuristics), engine/mcp_server.py (pass update=True), engine/intelligence.py (recap fallback)
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/27-this-phase-should-resolve-all-open-todos-and-gaps-that-can-be-identified-when-the-phase-starts/27-RESEARCH.md
@.planning/phases/27-this-phase-should-resolve-all-open-todos-and-gaps-that-can-be-identified-when-the-phase-starts/27-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix sb_edit frontmatter wipe — add update=True to write_note_atomic</name>
  <files>engine/capture.py, engine/mcp_server.py</files>
  <behavior>
    - write_note_atomic(path, post, conn, update=False) — new optional param, default False preserves backward compat
    - When update=True: SQL uses INSERT OR REPLACE INTO notes (full upsert)
    - When update=False (default): SQL uses INSERT INTO notes (existing behavior, all callers unaffected)
    - sb_edit in mcp_server.py passes update=True when calling write_note_atomic
    - test_sb_edit_preserves_frontmatter xfail promotes to pass
  </behavior>
  <action>
    Read engine/capture.py to find write_note_atomic definition.
    Read engine/mcp_server.py to find sb_edit implementation.

    Step 1 — engine/capture.py:
    - Add `update: bool = False` parameter to write_note_atomic() signature
    - Locate the INSERT INTO notes SQL string
    - Change it to: if update: use "INSERT OR REPLACE INTO notes ..." else: use existing "INSERT INTO notes ..."
    - Keep all other logic (disk write, temp file, os.replace) unchanged
    - The change is ONLY in the DB INSERT statement, not in the file-write path

    Step 2 — engine/mcp_server.py:
    - In sb_edit(), locate the write_note_atomic() call
    - Add update=True: write_note_atomic(p, post, conn, update=True)

    Verify: writing to an existing note path no longer raises UNIQUE constraint error.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_mcp.py::test_sb_edit_preserves_frontmatter -v 2>&1 | tail -10</automated>
  </verify>
  <done>test_sb_edit_preserves_frontmatter passes (promoted from xfail); all other test_mcp.py tests still pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix sb-recap empty results + improve capture context detection</name>
  <files>engine/intelligence.py, engine/capture.py</files>
  <behavior>
    - recap_main() with git context returning 0 rows → falls back to SELECT 5 most recent notes by updated_at/created_at
    - Fallback result is clearly labeled (e.g. header "Recent activity (no context match):") so user understands it's a fallback
    - capture_note() CLI main() heuristics: if title contains meeting/standup/retro → suggest type=meeting; if title matches "Firstname Lastname" two-word capitalized pattern → suggest type=people (best-effort, never blocks capture)
    - Context detection improvements are additive only — existing classification logic unchanged
  </behavior>
  <action>
    Read engine/intelligence.py to find recap_main() and the git-context query.
    Read engine/capture.py to find the CLI main() or type-detection logic.

    Step 1 — engine/intelligence.py (recap fallback):
    - After the git-context DB query returns results, check: if not results (empty list):
    - Add fallback query: SELECT path, title, type, updated_at FROM notes ORDER BY updated_at DESC LIMIT 5
    - Format as a "recent activity" fallback with a clear section header
    - If the DB is also empty (brand new brain), return a friendly "No notes found yet" message
    - Do not touch any other recap logic

    Step 2 — engine/capture.py (context detection):
    - Find the CLI main() function or the type-suggestion/classification code path
    - Add heuristic suggestions after title is known:
      - title_lower = title.lower()
      - if any(kw in title_lower for kw in ["meeting", "standup", "sync", "retro", "review", "1:1"]): suggest type="meeting"
      - elif re.match(r'^[A-Z][a-z]+ [A-Z][a-z]+$', title.strip()): suggest type="people"
    - These are suggestions for interactive prompts, not hard overrides
    - import re at top if not already present
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/ -q -x 2>&1 | tail -10</automated>
  </verify>
  <done>Full suite passes; no new test failures from recap or capture changes</done>
</task>

</tasks>

<verification>
uv run pytest tests/test_mcp.py tests/ -q 2>&1 | tail -10
Expected: test_sb_edit_preserves_frontmatter passes, all other tests pass
</verification>

<success_criteria>
- write_note_atomic accepts update=True parameter and uses INSERT OR REPLACE when set
- sb_edit in mcp_server.py passes update=True
- recap_main() falls back to 5 most-recent notes when git-context query returns 0 rows
- capture main() suggests meeting/people type based on title keywords/pattern
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/27-this-phase-should-resolve-all-open-todos-and-gaps-that-can-be-identified-when-the-phase-starts/27-03-SUMMARY.md`
</output>
