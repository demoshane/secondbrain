---
phase: "48.1"
plan: "02"
title: "batch-capture → capture_note + api.py _NP migration"
execution_strategy: direct
files_modified:
  - engine/api.py
---

<objective>
1. Rewrite POST /batch-capture to call capture_note() per file instead of raw SQL INSERT.
2. Migrate remaining ~26 store_path(p) calls in api.py routes to use _NP.relative.
</objective>

<context>
@engine/api.py — batch_capture endpoint (line ~1830), _resolve_note_path returns _NP
@engine/capture.py — capture_note() is the canonical write path
</context>

<tasks>
<task type="auto">
  <behavior>POST /batch-capture uses capture_note() for each untracked file</behavior>
  <action>
    Rewrite batch_capture() to:
    1. Walk brain_root for .md files not in DB
    2. For each: frontmatter.load, then capture_note(note_type, title, body, tags, people, ...)
    3. Return succeeded/failed counts
    This is slower (entity extraction per note) but correct — all side effects fire.
  </action>
  <verify><automated>uv run pytest tests/test_api.py -x -q</automated></verify>
  <done>batch-capture goes through capture_note pipeline</done>
</task>

<task type="auto">
  <behavior>All api.py routes use _NP.relative instead of store_path(p)</behavior>
  <action>
    For each route that does `p, brain_root = _resolve_note_path(...)` then
    `store_path(p)`: change to `np = _resolve_note_path(...)` and use
    np.absolute for disk, np.relative for DB. Mechanical find-replace.
  </action>
  <verify><automated>uv run pytest tests/test_api.py -x -q</automated></verify>
  <done>Zero store_path() calls remain in api.py route handlers</done>
</task>
</tasks>
