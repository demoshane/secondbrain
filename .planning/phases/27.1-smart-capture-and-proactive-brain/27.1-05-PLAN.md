---
phase: 27.1-smart-capture-and-proactive-brain
plan: 05
type: execute
wave: 3
depends_on:
  - 27.1-01
files_modified:
  - CLAUDE.md
autonomous: true
requirements:
  - 27.1-SC-04

must_haves:
  truths:
    - "CLAUDE.md exists at the repo root"
    - "CLAUDE.md describes what the project is and its core value"
    - "CLAUDE.md lists all entry points (sb-capture, sb-api, sb-gui, sb-mcp-server)"
    - "CLAUDE.md states the brain data directory path"
    - "CLAUDE.md explains the engine/ module layout"
    - "CLAUDE.md includes the test command"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Project documentation for Claude agents working in this repo"
      contains: "SecondBrain\nsb-capture\nsb-gui\nsb-mcp-server\nuv run pytest"
  key_links: []
---

<objective>
Write CLAUDE.md at the repo root — a concise project orientation document for Claude agents (and human developers) picking up this codebase for the first time.

Purpose: Satisfies SC-04; reduces "what is this" overhead for any future agent session in this repo.
Output: CLAUDE.md at /Users/tuomasleppanen/second-brain/CLAUDE.md
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27.1-smart-capture-and-proactive-brain/27.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write CLAUDE.md at repo root</name>
  <files>CLAUDE.md</files>
  <action>
    Create CLAUDE.md at the repo root (/Users/tuomasleppanen/second-brain/CLAUDE.md).

    The file must cover these sections in order, concisely:

    1. **What this is** — Personal second brain: capture, search, and surface notes via CLI, GUI, and MCP. Zero-friction capture that surfaces the right context at the right moment.

    2. **Brain data directory** — `~/SecondBrain` (env var `BRAIN_PATH` overrides). All notes live here as `.md` files with YAML frontmatter.

    3. **Key entry points** (from pyproject.toml scripts):
       - `sb-capture` — capture a note (CLI, interactive)
       - `sb-search` — search notes (FTS5 + semantic hybrid)
       - `sb-gui` — launch the desktop GUI (pywebview + Flask)
       - `sb-api` — start the Flask HTTP sidecar (port 5001 by default)
       - `sb-mcp-server` — start the FastMCP stdio server for Claude Desktop
       - `sb-health` — brain health check (orphans, broken links, duplicates)
       - `sb-reindex` — rebuild the SQLite index from disk

    4. **Engine layout** (engine/ directory):
       - `capture.py` — the single write path for all captures
       - `db.py` — SQLite schema, migrations, connection
       - `api.py` — Flask HTTP API (GUI sidecar)
       - `mcp_server.py` — FastMCP MCP server (all MCP tools)
       - `intelligence.py` — recap, action items, connections, stale nudges
       - `embeddings.py` — sentence-transformers embedding dispatch
       - `search.py` — FTS5 + semantic search, RRF hybrid
       - `entities.py` — regex entity extraction (people, places, topics)
       - `brain_health.py` — orphan/duplicate/broken-link checks
       - `paths.py` — canonical paths (BRAIN_ROOT, DB_PATH, CONFIG_PATH)

    5. **Database** — SQLite at `~/SecondBrain/.brain.db`. Key tables: `notes`, `notes_fts` (FTS5 virtual), `note_embeddings` (sqlite-vec), `relationships`, `action_items`, `audit_log`, `attachments`.

    6. **Test command** — `uv run pytest tests/ -q` (full suite, ~15s). Per-file: `uv run pytest tests/test_capture.py -x`.

    7. **Key project gotchas** (from MEMORY.md):
       - Intel Mac, Python 3.13 pinned. When on M-chip: re-run embedding setup (lazy import workaround in place).
       - `sb-api` and `sb-watch` may be running as stale launchd services. When debugging, check `launchctl list | grep second-brain` and restart if needed.
       - MCP tools must use `mcp__second-brain__sb_*` names in Claude Desktop config.
       - User is on Anthropic Max plan (no API key). AI features use Claude Code/MCP, not direct SDK calls.

    8. **MCP tools** (current as of Phase 27.1, 13 tools):
       sb_capture, sb_capture_batch, sb_search, sb_read, sb_edit, sb_recap,
       sb_digest, sb_connections, sb_actions, sb_actions_done, sb_files,
       sb_forget, sb_anonymize, sb_tools

    Keep the file under 120 lines. No emojis. No fluff. Write it as if handing it to a senior developer on their first day.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_entities.py -q 2>&1 | tail -5 && test -f CLAUDE.md && echo "CLAUDE.md exists" && wc -l CLAUDE.md</automated>
  </verify>
  <done>CLAUDE.md exists at repo root; contains "SecondBrain", "sb-capture", "sb-gui", "sb-mcp-server", "uv run pytest"; under 120 lines</done>
</task>

</tasks>

<verification>
test -f /Users/tuomasleppanen/second-brain/CLAUDE.md && grep -c "sb-capture\|sb-gui\|SecondBrain\|uv run pytest" /Users/tuomasleppanen/second-brain/CLAUDE.md
Should return 4 (all 4 patterns found).
</verification>

<success_criteria>
- CLAUDE.md exists at repo root
- Contains all 8 required sections
- Under 120 lines
- All 4 grep patterns match: sb-capture, sb-gui, SecondBrain, uv run pytest
</success_criteria>

<output>
After completion, create `.planning/phases/27.1-smart-capture-and-proactive-brain/27.1-05-SUMMARY.md`
</output>
