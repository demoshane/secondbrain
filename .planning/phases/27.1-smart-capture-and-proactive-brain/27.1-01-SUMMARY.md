---
phase: 27.1-smart-capture-and-proactive-brain
plan: "01"
subsystem: testing
tags: [tdd, xfail, entities, dedup, batch-capture, mcp-tools]
dependency_graph:
  requires: []
  provides: [Wave 0 RED baseline for Phase 27.1]
  affects: [tests/test_entities.py, tests/test_capture.py, tests/test_mcp.py]
tech_stack:
  added: []
  patterns: [xfail(strict=False) for Wave 0 stubs, auto-promotion on Wave 2 ship]
key_files:
  created:
    - tests/test_entities.py
  modified:
    - tests/test_capture.py
    - tests/test_mcp.py
decisions:
  - "[27.1-01] xfail(strict=False) chosen over pytest.skip — stubs are collected and will auto-promote to PASS once Wave 2 ships engine/entities.py and enrichment"
  - "[27.1-01] test_mcp.py stubs use direct mcp_mod.* function calls matching existing file style — not HTTP client"
  - "[27.1-01] test_sb_capture_dedup_warning captures once then recaptures same content — tests the idempotency trigger path"
metrics:
  duration: "4 minutes"
  completed_date: "2026-03-17"
  tasks_completed: 2
  files_modified: 3
---

# Phase 27.1 Plan 01: Wave 0 Test Scaffolds Summary

**One-liner:** 11 xfail stubs across 3 test files establish RED baseline for entity extraction, capture enrichment, dedup, batch MCP capture, and MCP self-documentation.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Create tests/test_entities.py with extract_entities() stubs | 1d27ef7 | tests/test_entities.py (new, 53 lines) |
| 2 | Add entity + dedup stubs to test_capture.py and batch + tools stubs to test_mcp.py | b2faf07 | tests/test_capture.py, tests/test_mcp.py (+92 lines) |

## Verification Results

```
collected 34 items
tests/test_entities.py xxxx                   [ 11%]
tests/test_capture.py .........xx             [ 44%]
tests/test_mcp.py ..............xxxxx         [100%]
23 passed, 11 xfailed in 27.39s
```

All 34 tests collected, 23 existing tests pass, 11 new stubs xfail as expected.

## Test Stubs Created

### tests/test_entities.py (new — 4 stubs)
- `test_extract_entities_people` — identifies named people in body text
- `test_extract_entities_topics` — identifies #hashtag topics
- `test_extract_entities_places` — identifies place names
- `test_extract_entities_never_raises` — empty input returns empty dicts, no exception

### tests/test_capture.py (2 stubs appended)
- `test_capture_stores_entities` — captured note frontmatter contains `entities` key after enrichment
- `test_dedup_returns_similar` — `check_capture_dedup()` returns list when near-duplicate exists

### tests/test_mcp.py (5 stubs appended)
- `test_sb_capture_dedup_warning` — duplicate capture returns `status: duplicate_warning`
- `test_sb_capture_dedup_confirm` — `confirm_token` param bypasses dedup
- `test_sb_capture_batch` — batch saves multiple notes with succeeded/failed lists
- `test_sb_capture_batch_partial_failure` — one failed note in batch doesn't block others
- `test_sb_tools` — `sb_tools()` returns list of dicts with name/description/parameters

## Deviations from Plan

### Design Adjustments

**1. test_mcp.py stubs use direct function calls, not HTTP client**

The plan noted that the HTTP endpoint paths in the stubs were placeholders. The existing test_mcp.py uses direct `mcp_mod.*` calls (not a `client` fixture), so all 5 new stubs follow that pattern. This ensures they behave consistently with the rest of the file and avoids introducing an unused `client` fixture dependency.

## Self-Check: PASSED

- tests/test_entities.py: FOUND
- Commit 1d27ef7: FOUND
- Commit b2faf07: FOUND
