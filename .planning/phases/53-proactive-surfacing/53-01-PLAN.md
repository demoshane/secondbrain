---
phase: 53-proactive-surfacing
plan: 01
type: execute
wave: 1
depends_on: [50-02, 51-01]
files_modified:
  - engine/intelligence.py
  - tests/test_intelligence.py
autonomous: true
execution_strategy: direct

must_haves:
  truths:
    - "surface_relevant() returns contextually related notes with relevance scores"
    - "results exclude notes seen in current session (via audit_log dedup)"
    - "dormant notes flagged separately from fresh matches"
    - "scoring uses _relevance_decay from Phase 51"
  artifacts:
    - path: "engine/intelligence.py"
      provides: "surface_relevant() function"
      exports: ["surface_relevant"]
---

<objective>
Build the core surfacing engine that finds contextually relevant notes given free-text input.

Purpose: Foundation for the sb_surface MCP tool — reusable function that combines
semantic search, decay scoring, session dedup, and dormant detection.
Output: `surface_relevant()` in intelligence.py.
</objective>

<context>
@.planning/phases/53-proactive-surfacing/53-CONTEXT.md
@engine/intelligence.py
@engine/search.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement surface_relevant() engine function</name>
  <files>engine/intelligence.py</files>
  <read_first>engine/intelligence.py (find_dormant_related function), engine/search.py (search_semantic, _relevance_decay), engine/db.py (audit_log schema)</read_first>
  <behavior>
    - `surface_relevant(conn, context: str, max_results: int = 5, session_minutes: int = 30) -> list[dict]`
    - Step 1: Run semantic search against context (limit=max_results * 3 for headroom)
    - Step 2: Score each result with _relevance_decay (access + type-aware decay)
    - Step 3: Query audit_log for mcp_read/mcp_search entries in last session_minutes — exclude those note_paths
    - Step 4: Flag dormant notes (not updated in 30+ days, similarity > 0.5) with reason="dormant_but_relevant"
    - Step 5: Sort by combined score (semantic_similarity * decay_multiplier), take top max_results
    - Returns: [{"path", "title", "snippet", "relevance_score", "reason"}]
    - reason: "semantically_similar" | "dormant_but_relevant" | "frequently_accessed"
  </behavior>
  <action>
    1. Implement `surface_relevant()` following the 5-step pipeline above
    2. Extract reusable parts from existing `find_dormant_related()` (don't duplicate logic)
    3. Write tests: basic surfacing, session dedup works, dormant flagging, empty brain handles gracefully
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_intelligence.py -x -v -k "surface"</automated>
  </verify>
  <done>surface_relevant returns scored, deduped, categorized suggestions</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_intelligence.py -x -v -k "surface"` passes
- surface_relevant returns results with reason field
- Session dedup excludes recently-seen notes
</verification>

<success_criteria>
Core surfacing engine function with semantic search, decay scoring, and session dedup.
</success_criteria>
