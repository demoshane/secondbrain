---
phase: 53-proactive-surfacing
plan: 02
type: execute
wave: 2
depends_on: [53-01, 52-02]
files_modified:
  - engine/mcp_server.py
autonomous: true
execution_strategy: direct

must_haves:
  truths:
    - "sb_surface MCP tool exists and returns proactive suggestions"
    - "sb_search supports optional context_hint parameter"
    - "sb_surface uses graph traversal to enrich results when available"
    - "tool docstring guides Claude on when to call it"
---

<objective>
Expose proactive surfacing via MCP — new sb_surface tool and context_hint on sb_search.

Purpose: Claude can now ask "what's relevant to this conversation?" and get notes the
user didn't explicitly search for. The brain becomes a participant.
Output: `sb_surface` tool + `context_hint` parameter on `sb_search`.
</objective>

<context>
@.planning/phases/53-proactive-surfacing/53-CONTEXT.md
@engine/mcp_server.py
@engine/intelligence.py
@engine/links.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sb_surface MCP tool</name>
  <files>engine/mcp_server.py</files>
  <read_first>engine/mcp_server.py (tool registration pattern, sb_search for reference)</read_first>
  <action>
    1. Add `sb_surface` tool:
       - Params: `context: str`, `max_results: int = 5`, `include_graph: bool = True`
       - Calls `surface_relevant()` from intelligence.py
       - If `include_graph` and traverse_graph available: for top 2 results, follow 1-hop
         graph links and include any person/project notes not already in results
       - Returns: `{"suggestions": [...], "graph_additions": [...]}`
       - Log to audit_log with event_type="mcp_surface"
    2. Docstring (critical for Claude's judgment):
       "Proactively surface notes relevant to the current conversation context.
        Call this when: starting work on a topic, the user mentions a person/project
        you haven't looked up, or you sense historical context would help.
        Do NOT call this on every message — use judgment."
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_api.py -x -v -k "surface"</automated>
  </verify>
  <done>sb_surface tool registered with graph enrichment and clear usage guidance</done>
</task>

<task type="auto">
  <name>Task 2: Add context_hint to sb_search</name>
  <files>engine/mcp_server.py</files>
  <read_first>engine/mcp_server.py (sb_search implementation)</read_first>
  <action>
    1. Add optional `context_hint: str | None = None` parameter to `sb_search`
    2. When provided: after primary search results are assembled, call
       `surface_relevant(conn, context_hint, max_results=3)` and append as
       `"proactive_suggestions"` field in response
    3. Filter out any notes already in primary results (no duplicates)
    4. Fire-and-forget pattern — if surfacing fails, primary results still return
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_api.py -x -v -k "search"</automated>
  </verify>
  <done>sb_search returns bonus proactive_suggestions when context_hint provided</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_api.py -x -v -k "surface or search"` passes
- sb_surface returns suggestions with relevance scores and reasons
- sb_search with context_hint returns proactive_suggestions field
</verification>

<success_criteria>
Proactive surfacing available via dedicated tool and as ambient bonus on search.
</success_criteria>
