---
phase: 05-gdpr-and-maintenance
plan: "03"
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified: []
autonomous: false
requirements:
  - GDPR-01
  - GDPR-02
  - GDPR-04

must_haves:
  truths:
    - "sb-forget <person> deletes the person note and sole-reference meetings on the real filesystem"
    - "sb-search <person> returns zero results after sb-forget"
    - "sb-read on a PII note prompts for passphrase in an interactive terminal"
    - "sb-read with correct passphrase displays note content"
    - "sb-read without passphrase prints access denied and shows no content"
  artifacts:
    - path: "engine/forget.py"
      provides: "Verified end-to-end erasure"
    - path: "engine/read.py"
      provides: "Verified interactive passphrase gate"
  key_links:
    - from: "pyproject.toml sb-forget"
      to: "engine.forget:main"
      via: "uv tool install"
    - from: "pyproject.toml sb-read"
      to: "engine.read:main"
      via: "uv tool install"
---

<objective>
Manual verification checkpoint: confirm the GDPR features work end-to-end in a real DevContainer with real filesystem state.

Purpose: Automated tests use in-memory SQLite and tmp_path fixtures. This checkpoint confirms the CLI entry points wire correctly through get_connection() to the named Docker volume and real brain directory.
Output: All three Phase 5 success criteria confirmed by human; phase marked complete.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-gdpr-and-maintenance/05-01-SUMMARY.md
@.planning/phases/05-gdpr-and-maintenance/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite before checkpoint</name>
  <files></files>
  <action>
Run the full test suite and confirm all tests pass including the 10 new GDPR tests:

uv run --no-project --with pytest pytest tests/ -q

If any test fails, diagnose and fix before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>uv run --no-project --with pytest pytest tests/ -q 2>&1 | tail -5</automated>
  </verify>
  <done>All tests pass (0 failures, 0 errors). The GDPR test count (10) is visible in the summary line.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — sb-forget and sb-read end-to-end</name>
  <action>Human verifies the sb-forget erasure cascade and sb-read PII passphrase gate in a live DevContainer session using the steps below.</action>
  <what-built>
    - engine/forget.py: forget_person() erasure cascade (GDPR-01) with FTS5 rebuild (GDPR-02)
    - engine/read.py: read_note() with PII passphrase gate (GDPR-04)
    - CLI entry points: sb-forget and sb-read registered in pyproject.toml
  </what-built>
  <how-to-verify>
Open a DevContainer terminal and run these steps in order:

**Step 1 — Create a test person note:**
```
sb-capture --type people
# Title: Test Erasure Person
# Body: This is a test person for GDPR erasure.
# Tags: test
```
Note the slug: test-erasure-person

**Step 2 — Confirm person exists:**
```
sb-search "Test Erasure Person"
# Expect: at least 1 result
```

**Step 3 — Run forget:**
```
sb-forget "Test Erasure Person"
# Expect: "Deleted 1 file(s)" and "FTS5 index rebuilt. Erasure complete."
```

**Step 4 — Confirm erasure (GDPR-01 + GDPR-02):**
```
sb-search "Test Erasure Person"
# Expect: zero results
ls ~/SecondBrain/people/test-erasure-person.md
# Expect: No such file
```

**Step 5 — Create a PII note:**
```
sb-capture --type personal
# Title: PII Test Note
# Body: Secret personal information.
# content_sensitivity: pii
```
Note the path from output.

**Step 6 — Try to read PII note without passphrase (GDPR-04 denial):**
```
unset SB_PII_PASSPHRASE
sb-read ~/SecondBrain/personal/YYYY-MM-DD-pii-test-note.md
# Expect: "Access denied: passphrase required for PII note." — no content printed
```

**Step 7 — Read PII note with correct passphrase (GDPR-04 allow):**
```
export SB_PII_PASSPHRASE="mypassphrase"
export SB_PII_PASSPHRASE_INPUT="mypassphrase"
sb-read ~/SecondBrain/personal/YYYY-MM-DD-pii-test-note.md
# Expect: note content printed with frontmatter header
```

**Step 8 — Confirm non-PII note needs no passphrase:**
```
unset SB_PII_PASSPHRASE
unset SB_PII_PASSPHRASE_INPUT
sb-read ~/SecondBrain/people/some-existing-person.md
# Expect: note content printed without passphrase prompt
```
  </how-to-verify>
  <verify>
    <automated>MISSING — this is a manual-only verification step</automated>
  </verify>
  <done>Human types "approved" after all 8 steps pass.</done>
  <resume-signal>Type "approved" if all 8 steps passed, or describe any failures so they can be diagnosed.</resume-signal>
</task>

</tasks>

<verification>
All 3 Phase 5 success criteria met:
1. /sb-forget: person markdown absent, sole-reference meetings absent, sb-search returns zero results including FTS5 (GDPR-01 + GDPR-02)
2. FTS5 rebuild confirmed — audit log contains "forget" event row
3. PII note access denied without passphrase; content displayed with correct passphrase (GDPR-04)
</verification>

<success_criteria>
Human approves all 8 verification steps. Phase 5 is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/05-gdpr-and-maintenance/05-03-SUMMARY.md`
</output>
