---
phase: 27.3-ui-ux-overhaul
plan: "04"
subsystem: ui
tags: [react, codemirror, react-markdown, shadcn, radix-ui, vitest, tdd]

requires:
  - phase: 27.3-03
    provides: Sidebar, TabBar, contexts (NoteContext, SearchContext, UIContext, SSEContext), hooks (useNoteActions, useCollapseState)

provides:
  - NoteViewer component with react-markdown rendering, tag chips, attachment list, edit toggle
  - NoteEditor component with CodeMirror 6, dark theme, save/cancel
  - RightPanel with backlinks, people, recap+refresh, connections, digest, brain health
  - ActionsPage with filterable table, assignee picker, done toggle
  - NewNoteModal, DeleteNoteModal, FileUploadModal, BatchCaptureModal
  - Topbar updated with onNewNote/onBatchCapture props
  - App.tsx fully wired — all placeholders replaced with real components
  - Real NoteViewer Vitest test (h1 rendered, no raw #, strong, tag chips)
  - shadcn Dialog, Table, Checkbox UI primitives

affects:
  - 27.3-05 (Playwright GUI test suite — all components now addressable by data-testid)

tech-stack:
  added:
    - "@radix-ui/react-checkbox@^1"
    - "@radix-ui/react-label@^2"
  patterns:
    - react-markdown + remark-gfm for note body rendering
    - CodeMirror 6 via @uiw/react-codemirror with markdown extension
    - fetch in useEffect with .catch(() => fallback) for all RightPanel data
    - All modals gated by boolean state in App.tsx (showNewNote, showDelete, etc.)

key-files:
  created:
    - frontend/src/components/NoteViewer.tsx
    - frontend/src/components/NoteEditor.tsx
    - frontend/src/components/RightPanel.tsx
    - frontend/src/components/ActionsPage.tsx
    - frontend/src/components/NewNoteModal.tsx
    - frontend/src/components/DeleteNoteModal.tsx
    - frontend/src/components/FileUploadModal.tsx
    - frontend/src/components/BatchCaptureModal.tsx
    - frontend/src/components/ui/dialog.tsx
    - frontend/src/components/ui/table.tsx
    - frontend/src/components/ui/checkbox.tsx
  modified:
    - frontend/src/components/Topbar.tsx
    - frontend/src/App.tsx
    - frontend/src/test/NoteViewer.test.tsx

key-decisions:
  - "NoteViewer uses attachment type from types.ts (id, note_path, filename, file_path) — no url field; links use file_path"
  - "RightPanel catches all fetch errors silently — UI degrades gracefully when endpoints unavailable"
  - "BatchCaptureModal calls POST /capture/batch with no body — endpoint scans brain directory itself"
  - "Topbar extended with Props interface (onNewNote, onBatchCapture) — no breaking change to existing test"

patterns-established:
  - "Modal pattern: Dialog open prop + onClose callback; state owned by App.tsx"
  - "TDD: write failing test -> implement -> GREEN before committing"

requirements-completed:
  - UIUX-02
  - UIUX-04

duration: 12min
completed: 2026-03-18
---

# Phase 27.3 Plan 04: Content Components and App Wiring Summary

**Full-feature React UI: NoteViewer (react-markdown), NoteEditor (CodeMirror 6), RightPanel (/intelligence), ActionsPage, and 4 modals wired into App.tsx — complete feature parity with vanilla JS GUI**

## Performance

- **Duration:** 12 min
- **Started:** 2026-03-18T13:51:38Z
- **Completed:** 2026-03-18T13:56:00Z
- **Tasks:** 2
- **Files modified:** 13

## Accomplishments

- NoteViewer renders markdown as HTML via react-markdown + remark-gfm; tag chips; attachment list from GET /notes/<path>/attachments
- NoteEditor provides CodeMirror 6 in-place editing with dark theme toggle, save/cancel, isDirty tracking
- RightPanel fetches and renders all three /intelligence fields (recap, connections, digest) plus backlinks, people, brain health
- ActionsPage: filterable action items table with status/assignee selects, done checkbox, and PUT /actions/<id> integration
- All 4 modals complete: New Note, Delete, File Upload, Batch Capture
- App.tsx fully wired — zero placeholder components remain
- NoteViewer Vitest test real (was stub): asserts h1, no raw #, strong, tag chips present
- shadcn Dialog, Table, Checkbox primitives hand-crafted (npx shadcn interactive unavailable)

## Task Commits

1. **Task 1: NoteViewer, NoteEditor, UI primitives (TDD)** - `4453cc7` (feat)
2. **Task 2: RightPanel, ActionsPage, modals, App.tsx wiring** - `9d0215a` (feat)

**Plan metadata:** (docs commit below)

## Files Created/Modified

- `frontend/src/components/NoteViewer.tsx` — react-markdown body, tag chips, attachment list, edit toggle
- `frontend/src/components/NoteEditor.tsx` — CodeMirror 6, markdown ext, dark theme, save/cancel
- `frontend/src/components/RightPanel.tsx` — backlinks, people, recap+refresh, connections, digest, health
- `frontend/src/components/ActionsPage.tsx` — filterable table, assignee picker, done toggle
- `frontend/src/components/NewNoteModal.tsx` — POST /notes dialog
- `frontend/src/components/DeleteNoteModal.tsx` — DELETE /notes/<path> confirmation dialog
- `frontend/src/components/FileUploadModal.tsx` — POST /notes/<path>/files multipart upload
- `frontend/src/components/BatchCaptureModal.tsx` — POST /capture/batch trigger
- `frontend/src/components/ui/dialog.tsx` — shadcn Dialog primitive
- `frontend/src/components/ui/table.tsx` — shadcn Table primitive
- `frontend/src/components/ui/checkbox.tsx` — shadcn Checkbox + @radix-ui/react-checkbox
- `frontend/src/components/Topbar.tsx` — added onNewNote/onBatchCapture props
- `frontend/src/App.tsx` — fully wired, all placeholders replaced

## Decisions Made

- `Attachment.file_path` used as href (not a `url` field) — matches types.ts Attachment interface exactly
- RightPanel uses silent `.catch(() => {})` on all fetches; sections only render when data is non-empty
- Modal state (4 booleans) owned by App.tsx — single source of truth; modals are pure controlled components
- TDD pattern: NoteViewer test written first (RED), NoteViewer.tsx created second (GREEN) before committing

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Attachment interface mismatch — `url` field does not exist in types.ts**
- **Found during:** Task 1 (NoteViewer)
- **Issue:** Plan code used `a.url` for attachment href but types.ts Attachment has `file_path` not `url`
- **Fix:** Changed `href={a.url}` to `href={a.file_path}` in the attachment list render
- **Files modified:** frontend/src/components/NoteViewer.tsx
- **Verification:** TypeScript build exits 0
- **Committed in:** 4453cc7 (Task 1 commit)

**2. [Rule 3 - Blocking] @radix-ui/react-checkbox not in package.json**
- **Found during:** Task 1 (creating Checkbox UI primitive)
- **Issue:** Checkbox.tsx requires @radix-ui/react-checkbox which was not installed
- **Fix:** `npm install @radix-ui/react-checkbox @radix-ui/react-label`
- **Files modified:** frontend/package.json, frontend/package-lock.json
- **Verification:** Build exits 0
- **Committed in:** 4453cc7 (Task 1 commit)

---

**Total deviations:** 2 auto-fixed (2 blocking)
**Impact on plan:** Both fixes required for TypeScript correctness. No scope creep.

## Issues Encountered

None beyond the two auto-fixed deviations above.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- All content components implemented; feature parity with vanilla JS GUI achieved
- All components have `data-testid` attributes — ready for Playwright GUI test suite (plan 27.3-05)
- Build green; 5 Vitest tests pass

---
*Phase: 27.3-ui-ux-overhaul*
*Completed: 2026-03-18*
