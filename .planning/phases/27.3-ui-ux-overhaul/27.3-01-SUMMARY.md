---
phase: 27.3-ui-ux-overhaul
plan: "01"
subsystem: frontend
tags: [react, vite, shadcn, tailwind, vitest, typescript, scaffold]
dependency_graph:
  requires: []
  provides:
    - frontend/vite.config.ts (build pipeline: outDir=../engine/gui/static)
    - frontend/src/types.ts (Note, ActionItem, Attachment, CollapsePrefs interfaces)
    - frontend/src/lib/utils.ts (getAPI() + cn() helpers)
    - frontend/src/test/*.test.tsx (4 Vitest stubs)
  affects:
    - engine/gui/static/ (cleared old vanilla JS; replaced with React bundle)
tech_stack:
  added:
    - React 19 + TypeScript (react-ts vite template)
    - Vite 8 (build tool, dev server with proxy)
    - shadcn/ui (component library, default/slate style, CSS variables)
    - Tailwind CSS v3 (utility CSS)
    - Vitest 4 + @testing-library/react (unit test framework)
    - react-markdown + remark-gfm (markdown rendering)
    - CodeMirror 6 (@uiw/react-codemirror, lang-markdown, theme-one-dark)
    - clsx + tailwind-merge (className helpers)
    - Radix UI primitives (dialog, select, separator, scroll-area, slot)
  patterns:
    - Vite proxy to Flask at 127.0.0.1:37491 for dev server
    - CSS variable design tokens for light/dark theming
    - Lazy window.API_BASE read in getAPI() (Flask injects at request time)
    - emptyOutDir clears old vanilla JS artifacts on each build
key_files:
  created:
    - frontend/ (entire directory — new Vite project)
    - frontend/vite.config.ts
    - frontend/components.json
    - frontend/tailwind.config.js
    - frontend/postcss.config.js
    - frontend/src/types.ts
    - frontend/src/lib/utils.ts
    - frontend/src/index.css (Tailwind directives + shadcn CSS vars)
    - frontend/src/test/setup.ts
    - frontend/src/test/NoteViewer.test.tsx
    - frontend/src/test/SSEContext.test.tsx
    - frontend/src/test/TabBar.test.tsx
    - frontend/src/test/ThemeProvider.test.tsx
  modified:
    - frontend/tsconfig.app.json (added paths @/*)
    - frontend/src/App.tsx (placeholder with data-testid=app-root)
    - frontend/src/main.tsx (React 18 createRoot pattern)
    - frontend/package.json (added test script)
    - engine/gui/static/ (rebuilt; old app.js/style.css/vendor/ deleted)
    - .secrets.baseline (updated for build artifact false positive)
decisions:
  - "[27.3-01] vitest/config defineConfig used instead of vite's — required for TypeScript to accept test: block in vite.config.ts"
  - "[27.3-01] shadcn init run manually (components.json + Radix installs) — CLI is interactive-only and cannot run non-interactively"
  - "[27.3-01] Playwright test_gui.py failures are expected — those tests test the old vanilla JS UI; they will be rewritten in plan 27.3-05"
metrics:
  duration: "25 min"
  completed_date: "2026-03-18"
  tasks_completed: 2
  files_changed: 20
---

# Phase 27.3 Plan 01: Vite + React + shadcn/ui Scaffold Summary

Scaffolded the React 19 + Vite 8 + shadcn/ui frontend project with full build pipeline, TypeScript type contracts, and Vitest test stubs for all four UIUX test requirements.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Scaffold Vite + React + shadcn/ui project | 8b9c088 | frontend/ (20 files), engine/gui/static/ rebuilt |
| 2 | Types, utils, and Vitest test stubs | 1c37576 | src/types.ts, src/lib/utils.ts, 4 test stubs, package.json |

## Verification Results

- `npm run build` exits 0; `engine/gui/static/index.html` present
- `npm run test -- --run`: 4/4 stubs pass (NoteViewer, SSEContext, TabBar, ThemeProvider)
- Backend pytest (non-GUI): all passing, backend untouched
- Old `vendor/`, `app.js`, `style.css` removed by `emptyOutDir: true`

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] vitest/config import required for TypeScript compatibility**
- **Found during:** Task 1 build
- **Issue:** `tsc -b` failed with `TS2769: test does not exist in type UserConfigExport` when using `defineConfig` from `vite`
- **Fix:** Changed import to `defineConfig` from `vitest/config` — this re-exports Vite's defineConfig with the test block type merged in
- **Files modified:** `frontend/vite.config.ts`
- **Commit:** 8b9c088

**2. [Rule 3 - Blocking] shadcn init is interactive-only**
- **Found during:** Task 1
- **Issue:** `npx shadcn@latest init` prompts for library choice interactively; cannot run `--yes` silently
- **Fix:** Created `components.json` manually with correct shadcn schema, then installed Radix UI primitives and other deps directly via `npm install`
- **Files modified:** `frontend/components.json`
- **Commit:** 8b9c088

**3. [Rule 3 - Blocking] Missing `test` script in package.json**
- **Found during:** Task 2 verification
- **Issue:** Vite scaffold does not include a `test` script by default; `npm run test` failed
- **Fix:** Added `"test": "vitest"` to scripts in package.json
- **Files modified:** `frontend/package.json`
- **Commit:** 1c37576

**4. [Rule 2 - Expected consequence] Playwright GUI tests fail after frontend replacement**
- **Found during:** Full pytest run
- **Issue:** `tests/test_gui.py` (8 Playwright tests) fail because they test the old vanilla JS UI which was removed by `emptyOutDir`
- **Action:** No fix — these failures are an expected, planned consequence of the migration. Documented as deferred to plan 27.3-05 (GUI test rewrite)
- **Pre-existing:** These tests were written against the old app.js frontend

## Self-Check: PASSED

- FOUND: frontend/vite.config.ts
- FOUND: frontend/src/types.ts
- FOUND: frontend/src/lib/utils.ts
- FOUND: engine/gui/static/index.html
- FOUND: frontend/src/test/NoteViewer.test.tsx
- FOUND commit 8b9c088: feat(27.3-01): scaffold Vite + React + shadcn/ui frontend project
- FOUND commit 1c37576: feat(27.3-01): add types, utils, and Vitest test stubs
