---
phase: 27.3-ui-ux-overhaul
plan: 05
type: execute
wave: 5
depends_on:
  - 27.3-04
files_modified:
  - tests/test_gui.py
  - engine/api.py
autonomous: false
requirements:
  - UIUX-01
  - UIUX-02
  - UIUX-03
  - UIUX-04

must_haves:
  truths:
    - "Flask serves the React build at /ui — pywebview loads it correctly"
    - "All existing backend pytest tests pass"
    - "Key Playwright GUI tests pass with updated selectors"
    - "window.API_BASE injection still works in the React build"
  artifacts:
    - path: "engine/gui/static/index.html"
      provides: "React app entry point; contains </head> for Flask API_BASE injection"
    - path: "tests/test_gui.py"
      provides: "Updated Playwright selectors using data-testid attributes from React components"
  key_links:
    - from: "engine/api.py gui_shell()"
      to: "engine/gui/static/index.html"
      via: "string-replace </head> with window.API_BASE script tag"
      pattern: "API_BASE"
    - from: "tests/test_gui.py"
      to: "React component data-testid attributes"
      via: "page.locator('[data-testid=...]')"
      pattern: "data-testid"
---

<objective>
Update Playwright test selectors to match React component data-testid attributes, verify the full build pipeline and Flask serving works end-to-end, then do a human sign-off.

Purpose: Confirms UIUX-04 (no regressions) and validates the complete migration works in the pywebview context.
Output: Updated test_gui.py with React-compatible selectors; human-verified working GUI.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27.3-ui-ux-overhaul/27.3-RESEARCH.md
@.planning/phases/27.3-ui-ux-overhaul/27.3-04-SUMMARY.md

<interfaces>
<!-- data-testid attributes added in plans 03-04 -->
<!-- data-testid="topbar" — Topbar component -->
<!-- data-testid="search-input" — Input in Topbar -->
<!-- data-testid="tab-bar" — TabBar container -->
<!-- data-testid="tab-notes" — Notes tab button -->
<!-- data-testid="tab-actions" — Action Items tab button -->
<!-- data-testid="sidebar" — Sidebar container -->
<!-- data-testid="note-item" + data-path="{path}" — sidebar note buttons -->
<!-- data-testid="note-viewer" — NoteViewer container -->
<!-- data-testid="note-title" — h1 in NoteViewer -->
<!-- data-testid="note-body" — markdown body container -->
<!-- data-testid="tag-chips" — tag chips container -->
<!-- data-testid="tag-{tagname}" — individual tag Badge -->
<!-- data-testid="edit-btn" — Edit button in NoteViewer -->
<!-- data-testid="note-editor" — NoteEditor container -->
<!-- data-testid="editor-save" — Save button in NoteEditor -->
<!-- data-testid="editor-cancel" — Cancel button in NoteEditor -->
<!-- data-testid="delete-btn" — Delete button in toolbar -->
<!-- data-testid="delete-note-modal" — DeleteNoteModal Dialog -->
<!-- data-testid="delete-confirm" — Confirm delete button -->
<!-- data-testid="delete-cancel" — Cancel delete button -->
<!-- data-testid="upload-btn" — Upload button in toolbar -->
<!-- data-testid="file-upload-modal" — FileUploadModal Dialog -->
<!-- data-testid="new-note-btn" — New Note button in Topbar -->
<!-- data-testid="new-note-modal" — NewNoteModal Dialog -->
<!-- data-testid="sse-status-dot" — SSE status indicator -->
<!-- data-testid="tag-filter-banner" — tag filter banner in Sidebar -->
<!-- data-testid="actions-page" — ActionsPage container -->
<!-- data-testid="right-panel" — RightPanel container -->
<!-- data-testid="recap-text" — recap paragraph in RightPanel -->
<!-- data-testid="connections-list" — connections ul in RightPanel -->
<!-- data-testid="digest-text" — digest paragraph in RightPanel -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Playwright selectors in test_gui.py + run backend suite</name>
  <files>tests/test_gui.py</files>
  <action>
Read tests/test_gui.py in full, then update ALL selectors that referenced old vanilla JS DOM structure.

Old selector patterns to replace:
- `#note-list li[data-path]` → `[data-testid="note-item"][data-path]`
- `#note-list` → `[data-testid="sidebar"]`
- `#viewer` → `[data-testid="note-viewer"]`
- `#viewer h1` → `[data-testid="note-title"]`
- `#editor` or `.CodeMirror` → `[data-testid="note-editor"]`
- `#sidebar-loading` → Remove (React loads inline, no dedicated loading element)
- `#search-input` → `[data-testid="search-input"]`
- `.note-item[data-path` → `[data-testid="note-item"][data-path`
- `#delete-btn` → `[data-testid="delete-btn"]`
- `#confirm-delete` or `#delete-confirm` → `[data-testid="delete-confirm"]`
- `#tag-filter` or `.tag-chip` → `[data-testid^="tag-"]`
- `.folder-section` → `[data-testid^="folder-"]`
- `.sse-dot` or `.status-dot` → `[data-testid="sse-status-dot"]`

After updating selectors, also ensure:
1. Tests that checked `#viewer` scrollability now use `[data-testid="note-body"]` — the scrollable content div
2. Path traversal test still uses direct fetch (not DOM selector), so it needs no selector update
3. Tests that check markdown rendering now use `[data-testid="note-body"] h1` etc.

Run verify command to confirm backend tests pass and test_gui.py has no syntax/import errors.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run python -m py_compile tests/test_gui.py && echo "SYNTAX OK" && uv run pytest tests/ -q --ignore=tests/test_gui.py 2>&1 | tail -10</automated>
  </verify>
  <done>All non-GUI pytest tests pass; test_gui.py compiles without syntax errors; selectors updated to use data-testid attributes throughout</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete React + Vite + shadcn/ui + Tailwind CSS migration of the Second Brain GUI:
    - frontend/ Vite project with npm run build → engine/gui/static/
    - Dark mode auto-detects prefers-color-scheme
    - All features reimplemented: sidebar, note viewer, CodeMirror editor, tab bar, actions page, file upload, batch capture, SSE live refresh, intelligence panel (recap + connections + digest)
    - Playwright selectors updated to data-testid attributes
  </what-built>
  <how-to-verify>
    1. Run the build: `cd /Users/tuomasleppanen/second-brain/frontend && npm run build`
       Expected: exits 0; engine/gui/static/index.html present; no old app.js/style.css/vendor/ present

    2. Run backend tests: `cd /Users/tuomasleppanen/second-brain && uv run pytest tests/ -q`
       Expected: all tests pass

    3. Start the GUI: `cd /Users/tuomasleppanen/second-brain && uv run sb-gui`
       Then visit http://localhost:5001/ui in a browser (or pywebview auto-opens)
       Expected: React app loads; three-panel layout visible

    4. Verify core features:
       a. Sidebar shows notes grouped by folder/type with collapse toggles
       b. Click a note — viewer shows rendered markdown (headings, bold, lists as HTML, not raw text)
       c. Click Edit — CodeMirror editor opens; make a change, Save — viewer updates
       d. Check SSE status dot (green = connected)
       e. Switch to Action Items tab — actions page renders with filters
       f. Toggle system dark mode — app switches automatically without page reload
       g. Right panel shows Recap, Connections, and Digest sections (all three from /intelligence)

    5. Verify window.API_BASE injection:
       Open browser devtools → Console → type `window.API_BASE`
       Expected: shows the Flask server URL (not undefined)
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` exits 0; engine/gui/static/index.html has `</head>` for Flask injection
- `uv run pytest tests/ -q` — full backend suite green
- `uv run python -m py_compile tests/test_gui.py` exits 0
- React app loads in browser via /ui with three-panel layout
- Dark mode responds to prefers-color-scheme
- window.API_BASE defined in browser console
</verification>

<success_criteria>
1. npm run build succeeds; engine/gui/static/ contains React bundle (not old app.js/style.css)
2. All backend pytest tests pass (UIUX-04 confirmed)
3. Playwright selectors updated to data-testid attributes; test_gui.py syntax error-free
4. Human verified: React GUI loads, notes render as HTML, dark mode works, SSE connected, right panel shows recap + connections + digest
5. window.API_BASE injection confirmed working
</success_criteria>

<output>
After completion, create `.planning/phases/27.3-ui-ux-overhaul/27.3-05-SUMMARY.md`
</output>
