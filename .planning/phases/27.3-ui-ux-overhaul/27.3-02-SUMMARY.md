---
phase: 27.3-ui-ux-overhaul
plan: "02"
subsystem: frontend
tags: [react, contexts, hooks, vitest, typescript, state-management, sse, theme]
dependency_graph:
  requires:
    - frontend/src/types.ts (Note, ActionItem, Attachment, CollapsePrefs — from plan 01)
    - frontend/src/lib/utils.ts (getAPI(), cn() — from plan 01)
  provides:
    - frontend/src/contexts/NoteContext.tsx (notes[], currentPath, currentNote, loadNotes, openNote, isDirty)
    - frontend/src/contexts/SearchContext.tsx (query, mode, results, search, clearSearch)
    - frontend/src/contexts/UIContext.tsx (currentView notes|actions, setCurrentView)
    - frontend/src/contexts/SSEContext.tsx (EventSource lifecycle, connected boolean)
    - frontend/src/hooks/useCollapseState.ts (GET/PUT /ui/prefs wrapper)
    - frontend/src/hooks/useNoteActions.ts (saveNote, deleteNote, createNote)
    - frontend/src/main.tsx (ThemeProvider + all 4 providers wrapping App)
  affects:
    - frontend/src/test/SSEContext.test.tsx (replaced stub with real test)
    - frontend/src/test/ThemeProvider.test.tsx (replaced stub with real test)
tech_stack:
  added: []
  patterns:
    - React Context + useCallback pattern for async data fetching
    - ThemeProvider adds 'dark' class to document.documentElement via matchMedia
    - SSEContext connects EventSource once (connect once; native reconnect)
    - useCollapseState: pessimistic local update + fire-and-forget PUT
key_files:
  created:
    - frontend/src/contexts/NoteContext.tsx
    - frontend/src/contexts/SearchContext.tsx
    - frontend/src/contexts/UIContext.tsx
    - frontend/src/contexts/SSEContext.tsx
    - frontend/src/hooks/useCollapseState.ts
    - frontend/src/hooks/useNoteActions.ts
  modified:
    - frontend/src/main.tsx (provider composition + ThemeProvider)
    - frontend/src/test/ThemeProvider.test.tsx (real test replacing stub)
    - frontend/src/test/SSEContext.test.tsx (real test replacing stub)
decisions:
  - "[27.3-02] SearchContext.search() interface uses 'hybrid'|'bm25'|'semantic' union (not string) to match implementation — avoids TS2322 assignability error"
  - "[27.3-02] vi.fn(() => mockSrc) cannot be used as constructor — EventSource mock uses regular function() instead of arrow fn"
  - "[27.3-02] SSEProvider useEffect dependency array is empty — connects once; EventSource handles native reconnect; loadNotes/openNote/currentPath closures are stable enough for SSE lifecycle"
metrics:
  duration: "9 min"
  completed_date: "2026-03-18"
  tasks_completed: 2
  files_changed: 9
---

# Phase 27.3 Plan 02: React Contexts and Hooks Summary

Implemented the full React state layer — 4 context providers, 2 hooks, updated main.tsx provider composition with ThemeProvider, and replaced both Vitest test stubs with real passing tests.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | React Contexts (NoteContext, SearchContext, UIContext, SSEContext) + main.tsx | 3228ecc | 5 files created/modified |
| 2 | Hooks + real Vitest tests for SSEContext and ThemeProvider | c016858, 210751d | 4 files created/modified |

## Verification Results

- `npm run build` exits 0; clean — no TS errors or warnings
- `npm run test -- --run`: 4/4 tests pass (NoteViewer stub, SSEContext real, TabBar stub, ThemeProvider real)
- `uv run pytest tests/ --ignore=tests/test_gui.py`: 330 passed, 0 failed

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] SearchContext interface mode parameter typed too broadly**
- **Found during:** Task 1 build verification
- **Issue:** Interface declared `mode?: string` but implementation inferred `'hybrid'|'bm25'|'semantic'` — TS2322 assignability error
- **Fix:** Changed interface to `mode?: 'hybrid' | 'bm25' | 'semantic'` to match implementation
- **Files modified:** `frontend/src/contexts/SearchContext.tsx`
- **Commit:** 3228ecc

**2. [Rule 1 - Bug] vi.fn arrow function non-constructible as EventSource mock**
- **Found during:** Task 2 test run
- **Issue:** `vi.fn(() => mockSrc)` cannot be called with `new` — arrow functions are not constructors; Vitest 4 enforces this
- **Fix:** Changed to `function() { return mockSrc }` — regular function can be called with `new`
- **Files modified:** `frontend/src/test/SSEContext.test.tsx`
- **Commit:** c016858

**3. [Rule 1 - Bug] Unused `beforeEach` import causing TS6133 build warning**
- **Found during:** Final build verification
- **Issue:** Plan template imported `beforeEach` but the SSEContext test has no `beforeEach` usage
- **Fix:** Removed unused import; build now clean
- **Files modified:** `frontend/src/test/SSEContext.test.tsx`
- **Commit:** 210751d

## Self-Check: PASSED

- FOUND: frontend/src/contexts/NoteContext.tsx
- FOUND: frontend/src/contexts/SearchContext.tsx
- FOUND: frontend/src/contexts/UIContext.tsx
- FOUND: frontend/src/contexts/SSEContext.tsx
- FOUND: frontend/src/hooks/useCollapseState.ts
- FOUND: frontend/src/hooks/useNoteActions.ts
- FOUND: frontend/src/main.tsx (updated with all providers + ThemeProvider)
- FOUND commit 3228ecc: feat(27.3-02): implement NoteContext, SearchContext, UIContext, SSEContext + provider composition in main.tsx
- FOUND commit c016858: feat(27.3-02): add hooks + replace test stubs with real Vitest tests
- FOUND commit 210751d: fix(27.3-02): remove unused beforeEach import from SSEContext test
