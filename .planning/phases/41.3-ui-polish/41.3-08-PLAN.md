---
plan: 41.3-08
phase: "41.3"
name: NoteViewer — [[wiki-links]] clickable navigation
wave: 2
depends_on: []
files_modified:
  - frontend/src/components/NoteViewer.tsx
autonomous: true
execution_strategy: direct
requirements: []
must_haves:
  truths:
    - "[[Note Title]] syntax in note body renders as a clickable chip/link"
    - "Clicking a wiki-link navigates to the linked note (setCurrentView('notes') + openNote)"
    - "If no note matches the title, the link renders as dimmed/non-clickable (not an error)"
    - "Regular markdown rendering is unaffected"
    - "Works in both view and preview modes"
  artifacts:
    - "frontend/src/components/NoteViewer.tsx — wiki-link renderer"
---

<objective>
Render [[wiki-link]] syntax as clickable navigation in NoteViewer. When the user writes
[[Meeting with Acme]] in a note, it should appear as a tappable link that jumps directly
to that note — same as how Obsidian/Roam handle it. Notes without a matching title show
as dimmed text rather than breaking the view.
</objective>

<context>
@frontend/src/components/NoteViewer.tsx
@frontend/src/App.tsx (for openNote / setCurrentView signatures)
</context>

<tasks>

<task type="auto" tdd="false">
<behavior>Parse and render [[wiki-links]] in note body</behavior>
<action>
In NoteViewer.tsx, before passing body content to ReactMarkdown, pre-process the text:
- Replace [[Title]] patterns with a custom placeholder or use a ReactMarkdown custom renderer.
- Preferred approach: use a ReactMarkdown plugin or custom component for `[[...]]` tokens.
  Simple alternative: pre-process body string to replace [[Title]] with a markdown link
  [Title](wiki:Title) and add a custom link renderer that intercepts wiki: scheme.
- On click: call openNote(matchedNote) where matchedNote is found by comparing note.title
  (case-insensitive) against the loaded notes list. If no match, render as <span> with
  opacity-50 (dimmed) instead of a link.
- Keep the implementation self-contained in NoteViewer — no new files.
</action>
<verify><automated>echo "Manual: create a note with [[wiki-link]], open it in viewer, verify link appears and navigates"</automated></verify>
<done>[[wiki-links]] render as clickable links; clicking navigates to the target note; unmatched titles appear dimmed.</done>
</task>

</tasks>
