---
phase: 41.3-ui-polish
verified: 2026-03-28T16:40:00Z
status: passed
score: 8/8 plan goals verified
re_verification: false
gaps: []
human_verification:
  - test: "Load extension popup in Chrome, click 'Summarise page', verify spinner appears then summary text, then 'Add to brain' saves a note"
    expected: "Spinner during LLM call, summary text rendered in popup, note saved with type=note and tags=['summary']"
    why_human: "Requires Chrome extension runtime and a live sb-api with a real LLM adapter"
  - test: "Open a note with [[wiki-link]] syntax in NoteViewer, verify it renders as a clickable chip and navigating to the target note works"
    expected: "Chip styled in primary/10 color; clicking it switches to Notes tab and opens the referenced note"
    why_human: "Requires live browser with notes containing wiki-link syntax"
  - test: "In sidebar, confirm notes appear under flat type headings (Note, Meeting, Person…) with no outer folder level"
    expected: "Single CollapsibleSection per type, no nested sections"
    why_human: "Visual layout requires browser verification after make dev"
  - test: "Upload a file to a note on LinksPage, PeoplePage, MeetingsPage, ProjectsPage — confirm it appears in Attachments section immediately without navigation"
    expected: "AttachmentsSection refreshes after upload, new file visible instantly"
    why_human: "Requires live upload flow and DOM inspection"
---

# Phase 41.3: UI Polish Verification Report

**Phase Goal:** UI polish pass covering sidebar type grouping, Chrome extension dark retheme, action items interactivity, inline body editing on detail pages, connections panel cleanup (tags + people), attachments visibility with live refresh, Chrome extension page summarisation, and wiki-link navigation in NoteViewer.

**Verified:** 2026-03-28T16:40:00Z
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Sidebar groups notes by type (flat), no outer folder level | VERIFIED | `groupByType()` in Sidebar.tsx returns `Map<string, Note[]>`; single-level render loop with `sectionId=sidebar-type-{type}`; `FOLDER_ORDER` and `groupByFolderThenType` absent |
| 2 | Chrome extension popup is dark-themed matching app tokens | VERIFIED | popup.css: `background: #0f1117` on body, `#191f2d` on inputs, `#5b6cf7` on save btn, `#d4dae8` foreground, `#2a3245` borders; 26 token matches confirmed |
| 3 | popup.html has brain identity marker | VERIFIED | `<h2>🧠 Save to Brain</h2>` at line 13 |
| 4 | ActionItemRow has inline due-date editor with onSetDue prop | VERIFIED | `onSetDue?: (id, date) => void` in props; `editingDue` state; `<input type="date">`; "Set deadline" ghost button; confirm/cancel handlers |
| 5 | PeoplePage and ProjectsPage have Add Action form + handleSetDue | VERIFIED | Both files contain `handleSetDue`, `showAddAction`, `handleAddAction`, `onSetDue={handleSetDue}` on ActionItemRow, `+ Add action` button |
| 6 | Inline body editing on all four detail pages (Links, People, Meetings, Projects) | VERIFIED | All four contain `editingBody` state, `PUT /notes/{encodedPath}` with `{body: editingBody}`, textarea + Save/Cancel, Cmd+Enter shortcut |
| 7 | RightPanel shows Tags section (always visible) and People section (real notes only) | VERIFIED | api.py `note_meta()` returns `tags` field and omits unresolved plain-text people; RightPanel has `addTag`/`removeTag`/`addPerson`/`removePerson`; Tags CollapsibleSection renders unconditionally |
| 8 | AttachmentsSection component exists and is wired to all five detail panels | VERIFIED | `attachments-section.tsx` created; `NoteViewer`, `LinksPage`, `PeoplePage`, `MeetingsPage`, `ProjectsPage` all import and render it with `refreshTick` + `FileUploadModal.onUploaded` callback |
| 9 | Chrome extension "Summarise page" button sends content to backend, shows summary | VERIFIED | `POST /summarise-url` endpoint in api.py; `handleSummarise()` in popup.js; summary-panel in popup.html; 6 TDD tests in test_summarise_url.py |
| 10 | [[wiki-links]] render as clickable chips in NoteViewer; unmatched links dimmed | VERIFIED | `preprocessWikiLinks()` pre-processes `[[Title]]` to `wiki:` scheme; custom ReactMarkdown `a` renderer intercepts; matched = primary-colored button, unmatched = opacity-50 span |

**Score:** 10/10 observable truths verified (note: mapped to 8 plan goals; some plans cover multiple truths)

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `frontend/src/components/Sidebar.tsx` | groupByType, single-level loop | VERIFIED | `groupByType` present; no `groupByFolderThenType`; no `FOLDER_ORDER`; single CollapsibleSection loop |
| `chrome-extension/popup.css` | Dark Visily tokens | VERIFIED | All 5 key tokens present (26 matches); SVG chevron for select |
| `chrome-extension/popup.html` | Brain identity + summarise section | VERIFIED | `🧠` in header; `#summarise-section` and `#summary-panel` present |
| `chrome-extension/popup.js` | Summarise flow | VERIFIED | `handleSummarise`, `handleAddTobrainFromSummary`, handlers wired |
| `frontend/src/components/ui/action-item-row.tsx` | onSetDue + editingDue | VERIFIED | Props, state, date input, Set deadline button all present |
| `frontend/src/components/PeoplePage.tsx` | handleSetDue + Add Action | VERIFIED | All required patterns present |
| `frontend/src/components/ProjectsPage.tsx` | handleSetDue + Add Action | VERIFIED | All required patterns present |
| `frontend/src/components/LinksPage.tsx` | editingBody inline editor | VERIFIED | editingBody state, PUT call, textarea render |
| `frontend/src/components/MeetingsPage.tsx` | editingBody inline editor | VERIFIED | editingBody state, PUT call, textarea render |
| `engine/api.py` | tags in note_meta + unresolved people filtered + POST /summarise-url | VERIFIED | `note_meta()` SELECT includes `tags`; `else` branch for unresolved strings removed; `/summarise-url` endpoint at line 2193 |
| `frontend/src/components/RightPanel.tsx` | Tags + People sections with add/remove | VERIFIED | `tags` state, `addTag`/`removeTag`/`addPerson`/`removePerson`, typeahead search, always-visible sections |
| `frontend/src/components/ui/attachments-section.tsx` | Reusable AttachmentsSection | VERIFIED | Created; `notePath`, `refreshTick`, `onUploadClick` props; fetches on refreshTick change |
| `frontend/src/components/FileUploadModal.tsx` | onUploaded callback + notePath override | VERIFIED | `onUploaded?: () => void` and `notePath` prop present; callback fires before onClose |
| `frontend/src/components/NoteViewer.tsx` | Wiki-links + AttachmentsSection with refreshTick | VERIFIED | `preprocessWikiLinks`, custom `a` renderer, `refreshTick` state, AttachmentsSection wired |
| `tests/test_summarise_url.py` | 6 TDD tests for /summarise-url | VERIFIED | File exists; 6 test methods covering 400 guards, happy path, truncation |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| Sidebar.tsx | NoteContext notes | `groupByType(filtered)` | WIRED | grouped var passed to render loop |
| ActionItemRow | `PUT /actions/{id}` | `onSetDue` prop → parent handler | WIRED | PeoplePage/ProjectsPage implement `handleSetDue` which calls the endpoint |
| PeoplePage/ProjectsPage Add Action | `POST /actions` | `handleAddAction` | WIRED | POSTs `{text, note_path: selectedPath}` |
| LinksPage/PeoplePage/MeetingsPage/ProjectsPage | `PUT /notes/{path}` | `handleSaveBody` | WIRED | All four fire PUT with `{body: editingBody}` |
| RightPanel tags/people | `PUT /notes/{path}` | `addTag`/`removeTag`/`addPerson`/`removePerson` | WIRED | Updates then calls `reloadMeta` |
| AttachmentsSection | `GET /notes/attachments?path=` | `useEffect([notePath, refreshTick])` | WIRED | Fetches on mount and tick change |
| FileUploadModal | AttachmentsSection refresh | `onUploaded` callback → `setRefreshTick(t+1)` | WIRED | Callback fires post-upload in all 5 pages |
| popup.js handleSummarise | `POST /summarise-url` | fetch | WIRED | 30s timeout; error handling; result shown in summary-panel |
| NoteViewer wiki-links | openNote + setCurrentView | `preprocessWikiLinks` → custom `a` renderer → `titleToPath()` | WIRED | Click calls `setCurrentView('notes')` and `openNote(targetPath)` |
| api.py note_meta | notes.tags column | `SELECT people, body, tags FROM notes` | WIRED | tags returned in response; no else-branch for unresolved names |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| Sidebar.tsx | `grouped` | `groupByType(filtered)` ← `notes` from NoteContext ← `/notes` API | Yes — populated from real DB query | FLOWING |
| RightPanel.tsx | `tags`, `people` | `reloadMeta()` → `GET /notes/{path}/meta` → `note_meta()` in api.py | Yes — reads `notes.tags` column from SQLite | FLOWING |
| AttachmentsSection | `attachments` | `GET /notes/attachments?path=` | Yes — reads `attachments` table | FLOWING |
| NoteViewer wiki-links | `titleToPath()` | `notes` array from NoteContext | Yes — iterates all loaded notes | FLOWING |
| api.py /summarise-url | `summary` | `_router.get_adapter('public').generate()` | Yes — calls real LLM adapter; mocked in tests | FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Check | Result | Status |
|----------|-------|--------|--------|
| Sidebar: no `groupByFolderThenType` symbol | `grep -c groupByFolderThenType Sidebar.tsx` | 0 matches | PASS |
| Sidebar: sectionId pattern | `grep sectionId Sidebar.tsx` | `sidebar-type-${type}` | PASS |
| popup.css dark token count | `grep -c "#0f1117\|#5b6cf7\|..."` | 26 matches | PASS |
| ActionItemRow: onSetDue prop | grep in file | present at line 12 | PASS |
| PeoplePage: all 5 required patterns | grep in file | all at expected lines | PASS |
| api.py: unresolved people omitted | grep comment "Unresolved plain-text" | line 1419 | PASS |
| api.py: tags in note_meta response | grep `note_tags` in return | line 1422 | PASS |
| api.py: /summarise-url endpoint | grep `@app.post("/summarise-url")` | line 2193 | PASS |
| NoteViewer: preprocessWikiLinks | grep | line 45 | PASS |
| AttachmentsSection: refreshTick | grep in all 5 pages | present in all | PASS |
| test_summarise_url.py: 6 tests | file line count | 107 lines, 6 test methods | PASS |

---

### Requirements Coverage

No formal requirement IDs are mapped to Phase 41.3. All goals derived from phase goal statement and plan must_haves. All 8 plan goals verified complete.

---

### Anti-Patterns Found

None. Scan of modified files (`placeholder` hits) were all legitimate HTML `placeholder=""` attributes on input elements — not stub implementations.

---

### Human Verification Required

#### 1. Chrome Extension Summarise Flow (end-to-end)

**Test:** Load the unpacked Chrome extension, navigate to any webpage, click "Summarise page"
**Expected:** Spinner appears while LLM generates; 1-3 paragraph summary text rendered in popup; "Add to brain" button saves a note with `type=note`, `tags=['summary']`, `source_url` set to tab URL
**Why human:** Requires Chrome extension runtime context, a real HTTP connection to sb-api, and a functioning LLM adapter (Ollama or similar)

#### 2. Wiki-link Navigation in NoteViewer

**Test:** Open a note containing `[[Title of Another Note]]` in NoteViewer
**Expected:** The pattern renders as a blue chip; clicking navigates to Notes tab and opens the matched note; an unresolved `[[No Such Note]]` appears as dimmed grey chip with no navigation
**Why human:** Requires live browser with actual notes containing wiki-link syntax

#### 3. Sidebar Visual Verification

**Test:** After `make dev`, open the GUI and inspect the sidebar
**Expected:** Notes appear under flat type headings (Meeting, Person, Note, etc.) with no outer folder wrapper; collapse/expand state persists across page reloads
**Why human:** Visual layout and localStorage persistence require browser interaction

#### 4. Attachments Live Refresh

**Test:** Open a note in LinksPage (or any detail page), upload a file, observe the Attachments section
**Expected:** File appears in the Attachments section immediately without navigating away; "No attachments yet" replaced by file link
**Why human:** Requires live file upload flow and real-time DOM observation

---

### Gaps Summary

None. All 10 observable truths are VERIFIED at code level. All artifacts exist, are substantive, and are wired to real data sources. Four items are flagged for human verification because they require a running browser/extension environment, not because of any code deficiency.

---

_Verified: 2026-03-28T16:40:00Z_
_Verifier: Claude (gsd-verifier)_
