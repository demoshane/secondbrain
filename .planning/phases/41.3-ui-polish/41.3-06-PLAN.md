---
plan: 41.3-06
phase: "41.3"
name: Attachments section on all detail panels
wave: 2
depends_on: []
files_modified:
  - frontend/src/components/ui/attachments-section.tsx
  - frontend/src/components/NoteViewer.tsx
  - frontend/src/components/FileUploadModal.tsx
  - frontend/src/components/LinksPage.tsx
  - frontend/src/components/PeoplePage.tsx
  - frontend/src/components/MeetingsPage.tsx
  - frontend/src/components/ProjectsPage.tsx
autonomous: true
execution_strategy: direct
---

<objective>
Make uploaded files immediately visible in an Attachments section on every detail panel.
Fix NoteViewer so the list refreshes right after upload (currently it doesn't because the
useEffect only fires on note.path change). Surface the same section on LinksPage,
PeoplePage, MeetingsPage, ProjectsPage which have their own detail panels and currently
show no attachments at all.
</objective>

<context>
- @frontend/src/components/NoteViewer.tsx — has Attachments section (lines 159-176) but
  refresh is broken: FileUploadModal.onClose just closes the modal, doesn't re-fetch;
  useEffect([note.path]) doesn't re-fire when the path is unchanged after upload
- @frontend/src/components/FileUploadModal.tsx — calls onClose() on success, no callback
  for callers to react to a completed upload
- LinksPage, PeoplePage, MeetingsPage, ProjectsPage — own detail panels, no attachment UI
- API: GET /notes/attachments?path={notePath} → { attachments: Attachment[] }
- API: POST /files/upload (FormData: file, note_path) → links file to note
</context>

<tasks>
  <task type="auto" tdd="false">
    <behavior>
      All detail panels show an Attachments section. After upload, the section refreshes
      immediately without requiring navigation or page reload. Section is always visible
      (not gated on length > 0) so users know where to look and can trigger upload.
    </behavior>
    <action>
      1. Create frontend/src/components/ui/attachments-section.tsx:
         - Props: notePath: string, refreshTick?: number
         - Fetches GET /notes/attachments?path={notePath} on mount and on refreshTick change
         - Renders "Attachments" header with Paperclip icon, file list as anchor links,
           and "No attachments yet" when empty
         - Always visible (no length > 0 gate)

      2. FileUploadModal: add optional onUploaded?: () => void prop; call it after
         successful upload (before onClose).

      3. NoteViewer: replace inline attachment block (lines 159-176) with
         AttachmentsSection. Add refreshTick state (starts 0); pass to AttachmentsSection.
         Pass onUploaded={() => setRefreshTick(t => t + 1)} to FileUploadModal.

      4. LinksPage, PeoplePage, MeetingsPage, ProjectsPage: add AttachmentsSection +
         Upload button to each detail panel, scoped to the currently selected note's path.
         Show only when a note is selected. Wire FileUploadModal the same way as NoteViewer.
    </action>
    <verify>
      <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/ -q -x 2>&1 | tail -5</automated>
    </verify>
    <done>
      Uploading a file to any note shows it in the Attachments section immediately.
      Section visible on NoteViewer, LinksPage, PeoplePage, MeetingsPage, ProjectsPage.
    </done>
  </task>
</tasks>
