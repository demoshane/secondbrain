---
plan: 41.3-04
phase: "41.3"
name: Inline note editing on detail pages
wave: 2
depends_on: []
files_modified:
  - frontend/src/components/LinksPage.tsx
  - frontend/src/components/PeoplePage.tsx
  - frontend/src/components/MeetingsPage.tsx
  - frontend/src/components/ProjectsPage.tsx
autonomous: true
execution_strategy: direct
---

<objective>
Add inline note editing to all detail-panel pages (Links, People, Meetings, Projects) so
the user can edit the selected note's content without being navigated away from the current
page. "Open as Note" stays and continues to navigate to the Notes tab. The edit action now
opens an in-place editor inside the detail panel.
</objective>

<context>
- @frontend/src/components/LinksPage.tsx — detail panel shows title, URL, body (read-only)
- @frontend/src/components/PeoplePage.tsx — detail panel shows person note body (read-only markdown)
- @frontend/src/components/MeetingsPage.tsx — detail panel shows meeting body (read-only)
- @frontend/src/components/ProjectsPage.tsx — detail panel shows project body (read-only)
- @frontend/src/components/NoteEditor.tsx — existing full-page editor (reference for save logic)
- All pages use PATCH /notes/{path} to update content
</context>

<tasks>
  <task type="auto" tdd="false">
    <behavior>
      Each detail panel gets an Edit button (pencil icon, alongside "Open as Note").
      Clicking Edit switches the body area from markdown-rendered view to a textarea
      pre-filled with the raw note body. Save (Cmd+Enter or Save button) calls
      PATCH /notes/{encodedPath} with the new body and flips back to read view.
      Cancel discards changes and flips back. "Open as Note" is unchanged.
      The same toggle pattern applies to all four pages.
    </behavior>
    <action>
      In each page component, add `editingBody: string | null` state (null = read mode).
      Replace the read-only body render with: if editingBody !== null → textarea + Save/Cancel
      buttons; else → existing markdown render + Edit button in the header toolbar.
      On Save: PATCH body, update local state, set editingBody to null.
      Textarea should be full-height of the content area, monospace font, dark-themed.
    </action>
    <verify>
      <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/ -q -x 2>&1 | tail -5</automated>
    </verify>
    <done>
      Edit button visible on all four pages' detail panels. Clicking it renders a textarea
      with raw body. Save persists changes and returns to read view. Cancel discards.
      "Open as Note" still navigates to Notes tab.
    </done>
  </task>
</tasks>
