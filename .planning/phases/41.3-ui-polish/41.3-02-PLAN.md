---
plan: 41.3-02
phase: "41.3"
name: Chrome extension dark Visily retheme
wave: 1
depends_on: []
files_modified:
  - chrome-extension/popup.css
  - chrome-extension/popup.html
autonomous: true
execution_strategy: direct
requirements: []
must_haves:
  truths:
    - "popup.css body background is #0f1117 (dark, matches app --background token)"
    - "popup.css input/select/textarea background is #191f2d (matches app --input token)"
    - "popup.css #save-btn background is #5b6cf7 (matches app --primary token)"
    - "popup.css foreground text color is #d4dae8 (matches app --foreground token)"
    - "popup.css border color is #2a3245 (matches app --border token)"
    - "popup.html has a Second Brain brain icon/logo in the header"
  artifacts:
    - "chrome-extension/popup.css — dark-themed"
---

<objective>
Retheme the Chrome extension popup from its current light theme (#fff background, #1a1a2e text) to match
the Second Brain app's dark Visily design system. Apply the same CSS tokens used by the main app.
No structural changes to popup.html logic — purely visual: colors, typography, spacing refinement.
</objective>

<context>
- @chrome-extension/popup.html (current structure — read before editing CSS)
- @chrome-extension/popup.css (file to rewrite)
- @ui-descriptions/UI-DESIGN-BRIEF.md (brand principles: dark mode, dense, minimal chrome)
</context>

<tasks>
<task type="auto" tdd="false">
  <name>Retheme popup.css to dark Visily tokens</name>

  <read_first>
    - chrome-extension/popup.html — understand all selectors used
    - chrome-extension/popup.css — current full file
    - frontend/src/index.css — lines with CSS variable definitions (--background, --card, --primary, etc.)
  </read_first>

  <action>
  Rewrite popup.css applying these exact hex values derived from the app's design tokens:

  **Token mapping:**
  - Background (body): `#0f1117`
  - Card/panel surfaces: `#151b27`
  - Input backgrounds: `#191f2d`
  - Muted backgrounds (history section): `#191f2d`
  - Primary text: `#d4dae8`
  - Muted/label text: `#7a8499`
  - Borders/dividers: `#2a3245`
  - Primary button bg: `#5b6cf7`
  - Primary button hover: `#4a5be0`
  - Primary button text: `#ffffff`
  - Secondary button bg: `#1e2535`
  - Secondary button border: `#2a3245`
  - Secondary button text: `#d4dae8`
  - Focus ring: `rgba(91, 108, 247, 0.35)`
  - Error bg: `#2d1b1b`, error text: `#f87171`, error border: `#dc2626`
  - Success bg: `#1b2d1b`, success text: `#4ade80`, success border: `#16a34a`
  - Status bar connected: `#16a34a`, disconnected: `#dc2626`, default: `#2a3245`
  - History type badge: bg `#1e2a4a`, text `#818cf8` (indigo tint matching primary)

  **Typography:** keep `system-ui, -apple-system, 'Segoe UI', sans-serif`. Labels stay uppercase + letter-spacing.

  **Sizing:** body width stays 400px, max-height 600px. Inputs/buttons keep same padding — only colors change.

  **Header:** Add a small brain emoji or `🧠` before the "Save to Brain" h2 text in popup.html to give it identity. Keep h2 font-size 1rem.

  **Select element:** add `appearance: none` and a subtle chevron via background-image (SVG data-URI, white arrow) so it looks native-dark rather than OS-styled.
  </action>

  <acceptance_criteria>
    - popup.css contains `background: #0f1117` on body
    - popup.css contains `background: #191f2d` on input/select/textarea
    - popup.css contains `background: #5b6cf7` on #save-btn
    - popup.css contains `color: #d4dae8` as primary foreground
    - popup.css contains `border.*#2a3245` pattern for borders
    - popup.html header contains "🧠" or similar identity marker
  </acceptance_criteria>

  <verify>
    <automated>grep -c "#0f1117\|#5b6cf7\|#d4dae8\|#191f2d\|#2a3245" /Users/tuomasleppanen/second-brain/chrome-extension/popup.css</automated>
  </verify>

  <done>popup.css fully dark-themed with Visily tokens, popup.html has header identity, visually consistent with main app</done>
</task>
</tasks>
