---
phase: 41-visual-redesign
plan: 03
type: execute
wave: 3
depends_on: ["41-01", "41-02"]
files_modified:
  - frontend/src/components/Sidebar.tsx
  - frontend/src/components/NoteViewer.tsx
  - frontend/src/components/NoteEditor.tsx
  - frontend/src/components/RightPanel.tsx
autonomous: true
requirements: []
execution_strategy: direct

must_haves:
  truths:
    - "Sidebar groups notes by type only (no folder level), with collapsible sections and count badges"
    - "Sidebar width is 256px with bg-card background"
    - "Active note row has left blue border and bg-secondary highlight"
    - "NoteViewer renders markdown body with proper prose styling"
    - "NoteEditor has save button with correct variant and styling"
    - "Right panel shows collapsible sections for Backlinks, People, and Actions"
    - "Right panel hides sections that have no content"
    - "Right panel can collapse to a 40px icon strip via chevron toggle"
  artifacts:
    - path: "frontend/src/components/Sidebar.tsx"
      provides: "Type-grouped sidebar with collapsible sections"
      exports: ["Sidebar"]
    - path: "frontend/src/components/NoteViewer.tsx"
      provides: "Note display with metadata, tags, people, markdown body"
      exports: ["NoteViewer"]
    - path: "frontend/src/components/NoteEditor.tsx"
      provides: "Note editing with textarea and save button"
      exports: ["NoteEditor"]
    - path: "frontend/src/components/RightPanel.tsx"
      provides: "Right panel with backlinks, people, actions sections"
      exports: ["RightPanel"]
  key_links:
    - from: "frontend/src/components/Sidebar.tsx"
      to: "frontend/src/contexts/NoteContext.tsx"
      via: "notes, currentPath, openNote"
      pattern: "useNoteContext"
    - from: "frontend/src/components/NoteViewer.tsx"
      to: "API /notes/<path>"
      via: "fetch for note detail, actions, backlinks"
      pattern: "getAPI.*notes"
    - from: "frontend/src/components/RightPanel.tsx"
      to: "API /notes/<path>/actions, /notes/<path>/backlinks"
      via: "fetch in useEffect"
      pattern: "getAPI.*backlinks\|actions"
---

<objective>
Rebuild the Notes view: Sidebar (type-grouped, w-64, collapsible), NoteViewer (markdown rendering, metadata, upload/delete actions), NoteEditor (styled textarea + save), RightPanel (collapsible backlinks/people/actions with panel-level collapse).

Purpose: The Notes view is the primary interface (>80% of time spent here). Three-column layout: sidebar | viewer/editor | right panel. Depends on Plan 02 completing first because Plan 02 removes the inline Upload/Delete action bar from App.tsx -- this plan re-adds delete inside NoteViewer.
Output: 4 rewritten component files matching Visily Notes view mockup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-visual-redesign/41-CONTEXT.md
@.planning/phases/41-visual-redesign/41-UI-SPEC.md
@.planning/phases/41-visual-redesign/41-01-SUMMARY.md
@.planning/phases/41-visual-redesign/41-02-SUMMARY.md
@.claude/LEARNINGS.md
@ui-descriptions/notes-view.md
@ui-descriptions/sidebar.md
@ui-descriptions/right-panel.md
@ui-descriptions/UI-DESIGN-BRIEF.md

<interfaces>
<!-- Shared components from Plan 01 -->
From frontend/src/components/ui/collapsible-section.tsx:
  export function CollapsibleSection({ title, count, sectionId, defaultOpen, children }: Props)

From frontend/src/components/ui/note-type-badge.tsx:
  export function NoteTypeBadge({ type, className }: { type: string; className?: string })

From frontend/src/components/ui/empty-state.tsx:
  export function EmptyState({ icon, heading, body, actionLabel, onAction }: Props)

From frontend/src/components/ui/person-badge.tsx:
  export function PersonBadge({ name, path, onRemove, onClick }: Props)

From frontend/src/components/ui/tag-badge.tsx:
  export function TagBadge({ tag, onClick, onRemove }: Props)

From frontend/src/components/ui/action-item-row.tsx:
  export function ActionItemRow({ item, onToggle, onDelete, onAssign, showSource }: Props)

From frontend/src/components/ui/confirm-dialog.tsx:
  export function ConfirmDialog({ open, onClose, onConfirm, title, description, confirmLabel, cancelLabel, variant }: Props)

From frontend/src/components/ui/skeleton-list.tsx:
  export function SkeletonList({ count, rowHeight }: Props)

<!-- Contexts -->
From frontend/src/contexts/NoteContext.tsx:
  useNoteContext() returns { notes, currentNote, currentPath, openNote, loadNotes, isDirty, setIsDirty }
  NOTE: No deleteNote in context. Use direct API call for delete: fetch(getAPI() + '/notes/' + encodeURIComponent(path), { method: 'DELETE' })

From frontend/src/contexts/SearchContext.tsx:
  useSearchContext() returns { results, tagFilter, setTagFilter, clearSearch }

From frontend/src/hooks/useCollapseState.ts:
  useCollapseState() returns { prefs, toggle }

From frontend/src/types.ts:
  Note { path, title, type, body, tags, people, folder, created_at, updated_at }
  ActionItem { id, note_path, text, done, due_date, assignee_path }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild Sidebar with type-only grouping</name>
  <files>frontend/src/components/Sidebar.tsx</files>
  <read_first>
    - frontend/src/components/Sidebar.tsx (current 2-level grouping logic, extract hook wiring)
    - .planning/phases/41-visual-redesign/41-UI-SPEC.md (sidebar specs)
    - ui-descriptions/sidebar.md (sidebar behavioral spec, known issues)
    - frontend/src/hooks/useCollapseState.ts (collapse persistence)
    - frontend/src/types.ts (Note type)
  </read_first>
  <action>
**Rewrite Sidebar.tsx entirely.** Keep imports: useNoteContext, useSearchContext, useCollapseState, ScrollArea, Button, cn, Note type.

**New grouping function** -- group by type only, no folder level:
```typescript
const TYPE_ORDER = ['meeting', 'people', 'projects', 'idea', 'note', 'strategy', 'coding', 'personal', 'link', 'files']

function groupByType(notes: Note[]): Map<string, Note[]> {
  const grouped = new Map<string, Note[]>()
  for (const note of notes) {
    const type = note.type || 'note'
    if (!grouped.has(type)) grouped.set(type, [])
    grouped.get(type)!.push(note)
  }
  // Sort by TYPE_ORDER
  const sorted = new Map<string, Note[]>()
  for (const t of TYPE_ORDER) {
    if (grouped.has(t)) sorted.set(t, grouped.get(t)!)
  }
  // Append any types not in TYPE_ORDER
  for (const [t, notes] of grouped) {
    if (!sorted.has(t)) sorted.set(t, notes)
  }
  return sorted
}
```

**Layout:**
- Root: `w-64 border-r border-border flex flex-col bg-card` (256px = w-64)
- Tag filter banner: keep existing behavior (show when tagFilter is set, X button to clear)
- ScrollArea wrapping all sections

**Each type section** uses `CollapsibleSection` from plan 01:
```tsx
<CollapsibleSection
  title={capitalize(type)}
  count={notes.length}
  sectionId={`sidebar-${type}`}
  defaultOpen={true}
>
  {notes.map(note => (
    <NoteRow key={note.path} note={note} isActive={note.path === currentPath} onClick={() => openNote(note.path)} />
  ))}
</CollapsibleSection>
```

**NoteRow** (inline component or separate):
- Root: `flex items-center gap-2 px-3 py-1.5 cursor-pointer text-sm truncate group`
- Active: `bg-secondary text-foreground border-l-2 border-primary`
- Inactive: `text-muted-foreground hover:bg-secondary/50`
- Content: NoteTypeBadge (tiny, text-[10px]), then truncated title
- data-testid: `note-row-${encodeURIComponent(note.path)}`

Keep data-testid="sidebar" on root.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && grep -c "CollapsibleSection" frontend/src/components/Sidebar.tsx && grep -c "w-64" frontend/src/components/Sidebar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Sidebar.tsx contains `w-64` (256px width)
    - Sidebar.tsx contains `bg-card` (card background)
    - Sidebar.tsx imports CollapsibleSection from `@/components/ui/collapsible-section`
    - Sidebar.tsx imports NoteTypeBadge from `@/components/ui/note-type-badge`
    - Sidebar.tsx does NOT contain `grouped.has(folder)` or folder-level grouping
    - Sidebar.tsx contains `groupByType` function (type-only grouping)
    - Sidebar.tsx contains `border-l-2 border-primary` (active note indicator)
    - Sidebar.tsx retains data-testid="sidebar"
  </acceptance_criteria>
  <done>Sidebar groups by type only with collapsible sections, 256px width, card background, active note left border indicator</done>
</task>

<task type="auto">
  <name>Task 2a: Rebuild NoteViewer and NoteEditor</name>
  <files>frontend/src/components/NoteViewer.tsx, frontend/src/components/NoteEditor.tsx</files>
  <read_first>
    - frontend/src/components/NoteViewer.tsx (current rendering, API calls for backlinks/actions/attachments)
    - frontend/src/components/NoteEditor.tsx (current edit form, save logic)
    - .planning/phases/41-visual-redesign/41-UI-SPEC.md (layout contracts, interaction contracts)
    - ui-descriptions/notes-view.md (viewer behavioral spec)
    - frontend/src/types.ts (Note, ActionItem types)
    - frontend/src/components/ui/confirm-dialog.tsx (for delete confirmation)
  </read_first>
  <action>
**NoteViewer.tsx -- rewrite.** Keep the same props (`{ note: Note }`). Keep existing API fetch logic for backlinks, actions, attachments.

Layout: `flex-1 overflow-y-auto p-6 bg-background`

Structure:
1. **Title**: `text-xl font-semibold text-foreground mb-1` -- h1 with note.title
2. **Metadata row**: `flex items-center gap-3 text-xs text-muted-foreground mb-4`
   - NoteTypeBadge for note.type
   - Updated date (relative time, e.g., "2 days ago" -- use a simple relative time helper)
   - Created date
3. **Tags row** (if note.tags.length > 0): `flex flex-wrap gap-1 mb-3`
   - TagBadge for each tag; onClick triggers tag filter via useSearchContext
4. **People row** (if note.people.length > 0): `flex flex-wrap gap-1 mb-3`
   - PersonBadge for each person
5. **Action bar**: `flex items-center gap-1 mb-4 border-b border-border pb-3`
   - Edit button (variant=outline, size=sm, icon=Pencil, label="Edit Note") -- triggers edit mode
   - Upload button (variant=ghost, size=sm, icon=Upload)
   - Delete button (variant=ghost, size=sm, icon=Trash2, className="text-destructive opacity-0 group-hover:opacity-100") -- opens ConfirmDialog
   - Import ConfirmDialog; manage `showDelete` state; on confirm call direct API DELETE: `fetch(getAPI() + '/notes/' + encodeURIComponent(note.path), { method: 'DELETE' })` then call `loadNotes()` from context to refresh sidebar. Do NOT use context.deleteNote (it does not exist).
6. **Markdown body**: Render note.body with `react-markdown` + `remark-gfm`. Apply manual prose styles:
   - Container: `text-sm leading-relaxed text-foreground [&_h1]:text-lg [&_h1]:font-semibold [&_h1]:mt-4 [&_h1]:mb-2 [&_h2]:text-base [&_h2]:font-semibold [&_h2]:mt-3 [&_h2]:mb-1 [&_a]:text-primary [&_a]:underline [&_code]:bg-muted [&_code]:px-1 [&_code]:rounded [&_pre]:bg-muted [&_pre]:p-3 [&_pre]:rounded [&_ul]:list-disc [&_ul]:ml-4 [&_ol]:list-decimal [&_ol]:ml-4 [&_blockquote]:border-l-2 [&_blockquote]:border-primary [&_blockquote]:pl-3 [&_blockquote]:text-muted-foreground`
7. **Attachments section** (if any): list below body

**NoteEditor.tsx -- rewrite.** Keep save logic (PUT to API, toast on success/failure).

Layout: `flex-1 overflow-y-auto p-6 bg-background`
- Title input: `text-xl font-semibold bg-transparent border-none outline-none text-foreground w-full mb-4`
- Body textarea: `flex-1 w-full bg-input border border-border rounded p-3 text-sm text-foreground font-mono resize-none min-h-[300px]`
- Bottom bar: `flex items-center gap-2 mt-4`
  - Save button: `Button variant="default"` label "Save Note"
  - Cancel button: `Button variant="ghost"` label "Cancel"
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && grep -c "react-markdown" frontend/src/components/NoteViewer.tsx && grep -c "ConfirmDialog" frontend/src/components/NoteViewer.tsx && grep -c "Save Note" frontend/src/components/NoteEditor.tsx</automated>
  </verify>
  <acceptance_criteria>
    - NoteViewer.tsx imports ReactMarkdown from react-markdown
    - NoteViewer.tsx imports NoteTypeBadge, TagBadge, PersonBadge from @/components/ui/
    - NoteViewer.tsx contains `text-xl font-semibold` (title styling)
    - NoteViewer.tsx contains ConfirmDialog import for delete
    - NoteViewer.tsx delete uses direct fetch DELETE call, NOT context.deleteNote
    - NoteEditor.tsx contains "Save Note" string literal
    - NoteEditor.tsx contains `variant="default"` on save button
  </acceptance_criteria>
  <done>NoteViewer renders markdown with metadata/tags/people and direct API delete; NoteEditor has styled save flow</done>
</task>

<task type="auto">
  <name>Task 2b: Rebuild RightPanel with panel-level collapse</name>
  <files>frontend/src/components/RightPanel.tsx</files>
  <read_first>
    - frontend/src/components/RightPanel.tsx (current panel with backlinks/people/actions sections)
    - .planning/phases/41-visual-redesign/41-UI-SPEC.md (layout contracts, interaction contracts)
    - ui-descriptions/right-panel.md (right panel spec)
    - frontend/src/types.ts (ActionItem type)
  </read_first>
  <action>
**RightPanel.tsx -- rewrite.** Keep existing API fetch logic for backlinks, actions, people.

**Panel-level collapse behavior (per UI-SPEC):**
- Add a chevron toggle button on the left edge of the panel header.
- Collapsed state: panel shrinks to `w-10` (40px) icon strip showing only section icons (Link for backlinks, Users for people, CheckSquare for actions) stacked vertically. No section content visible.
- Expanded state: full `w-64` with all sections.
- Persist collapse state to localStorage keyed by `rp-collapsed`. On mount, read `localStorage.getItem('rp-collapsed')` and parse as boolean (default false = expanded). On toggle, write to localStorage.
- Transition: `transition-all duration-200` on width change.

**Collapsed layout (w-10):**
- Root: `w-10 border-l border-border bg-card flex flex-col items-center py-2 gap-3 transition-all duration-200`
- Chevron button at top: `ChevronLeft` icon (16px), on click expand panel
- Section icons stacked below: Link (16px, text-muted-foreground), Users (16px), CheckSquare (16px)
- Each icon only visible if that section has data (backlinks.length > 0, etc.)

**Expanded layout (w-64):**
- Root: `w-64 border-l border-border bg-card flex flex-col overflow-y-auto transition-all duration-200`
- Header row: `flex items-center justify-between px-3 py-2 border-b border-border`
  - Chevron button: `ChevronRight` icon (16px), on click collapse panel

Sections using CollapsibleSection (hide section entirely when data is empty):
1. **Backlinks** -- `CollapsibleSection title="Backlinks" count={backlinks.length} sectionId="rp-backlinks"`
   - Each backlink: clickable row with note title, onClick navigates to that note
2. **People** -- `CollapsibleSection title="People" count={people.length} sectionId="rp-people"`
   - PersonBadge for each linked person
3. **Actions** -- `CollapsibleSection title="Actions" count={actions.length} sectionId="rp-actions"`
   - ActionItemRow for each action item

When ALL sections are empty, show EmptyState with icon=Unlink, heading="No connections yet", body="Add tags or link this note to others to see backlinks, people, and actions here."

Keep data-testid="right-panel".
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && grep -c "CollapsibleSection" frontend/src/components/RightPanel.tsx && grep -c "w-10" frontend/src/components/RightPanel.tsx && grep -c "rp-collapsed" frontend/src/components/RightPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - RightPanel.tsx contains `w-64` (256px expanded width)
    - RightPanel.tsx contains `w-10` (40px collapsed width)
    - RightPanel.tsx contains `bg-card` (card background)
    - RightPanel.tsx contains `rp-collapsed` (localStorage key for panel collapse persistence)
    - RightPanel.tsx contains `transition-all duration-200` (smooth width transition)
    - RightPanel.tsx imports CollapsibleSection
    - RightPanel.tsx imports EmptyState with `heading="No connections yet"`
    - RightPanel.tsx imports ActionItemRow
    - RightPanel.tsx imports ChevronLeft or ChevronRight from lucide-react
    - RightPanel.tsx conditionally hides sections when data array is empty
    - RightPanel.tsx retains data-testid="right-panel"
  </acceptance_criteria>
  <done>RightPanel shows collapsible backlinks/people/actions with panel-level collapse to 40px icon strip via chevron toggle, localStorage persistence, and empty state</done>
</task>

</tasks>

<verification>
- Sidebar shows type-grouped notes without folder level
- NoteViewer renders markdown body with styled headings, links, code blocks
- NoteViewer delete button uses direct API fetch, not context.deleteNote
- RightPanel shows collapsible sections, hides empty ones
- RightPanel collapses to 40px icon strip via chevron toggle
- RightPanel collapse state persists across page refresh (localStorage)
- NoteEditor save button works
- Vite build succeeds: `cd frontend && npx vite build --mode development 2>&1 | tail -5`
</verification>

<success_criteria>
- Three-column Notes layout matches Visily: sidebar (w-64 bg-card) | viewer (flex-1 bg-background) | right panel (w-64 bg-card, collapsible to w-10)
- Sidebar type grouping replaces folder+type grouping
- NoteViewer markdown rendering uses react-markdown with proper prose styles
- Right panel hides empty sections; shows full empty state when all are empty
- Right panel collapse/expand persists to localStorage
</success_criteria>

<output>
After completion, create `.planning/phases/41-visual-redesign/41-03-SUMMARY.md`

Note: After all tasks complete, run `make dev` on HOST to build frontend, reinstall, and restart services.
</output>
