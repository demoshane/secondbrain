---
phase: 41-visual-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/index.css
  - frontend/tailwind.config.js
  - frontend/src/App.tsx
  - frontend/src/components/ui/badge.tsx
  - frontend/src/components/ui/collapsible-section.tsx
  - frontend/src/components/ui/empty-state.tsx
  - frontend/src/components/ui/action-item-row.tsx
  - frontend/src/components/ui/person-badge.tsx
  - frontend/src/components/ui/tag-badge.tsx
  - frontend/src/components/ui/note-type-badge.tsx
  - frontend/src/components/ui/confirm-dialog.tsx
  - frontend/src/components/ui/health-score-gauge.tsx
  - frontend/src/components/ui/skeleton-list.tsx
autonomous: true
requirements: []
execution_strategy: direct

must_haves:
  truths:
    - "All UI surfaces use the dark navy palette (#0f1117 background, #151b27 cards)"
    - "No .dark class conditionals exist anywhere in the frontend"
    - "Note type badges show correct per-type colors from the design spec"
    - "Every page using shared components renders without TypeScript compile errors or import failures"
  artifacts:
    - path: "frontend/src/index.css"
      provides: "Visily dark palette CSS vars"
      contains: "--background: 220 20% 7%"
    - path: "frontend/src/components/ui/note-type-badge.tsx"
      provides: "Per-type colored badges"
      exports: ["NoteTypeBadge"]
    - path: "frontend/src/components/ui/empty-state.tsx"
      provides: "Reusable empty state component"
      exports: ["EmptyState"]
    - path: "frontend/src/components/ui/collapsible-section.tsx"
      provides: "Collapsible section with count badge"
      exports: ["CollapsibleSection"]
    - path: "frontend/src/components/ui/confirm-dialog.tsx"
      provides: "Destructive action confirmation"
      exports: ["ConfirmDialog"]
  key_links:
    - from: "frontend/src/index.css"
      to: "frontend/tailwind.config.js"
      via: "CSS vars consumed by hsl(var(--*))"
      pattern: "hsl\\(var\\(--"
    - from: "frontend/src/components/ui/note-type-badge.tsx"
      to: "Note type color map"
      via: "type prop maps to bg/text hex pairs"
      pattern: "note_type.*badge"
---

<objective>
Replace the entire design token system with the Visily dark palette and build all 9 new shared UI components that downstream plans depend on.

Purpose: Every subsequent plan (02-05) imports these tokens and components. This is the foundation -- nothing else can start until the palette and shared components exist.
Output: Updated index.css, tailwind.config.js, App.tsx (dark class removal), badge.tsx (note-type variant), plus 9 new components in frontend/src/components/ui/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-visual-redesign/41-CONTEXT.md
@.planning/phases/41-visual-redesign/41-UI-SPEC.md
@.claude/LEARNINGS.md
@ui-descriptions/UI-DESIGN-BRIEF.md
@frontend/src/index.css
@frontend/tailwind.config.js
@frontend/src/App.tsx
@frontend/src/components/ui/badge.tsx
@frontend/src/components/ui/dialog.tsx
@frontend/src/components/ui/checkbox.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace design tokens and remove dark mode conditionals</name>
  <files>frontend/src/index.css, frontend/tailwind.config.js, frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/index.css (current CSS vars to replace)
    - frontend/tailwind.config.js (current config)
    - frontend/src/App.tsx (has dark class on root div)
    - .planning/phases/41-visual-redesign/41-UI-SPEC.md (exact CSS var values)
  </read_first>
  <action>
1. **Replace `frontend/src/index.css` entirely.** Keep `@tailwind base/components/utilities` directives. Replace the `@layer base` block:
   - Delete the `.dark { ... }` block completely.
   - Replace `:root { ... }` with the exact values from UI-SPEC.md Color section:
     ```css
     :root {
       --background: 220 20% 7%;
       --card: 222 22% 11%;
       --popover: 222 24% 13%;
       --foreground: 215 25% 87%;
       --card-foreground: 215 25% 87%;
       --popover-foreground: 215 25% 87%;
       --primary: 231 87% 65%;
       --primary-foreground: 0 0% 100%;
       --secondary: 222 22% 16%;
       --secondary-foreground: 215 25% 87%;
       --muted: 222 22% 14%;
       --muted-foreground: 215 15% 52%;
       --accent: 25 95% 53%;
       --accent-foreground: 0 0% 100%;
       --border: 222 20% 20%;
       --input: 222 22% 14%;
       --ring: 231 87% 65%;
       --destructive: 0 84% 60%;
       --destructive-foreground: 0 0% 100%;
       --radius: 0.375rem;
     }
     ```
   - Keep the `* { @apply border-border; }` and `body { @apply bg-background text-foreground; margin: 0; }` rules.

2. **Update `frontend/tailwind.config.js`:**
   - Remove `darkMode: ['class']` line entirely (always dark, no class toggling).
   - All color references (`hsl(var(--*))`) remain unchanged -- only CSS var values changed.

3. **Update `frontend/src/App.tsx`:**
   - The root `<div>` currently has `className="flex flex-col h-screen bg-background text-foreground"`. It does NOT have a `dark` class, so no change needed there.
   - Grep the entire `frontend/src/` for any `dark:` Tailwind prefixes or `.dark` class additions and remove them. These are dead code now.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && grep -c '\.dark' frontend/src/index.css | grep -q '^0$' && ! grep -q 'darkMode' frontend/tailwind.config.js && test $(grep -rl 'dark:' frontend/src --include='*.tsx' | wc -l | tr -d ' ') -eq 0 && echo 'PASS: dark mode fully removed' || echo 'FAIL: dark mode artifacts remain'</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/index.css contains `--background: 220 20% 7%`
    - frontend/src/index.css contains `--primary: 231 87% 65%`
    - frontend/src/index.css contains `--accent: 25 95% 53%`
    - frontend/src/index.css contains `--radius: 0.375rem`
    - frontend/src/index.css does NOT contain `.dark {`
    - frontend/tailwind.config.js does NOT contain `darkMode`
    - No files in frontend/src/ contain `dark:` Tailwind prefix (grep returns 0 matches)
  </acceptance_criteria>
  <done>Visily dark palette active via CSS vars; .dark conditionals removed; tailwind darkMode disabled</done>
</task>

<task type="auto">
  <name>Task 2a: Build display components (note-type-badge, tag-badge, person-badge, skeleton-list, empty-state) and update badge.tsx</name>
  <files>
    frontend/src/components/ui/badge.tsx,
    frontend/src/components/ui/note-type-badge.tsx,
    frontend/src/components/ui/tag-badge.tsx,
    frontend/src/components/ui/person-badge.tsx,
    frontend/src/components/ui/skeleton-list.tsx,
    frontend/src/components/ui/empty-state.tsx
  </files>
  <read_first>
    - frontend/src/components/ui/badge.tsx (add note-type variant)
    - .planning/phases/41-visual-redesign/41-UI-SPEC.md (component inventory, note type colors)
    - frontend/src/lib/utils.ts (cn helper)
  </read_first>
  <action>
Create 5 new components and update 1 existing component. All components use `cn()` from `@/lib/utils` for class merging.

**1. Update `badge.tsx`** -- add a `noteType` variant that accepts a `noteType` prop and maps to the color pairs from UI-SPEC:
- Design: bg-[#1e3a5f] text-[#60a5fa]
- Research: bg-[#2d1f5e] text-[#a78bfa]
- Meeting: bg-[#1a2f1a] text-[#4ade80]
- Project: bg-[#2f1f0e] text-[#fb923c]
- Idea: bg-[#1f2a1a] text-[#86efac]
- Strategy: bg-[#1a1f3a] text-[#818cf8]
- People/Person: bg-[#2a1f1f] text-[#f87171]
- Link: bg-[#1a2a2a] text-[#2dd4bf]
- Default (note): bg-secondary text-[#94a3b8]

**2. `note-type-badge.tsx`** -- wrapper around Badge that takes `type: string` and renders the correct color. Props: `{ type: string; className?: string }`. Export `NoteTypeBadge`. Renders `<Badge>` with noteType variant, text is the type name capitalized.

**3. `tag-badge.tsx`** -- tag chip. Props: `{ tag: string; onClick?: () => void; onRemove?: () => void }`. Pill shape (rounded-full bg-primary/20 text-primary px-2 py-0.5 text-xs). Hash prefix displayed. Export `TagBadge`.

**4. `person-badge.tsx`** -- person name chip. Props: `{ name: string; path?: string; onRemove?: () => void; onClick?: () => void }`. Pill shape (rounded-full bg-secondary px-2 py-0.5 text-xs). Optional X button for removal. Export `PersonBadge`.

**5. `skeleton-list.tsx`** -- loading placeholder. Props: `{ count?: number; rowHeight?: string }`. Renders `count` (default 5) animated placeholder rows. Each row: `bg-muted animate-pulse rounded h-[rowHeight]` (default h-10). Export `SkeletonList`.

**6. `empty-state.tsx`** -- empty state display. Props: `{ icon: LucideIcon; heading: string; body: string; actionLabel?: string; onAction?: () => void }`. Centered flex-col layout, icon 48px text-muted-foreground, heading text-lg font-semibold, body text-sm text-muted-foreground, optional Button (variant=default). Export `EmptyState`.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && for f in note-type-badge tag-badge person-badge skeleton-list empty-state; do test -f "frontend/src/components/ui/$f.tsx" && echo "OK: $f" || echo "MISSING: $f"; done && grep -c '#1e3a5f' frontend/src/components/ui/badge.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All 5 new .tsx files exist in frontend/src/components/ui/
    - badge.tsx contains color pairs for at least 8 note types (grep for `#1e3a5f` confirms Design type)
    - note-type-badge.tsx exports NoteTypeBadge
    - tag-badge.tsx exports TagBadge with hash prefix
    - person-badge.tsx exports PersonBadge
    - skeleton-list.tsx exports SkeletonList
    - empty-state.tsx exports EmptyState with `icon` prop typed as LucideIcon
  </acceptance_criteria>
  <done>5 display components created and badge.tsx updated with note-type variant colors</done>
</task>

<task type="auto">
  <name>Task 2b: Build interactive components (collapsible-section, action-item-row, confirm-dialog, health-score-gauge)</name>
  <files>
    frontend/src/components/ui/collapsible-section.tsx,
    frontend/src/components/ui/action-item-row.tsx,
    frontend/src/components/ui/confirm-dialog.tsx,
    frontend/src/components/ui/health-score-gauge.tsx
  </files>
  <read_first>
    - frontend/src/components/ui/dialog.tsx (base for ConfirmDialog)
    - frontend/src/components/ui/checkbox.tsx (used in ActionItemRow)
    - .planning/phases/41-visual-redesign/41-UI-SPEC.md (component inventory, interaction contracts, copywriting)
    - frontend/src/types.ts (ActionItem type)
    - frontend/src/lib/utils.ts (cn helper)
  </read_first>
  <action>
Create 4 new interactive components. All use `cn()` from `@/lib/utils`.

**1. `collapsible-section.tsx`** -- collapsible panel. Props: `{ title: string; count: number; sectionId: string; defaultOpen?: boolean; children: ReactNode }`. Uses localStorage persistence keyed by sectionId. ChevronRight/ChevronDown icons. Count badge (bg-secondary text-muted-foreground rounded-full px-2 text-xs). Smooth transition: `transition-all duration-200`. Export `CollapsibleSection`.

**2. `action-item-row.tsx`** -- action item with checkbox, text, date, assignee, delete. Props: `{ item: ActionItem; onToggle: (id: number) => void; onDelete: (id: number) => void; onAssign?: (id: number, path: string | null) => void; showSource?: boolean }`. Checkbox 20px (h-5 w-5). Done items: line-through text-muted-foreground. Overdue dates: text-red-400. Delete: hover-reveal trash icon (opacity-0 group-hover:opacity-100). Export `ActionItemRow`.

**3. `confirm-dialog.tsx`** -- destructive confirmation dialog. Props: `{ open: boolean; onClose: () => void; onConfirm: () => void; title: string; description: string; confirmLabel?: string; cancelLabel?: string; variant?: 'destructive' | 'default' }`. Uses Dialog from shadcn. Red border for destructive variant. AlertTriangle icon. Confirm button uses variant=destructive by default. Cancel button uses variant=ghost. Export `ConfirmDialog`.

**4. `health-score-gauge.tsx`** -- numeric score display. Props: `{ score: number }`. Large number (text-[32px] font-semibold). Color: green-500 if score >= 80, amber-400 if 50-79, red-500 if < 50. Label "Brain Health" below. Export `HealthScoreGauge`.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && for f in collapsible-section action-item-row confirm-dialog health-score-gauge; do test -f "frontend/src/components/ui/$f.tsx" && echo "OK: $f" || echo "MISSING: $f"; done</automated>
  </verify>
  <acceptance_criteria>
    - All 4 new .tsx files exist in frontend/src/components/ui/
    - collapsible-section.tsx exports CollapsibleSection with localStorage persistence
    - action-item-row.tsx exports ActionItemRow and imports ActionItem from @/types
    - confirm-dialog.tsx exports ConfirmDialog and imports Dialog from @/components/ui/dialog
    - health-score-gauge.tsx contains conditional color logic (green-500, amber-400, red-500)
  </acceptance_criteria>
  <done>4 interactive components created; all exportable; ready for consumption by plans 02-05</done>
</task>

</tasks>

<verification>
- `grep -c "\.dark" frontend/src/index.css` returns 0
- `grep "--background: 220 20% 7%" frontend/src/index.css` succeeds
- All 9 new component files exist in frontend/src/components/ui/
- `grep -r "dark:" frontend/src/ --include="*.tsx" --include="*.css" | wc -l` returns 0
- Vite dev build compiles without errors: `cd frontend && npx vite build --mode development 2>&1 | tail -5`
</verification>

<success_criteria>
- Visily dark palette is the only color scheme (no light mode, no .dark conditionals)
- All 9 new shared UI components exist with correct exports and props
- Existing shadcn components (button, input, dialog, etc.) automatically pick up new colors via CSS vars
- Vite build succeeds with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/41-visual-redesign/41-01-SUMMARY.md`

Note: After all tasks complete, run `make dev` on HOST to build frontend, reinstall, and restart services.
</output>
