---
phase: 41-visual-redesign
plan: 02
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - frontend/src/App.tsx
  - frontend/src/components/Topbar.tsx
  - frontend/src/components/TabBar.tsx
  - frontend/src/components/CommandPalette.tsx
autonomous: true
requirements: []
execution_strategy: direct

must_haves:
  truths:
    - "Topbar shows labeled buttons for New Note, Smart Capture, and Batch"
    - "Search mode selector is hidden behind an advanced toggle"
    - "Tab bar shows icons next to labels for all 8 tabs"
    - "Active tab has blue-indigo bottom border and semibold text"
    - "Cmd+K opens the command palette with dark popover styling"
    - "Connection status dot shows green/amber/red states"
  artifacts:
    - path: "frontend/src/components/Topbar.tsx"
      provides: "Redesigned topbar with labeled buttons and hidden search mode"
      exports: ["Topbar"]
    - path: "frontend/src/components/TabBar.tsx"
      provides: "Tab bar with icons and active state styling"
      exports: ["TabBar"]
    - path: "frontend/src/components/CommandPalette.tsx"
      provides: "Restyled command palette with dark popover bg"
      exports: ["CommandPalette"]
    - path: "frontend/src/App.tsx"
      provides: "Updated app shell with topbar height 52px, tabbar height 40px"
  key_links:
    - from: "frontend/src/components/TabBar.tsx"
      to: "frontend/src/contexts/UIContext.tsx"
      via: "setCurrentView on tab click"
      pattern: "setCurrentView"
    - from: "frontend/src/components/Topbar.tsx"
      to: "frontend/src/contexts/SearchContext.tsx"
      via: "query, search, clearSearch"
      pattern: "useSearchContext"
    - from: "frontend/src/App.tsx"
      to: "Plan 41-03 NoteViewer"
      via: "Plan 02 removes inline Upload/Delete action bar; Plan 03 re-adds delete inside NoteViewer. Plan 03 depends_on 02 to ensure no gap."
      pattern: "NoteViewer"
---

<objective>
Rebuild the app shell: Topbar (h-52px, labeled buttons, hidden search mode), TabBar (icons + labels, active indicator), CommandPalette (dark styling), and App.tsx layout (topbar + tabbar + content area with correct backgrounds).

Purpose: The app shell frames every page. Users see the topbar and tab bar on every view, so visual consistency starts here.
Output: 4 rewritten component files matching the Visily mockups.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-visual-redesign/41-CONTEXT.md
@.planning/phases/41-visual-redesign/41-UI-SPEC.md
@.planning/phases/41-visual-redesign/41-01-SUMMARY.md
@.claude/LEARNINGS.md
@ui-descriptions/app-shell.md
@ui-descriptions/command-palette.md
@ui-descriptions/UI-DESIGN-BRIEF.md

<interfaces>
<!-- From existing contexts/hooks the executor needs -->

From frontend/src/contexts/UIContext.tsx:
- useUIContext() returns { currentView, setCurrentView }
- ViewType = 'notes' | 'actions' | 'people' | 'meetings' | 'projects' | 'intelligence' | 'inbox' | 'links'

From frontend/src/contexts/SearchContext.tsx:
- useSearchContext() returns { query, setQuery, mode, setMode, search, clearSearch, results, tagFilter, setTagFilter }

From frontend/src/contexts/SSEContext.tsx:
- useSSEContext() returns { connected }

From frontend/src/contexts/NoteContext.tsx:
- useNoteContext() returns { loadNotes, currentNote, currentPath, openNote, notes }

From frontend/src/lib/utils.ts:
- cn(...inputs: ClassValue[]) — Tailwind class merger
- getAPI() — returns API base URL string
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild Topbar and TabBar</name>
  <files>frontend/src/components/Topbar.tsx, frontend/src/components/TabBar.tsx</files>
  <read_first>
    - frontend/src/components/Topbar.tsx (current implementation, extract API/hook wiring)
    - frontend/src/components/TabBar.tsx (current implementation, extract tab list)
    - .planning/phases/41-visual-redesign/41-UI-SPEC.md (topbar buttons, tab icons, interaction contracts)
    - ui-descriptions/app-shell.md (behavioral spec)
    - frontend/src/contexts/SearchContext.tsx (search mode types)
  </read_first>
  <action>
**Topbar.tsx — rewrite entirely.** Keep the same Props interface (`onNewNote`, `onBatchCapture`, `onSmartCapture`). Keep all hook imports (useSearchContext, useSSEContext, useNoteContext).

Layout: `h-[52px] flex items-center gap-2 px-4 border-b border-border bg-background`

Elements left-to-right:
1. Search icon (Search, 16px, text-muted-foreground)
2. Search Input (flex-1, h-8, bg-input, placeholder "Search notes...")
3. Search mode selector — **hidden by default**. Add a `showAdvanced` boolean state (default false). Render a small ghost button with `SlidersHorizontal` icon that toggles `showAdvanced`. When true, show the Select dropdown for hybrid/bm25/semantic. When false, hide it.
4. **New Note button**: `Button variant="default" size="sm"` with `Plus` icon + "New Note" label. This is the primary CTA.
5. **Smart Capture button**: `Button variant="outline" size="sm"` with `Sparkles` icon (add `className="text-orange-400"` to the icon) + "Smart Capture" label.
6. **Batch button**: `Button variant="ghost" size="sm"` with `FolderUp` icon + "Batch" label.
7. **Connection dot**: 8px circle (`h-2 w-2 rounded-full`). Green-500 when connected, amber-400 when reconnecting (use `connected` boolean — green for true, red-500 for false). Tooltip via `title` attribute.

Keep Enter-to-search and Escape-to-clear keyboard handling.

**TabBar.tsx — rewrite entirely.** Keep the useUIContext hook.

Layout: `h-10 flex items-center border-b border-border bg-card px-2`

Tab config array with icons from lucide-react:
```
Notes: FileText
Actions: CheckSquare
People: Users
Meetings: Calendar
Projects: Briefcase
Intelligence: Brain
Inbox: Inbox
Links: Link
```

Each tab: button with icon (16px) + label text. Gap-1.5 between icon and label.
- Active: `border-b-2 border-primary text-foreground font-semibold`
- Inactive: `text-muted-foreground border-b-2 border-transparent`
- Hover: `hover:text-foreground hover:bg-secondary/50`
- All tabs: `rounded-none h-full px-3 text-sm`

Keep data-testid attributes: `tab-${tab.id}`.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && grep -c "Smart Capture" frontend/src/components/Topbar.tsx && grep -c "FileText\|CheckSquare\|Brain\|Briefcase" frontend/src/components/TabBar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Topbar.tsx contains "Smart Capture" string literal (labeled button)
    - Topbar.tsx contains "Batch" string literal (labeled button)
    - Topbar.tsx contains "New Note" string literal
    - Topbar.tsx contains `SlidersHorizontal` import (advanced toggle icon)
    - Topbar.tsx contains `h-[52px]` (correct height)
    - Topbar.tsx contains `text-orange-400` (Smart Capture icon color)
    - TabBar.tsx imports FileText, CheckSquare, Users, Calendar, Briefcase, Brain, Inbox, Link from lucide-react
    - TabBar.tsx contains `h-10` (correct height)
    - TabBar.tsx contains `bg-card` (per UI-SPEC)
    - TabBar.tsx contains `font-semibold` for active tab
    - Both files retain data-testid attributes
  </acceptance_criteria>
  <done>Topbar shows labeled buttons with hidden search mode; TabBar shows all 8 tabs with icons and active state</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild CommandPalette and update App.tsx shell layout</name>
  <files>frontend/src/components/CommandPalette.tsx, frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/components/CommandPalette.tsx (current implementation, extract note navigation logic)
    - frontend/src/App.tsx (current shell layout)
    - .planning/phases/41-visual-redesign/41-UI-SPEC.md (command palette styling, layout contracts)
    - ui-descriptions/command-palette.md (behavioral spec)
  </read_first>
  <action>
**CommandPalette.tsx — restyle and restructure.** Keep the same Props interface (`open`, `onClose`, `onOpenSmartCapture`, `onOpenNewNote`). Keep all cmdk logic and note navigation.

Styling changes:
- Backdrop: `bg-black/60` (was likely bg-black/50 or similar)
- Panel: `max-w-[600px] w-full bg-popover border border-border rounded-lg shadow-2xl`
- Input: `bg-transparent border-b border-border text-foreground placeholder:text-muted-foreground`
- Group heading: `text-xs text-muted-foreground px-3 py-1.5 font-medium`
- Item: `px-3 py-2 text-sm text-foreground rounded cursor-pointer`
- Selected item: `bg-secondary` (aria-selected or data-selected state)
- Item icon: 16px, text-muted-foreground, mr-2

Keep existing functionality: note search, Smart Capture shortcut, New Note shortcut, note navigation via openNote + setCurrentView('notes').

**App.tsx — update shell layout.** Keep all imports, state, effects, and modal rendering. Update the layout structure:

Root div: `flex flex-col h-screen bg-background text-foreground` (unchanged)

Notes view content area: Remove the inline action bar (Upload + Delete buttons between Topbar and NoteViewer). These actions will be inside NoteViewer in plan 03. For now, render NoteViewer directly:
```tsx
{currentView === 'notes' ? (
  currentNote ? (
    <NoteViewer note={currentNote} />
  ) : (
    <EmptyState
      icon={FileText}
      heading="No note open"
      body="Select a note from the sidebar or create a new one."
      actionLabel="New Note"
      onAction={() => setShowNewNote(true)}
    />
  )
) : ...}
```

Import EmptyState from `@/components/ui/empty-state` and FileText from lucide-react. Remove the Upload/Trash2 icon imports if they are only used in the removed action bar.

Keep the Toaster position as `bottom-right`.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && grep -c "bg-black/60" frontend/src/components/CommandPalette.tsx && grep -c "EmptyState" frontend/src/App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - CommandPalette.tsx contains `bg-black/60` (backdrop)
    - CommandPalette.tsx contains `bg-popover` (panel background)
    - CommandPalette.tsx contains `max-w-[600px]` (panel width)
    - CommandPalette.tsx contains `bg-secondary` (selected item)
    - App.tsx imports EmptyState from `@/components/ui/empty-state`
    - App.tsx imports FileText from lucide-react
    - App.tsx does NOT contain the inline Upload/Delete action bar between topbar and NoteViewer
    - App.tsx contains `heading="No note open"` (empty state for no note selected)
  </acceptance_criteria>
  <done>CommandPalette restyled with dark popover theme; App.tsx shell uses EmptyState for no-note-selected; inline action bar removed (moves to NoteViewer in plan 03)</done>
</task>

</tasks>

<verification>
- Topbar shows 3 labeled buttons: "New Note", "Smart Capture", "Batch"
- TabBar shows 8 tabs with icons
- Command palette opens on Cmd+K with dark overlay
- No note selected shows EmptyState with "No note open" heading
- Vite build succeeds: `cd frontend && npx vite build --mode development 2>&1 | tail -5`
</verification>

<success_criteria>
- App shell matches Visily mockups: 52px topbar, 40px tabbar, dark backgrounds
- All labeled buttons visible in topbar
- All tab icons visible in tab bar
- Command palette styled with popover background and border
- Empty state shown when no note selected
</success_criteria>

<output>
After completion, create `.planning/phases/41-visual-redesign/41-02-SUMMARY.md`

Note: After all tasks complete, run `make dev` on HOST to build frontend, reinstall, and restart services.
</output>
