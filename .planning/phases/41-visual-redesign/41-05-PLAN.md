---
phase: 41-visual-redesign
plan: 05
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - frontend/src/components/ActionsPage.tsx
  - frontend/src/components/InboxPage.tsx
  - frontend/src/components/IntelligencePage.tsx
  - frontend/src/components/LinksPage.tsx
autonomous: true
requirements: []
execution_strategy: direct

must_haves:
  truths:
    - "Actions page shows action items with checkboxes, grouped or filterable by status"
    - "Inbox page shows two-column layout: triage list + preview panel"
    - "Intelligence page shows two-column layout with health score gauge, stale notes, and recap"
    - "Links page shows two-column layout: link list + content preview"
    - "All four pages use EmptyState when no data or no selection"
  artifacts:
    - path: "frontend/src/components/ActionsPage.tsx"
      provides: "Action items list with filtering and batch operations"
      exports: ["ActionsPage"]
    - path: "frontend/src/components/InboxPage.tsx"
      provides: "Inbox triage with unprocessed notes and unassigned actions"
      exports: ["InboxPage"]
    - path: "frontend/src/components/IntelligencePage.tsx"
      provides: "Intelligence dashboard with health, recap, stale notes"
      exports: ["IntelligencePage"]
    - path: "frontend/src/components/LinksPage.tsx"
      provides: "Links list with content preview"
      exports: ["LinksPage"]
  key_links:
    - from: "frontend/src/components/ActionsPage.tsx"
      to: "GET /actions"
      via: "fetch for action items list"
      pattern: "getAPI.*actions"
    - from: "frontend/src/components/IntelligencePage.tsx"
      to: "GET /brain-health"
      via: "fetch for health score and data"
      pattern: "getAPI.*brain-health"
    - from: "frontend/src/components/InboxPage.tsx"
      to: "GET /inbox"
      via: "fetch for inbox data"
      pattern: "getAPI.*inbox"
---

<objective>
Rebuild the Actions, Inbox, Intelligence, and Links pages to match Visily mockups with correct layouts, empty states, and interaction patterns.

Purpose: These 4 remaining pages complete the visual redesign. Actions is full-width; Inbox, Intelligence, and Links use two-column layouts.
Output: 4 rewritten page components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-visual-redesign/41-CONTEXT.md
@.planning/phases/41-visual-redesign/41-UI-SPEC.md
@.planning/phases/41-visual-redesign/41-01-SUMMARY.md
@.claude/LEARNINGS.md
@ui-descriptions/actions-page.md
@ui-descriptions/inbox-page.md
@ui-descriptions/intelligence-page.md
@ui-descriptions/links-page.md
@ui-descriptions/UI-DESIGN-BRIEF.md
@frontend/src/types.ts

<interfaces>
<!-- Shared components from Plan 01 -->
From frontend/src/components/ui/action-item-row.tsx:
  export function ActionItemRow({ item, onToggle, onDelete, onAssign, showSource })

From frontend/src/components/ui/empty-state.tsx:
  export function EmptyState({ icon, heading, body, actionLabel, onAction })

From frontend/src/components/ui/collapsible-section.tsx:
  export function CollapsibleSection({ title, count, sectionId, defaultOpen, children })

From frontend/src/components/ui/health-score-gauge.tsx:
  export function HealthScoreGauge({ score })

From frontend/src/components/ui/skeleton-list.tsx:
  export function SkeletonList({ count, rowHeight })

From frontend/src/components/ui/confirm-dialog.tsx:
  export function ConfirmDialog({ open, onClose, onConfirm, title, description, confirmLabel, cancelLabel, variant })

From frontend/src/components/ui/note-type-badge.tsx:
  export function NoteTypeBadge({ type, className })

<!-- Types -->
From frontend/src/types.ts:
  ActionItem { id, note_path, text, done, due_date, assignee_path }
  BrainHealth { score, total_notes, orphan_count, orphans, empty_count, empty_notes, broken_link_count, broken_links, duplicate_count, duplicate_candidates }
  InboxData { unassigned_actions, unassigned_actions_total, unprocessed_notes, empty_notes, total_count }
  Note { path, title, type, body, tags, people, folder, created_at, updated_at }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild ActionsPage and InboxPage</name>
  <files>frontend/src/components/ActionsPage.tsx, frontend/src/components/InboxPage.tsx</files>
  <read_first>
    - frontend/src/components/ActionsPage.tsx (current API calls, filter logic, action item toggle/delete handlers)
    - frontend/src/components/InboxPage.tsx (current inbox data fetching, triage UI)
    - .planning/phases/41-visual-redesign/41-UI-SPEC.md (action item rows, empty states, copywriting)
    - ui-descriptions/actions-page.md (actions behavioral spec)
    - ui-descriptions/inbox-page.md (inbox behavioral spec)
    - frontend/src/types.ts (ActionItem, InboxData types)
  </read_first>
  <action>
**ActionsPage.tsx — rewrite.** Keep: GET /actions API call, toggle done (PUT), delete (DELETE), assignee update handlers.

Layout: Full-width single column. `flex-1 overflow-y-auto p-6 bg-background`

Structure:
1. **Header**: `flex items-center justify-between mb-6`
   - Title "Action Items" (text-xl font-semibold)
   - "New Action" button (variant=default, size=sm, icon=Plus) — opens inline form or modal
2. **Filter bar**: `flex items-center gap-2 mb-4`
   - Status filter: buttons or toggle for "Open" / "Done" / "All" (use ghost buttons with active state bg-secondary)
   - Assignee filter if applicable
3. **Action items list**: Use ActionItemRow for each item.
   - Set `showSource={true}` so each row shows which note the action came from
   - Group by source note if the API supports grouped endpoint (check if /actions/grouped exists), otherwise flat list
4. **Empty states**:
   - All done: EmptyState icon=CheckCircle2 heading="All clear" body="No open action items. You're on top of things."
   - Filtered empty: EmptyState icon=Filter heading="No matching items" body="Try clearing your filters." actionLabel="Clear filters" onAction={clearFilters}
   - No actions at all: same as all done

Import: CheckCircle2, Filter, Plus from lucide-react.

**InboxPage.tsx — rewrite.** Keep: GET /inbox API call, triage actions.

Two-column layout:
```
<div className="flex flex-1 overflow-hidden">
  <div className="w-80 border-r border-border bg-card flex flex-col overflow-hidden">
    {/* List column */}
  </div>
  <div className="flex-1 bg-background overflow-y-auto p-6">
    {/* Preview/detail column */}
  </div>
</div>
```

List column (w-80):
- Header: "Inbox" title + count badge showing total_count
- Three sections using CollapsibleSection:
  1. "Unassigned Actions" (count: unassigned_actions.length) — each item: ActionItemRow with onAssign handler
  2. "Unprocessed Notes" (count: unprocessed_notes.length) — each item: note title + date, clickable
  3. "Empty Notes" (count: empty_notes.length) — each item: note title, with batch "Delete All" button in section header

Detail column (flex-1):
- When no item selected: EmptyState icon=Inbox heading="Inbox ready" body="Select an item from the list to preview its contents and triage it into your Second Brain."
- When an item is selected: show note preview (title, body rendered as markdown, tags)
- For unassigned actions: show the action item with assignee picker

Loading: SkeletonList while fetching.
When inbox fully clear: EmptyState icon=Inbox heading="Inbox ready" body shown above.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && grep -c "ActionItemRow" frontend/src/components/ActionsPage.tsx && grep -c "w-80" frontend/src/components/InboxPage.tsx && grep -c "EmptyState" frontend/src/components/ActionsPage.tsx && grep -c "EmptyState" frontend/src/components/InboxPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ActionsPage.tsx imports ActionItemRow from @/components/ui/action-item-row
    - ActionsPage.tsx contains `showSource={true}` on ActionItemRow
    - ActionsPage.tsx imports EmptyState with heading="All clear"
    - ActionsPage.tsx contains filter UI with "Open", "Done", "All" options
    - ActionsPage.tsx contains "New Action" button label
    - InboxPage.tsx contains `w-80` (list column width)
    - InboxPage.tsx contains `bg-card` (list column background)
    - InboxPage.tsx imports CollapsibleSection for the three inbox sections
    - InboxPage.tsx imports EmptyState with heading="Inbox ready"
    - InboxPage.tsx imports ActionItemRow for unassigned actions
    - Both pages import SkeletonList for loading states
  </acceptance_criteria>
  <done>ActionsPage rebuilt with ActionItemRow, filters, empty states; InboxPage rebuilt as two-column triage interface with collapsible sections</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild IntelligencePage and LinksPage</name>
  <files>frontend/src/components/IntelligencePage.tsx, frontend/src/components/LinksPage.tsx</files>
  <read_first>
    - frontend/src/components/IntelligencePage.tsx (current health fetch, recap generation, stale notes)
    - frontend/src/components/LinksPage.tsx (current link list, detail view)
    - .planning/phases/41-visual-redesign/41-UI-SPEC.md (intelligence two-column, links two-column, health gauge, empty states)
    - ui-descriptions/intelligence-page.md (intelligence behavioral spec)
    - ui-descriptions/links-page.md (links behavioral spec)
    - frontend/src/types.ts (BrainHealth type)
  </read_first>
  <action>
**IntelligencePage.tsx — rewrite.** Keep: GET /brain-health API call, recap generation (POST /recap or similar), stale notes fetch.

Two unequal columns layout:
```
<div className="flex flex-1 overflow-hidden">
  <div className="flex-[2] overflow-y-auto p-6 bg-background">
    {/* Left column ~65% — Brain Health + Stale Notes */}
  </div>
  <div className="flex-1 border-l border-border bg-card overflow-y-auto p-6">
    {/* Right column ~35% — Recap + Quick Actions */}
  </div>
</div>
```

Left column:
1. **Brain Health card**: `bg-card rounded-lg border border-border p-6 mb-6`
   - HealthScoreGauge showing score
   - Stats row: total notes, orphan count, empty count, broken links, duplicates
   - Each stat: number (text-2xl font-semibold) + label (text-xs text-muted-foreground)
   - Color-coded: orphans/broken links use text-red-400, duplicates use text-amber-400
2. **Stale Notes section**: CollapsibleSection "Stale Notes" with list of stale notes (title + last updated)
3. **Duplicate Candidates**: CollapsibleSection "Potential Duplicates" — show pairs with similarity score

Right column:
1. **Weekly Recap** card: bg-card rounded-lg border p-6
   - "Weekly Recap" heading with orange Brain sparkle accent
   - If no recap: EmptyState icon=Brain heading="No recap generated" body="Generate a weekly recap to see what your brain has been focused on." actionLabel="Generate Recap" onAction={generateRecap}
   - If recap exists: rendered markdown content
2. **Quick Actions** section:
   - "Run Health Check" button (variant=outline, size=sm)
   - "Reindex Brain" button (variant=outline, size=sm) — if endpoint exists

**LinksPage.tsx — rewrite.** Keep: GET /links or similar API call for link notes.

Two-column layout (same w-80 pattern):

List column (w-80):
- Header: "Links" title
- ScrollArea with link rows:
  - Each row: title (text-sm font-medium), URL domain extracted (text-xs text-muted-foreground), date
  - Active: bg-secondary

Detail column (flex-1):
- When no link selected: EmptyState icon=Link heading="Select a link" body="Choose a saved link to view its content and metadata."
- When link selected:
  - Title heading
  - Source URL (clickable, text-primary text-sm, opens in new tab)
  - Tags row
  - Rendered markdown body
  - Delete button with ConfirmDialog
- When list empty: EmptyState icon=Link heading="No saved links" body="Save web links from the Chrome extension or via capture."

Use SkeletonList while loading.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && grep -c "HealthScoreGauge" frontend/src/components/IntelligencePage.tsx && grep -c "w-80" frontend/src/components/LinksPage.tsx && grep -c "EmptyState" frontend/src/components/IntelligencePage.tsx && grep -c "EmptyState" frontend/src/components/LinksPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - IntelligencePage.tsx imports HealthScoreGauge from @/components/ui/health-score-gauge
    - IntelligencePage.tsx contains `flex-[2]` (left column takes ~65%)
    - IntelligencePage.tsx imports CollapsibleSection for Stale Notes and Duplicates sections
    - IntelligencePage.tsx imports EmptyState with heading="No recap generated"
    - IntelligencePage.tsx contains "Generate Recap" button label
    - IntelligencePage.tsx contains `bg-card` for health card and recap card
    - LinksPage.tsx contains `w-80` (list column width)
    - LinksPage.tsx imports EmptyState with heading="Select a link" and heading="No saved links"
    - LinksPage.tsx contains ConfirmDialog import for delete
    - LinksPage.tsx contains `bg-card` (list column background)
    - Both pages import SkeletonList for loading states
  </acceptance_criteria>
  <done>IntelligencePage rebuilt with health gauge, stale notes, recap; LinksPage rebuilt as two-column link browser with preview</done>
</task>

</tasks>

<verification>
- Actions page shows ActionItemRow components with toggle/delete, filter bar
- Inbox shows three collapsible sections in list column
- Intelligence shows health score gauge with colored stats
- Links page shows two-column layout with link preview
- All four pages show correct EmptyState components
- Vite build succeeds: `cd frontend && npx vite build --mode development 2>&1 | tail -5`
</verification>

<success_criteria>
- All 4 pages match Visily mockup layouts
- ActionsPage uses ActionItemRow with source note display
- IntelligencePage shows HealthScoreGauge and recap with correct column proportions
- InboxPage has three collapsible triage sections
- LinksPage has two-column link browser
- All pages have correct empty states per UI-SPEC copywriting
</success_criteria>

<output>
After completion, create `.planning/phases/41-visual-redesign/41-05-SUMMARY.md`
</output>
