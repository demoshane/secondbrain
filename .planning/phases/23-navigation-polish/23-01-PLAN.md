---
phase: 23-navigation-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_api_tags.py
  - engine/api.py
autonomous: true
requirements:
  - GNAV-02
  - GNAV-03

must_haves:
  truths:
    - "PUT /notes/<path> with only `tags` in body updates frontmatter file and DB without full reindex"
    - "POST /search with `tags` param returns only notes containing all specified tags (AND logic)"
    - "GET /notes returns `tags` as a parsed JS array, not a raw JSON string"
  artifacts:
    - path: "tests/test_api_tags.py"
      provides: "Automated tests for tags-only save, tag search filter, list_notes tags parsing"
      exports: ["TestTagsOnlySave", "TestTagSearch", "TestListNotesTags"]
    - path: "engine/api.py"
      provides: "Tags-only PUT path, POST /search tags filter, GET /notes tags parse"
      contains: "tags_val = body.get(\"tags\")"
  key_links:
    - from: "engine/api.py:save_note()"
      to: "notes.tags column"
      via: "UPDATE notes SET tags=? WHERE path=?"
      pattern: "UPDATE notes SET tags"
    - from: "engine/api.py:search()"
      to: "notes.tags column"
      via: "python-side AND filter on json.loads(row[tags])"
      pattern: "all\\(t in note_tags"
---

<objective>
Extend three API endpoints to support tags — tags-only save, tag-filtered search, and tags parsing on list — and create the test file that verifies all three.

Purpose: Establishes the backend contract that the frontend tag chips and tag filter depend on.
Output: tests/test_api_tags.py passing; api.py extended at three endpoints.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-navigation-polish/23-CONTEXT.md
@.planning/phases/23-navigation-polish/23-RESEARCH.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from engine/api.py -->

PUT /notes/<path:note_path>  — currently only accepts {"content": str}
GET /notes                   — currently returns tags as raw JSON string from DB
POST /search                 — currently accepts {"query": str, "mode": str}; no tags param

Existing helpers used inside api.py:
  _resolve_note_path(note_path: str) -> tuple[Path, Path]
  get_connection() -> sqlite3.Connection
  suppress_next_delete(path: str) -> None
  _fm.loads(text: str) -> frontmatter.Post
  _fm.dumps(post: frontmatter.Post) -> str

DB schema (confirmed engine/db.py):
  notes.tags TEXT NOT NULL DEFAULT '[]'   -- JSON array string

tests/conftest.py fixtures available:
  client      — Flask test client
  tmp_path    — pytest tmp dir
  monkeypatch — pytest monkeypatch
  (existing tests in tests/ use BRAIN_PATH monkeypatch pattern from Phase 22)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test scaffold — tests/test_api_tags.py</name>
  <files>tests/test_api_tags.py</files>
  <behavior>
    - TestListNotesTags: GET /notes returns {"notes": [...]} where each note.tags is a Python list (not a string)
    - TestTagsOnlySave.test_tags_only_updates_file: PUT /notes/<path> with body {"tags": ["work","idea"]} returns 200; re-reading the file shows updated YAML frontmatter tags
    - TestTagsOnlySave.test_tags_updates_db: after the PUT, SELECT tags FROM notes WHERE path=? returns '["work","idea"]'
    - TestTagsOnlySave.test_content_unchanged: a PUT with only tags does not alter the note body
    - TestTagSearch.test_filter_returns_matching: POST /search {"query":"", "tags":["work"]} returns only notes that have "work" in their tags array
    - TestTagSearch.test_and_logic: POST /search {"query":"some text", "tags":["work"]} returns intersection (must match both)
    - TestTagSearch.test_no_tag_param: POST /search without tags param still returns full results (backward compatible)
  </behavior>
  <action>
    Create tests/test_api_tags.py with three test classes. Use the BRAIN_PATH monkeypatch pattern from Phase 22 (tests/test_delete.py) to set a tmp brain directory. Create fixture notes with known tags in the DB and on-disk frontmatter. All tests should FAIL before Task 2 implements the endpoints — this is the RED phase.

    Write test for GET /notes tags parsing first (simplest). Then tags-only PUT. Then POST /search tag filter. Use json.loads to assert tags column values in DB assertions.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_api_tags.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>tests/test_api_tags.py exists; running the suite produces FAILED (not import errors or syntax errors) — RED phase confirmed</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Backend — extend api.py at three endpoints (GREEN)</name>
  <files>engine/api.py</files>
  <behavior>
    - GET /notes: each note dict has tags parsed from JSON string to list before jsonify
    - PUT /notes/<path> tags-only path: when body has "tags" key and no "content" key, read file with _fm.loads, set post.metadata["tags"] = tags_val, write back with _fm.dumps via tempfile+os.replace, call suppress_next_delete, UPDATE notes SET tags=?, updated_at=? WHERE path=?, return {"saved": True, "path": ...}
    - POST /search: after getting results from search_notes(), if body has "tags" key, filter results to only those where all specified tags appear in the note's tags column (json.loads); no tag param = no filtering (backward compat)
  </behavior>
  <action>
    Extend three existing functions in engine/api.py:

    1. list_notes() at GET /notes:
       - In the row-to-dict loop, add: d["tags"] = json.loads(d.get("tags") or "[]")

    2. save_note() at PUT /notes/<path:note_path>:
       - After parsing body, add tags-only branch BEFORE the existing content branch:
         tags_val = body.get("tags")
         if tags_val is not None and "content" not in body:
             [implement tags-only update using _fm.loads/_fm.dumps + tempfile + os.replace + suppress_next_delete + targeted DB UPDATE]
             return jsonify({"saved": True, "path": str(p)})
       - Ensure the existing content-save path is untouched below this branch.

    3. search() at POST /search:
       - After obtaining results list, add:
         tags_filter = body.get("tags")  # list[str] | None
         if tags_filter:
             conn2 = get_connection()
             [filter results: for each r, fetch tags from DB, json.loads, check all(t in note_tags for t in tags_filter)]
             conn2.close()
             results = filtered
       - Return filtered results in the existing jsonify call.

    Use import json (already imported as json in api.py). Use datetime.datetime.utcnow().isoformat() for updated_at.
    Do NOT modify any other endpoints.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_api_tags.py -x -q</automated>
  </verify>
  <done>All tests in tests/test_api_tags.py pass (GREEN). Full suite still green: uv run pytest -x -q</done>
</task>

</tasks>

<verification>
uv run pytest tests/test_api_tags.py -v
uv run pytest -x -q
</verification>

<success_criteria>
1. tests/test_api_tags.py passes all cases: GET /notes tags parsed as array, PUT tags-only updates file+DB, POST /search tags AND filter works
2. Existing tests unaffected (full suite green)
3. No new dependencies introduced
</success_criteria>

<output>
After completion, create .planning/phases/23-navigation-polish/23-01-SUMMARY.md
</output>
