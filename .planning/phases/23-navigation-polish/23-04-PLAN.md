---
phase: 23-navigation-polish
plan: "04"
type: execute
wave: 4
depends_on:
  - "23-03"
files_modified: []
autonomous: false
requirements:
  - GNAV-01
  - GNAV-02
  - GNAV-03

must_haves:
  truths:
    - "All three phase success criteria verified by a human user"
    - "No regressions in existing functionality (note open, edit, save, delete, SSE refresh)"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete Phase 23 feature set: sidebar hierarchy, tag chip editing, and tag filter.

Purpose: Nyquist sign-off — confirm all three requirements (GNAV-01, GNAV-02, GNAV-03) work end-to-end in the live GUI.
Output: Human approval or issues list for gap closure.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/23-navigation-polish/23-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite sign-off</name>
  <files>tests/</files>
  <action>Run the full test suite and confirm green. Fix any regressions before proceeding to the checkpoint.</action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest -x -q</automated>
  </verify>
  <done>All tests pass with 0 failures.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — all Phase 23 features</name>
  <action>Present the verification checklist to the user and wait for approval or issue report.</action>
  <what-built>
    Plan 23-01: Backend — tags-only PUT, tag-filtered POST /search, GET /notes tags parsed as arrays
    Plan 23-02: Sidebar hierarchy — Recent section + folder > type two-level grouping with collapse/expand (localStorage persisted)
    Plan 23-03: Tag chips in viewer — display, double-click inline edit, single-click tag filter, filter banner with x to clear
  </what-built>
  <how-to-verify>
    Start the GUI: cd /Users/tuomasleppanen/second-brain && uv run sb-gui

    **GNAV-01 — Sidebar hierarchy:**
    1. Open the GUI. Confirm the sidebar shows a "Recent (N)" section at the top.
    2. Confirm notes are grouped below by folder name (e.g. "projects/ (3)") then by type within folder.
    3. Click a folder header — confirm it collapses. Click again — expands.
    4. Reload the page — confirm collapsed/expanded state is restored.

    **GNAV-02 — Tag editing:**
    5. Click any note that has tags. Confirm tag chips appear below the title (e.g. "#work #idea + Add tag").
    6. Double-click a tag chip. Confirm it transforms into an inline text input.
    7. Type a new tag name, press Enter. Confirm chip updates immediately.
    8. Reopen the note (click away then back) — confirm the new tag is still shown.
    9. Click "+ Add tag", type a new tag, Enter. Confirm it appears as a chip.
    10. Press Escape during edit — confirm chip reverts without saving.

    **GNAV-03 — Tag filter:**
    11. Single-click a tag chip. Confirm the sidebar switches to a flat list of notes with that tag.
    12. Confirm a filter banner appears above the note list showing "Filtering: #tagname x".
    13. Type something in the search box while the filter is active. Confirm results are narrowed (AND logic).
    14. Click x in the filter banner. Confirm the sidebar returns to the hierarchy view.

    **Regression check:**
    15. Open a note, edit it, save with Cmd+S. Confirm save still works.
    16. Delete a note via the delete button — confirm it disappears from the sidebar.
    17. Create a note via the + button — confirm it appears in the sidebar.
  </how-to-verify>
  <verify>
    <automated>MISSING — human verification required; no automated substitute</automated>
  </verify>
  <done>Human types "approved" confirming all 17 steps pass.</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
Human verified all three requirements (GNAV-01, GNAV-02, GNAV-03) and regression checks.
</verification>

<success_criteria>
1. Human approves all 17 verification steps
2. No regressions found in existing features
3. Phase 23 marked complete in ROADMAP.md
</success_criteria>

<output>
After completion, create .planning/phases/23-navigation-polish/23-04-SUMMARY.md
</output>
