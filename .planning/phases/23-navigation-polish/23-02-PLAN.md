---
phase: 23-navigation-polish
plan: "02"
type: execute
wave: 2
depends_on:
  - "23-01"
files_modified:
  - engine/gui/static/app.js
  - engine/gui/static/style.css
autonomous: true
requirements:
  - GNAV-01

must_haves:
  truths:
    - "Sidebar shows a 'Recent' section at the top (last 10 notes, flat list, collapsible)"
    - "Below Recent, notes are grouped by folder (top-level brain directory) then by type within each folder"
    - "Each folder header and each type-within-folder header has an independent collapse/expand toggle"
    - "Collapse state persists in localStorage and is restored on page reload"
    - "Search results and tag-filtered results still display as a flat list (no hierarchy)"
  artifacts:
    - path: "engine/gui/static/app.js"
      provides: "renderHierarchySidebar(), renderFlatList(), folderName(), getCollapseState(), setCollapseState()"
      contains: "sb-sidebar-collapse"
    - path: "engine/gui/static/style.css"
      provides: ".folder-header, .type-header, .collapse-toggle, .section-count"
      contains: ".folder-header"
  key_links:
    - from: "app.js:loadNotes()"
      to: "renderHierarchySidebar(notes)"
      via: "replaces the renderSidebar(notes) call in default browse mode"
      pattern: "renderHierarchySidebar"
    - from: "app.js:runSearch()"
      to: "renderFlatList(notes)"
      via: "search results always use flat list"
      pattern: "renderFlatList"
---

<objective>
Refactor the sidebar to show a two-level folder > type hierarchy with collapsible sections, persisted via localStorage. Search results continue to use a flat list.

Purpose: Satisfies GNAV-01 — users can navigate their brain by folder structure, not just type.
Output: app.js with new hierarchy rendering; style.css with new header/toggle classes.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-navigation-polish/23-CONTEXT.md
@.planning/phases/23-navigation-polish/23-RESEARCH.md
@engine/gui/static/app.js
@engine/gui/static/style.css

<interfaces>
<!-- Existing renderSidebar() to replace — from app.js lines 21-46 -->
<!-- Currently: groups by type only, renders into #note-list ul -->

Current renderSidebar signature:
  function renderSidebar(notes)   // notes = [{path, title, type, tags, ...}]

Called from:
  loadNotes()      — line 18  → replace with renderHierarchySidebar(notes)
  runSearch()      — line 165 → keep as renderFlatList(notes) (flat, same as current)

Note object shape (after 23-01, tags is now a parsed array):
  { path: "/abs/path/to/file.md", title: "...", type: "note"|"idea"|..., tags: [...] }

Target DOM:
  #note-list ul
    ├── .folder-section[data-folder="recent"]     ← "Recent" always first
    │     .folder-header  "▼ Recent (10)"
    │     ul.folder-notes > li[data-path]
    ├── .folder-section[data-folder="projects"]
    │     .folder-header  "▼ projects/ (5)"
    │     .type-section[data-type-key="projects::note"]
    │         .type-header  "▼ note (3)"
    │         ul.type-notes > li[data-path]
    └── ...

localStorage schema (RESEARCH.md Pattern 2):
  key: "sb-sidebar-collapse"
  value: JSON {"projects": false, "projects::note": true, "recent": false}
  false = expanded (default), true = collapsed
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor app.js — hierarchy sidebar + collapse state</name>
  <files>engine/gui/static/app.js</files>
  <action>
    Replace renderSidebar() with two functions:

    **renderFlatList(notes)** — identical to current renderSidebar() behavior (type-grouped flat list). Used by runSearch() and (in Plan 23-03) tag filter mode.

    **renderHierarchySidebar(notes)** — new 2-level hierarchy:
    1. Extract folder name from each note path: `parts[parts.length - 2] || 'other'`
    2. Build "Recent" section: slice first 10 from notes array (server already returns created_at DESC order). Render as flat collapsible section.
    3. Build folder > type map: `{ folderName: { typeName: [notes] } }`
    4. For each folder, render a `.folder-section` with a `.folder-header` showing `▼/▶ folderName/ (total_count)`. Clicking toggles collapse state and persists via setCollapseState(folderName, collapsed).
    5. Inside each folder, for each type group, render a `.type-section` with a `.type-header` showing `▼/▶ typeName (count)`. Key: `folderName::typeName`. Clicking toggles independently.
    6. Each note renders as `<li data-path="{path}">{title}</li>` with click → openNote(path).

    **getCollapseState() / setCollapseState(key, collapsed)** — localStorage helpers using key `"sb-sidebar-collapse"` (see RESEARCH.md Pattern 2). Wrap in try/catch for private-mode browsers.

    Update loadNotes() line 18: change `renderSidebar(notes)` → `renderHierarchySidebar(notes)`.
    Update runSearch() line 165: change `renderSidebar(...)` → `renderFlatList(...)`.

    Keep existing note click handler pattern (`li.addEventListener('click', () => openNote(n.path))`).

    Do NOT remove or rename any other existing functions — other plans depend on openNote, saveNote, etc.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    No test regressions. Manual: open sb-gui, sidebar shows "Recent" at top, then folder sections (e.g. "projects/ (N)"), clicking a folder header collapses it, reload restores collapsed state.
  </done>
</task>

<task type="auto">
  <name>Task 2: Style — folder/type headers and collapse toggles</name>
  <files>engine/gui/static/style.css</files>
  <action>
    Add the following CSS classes to style.css (append after existing rules, do not touch existing selectors):

    **.folder-header** — clickable section header for top-level folders. Cursor pointer, slightly bolder weight, padding matches existing `.type-group-header` but with a subtle left accent (e.g. 3px left border in the accent color). Show count via `.section-count` span (muted color, font-size smaller). Include `user-select: none`.

    **.type-header** — clickable sub-section header for type-within-folder. Indented (padding-left: 1.2rem), lighter weight than folder-header, same cursor pointer and user-select rule.

    **.collapse-toggle** — the ▼/▶ indicator character span inside headers. Inline-block, small font, transitions rotation if animation is desired (Claude's discretion per CONTEXT.md).

    **.folder-section.collapsed > ul, .type-section.collapsed > ul** — `display: none` to hide collapsed content.

    **.section-count** — muted color (var(--muted) or similar), font-size: 0.8em, margin-left: 0.3em.

    Style should be consistent with the existing dark-theme variables already in style.css. Use existing CSS variables (--bg, --sidebar-bg, --text, --muted, --accent, etc.) — do not hardcode colors.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>style.css contains .folder-header, .type-header, .collapse-toggle, .folder-section.collapsed > ul { display: none }. Full test suite still green.</done>
</task>

</tasks>

<verification>
uv run pytest -x -q
Manual: Open GUI, confirm sidebar hierarchy with Recent + folder sections; collapse/expand toggles work; reload restores state.
</verification>

<success_criteria>
1. Sidebar shows Recent (top, 10 items) then folder > type hierarchy in default browse mode
2. Each level independently collapses with ▼/▶ toggle
3. Collapse state survives page reload (localStorage)
4. Search results still show flat list (renderFlatList path)
5. No test regressions
</success_criteria>

<output>
After completion, create .planning/phases/23-navigation-polish/23-02-SUMMARY.md
</output>
