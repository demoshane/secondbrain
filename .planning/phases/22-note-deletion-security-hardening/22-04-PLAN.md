---
phase: 22-note-deletion-security-hardening
plan: 04
type: execute
wave: 3
depends_on:
  - "22-03"
files_modified: []
autonomous: false
requirements:
  - GUIX-06

must_haves:
  truths:
    - "User can visually see the red Delete button in the viewer toolbar"
    - "Confirmation modal shows the correct note filename"
    - "Note disappears from sidebar immediately after confirming deletion"
    - "Viewer shows 'Note deleted' message then clears to blank state"
    - "Deleted note does not reappear after the SSE watcher fires"
    - "Watcher does not re-index the deleted note (no false-positive entry)"
  artifacts:
    - path: "engine/gui/static/index.html"
      provides: "delete button and modal confirmed present"
    - path: "engine/gui/static/app.js"
      provides: "delete flow confirmed functional end-to-end"
  key_links:
    - from: "GUI delete button"
      to: "DELETE /notes/<path> API"
      via: "user interaction → modal confirm → fetch"
      pattern: "manual verification"
---

<objective>
Human verification of the complete deletion flow and security guard, plus full test suite sign-off.

Purpose: Confirm the GUI delete UX works end-to-end and no watcher race condition fires after deletion.
Output: Verified phase, VALIDATION.md updated to nyquist_compliant: true.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-note-deletion-security-hardening/22-CONTEXT.md
@.planning/phases/22-note-deletion-security-hardening/22-VALIDATION.md
@.planning/phases/22-note-deletion-security-hardening/22-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite sign-off</name>
  <files></files>
  <action>
Run the full test suite and verify all tests pass:
`uv run pytest -x -q`

If any tests fail, fix them before proceeding to the human-verify checkpoint. Pay particular attention to:
- tests/test_delete.py — all 12 tests must be green
- tests/test_api.py — no regressions from api.py changes
- tests/test_api_sse.py — no regressions from watcher changes
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>Full test suite passes with no failures</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete note deletion flow:
- Red "Delete" button in viewer toolbar (view mode only, hidden when editing)
- Confirmation modal with note filename + warning + red Delete / grey Cancel buttons
- Optimistic sidebar removal on confirm
- Transient "Note deleted" message (~2s), then viewer clears
- DELETE /notes/{path} API with path traversal 403 guard
- delete_note() cascade: file + notes + note_embeddings + relationships + action_items + audit_log
- suppress_next_delete() prevents false-positive watcher SSE event after deletion
  </what-built>
  <how-to-verify>
Start the GUI: `cd /Users/tuomasleppanen/second-brain && uv run sb-gui`

1. Open a note in the viewer. Verify the red "Delete" button appears on the right side of the toolbar.
2. Click the "Edit" button. Verify the Delete button disappears in edit mode. Click "Cancel" to exit edit mode — verify Delete button reappears.
3. With a note open in view mode, click "Delete". Verify the confirmation modal appears and shows the note's filename prominently.
4. Click "Cancel". Verify the modal closes and the note is unchanged.
5. Open a different (test/throwaway) note, click "Delete", click the red "Delete" button in the modal. Verify:
   a. Modal closes immediately.
   b. Note disappears from the sidebar immediately (before page refresh).
   c. Viewer shows "Note deleted." in italic for about 2 seconds, then clears.
   d. The note does NOT reappear in the sidebar after 3 seconds (watcher suppression working).
6. Search for the deleted note's title via the search box. Verify it returns no results.
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 checks pass, or describe any issues found.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update VALIDATION.md to nyquist_compliant</name>
  <files>.planning/phases/22-note-deletion-security-hardening/22-VALIDATION.md</files>
  <action>
Update the VALIDATION.md frontmatter:
- Set `nyquist_compliant: true`
- Set `wave_0_complete: true`
- Update all task rows in the Per-Task Verification Map from `⬜ pending` to `✅ green`
- Set `status: complete` in frontmatter
- Update Approval line at the bottom: `**Approval:** {date} — human verified`
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && grep "nyquist_compliant: true" .planning/phases/22-note-deletion-security-hardening/22-VALIDATION.md</automated>
  </verify>
  <done>VALIDATION.md shows nyquist_compliant: true and all task rows green</done>
</task>

</tasks>

<verification>
uv run pytest -x -q
# Full suite green

Manual GUI verification (6 checks above) — approved by user.
</verification>

<success_criteria>
- Full pytest suite passes (no failures)
- Human verified: Delete button visible in view mode, hidden in edit mode
- Human verified: Modal shows correct filename, Cancel leaves note intact
- Human verified: Confirm deletes note, removes from sidebar immediately, shows transient message
- Human verified: Deleted note does not reappear from watcher (suppress working)
- VALIDATION.md updated to nyquist_compliant: true
</success_criteria>

<output>
After completion, create `.planning/phases/22-note-deletion-security-hardening/22-04-SUMMARY.md`
</output>
