---
phase: 39-codebase-review
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/39-codebase-review/39-findings-architecture.md
autonomous: true
execution_strategy: multi-agent
requirements: []

must_haves:
  truths:
    - "Backend module coupling and circular imports reviewed"
    - "Dual-write consistency (JSON columns vs junction tables) verified"
    - "Dead modules identified with import evidence"
    - "api.py size and structure assessed for maintainability"
  artifacts:
    - path: ".planning/phases/39-codebase-review/39-findings-architecture.md"
      provides: "Architecture dimension findings"
      contains: "ARCH-"
  key_links: []
---

<objective>
Architecture audit of backend module structure, coupling, patterns, and dead code.

Purpose: Identify structural issues — circular imports, dead modules, dual-write inconsistencies, oversized files, deprecated patterns left from 38 phases of iterative development.
Output: 39-findings-architecture.md with severity-ranked findings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-codebase-review/39-CONTEXT.md
@.planning/phases/39-codebase-review/39-RESEARCH.md
@.claude/LEARNINGS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Architecture audit — module structure, coupling, dead code</name>
  <read_first>
    engine/db.py
    engine/capture.py
    engine/paths.py
    engine/api.py
    engine/mcp_server.py
    engine/forget.py
    engine/rag.py
    engine/ai.py
    engine/templates.py
    engine/ratelimit.py
    engine/consolidate.py
    engine/brain_health.py
    engine/health.py
    engine/backup.py
    engine/sharding.py
    engine/config_loader.py
    engine/attachments.py
    engine/merge_cli.py
    engine/links.py
    engine/link_capture.py
    pyproject.toml
  </read_first>
  <files>.planning/phases/39-codebase-review/39-findings-architecture.md</files>
  <action>
    Systematically audit backend architecture. Per D-04, focus on coupling, circular imports, dead modules, dual-write consistency.

    **Dead code analysis:**
    - For each module: grep all engine/*.py and tests/*.py for imports of that module
    - Cross-reference with pyproject.toml [project.scripts] for CLI entry points
    - Pre-identified candidates: rag.py (D-01), templates.py (D-02), ratelimit.py (D-04), ai.py (D-03)
    - For each candidate: document import count, callers, and whether it's reachable in production

    **Dual-write consistency (A-01):**
    - Verify that every write to `notes.tags` JSON column also writes to `note_tags` junction table (and vice versa)
    - Verify that every write to `notes.people` JSON column also writes to `note_people` junction table
    - Check if any read path uses JSON column when it should use junction table (or vice versa)

    **Module coupling:**
    - Map import graph: which modules import which? Any circular dependencies?
    - Verify lazy imports (consolidate.py -> brain_health per A-04) are still necessary
    - Late BRAIN_ROOT imports inside api.py functions (A-03, A-06): are they still needed?

    **Structural concerns:**
    - api.py at 1754 lines (A-05): identify natural grouping boundaries
    - Duplicate imports in api.py (A-03/D-05)
    - Any functions >100 lines that should be split?

    **Frontend dead code:**
    - Check if NoteEditor.tsx (D-07) is imported by any component
    - Any other React components not reachable from App.tsx?

    Write findings to `39-findings-architecture.md`:
    ```
    # Architecture Audit Findings

    ## Finding: ARCH-NN
    - **Severity:** Critical/High/Medium/Low
    - **File:** path:line
    - **Description:** what's wrong
    - **Root cause:** why
    - **Recommended fix:** specific action
    - **Blast radius:** what breaks if fixed wrong
    ```
  </action>
  <verify>
    <automated>test -f .planning/phases/39-codebase-review/39-findings-architecture.md && grep -c "ARCH-" .planning/phases/39-codebase-review/39-findings-architecture.md</automated>
  </verify>
  <acceptance_criteria>
    - 39-findings-architecture.md exists
    - Pre-identified items A-01 through A-05 and D-01 through D-07 from RESEARCH.md addressed
    - Dead code analysis includes import count evidence for each candidate module
    - Dual-write paths verified with specific function:line references
  </acceptance_criteria>
  <done>Architecture findings documented with dead code evidence, dual-write verification, and coupling analysis</done>
</task>

</tasks>

<verification>
- 39-findings-architecture.md exists with structured findings
- Dead code candidates verified with grep evidence
- Dual-write consistency checked with specific code references
</verification>

<success_criteria>
Complete architecture audit covering module coupling, dead code, dual-write consistency, and structural concerns.
</success_criteria>

<output>
After completion, create `.planning/phases/39-codebase-review/39-02-SUMMARY.md`
</output>
