---
phase: 39-codebase-review
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/39-codebase-review/39-findings-performance.md
autonomous: true
execution_strategy: multi-agent
requirements: []

must_haves:
  truths:
    - "N+1 query patterns identified with call frequency context"
    - "Unbounded loops and full-directory walks flagged with scale impact"
    - "Missing DB indexes identified"
    - "ANN fallback cost and search performance reviewed"
  artifacts:
    - path: ".planning/phases/39-codebase-review/39-findings-performance.md"
      provides: "Performance dimension findings"
      contains: "PERF-"
  key_links: []
---

<objective>
Performance audit of search, query patterns, and scale-sensitive code paths.

Purpose: Identify N+1 queries, unbounded loops, missing indexes, and operations that degrade at 100K+ notes (Phase 38 scale target).
Output: 39-findings-performance.md with severity-ranked findings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-codebase-review/39-CONTEXT.md
@.planning/phases/39-codebase-review/39-RESEARCH.md
@.claude/LEARNINGS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Performance audit — queries, loops, indexes, scale bottlenecks</name>
  <read_first>
    engine/api.py
    engine/search.py
    engine/ann_index.py
    engine/embeddings.py
    engine/intelligence.py
    engine/brain_health.py
    engine/reindex.py
    engine/consolidate.py
    engine/db.py
    engine/health.py
    engine/digest.py
  </read_first>
  <files>.planning/phases/39-codebase-review/39-findings-performance.md</files>
  <action>
    Systematically audit performance-sensitive code paths. Per D-04, focus on N+1 queries, unbounded loops, missing indexes.

    **Query patterns (api.py + intelligence.py):**
    - Find all DB query loops: function fetches list, then queries per item
    - Pre-identified: P-01 (note_meta multiple separate DB calls for backlinks/related/people)
    - Check if any endpoint calls note_meta in a loop (list views calling per-note metadata)
    - Review pagination: are all list queries using LIMIT/OFFSET?

    **Unbounded operations:**
    - Pre-identified: P-02 (rglob("*.md") in capture endpoint — walks entire brain dir)
    - Find all `os.walk`, `rglob`, `glob` calls — are they bounded? Do they use DB index instead?
    - Intelligence functions: any that scan all notes without pagination?
    - brain_health.py: health checks that touch every note — what's the time complexity?

    **Missing indexes:**
    - Review db.py CREATE TABLE and migration statements for index coverage
    - Common query patterns (WHERE path=?, WHERE note_type=?, WHERE created_at>?) — are they indexed?
    - FTS5 table coverage: are all text-search queries using the FTS5 index?

    **Search performance:**
    - ANN fallback cost (P-04): verify hnswlib → sqlite-vec fallback is bounded
    - Hybrid search RRF merge: is it O(n) or O(n log n)?
    - Chunk-level search: does it degrade with many chunks per note?

    **Scale-sensitive paths:**
    - Reindex: what's the complexity of a full reindex at 100K notes?
    - Consolidation: does the scheduled job have bounded execution time?

    Write findings to `39-findings-performance.md`:
    ```
    # Performance Audit Findings

    ## Finding: PERF-NN
    - **Severity:** Critical/High/Medium/Low
    - **File:** path:line
    - **Description:** what's wrong
    - **Scale impact:** at what note count does this become a problem?
    - **Root cause:** why
    - **Recommended fix:** specific action
    ```
  </action>
  <verify>
    <automated>test -f .planning/phases/39-codebase-review/39-findings-performance.md && grep -c "PERF-" .planning/phases/39-codebase-review/39-findings-performance.md</automated>
  </verify>
  <acceptance_criteria>
    - 39-findings-performance.md exists
    - Pre-identified items P-01 through P-04 from RESEARCH.md addressed
    - Each finding includes scale impact estimate (note count threshold)
    - N+1 patterns identified with caller context (is it called in a loop?)
  </acceptance_criteria>
  <done>Performance findings documented with scale impact estimates covering queries, loops, indexes, and search paths</done>
</task>

</tasks>

<verification>
- 39-findings-performance.md exists with structured findings
- Scale-sensitive operations identified with complexity analysis
</verification>

<success_criteria>
Complete performance audit covering query patterns, unbounded operations, missing indexes, and scale bottlenecks.
</success_criteria>

<output>
After completion, create `.planning/phases/39-codebase-review/39-03-SUMMARY.md`
</output>
