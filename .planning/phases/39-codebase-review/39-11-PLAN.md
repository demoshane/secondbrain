---
phase: 39-codebase-review
plan: 11
type: execute
wave: 5
depends_on: ["39-08"]
gap_closure: true
files_modified:
  - engine/templates.py
  - tests/test_capture.py
  - engine/api.py
autonomous: true
execution_strategy: direct
requirements: []
must_haves:
  truths:
    - "engine/templates.py does not exist"
    - "No test references load_template or render_template"
    - "Late BRAIN_ROOT re-imports removed or justified"
  artifacts:
    - path: "tests/test_capture.py"
      provides: "Dead test block removed"
      contains: "# templates.py tests removed"
  key_links: []
---

<objective>
Remove confirmed dead code: templates.py module and associated test, plus late BRAIN_ROOT re-imports.

Purpose: Close F-02 (dead templates.py) and F-11 (late BRAIN_ROOT re-imports in api.py). Both are dead code that adds maintenance burden and confusion.
Output: Deleted templates.py, cleaned test_capture.py, cleaned api.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-codebase-review/39-FINDINGS.md
@.planning/phases/39-codebase-review/39-REMEDIATION-SCOPE.md
@.claude/LEARNINGS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete templates.py, remove dead test, remove late BRAIN_ROOT re-imports</name>
  <read_first>engine/templates.py, tests/test_capture.py, engine/api.py</read_first>
  <files>engine/templates.py, tests/test_capture.py, engine/api.py</files>
  <action>
1. **Verify templates.py is dead** (F-02):
   Run `grep -rn "load_template\|render_template\|from engine.templates\|import templates" engine/` — confirm 0 results (exclude templates.py itself). If any live caller exists, STOP and report.

2. **Delete engine/templates.py**:
   Remove the file entirely.

3. **Remove the dead test block** in tests/test_capture.py around lines 83-88:
   Find the test that imports from `engine.templates` (e.g., `test_load_template` or similar). Delete the entire test function/block. Add a comment where it was: `# templates.py tests removed — module deleted in Phase 39 remediation`

4. **Handle late BRAIN_ROOT re-imports** (F-11) in engine/api.py at lines ~1194, ~1584, ~1640:
   Search for `from engine.paths import BRAIN_ROOT` inside function bodies (not the module-level import).

   First, check if any tests monkeypatch `engine.paths.BRAIN_ROOT` for these specific endpoints:
   ```bash
   grep -rn "monkeypatch.*BRAIN_ROOT\|patch.*BRAIN_ROOT" tests/
   ```

   - If tests DO monkeypatch `engine.paths.BRAIN_ROOT`: the late imports exist to pick up the patched value. In this case, replace the late import with direct reference to the module-level import (already imported at top). Update the test fixtures to patch `engine.api.BRAIN_ROOT` instead, or use `BRAIN_PATH` env var pattern.
   - If tests do NOT monkeypatch: remove all 3 late imports. The module-level import is sufficient.

   After removal, run `uv run pytest tests/ -q --tb=short` to verify nothing breaks.
  </action>
  <acceptance_criteria>
    - `ls engine/templates.py 2>/dev/null` returns nothing (file deleted)
    - `grep -rn "from engine.templates" tests/` returns 0 matches
    - `grep -c "from engine.paths import BRAIN_ROOT" engine/api.py` returns exactly 1 (the module-level import only)
    - `uv run pytest tests/ -q --tb=short` passes
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && ! test -f engine/templates.py && echo "templates.py deleted" && uv run pytest tests/ -q --tb=short -x 2>&1 | tail -20</automated>
  </verify>
  <done>templates.py deleted. Dead test removed. Late BRAIN_ROOT re-imports handled (removed or justified). All tests pass.</done>
</task>

</tasks>

<verification>
```bash
# templates.py gone
test -f engine/templates.py && echo "FAIL: still exists" || echo "OK: deleted"
# No references to dead module
grep -rn "from engine.templates" engine/ tests/  # expect 0
# Only one BRAIN_ROOT import in api.py (module-level)
grep -c "from engine.paths import BRAIN_ROOT" engine/api.py  # expect 1
# Full test suite
uv run pytest tests/ -q --tb=short
```
</verification>

<success_criteria>
- F-02: engine/templates.py deleted, associated test removed
- F-11: Late BRAIN_ROOT re-imports at api.py:1194, 1584, 1640 removed (or documented if test-required)
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/39-codebase-review/39-11-SUMMARY.md`
</output>
