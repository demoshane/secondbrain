---
phase: 39-codebase-review
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/39-codebase-review/39-findings-deadcode.md
autonomous: true
execution_strategy: multi-agent
requirements: []

must_haves:
  truths:
    - "Every deprecated function identified with caller evidence"
    - "Unreachable code paths documented with import graph proof"
    - "Duplicate logic across modules identified"
    - "Frontend dead components verified against App.tsx import tree"
  artifacts:
    - path: ".planning/phases/39-codebase-review/39-findings-deadcode.md"
      provides: "Dead code and optimisation findings"
      contains: "DEAD-"
  key_links: []
---

<objective>
Dead code and optimisation audit across backend and frontend.

Purpose: Find deprecated functions, unreachable code paths, duplicate logic, and simplification opportunities left from 38 phases of iterative development.
Output: 39-findings-deadcode.md with severity-ranked findings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-codebase-review/39-CONTEXT.md
@.planning/phases/39-codebase-review/39-RESEARCH.md
@.claude/LEARNINGS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dead code + optimisation audit — stale paths, unused functions, duplication</name>
  <read_first>
    engine/rag.py
    engine/ai.py
    engine/templates.py
    engine/ratelimit.py
    engine/classifier.py
    engine/segmenter.py
    engine/links.py
    engine/link_capture.py
    engine/people.py
    engine/entities.py
    frontend/src/App.tsx
    frontend/src/components/NoteEditor.tsx
    pyproject.toml
  </read_first>
  <files>.planning/phases/39-codebase-review/39-findings-deadcode.md</files>
  <action>
    Systematically audit for dead code and optimisation opportunities. Per D-04, scan for stale code paths, unused functions, and duplicate logic.

    **Dead module candidates (from RESEARCH.md):**
    - rag.py (D-01): grep all engine/ and tests/ for `from engine.rag` or `import rag`. Check if augment_prompt is called in any live path.
    - templates.py (D-02): grep for imports. Only test_capture.py imports it? If engine code doesn't use it, it's dead.
    - ratelimit.py (D-04): grep for imports. If zero engine imports, it's dead.
    - ai.py (D-03): grep for imports. Check ask_followup_questions and update_memory callers. Cross-reference pyproject.toml scripts.

    **CRITICAL: Before marking ANY module dead:**
    1. grep engine/ for `from engine.{module}` AND `import {module}`
    2. grep tests/ for same
    3. Check pyproject.toml [project.scripts] for CLI entry points referencing it
    4. Check for dynamic imports: `importlib`, `__import__`, lazy imports inside function bodies

    **Dead functions within live modules:**
    - For each engine module: are there exported functions that no other module calls?
    - Focus on api.py helper functions, intelligence.py utility functions
    - Check for functions defined but only used in commented-out code

    **Frontend dead code:**
    - NoteEditor.tsx (D-07): grep all .tsx files for "NoteEditor"
    - Any components not reachable from App.tsx's import tree?
    - Any unused CSS classes, dead routes, or stale event handlers?

    **Duplicate logic:**
    - Same pattern implemented in multiple places? (e.g., path validation in api.py vs mcp_server.py)
    - Same DB query written differently in different modules?
    - Shared logic that should be extracted to a common utility?

    **Optimisation opportunities:**
    - Redundant DB queries (same data fetched twice in a request)
    - String operations that could use compiled regex
    - Import-time side effects that slow startup

    Write findings to `39-findings-deadcode.md`:
    ```
    # Dead Code + Optimisation Audit Findings

    ## Dead Module Assessment
    | Module | Import Count (engine) | Import Count (tests) | CLI Entry Point | Verdict |
    |...

    ## Finding: DEAD-NN
    - **Severity:** Critical/High/Medium/Low
    - **Type:** dead-module / dead-function / duplicate-logic / optimisation
    - **File:** path:line
    - **Description:** what's wrong
    - **Evidence:** grep output or import graph
    - **Recommended fix:** specific action
    - **Risk if removed:** what might break (dynamic imports, CLI entry points, etc.)
    ```
  </action>
  <verify>
    <automated>test -f .planning/phases/39-codebase-review/39-findings-deadcode.md && grep -c "DEAD-" .planning/phases/39-codebase-review/39-findings-deadcode.md</automated>
  </verify>
  <acceptance_criteria>
    - 39-findings-deadcode.md exists
    - Dead module assessment table covers rag.py, templates.py, ratelimit.py, ai.py
    - Each dead code finding includes grep evidence (import count)
    - Frontend NoteEditor.tsx verdict documented
    - No module marked dead without checking pyproject.toml scripts
  </acceptance_criteria>
  <done>Dead code and optimisation findings documented with import evidence and risk assessment for each candidate</done>
</task>

</tasks>

<verification>
- 39-findings-deadcode.md exists with structured findings
- Dead module assessment table has grep evidence for each candidate
</verification>

<success_criteria>
Complete dead code and optimisation audit with import evidence, frontend component analysis, and duplicate logic identification.
</success_criteria>

<output>
After completion, create `.planning/phases/39-codebase-review/39-05-SUMMARY.md`
</output>
