---
phase: 03-ai-layer
plan: "05"
type: execute
wave: 4
depends_on: ["03-03", "03-04"]
files_modified: []
autonomous: false
requirements:
  - AI-01
  - AI-02
  - AI-03
  - AI-04
  - AI-05
  - AI-06
  - AI-07
  - AI-08
  - AI-09
  - AI-10
  - CAP-06

must_haves:
  truths:
    - "Full pytest suite is green (all Phase 1-3 tests)"
    - "PII note capture verified not to call Anthropic endpoint"
    - "Public note capture routes to ClaudeAdapter (claude -p subprocess)"
    - "Follow-up questions appear on /sb-capture invocation"
    - "second-brain subagent invokable from Claude Code session"
  artifacts:
    - path: "tests/test_classifier.py"
      provides: "Passing AI-02 tests"
    - path: "tests/test_router.py"
      provides: "Passing AI-03/04/05/06 tests"
    - path: "tests/test_ai.py"
      provides: "Passing AI-01/10/CAP-06 tests"
    - path: "tests/test_adapters.py"
      provides: "Passing adapter unit tests"
    - path: "tests/test_subagent.py"
      provides: "Passing AI-07/08/09 tests"
  key_links:
    - from: "human verification"
      to: "zero Anthropic calls on PII note"
      via: "mock assertion or network log"
      pattern: "test_pii_zero_anthropic_calls"
---

<objective>
Verify all Phase 3 automated tests pass and confirm the critical manual behaviors: PII routing, claude -p subprocess, and subagent invocability.

Purpose: Gate before marking Phase 3 complete. Catches any broken wiring before Phase 4 automation builds on this foundation.
Output: Confirmed green test suite; manual sign-off on four behaviors.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-ai-layer/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite green</name>
  <files></files>
  <action>
    Run the full test suite. Fix any failures before the checkpoint.

    If any test fails:
    - Read the failure message
    - Fix the root cause (do not skip or xfail unless the test has a documented reason)
    - Re-run until green

    Common failure causes to check:
    - ollama package not installed: run `uv add ollama` in repo root
    - Missing CONFIG_PATH in engine/paths.py: add `CONFIG_PATH = BRAIN_ROOT / ".meta" / "config.toml"`
    - test_subagent.py checking file existence from wrong cwd: tests run from repo root, paths are relative
    - conftest.py mock_subprocess_claude fixture scope issue: ensure it returns a context manager
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run --no-project --with pytest --with ollama tests/ -x -q 2>&1 | tail -15</automated>
  </verify>
  <done>All tests pass with 0 failures, 0 errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full AI layer:
    - PII classifier (engine/classifier.py)
    - OllamaAdapter + ClaudeAdapter (engine/adapters/)
    - ModelRouter reading config.toml (engine/router.py)
    - Proactive questions on /sb-capture (engine/ai.py + capture.py)
    - RateLimiter utility (engine/ratelimit.py)
    - Claude Code subagent (.claude/agents/second-brain.md)
    - /sb-capture slash command (.claude/commands/sb-capture.md)
  </what-built>
  <how-to-verify>
    Verify these 4 behaviors. Each can be done without running a live AI call if Ollama/claude CLI is unavailable — use the mock test as a proxy.

    **1. PII routing (zero Anthropic calls):**
    ```bash
    cd /Users/tuomasleppanen/second-brain
    uv run --no-project --with pytest --with ollama tests/test_router.py::test_pii_zero_anthropic_calls -v
    ```
    Expected: PASSED — OllamaAdapter returned, not ClaudeAdapter

    **2. Claude -p PATH check (if inside DevContainer):**
    ```bash
    which claude
    ```
    Expected: path to claude binary, or "not found" (acceptable — ClaudeAdapter handles this with clear error)

    **3. Subagent file structure:**
    ```bash
    head -10 /Users/tuomasleppanen/second-brain/.claude/agents/second-brain.md
    ```
    Expected: YAML frontmatter with name, description, tools fields

    **4. Follow-up questions smoke test (optional — requires Ollama running):**
    ```bash
    # Only if Ollama is running on host with llama3.2 pulled:
    echo "Test" | sb-capture --type meeting --title "Q1 Planning" --sensitivity public
    ```
    Expected: 2-3 follow-up questions printed before note is written

    **Confirm:** All 4 checks pass, or document which need environment setup (Ollama pull, claude CLI install).
  </how-to-verify>
  <resume-signal>Type "approved" if tests pass and structure looks correct. Or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
Checkpoint gate — human approval required before Phase 3 is marked complete.
</verification>

<success_criteria>
- All automated tests pass (0 failures)
- Human confirms PII routing test green
- Human confirms subagent file has valid frontmatter
- Phase 3 can be marked complete in STATE.md and ROADMAP.md
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-layer/03-05-SUMMARY.md`
Update ROADMAP.md Phase 3 plans count and plan list.
Update STATE.md to mark Phase 3 complete.
</output>
