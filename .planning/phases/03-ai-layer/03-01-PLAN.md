---
phase: 03-ai-layer
plan: "01"
type: execute
wave: 1
depends_on: ["03-00"]
files_modified:
  - engine/classifier.py
  - engine/adapters/__init__.py
  - engine/adapters/base.py
  - engine/adapters/ollama_adapter.py
  - engine/adapters/claude_adapter.py
  - pyproject.toml
autonomous: true
requirements:
  - AI-02
  - AI-03
  - AI-04
  - AI-06
  - AI-10

must_haves:
  truths:
    - "classifier.classify('pii', any_body) returns 'pii' regardless of body content"
    - "classifier.classify('', 'salary 100k') returns 'pii' via keyword scan"
    - "classifier.classify('', 'clean body') returns 'public'"
    - "OllamaAdapter.generate() sends system and user messages to ollama.Client.chat()"
    - "ClaudeAdapter.generate() calls claude -p subprocess with user content, never injects user content into system string"
    - "ClaudeAdapter.generate() raises RuntimeError with 'claude CLI not found' when shutil.which('claude') is None"
  artifacts:
    - path: "engine/classifier.py"
      provides: "PII classification (frontmatter + keyword scan)"
      exports: ["classify"]
    - path: "engine/adapters/base.py"
      provides: "BaseAdapter abstract class"
      exports: ["BaseAdapter"]
    - path: "engine/adapters/ollama_adapter.py"
      provides: "OllamaAdapter using ollama PyPI library"
      exports: ["OllamaAdapter"]
    - path: "engine/adapters/claude_adapter.py"
      provides: "ClaudeAdapter using claude -p subprocess"
      exports: ["ClaudeAdapter"]
  key_links:
    - from: "engine/adapters/claude_adapter.py"
      to: "subprocess.run(['claude', '-p', ...])"
      via: "shlex + subprocess"
      pattern: "subprocess.run.*claude.*-p"
    - from: "engine/classifier.py"
      to: "content_sensitivity frontmatter field"
      via: "direct string comparison"
      pattern: "if content_sensitivity in SENSITIVITY_VALUES"
---

<objective>
Implement the PII classifier and all three adapter classes (base, Ollama, Claude).

Purpose: These are the GDPR enforcement boundary and adapter pattern foundation. ModelRouter in plan 02 depends on these.
Output: engine/classifier.py, engine/adapters/ package with base + Ollama + Claude adapters, ollama dep added.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-ai-layer/03-RESEARCH.md

# Key existing patterns
# - pathlib.Path mandatory (FOUND-12)
# - Error messages use type(e).__name__ only, never body content (GDPR-05)
# - uv run --no-project --with pytest for test runs
</context>

<interfaces>
<!-- Contracts this plan creates — consumed by 03-02, 03-03 -->

engine/classifier.py:
```python
SENSITIVITY_VALUES: frozenset  # {"pii", "private", "public"}
PII_KEYWORDS: list[str]        # regex patterns

def classify(content_sensitivity: str, body: str) -> str:
    """Return one of 'pii', 'private', 'public'.
    Frontmatter field wins over keyword scan.
    Never makes network calls.
    """
```

engine/adapters/base.py:
```python
from abc import ABC, abstractmethod

class BaseAdapter(ABC):
    @abstractmethod
    def generate(self, user_content: str, system_prompt: str = "") -> str: ...
```

engine/adapters/ollama_adapter.py:
```python
class OllamaAdapter(BaseAdapter):
    def __init__(self, model: str, host: str = "http://host.docker.internal:11434"): ...
    def generate(self, user_content: str, system_prompt: str = "") -> str: ...
```

engine/adapters/claude_adapter.py:
```python
class ClaudeAdapter(BaseAdapter):
    def __init__(self, model: str = ""): ...
    def generate(self, user_content: str, system_prompt: str = "") -> str:
        """Raises RuntimeError if claude CLI not on PATH or returns non-zero."""
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PII classifier</name>
  <files>engine/classifier.py</files>
  <behavior>
    - classify("pii", anything) == "pii"
    - classify("private", anything) == "private"
    - classify("public", anything) == "public"
    - classify("", "salary is 100k") == "pii"
    - classify("", "medical diagnosis") == "pii"
    - classify("", "architecture notes") == "public"
    - classify("invalid_value", "clean body") == "public"
    - classify("", body_with_ssn_pattern) == "pii"  # "123-45-6789"
  </behavior>
  <action>
    Create engine/classifier.py. Implement exactly as specified in 03-RESEARCH.md Pattern 1.

    SENSITIVITY_VALUES = frozenset({"pii", "private", "public"})

    PII_KEYWORDS list (regex patterns, case-insensitive):
    - r"\b\d{3}-\d{2}-\d{4}\b"  — SSN
    - r"\b\d{16}\b"              — 16-digit credit card
    - r"\bpassword\b", r"\bpasswd\b"
    - r"\bsalary\b", r"\bcompensation\b"
    - r"\bhealth\b", r"\bmedical\b", r"\bdiagnosis\b"
    - r"\bpersonal address\b"

    Compile to _PII_RE at module level with re.IGNORECASE.

    classify() logic:
    1. If content_sensitivity in SENSITIVITY_VALUES → return it directly
    2. If _PII_RE.search(body) → return "pii"
    3. Otherwise → return "public"

    No network calls. No imports beyond stdlib `re`.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run --no-project --with pytest tests/test_classifier.py -x -q</automated>
  </verify>
  <done>All 5 classifier tests pass (GREEN)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adapter base class, OllamaAdapter, ClaudeAdapter</name>
  <files>engine/adapters/__init__.py, engine/adapters/base.py, engine/adapters/ollama_adapter.py, engine/adapters/claude_adapter.py, pyproject.toml</files>
  <behavior>
    - OllamaAdapter.generate("user", "system") calls ollama.Client.chat() with system+user messages
    - OllamaAdapter uses host="http://host.docker.internal:11434" as default
    - ClaudeAdapter.generate("user", "system") calls subprocess.run(["claude", "-p", ...])
    - ClaudeAdapter.generate() first checks shutil.which("claude") — raises RuntimeError("ClaudeAdapter: claude CLI not found") if None
    - ClaudeAdapter.generate() raises RuntimeError on returncode != 0
    - ClaudeAdapter: system_prompt + separator + user_content in full_prompt; system_prompt is always before user_content
    - ClaudeAdapter: --allowedTools "" passed to subprocess (AI-10 isolation)
    - BaseAdapter has single abstract method generate(user_content, system_prompt="") -> str
  </behavior>
  <action>
    1. Create engine/adapters/__init__.py — empty (just marks package)

    2. Create engine/adapters/base.py — BaseAdapter ABC with abstract generate() method. Docstring must note: "user_content is never to be passed inside system_prompt (AI-10)".

    3. Create engine/adapters/ollama_adapter.py — OllamaAdapter(BaseAdapter):
       - __init__(self, model: str, host: str = "http://host.docker.internal:11434")
       - generate(): builds messages list: if system_prompt, prepend {"role":"system","content":system_prompt}; always append {"role":"user","content":user_content}
       - Calls self._client.chat(model=self._model, messages=messages)
       - Returns response.message.content
       - _client = ollama.Client(host=host) in __init__

    4. Create engine/adapters/claude_adapter.py — ClaudeAdapter(BaseAdapter):
       - __init__(self, model: str = "") — model param accepted but unused (claude CLI picks model from session)
       - generate():
         a. Check shutil.which("claude") — if None: raise RuntimeError("ClaudeAdapter: claude CLI not found — run sb-capture from the host or install claude inside the container")
         b. Build full_prompt: if system_prompt → f"{system_prompt}\n\n---\n\n{user_content}" else user_content
         c. subprocess.run(["claude", "-p", full_prompt, "--allowedTools", ""], capture_output=True, text=True, timeout=60)
         d. If returncode != 0: raise RuntimeError(f"ClaudeAdapter: subprocess returned {result.returncode}")
         e. Return result.stdout.strip()
       - Imports: subprocess, shutil (stdlib only — NO import anthropic)

    5. Add `ollama` to pyproject.toml dependencies array (inline array in [project] section, per Phase 1 decision). Use `"ollama>=0.6"`.

    Run: cd /Users/tuomasleppanen/second-brain && uv add ollama
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run --no-project --with pytest --with ollama tests/test_adapters.py -x -q</automated>
  </verify>
  <done>All 5 adapter tests pass; ollama in pyproject.toml dependencies; no `import anthropic` anywhere in engine/adapters/</done>
</task>

</tasks>

<verification>
Full verify after both tasks:
`cd /Users/tuomasleppanen/second-brain && uv run --no-project --with pytest --with ollama tests/test_classifier.py tests/test_adapters.py -v`

Also confirm no anthropic import:
`grep -r "import anthropic" /Users/tuomasleppanen/second-brain/engine/` must return nothing.
</verification>

<success_criteria>
- engine/classifier.py exists, all classifier tests green
- engine/adapters/ package exists with 4 files
- All adapter tests green
- ollama added to pyproject.toml
- Zero `import anthropic` in engine/
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-layer/03-01-SUMMARY.md`
</output>
