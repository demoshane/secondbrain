# Plan 33-01: Pagination on all list endpoints + MCP page param

<objective>
All list endpoints cap their results and support cursor-based pagination.
Default limit 50. MCP tools expose a page parameter. Prevents the GUI
sidebar and API from flooding as the brain grows.
</objective>

<context>
@.planning/PROJECT.md
@.claude/LEARNINGS.md
@engine/api.py
@engine/mcp_server.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add limit/offset to all list API endpoints</name>
  <files>engine/api.py</files>
  <behavior>
    Endpoints to paginate: GET /notes, /people, /meetings, /projects, /actions, /files
    - Accept query params: limit (default 50, max 200), offset (default 0)
    - Response adds: "total": N, "limit": N, "offset": N alongside the existing list key
    - /notes already has search; pagination applies to full-list mode only (no search query)
    - Maintain backwards compat: omitting limit/offset returns first 50 (not all)
  </behavior>
  <action>
    Read engine/api.py; update each route's SQL query with LIMIT ? OFFSET ?;
    add total COUNT(*) query; update response shape; run suite
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_gui.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>All list endpoints paginated; response includes total; tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add page parameter to MCP list tools</name>
  <files>engine/mcp_server.py</files>
  <behavior>
    MCP tools to update: sb_search, sb_files, sb_actions
    - Add page: int = 1 param (1-based); convert to offset = (page-1) * limit
    - Default limit: 50
    - Response: add "page": N, "total_pages": N, "total": N
    - sb_search: pagination applies after RRF ranking (slice results post-sort)
  </behavior>
  <action>
    Read engine/mcp_server.py; update tools; run suite
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_mcp.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>MCP tools paginated; tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests for pagination</name>
  <files>tests/test_gui.py, tests/test_mcp.py</files>
  <behavior>
    test_gui.py: test_people_pagination — seed 60 person notes; GET /people → assert len == 50,
      total == 60; GET /people?offset=50 → assert len == 10
    test_mcp.py: test_sb_files_page2 — seed 60 notes; call sb_files(page=2) →
      assert len > 0 and page == 2 in response
  </behavior>
  <action>
    Add tests; run suite
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/ -q 2>&1 | tail -10</automated>
  </verify>
  <done>Pagination tests pass; suite green</done>
</task>

</tasks>

<verification>
cd /Users/tuomasleppanen/second-brain && uv run pytest tests/ -q 2>&1 | tail -10
</verification>

<success_criteria>
- All list endpoints support limit/offset; default 50
- MCP tools support page param
- Pagination tests pass
- Full suite green
</success_criteria>

<output>After completion create `.planning/phases/33-performance-scale-hardening/33-01-SUMMARY.md`</output>
