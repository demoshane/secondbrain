---
phase: 25-file-capture-batch-capture
plan: 04
type: execute
wave: 4
depends_on:
  - 25-03
files_modified: []
autonomous: false
requirements:
  - GUIF-01
  - ENGL-01

must_haves:
  truths:
    - "Full pytest suite passes with no regressions"
    - "User confirms upload, attachment display, and batch capture work end-to-end in live GUI"
  artifacts:
    - path: "tests/test_api_upload.py"
      provides: "All 7 tests green (not xfail)"
    - path: "tests/test_note_watcher.py"
      provides: "TestWatcherDedup passes"
  key_links:
    - from: "pytest suite"
      to: "all phase 25 requirements"
      via: "test_api_upload.py + TestWatcherDedup"
      pattern: "GUIF-01.*ENGL-01"
---

<objective>
Final sign-off: full pytest suite green, then human verification of the live GUI.

Purpose: Confirm that all phase 25 requirements are met — both automated (test suite) and functional (real GUI interaction).
Output: No new files. Either phase is complete or gap-closure plans are created.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/25-file-capture-batch-capture/25-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite sign-off</name>
  <files></files>
  <action>
Run the full pytest suite. Fix any failures before proceeding to the checkpoint — do not skip or xfail any tests that were passing before this phase.

```bash
uv run pytest tests/ -x -q
```

If `tests/test_api_upload.py` has any remaining xfail (not-implemented), investigate and fix the underlying implementation in the relevant Plan 02 or Plan 03 files.

Expected: all tests green, including:
- `tests/test_api_upload.py` — 7 tests pass
- `tests/test_note_watcher.py::TestWatcherDedup` — passes
- All pre-existing tests still green (no regressions)
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/ -q 2>&1 | tail -5</automated>
  </verify>
  <done>Zero failures, zero errors in full suite output. Any xfail are pre-existing (unrelated to phase 25).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - Upload button in sidebar toolbar (+ File) — attaches files to the currently-open note
    - Upload button in viewer toolbar — same action
    - Drag-and-drop onto the viewer area — drops file onto current note
    - Attachment list section below note body — shows filename · date · size
    - Batch Capture button in sidebar toolbar — indexes all untracked .md files, shows result in Intelligence panel
    - Watcher dedup guard — prevents duplicate indexing when GUI upload and file watcher fire on same path
  </what-built>
  <how-to-verify>
    Start the GUI: `cd ~/second-brain && uv run sb-gui`

    1. **Upload via button**: Open any note. Click "+ File" in the sidebar toolbar. Select any PDF or image from your filesystem. Confirm: the attachment appears below the note body showing "filename · date · size". No toast or modal.

    2. **Upload via drag-and-drop**: With a note open, drag a different file (e.g. a PNG) onto the viewer panel. Confirm: attachment appears in the list below the note body.

    3. **Viewer toolbar button**: With a note open, confirm a "+ File" button also appears in the viewer toolbar (near Edit/Delete). Click it — should open the file picker.

    4. **Attachment list format**: Confirm format is "Q1-report.pdf · 2026-03-16 · 2.4 MB" (approximate — any file name/size). Date and size both present.

    5. **Batch Capture**: Click "Batch Capture" in the sidebar toolbar. Confirm: Intelligence panel shows "Batch capture: N captured, 0 failed" (N may be 0 if all notes already indexed — that is correct). Button re-enables after completion.

    6. **File type rejection**: Try uploading an `.sh` or `.exe` file. Confirm: no attachment appears (silent failure or console error — 415 response).

    7. **No duplicate on repeated batch capture**: Click "Batch Capture" twice. Confirm: second run shows "0 captured" (already-indexed files skipped).
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
Full pytest green + human confirmed all 7 GUI checks pass.
</verification>

<success_criteria>
1. `uv run pytest tests/ -q` — zero failures
2. File upload saves to `files/` and appears in attachment list below note body
3. Drag-and-drop onto viewer works as upload target
4. Batch capture indexes absent .md files and shows count in Intelligence panel
5. Repeated batch capture is idempotent (second run captures 0)
6. Executable file upload is silently rejected (415 from server)
7. No duplicate DB rows when watcher fires after GUI upload
</success_criteria>

<output>
After completion, create `.planning/phases/25-file-capture-batch-capture/25-04-SUMMARY.md`
</output>
