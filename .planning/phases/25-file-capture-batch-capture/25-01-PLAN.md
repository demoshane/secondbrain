---
phase: 25-file-capture-batch-capture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/db.py
  - tests/test_api_upload.py
  - tests/test_note_watcher.py
autonomous: true
requirements:
  - GUIF-01
  - ENGL-01

must_haves:
  truths:
    - "attachments table exists in brain.db after init_schema runs"
    - "test_api_upload.py has failing stubs for all GUIF-01 and ENGL-01 test cases"
    - "TestWatcherDedup class stub exists in test_note_watcher.py"
  artifacts:
    - path: "engine/db.py"
      provides: "migrate_add_attachments_table() called from init_schema()"
      contains: "CREATE TABLE IF NOT EXISTS attachments"
    - path: "tests/test_api_upload.py"
      provides: "Failing test stubs for upload, attachment list, batch capture"
      exports:
        - "TestFileUpload::test_upload_saves_file"
        - "TestFileUpload::test_upload_inserts_attachment_row"
        - "TestFileUpload::test_upload_rejects_executable"
        - "TestAttachmentsList::test_list_attachments"
        - "TestBatchCapture::test_batch_captures_unindexed"
        - "TestBatchCapture::test_batch_skips_indexed"
        - "TestBatchCapture::test_batch_returns_structured_result"
    - path: "tests/test_note_watcher.py"
      provides: "TestWatcherDedup stub class"
      contains: "TestWatcherDedup"
  key_links:
    - from: "engine/db.py"
      to: "init_schema()"
      via: "migrate_add_attachments_table(conn) called at end"
      pattern: "migrate_add_attachments_table"
---

<objective>
Add the `attachments` DB table migration and failing test scaffolds.

Purpose: Lay the foundation — schema must exist before any attachment code runs; stubs define the contract for Wave 2 implementation.
Output: `engine/db.py` with idempotent migration, `tests/test_api_upload.py` with 7 failing stubs, `TestWatcherDedup` class added to `tests/test_note_watcher.py`.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-file-capture-batch-capture/25-CONTEXT.md
@.planning/phases/25-file-capture-batch-capture/25-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB migration — attachments table</name>
  <files>engine/db.py</files>
  <action>
Read `engine/db.py`. Locate the `init_schema()` function and the existing `migrate_add_action_items_table()` pattern.

Add a new function immediately after `migrate_add_action_items_table`:

```python
def migrate_add_attachments_table(conn: sqlite3.Connection) -> None:
    """Idempotent migration: create attachments table if absent."""
    conn.execute("""
        CREATE TABLE IF NOT EXISTS attachments (
            id          INTEGER PRIMARY KEY,
            note_path   TEXT NOT NULL,
            file_path   TEXT NOT NULL,
            filename    TEXT NOT NULL,
            size        INTEGER NOT NULL DEFAULT 0,
            uploaded_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%SZ', 'now'))
        )
    """)
    conn.commit()
```

At the end of `init_schema()`, add the call:
```python
migrate_add_attachments_table(conn)
```
(after the existing `migrate_add_action_items_table(conn)` call).

Do NOT change anything else in `db.py`.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_db.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>Existing db tests still pass; `attachments` table created when `init_schema()` is called on a fresh connection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test scaffolds — upload, batch capture, watcher dedup</name>
  <files>tests/test_api_upload.py, tests/test_note_watcher.py</files>
  <behavior>
    - TestFileUpload::test_upload_saves_file — POST /files/upload with a PDF; expect file saved to files/ dir
    - TestFileUpload::test_upload_inserts_attachment_row — POST /files/upload; expect row in attachments table for the note_path
    - TestFileUpload::test_upload_rejects_executable — POST /files/upload with .exe MIME; expect 415
    - TestAttachmentsList::test_list_attachments — GET /notes/test.md/attachments; expect list of attachment dicts
    - TestBatchCapture::test_batch_captures_unindexed — POST /batch-capture with .md file absent from DB; expect it in succeeded list
    - TestBatchCapture::test_batch_skips_indexed — POST /batch-capture with .md already in notes table; expect it NOT in succeeded list
    - TestBatchCapture::test_batch_returns_structured_result — POST /batch-capture; response has "succeeded" and "failed" keys
    - TestWatcherDedup::test_dedup_skips_already_indexed — uploading a file via GUI then firing a watcher created event for same path does not duplicate the attachments row
  </behavior>
  <action>
Create `tests/test_api_upload.py` with all 7 test stubs as `pytest.mark.xfail(strict=False, reason="not implemented yet")`. Follow the exact fixture pattern from `tests/test_api.py`: use `tmp_path`, monkeypatch `BRAIN_PATH`, Flask test client via `app.test_client()`.

Import `app` from `engine.api`. Use `monkeypatch.setenv("BRAIN_PATH", str(tmp_path))` and import `init_schema` + `get_connection` from `engine.db` to set up a fresh DB per test.

For `TestBatchCapture` tests, write actual .md files to `tmp_path` before calling the endpoint.

Then open `tests/test_note_watcher.py`. Add a new class at the bottom:

```python
class TestWatcherDedup:
    def test_dedup_skips_already_indexed(self, tmp_path, monkeypatch):
        pytest.xfail("not implemented yet")
```

Do NOT modify any existing tests in `test_note_watcher.py`.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_api_upload.py tests/test_note_watcher.py::TestWatcherDedup -v 2>&1 | tail -20</automated>
  </verify>
  <done>All 7 stubs in test_api_upload.py collected by pytest; TestWatcherDedup stub collected; no ImportError; all show as xfail or error (not implemented — that is expected at this wave).</done>
</task>

</tasks>

<verification>
`uv run pytest tests/test_db.py tests/test_api_upload.py -x -q` — db tests green, upload stubs collected.
`uv run python -c "from engine.db import init_schema, get_connection; import sqlite3; conn = sqlite3.connect(':memory:'); init_schema(conn); print(conn.execute(\"SELECT name FROM sqlite_master WHERE name='attachments'\").fetchone())"` — prints `('attachments',)`.
</verification>

<success_criteria>
1. `attachments` table created by `init_schema()` on fresh DB connection
2. All 7 test stubs in `test_api_upload.py` collected by pytest without ImportError
3. `TestWatcherDedup` stub exists in `test_note_watcher.py`
4. `uv run pytest tests/test_db.py -x -q` still fully green
</success_criteria>

<output>
After completion, create `.planning/phases/25-file-capture-batch-capture/25-01-SUMMARY.md`
</output>
