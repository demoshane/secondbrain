---
phase: 27.2-action-items-page-nav-scaffold
plan: "01"
subsystem: tests
tags: [tdd, test-stubs, action-items, tab-bar, db-migration]
dependency_graph:
  requires: []
  provides:
    - tests/test_api.py::TestTabBar (xfail stub — GPAG-01)
    - tests/test_api.py::TestActionItems::test_actions_filter_done_param (xfail — GPAG-02)
    - tests/test_api.py::TestActionItems::test_actions_filter_assignee (xfail — GPAG-02)
    - tests/test_api.py::TestActionItems::test_action_assign (xfail — GPAG-02)
    - tests/test_api.py::TestActionItems::test_my_actions (xfail — GPAG-02)
    - tests/test_db.py::test_migrate_assignee_path (xfail — GPAG-03)
    - tests/test_db.py::test_migrate_due_date (xfail — GPAG-03)
  affects:
    - engine/api.py (tab bar route, action filter/assign endpoints — to be implemented)
    - engine/db.py (assignee_path + due_date columns — to be implemented)
tech_stack:
  added: []
  patterns:
    - xfail(strict=False) stubs auto-promote to PASS once implementation ships (same pattern as Phase 25-27.1)
key_files:
  created: []
  modified:
    - tests/test_api.py
    - tests/test_db.py
decisions:
  - "[27.2-01] TestTabBar placed after TestNoteMeta as a standalone class; TestActionItems stubs added as methods to existing class — no duplication"
  - "[27.2-01] Migration idempotency verified inline in stub (init_schema called twice); no separate idempotency test needed"
  - "[27.2-01] xfail(strict=False) used consistently — stubs auto-promote once implementation ships"
metrics:
  duration: 7 min
  completed: 2026-03-17
  tasks_completed: 2
  files_modified: 2
---

# Phase 27.2 Plan 01: Wave 0 Test Scaffolds Summary

xfail stubs for all Phase 27.2 behaviors added to test_api.py and test_db.py — contracts defined before any production code written.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Add TestTabBar stub and extend TestActionItems | 0474af4 | tests/test_api.py |
| 2 | Add migration stubs to test_db.py | 905bc77 | tests/test_db.py |

## What Was Built

**tests/test_api.py:**
- `TestTabBar` class with `test_tab_bar_html` (GPAG-01: GET /ui contains id="tab-bar")
- 4 new methods on `TestActionItems`:
  - `test_actions_filter_done_param` — GET /actions?done=1 returns only done items
  - `test_actions_filter_assignee` — GET /actions?assignee=path returns items with matching assignee_path
  - `test_action_assign` — PUT /actions/<id> with assignee_path returns {"updated": True}
  - `test_my_actions` — GET /actions?assignee=path&done=0 excludes done items

**tests/test_db.py:**
- `test_migrate_assignee_path` — PRAGMA table_info(action_items) contains assignee_path after init_schema
- `test_migrate_due_date` — PRAGMA table_info(action_items) contains due_date after init_schema
- Both verify idempotency: init_schema called twice must not error

## Verification

Full suite result: all new stubs xfail as expected, no regressions introduced.

```
uv run pytest tests/ -q --tb=no  => all pass/xfail
```

## Deviations from Plan

None — plan executed exactly as written.

## Self-Check: PASSED

- tests/test_api.py: FOUND
- tests/test_db.py: FOUND
- commit 0474af4 (Task 1): FOUND
- commit 905bc77 (Task 2): FOUND
