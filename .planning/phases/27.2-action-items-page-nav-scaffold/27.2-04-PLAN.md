---
phase: 27.2-action-items-page-nav-scaffold
plan: 04
type: execute
wave: 4
depends_on:
  - 27.2-03
files_modified: []
autonomous: false
requirements:
  - GPAG-01
  - GPAG-02
  - GPAG-03
  - GPAG-04

must_haves:
  truths:
    - "Tab bar visible below topbar with Notes and Action Items tabs"
    - "Clicking Action Items tab shows full-width table with correct columns"
    - "Filter dropdowns work (status and assignee)"
    - "Clicking Assignee cell shows inline picker with person notes"
    - "Person note shows My Actions section with assigned open items"
    - "Full pytest suite is green"
  artifacts:
    - path: "engine/gui/static/index.html"
      provides: "tab bar and actions page HTML"
      contains: "id=\"tab-bar\""
    - path: "engine/gui/static/app.js"
      provides: "all JS logic for tabs, actions page, assignee picker, My Actions"
      contains: "showView"
  key_links:
    - from: "human tester"
      to: "tab bar in browser"
      via: "visual verification at http://localhost:5001"
---

<objective>
Run full test suite and human-verify the entire Phase 27.2 feature set in the GUI before sign-off.

Purpose: Automated tests confirm backend behavior; human eyes confirm the interactive frontend UX.
Output: Phase 27.2 approved, STATE.md and ROADMAP.md updated.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27.2-action-items-page-nav-scaffold/27.2-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite sign-off</name>
  <files></files>
  <action>
Run the full pytest suite. All tests must pass (or xfail — no unexpected failures).

```bash
uv run pytest tests/ -q
```

If any test fails:
1. Check if it's a pre-existing failure (not introduced by Phase 27.2)
2. If Phase 27.2 caused it: fix in the appropriate source file before proceeding
3. Do not skip or xfail tests that were previously passing

Also verify the stale launchd guard: if `sb-api` is running, restart it so it picks up code changes:
```bash
launchctl list | grep second-brain
# if running: launchctl stop com.secondbrain.api && launchctl start com.secondbrain.api
```
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/ -q 2>&1 | tail -15</automated>
  </verify>
  <done>Full suite passes. No unexpected failures. xfail count stable or decreased.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify Phase 27.2 GUI features</name>
  <action>
Start the GUI if not running: `cd /Users/tuomasleppanen/second-brain && uv run sb-gui`

Verify each requirement:

**GPAG-01 — Tab bar:**
1. GUI loads at http://localhost:5001 (or via pywebview)
2. Tab bar is visible below the top header with "Notes" and "Action Items" tabs
3. Active tab has a filled/pill background highlight
4. Clicking "Notes" tab shows the sidebar + note viewer; clicking "Action Items" switches to the full-width table

**GPAG-02 — Action Items page:**
5. Action Items page shows a table with columns: Done | Task | Source | Assignee | Created
6. Open items are shown by default
7. "Show done" checkbox reveals completed items when checked
8. Status dropdown filters work (open / done)

**GPAG-03 — Assignee picker:**
9. Click the Assignee cell of any row — a dropdown appears listing people notes
10. Select a person note — the cell updates with that person's name
11. Assignee filter dropdown shows people notes; selecting one filters the table

**GPAG-04 — My Actions on person note:**
12. Click "Notes" tab
13. Open a person note (type: people)
14. The right meta panel shows a "My Actions" section below Backlinks/People
15. If that person has assigned actions, they appear; otherwise "None assigned"
16. Open a non-person note — My Actions section is not visible
  </action>
  <verify>
    <automated>MISSING — this task requires human visual verification of the GUI</automated>
  </verify>
  <done>All 16 checks pass. User types "approved".</done>
  <what-built>
Tab bar (Notes / Action Items tabs), dedicated Action Items page with table and filters, inline assignee picker, and My Actions section on person notes.
  </what-built>
  <resume-signal>Type "approved" if all 16 checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Phase complete when:
- All 16 manual checks pass
- Full pytest suite green
</verification>

<success_criteria>
- Tab bar visible and functional in GUI
- Action Items page renders table with correct columns
- Assignee picker works inline
- My Actions section appears only on person notes
- pytest suite: 0 unexpected failures
</success_criteria>

<output>
After completion, create `.planning/phases/27.2-action-items-page-nav-scaffold/27.2-04-SUMMARY.md`

Update:
- `.planning/STATE.md` — mark Phase 27.2 complete
- `.planning/ROADMAP.md` — mark Phase 27.2 checkbox done, update plan count
</output>
