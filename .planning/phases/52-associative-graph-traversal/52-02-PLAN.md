---
phase: 52-graph-traversal
plan: 02
type: execute
wave: 2
depends_on: [52-01]
files_modified:
  - engine/mcp_server.py
autonomous: true
execution_strategy: direct

must_haves:
  truths:
    - "sb_traverse MCP tool exists and returns graph subsets"
    - "sb_person_context includes 2nd-degree connections"
    - "sb_traverse respects max_depth cap of 3"
---

<objective>
Expose graph traversal via MCP and enhance person context with 2nd-degree connections.

Purpose: Claude can now explore associative chains and discover indirect relationships.
Output: New `sb_traverse` tool + enhanced `sb_person_context`.
</objective>

<context>
@.planning/phases/52-associative-graph-traversal/52-CONTEXT.md
@engine/mcp_server.py
@engine/links.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sb_traverse MCP tool</name>
  <files>engine/mcp_server.py</files>
  <read_first>engine/mcp_server.py (tool registration pattern, any existing tool for reference)</read_first>
  <action>
    1. Add `sb_traverse` tool:
       - Params: `start_path: str`, `max_depth: int = 2`, `rel_types: str | None = None`
       - Calls `traverse_graph()` from links.py
       - Returns nodes with titles, depths, activation scores; edges with types and strengths
       - Log to audit_log with event_type="mcp_traverse"
    2. Docstring: "Explore associative connections from a starting note. Follow relationship
       chains up to N hops to discover indirect connections between notes, people, and projects.
       Use when you want to understand how topics are connected beyond direct links."
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_api.py -x -v -k "traverse"</automated>
  </verify>
  <done>sb_traverse tool registered and functional</done>
</task>

<task type="auto">
  <name>Task 2: Enhance sb_person_context with 2nd-degree connections</name>
  <files>engine/mcp_server.py</files>
  <read_first>engine/mcp_server.py (sb_person_context — full implementation)</read_first>
  <action>
    1. After primary person context is assembled, call `traverse_graph(conn, person_path, max_depth=2)`
    2. Filter traversal results to person-type notes only (2nd-degree people connections)
    3. Add to response: `"connected_people": [{"path", "title", "via", "depth"}]`
    4. "via" field: the intermediate note that connects them (meeting, project, etc.)
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_api.py -x -v -k "person"</automated>
  </verify>
  <done>Person context includes 2nd-degree people connections via graph traversal</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_api.py -x -v -k "traverse or person"` passes
- sb_traverse returns nodes and edges for connected notes
- sb_person_context includes connected_people field
</verification>

<success_criteria>
Graph traversal exposed via MCP with enhanced person context showing indirect connections.
</success_criteria>
