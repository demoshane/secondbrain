---
phase: 52-graph-traversal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/db.py
  - engine/links.py
  - tests/test_links.py
autonomous: true
execution_strategy: direct

must_haves:
  truths:
    - "relationships table has strength REAL DEFAULT 1.0 column"
    - "composite index on relationships(source_path, rel_type) exists"
    - "traverse_graph returns nodes up to N hops with activation scores"
    - "find_path returns shortest path between two notes"
  artifacts:
    - path: "engine/links.py"
      provides: "traverse_graph() and find_path() functions"
      exports: ["traverse_graph", "find_path"]
---

<objective>
Add multi-hop graph traversal to the relationship system.

Purpose: Enable associative recall — follow chains of connections to discover indirect
relationships between notes, people, and projects.
Output: `traverse_graph()` and `find_path()` in links.py using recursive CTEs.
</objective>

<context>
@.planning/phases/52-associative-graph-traversal/52-CONTEXT.md
@engine/db.py
@engine/links.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add strength column and index to relationships</name>
  <files>engine/db.py</files>
  <read_first>engine/db.py (relationships CREATE TABLE, existing migration pattern)</read_first>
  <behavior>
    - Add `strength REAL NOT NULL DEFAULT 1.0` column via ALTER TABLE migration
    - Add index: `CREATE INDEX IF NOT EXISTS idx_rel_source_type ON relationships(source_path, rel_type)`
    - Add index: `CREATE INDEX IF NOT EXISTS idx_rel_target ON relationships(target_path)`
    - Migration is idempotent (try/except OperationalError pattern)
  </behavior>
  <action>
    1. Add `_add_relationship_strength_column(conn)` migration function
    2. Add `_add_relationship_indexes(conn)` function
    3. Call both from `init_schema()` alongside other migrations
    4. Write tests for migration idempotency
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_db_helpers.py -x -v -k "relationship"</automated>
  </verify>
  <done>Relationships table has strength column and indexes</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement traverse_graph and find_path</name>
  <files>engine/links.py</files>
  <read_first>engine/links.py (full — understand current link operations), engine/db.py (relationships schema)</read_first>
  <behavior>
    - `traverse_graph(conn, start_path, max_depth=2, rel_types=None) -> dict`
    - Returns: {"nodes": [{"path", "title", "depth", "activation"}], "edges": [{"source", "target", "type", "strength"}]}
    - Activation score: strength / (2 ^ depth) — decays exponentially with distance
    - Deduplicates: if node reachable via multiple paths, keeps highest activation
    - Respects max_depth cap of 3 (hard limit even if caller passes higher)
    - `find_path(conn, source_path, target_path, max_depth=3) -> list[dict] | None`
    - Returns list of nodes along shortest path, or None if no path exists
    - Both functions use bidirectional relationship traversal (follow both source→target and target→source)
  </behavior>
  <action>
    1. Implement `traverse_graph()` using recursive CTE:
       ```sql
       WITH RECURSIVE traverse AS (
         SELECT target_path, rel_type, strength, 1 AS depth FROM relationships WHERE source_path = ?
         UNION ALL
         SELECT r.target_path, r.rel_type, r.strength, t.depth + 1
         FROM traverse t JOIN relationships r ON r.source_path = t.target_path
         WHERE t.depth < ?
       ) SELECT DISTINCT ...
       ```
    2. Add bidirectional support: UNION source→target and target→source in base case
    3. Implement `find_path()` using BFS variant of the same CTE pattern
    4. Write comprehensive tests: single hop, multi-hop, cycle handling, no-path case
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run pytest tests/test_links.py -x -v -k "traverse or find_path"</automated>
  </verify>
  <done>Graph traversal functions work with recursive CTEs, handle cycles, respect depth limits</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_links.py -x -v -k "traverse or find_path"` passes
- `uv run pytest tests/test_db_helpers.py -x -v -k "relationship"` passes
</verification>

<success_criteria>
Multi-hop graph traversal with weighted activation scores, tested with cycles and edge cases.
</success_criteria>
