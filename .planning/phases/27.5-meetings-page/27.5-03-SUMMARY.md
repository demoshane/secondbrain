---
phase: 27.5-meetings-page
plan: "03"
subsystem: tests
tags: [playwright, gui-tests, meetings-page, fixture]
dependency_graph:
  requires: [27.5-01, 27.5-02]
  provides: [meetings-playwright-tests]
  affects: [tests/test_gui.py, tests/conftest.py]
tech_stack:
  added: []
  patterns: [playwright-locator-wait_for, session-scoped-fixture-seeding]
key_files:
  modified:
    - tests/conftest.py
    - tests/test_gui.py
decisions:
  - "[27.5-03] page.goto('/ui') used in new tests — not bare live_server_url — matching all existing test_gui.py conventions"
  - "[27.5-03] Meeting seed uses INSERT OR REPLACE with people column (not tags) to match meetings schema"
metrics:
  duration: "8 min"
  completed_date: "2026-03-18"
  tasks_completed: 1
  files_modified: 2
requirements:
  - 27.5-MP-03
  - 27.5-MP-04
---

# Phase 27.5 Plan 03: Meetings Page Playwright Tests Summary

**One-liner:** Playwright E2E tests for Meetings tab navigation and meeting row detail panel with seeded meeting fixture.

## Tasks Completed

| # | Name | Commit | Files |
|---|------|--------|-------|
| 1 | Extend conftest gui_brain fixture + add Playwright tests | b23b212 | tests/conftest.py, tests/test_gui.py |

## What Was Built

Two new Playwright tests in `tests/test_gui.py`:

- `test_meetings_tab` — navigates to `/ui`, confirms `[data-testid="tab-meetings"]` is visible, clicks it, waits for `[data-testid="meetings-page"]`
- `test_meetings_page_row_click` — clicks Meetings tab, waits for "Q1 Kickoff" row (seeded), clicks it, waits for `[data-testid="participants-section"]`

`tests/conftest.py` `gui_brain` fixture extended with a meeting seed block:
- Creates `meetings/q1-kickoff.md` on disk with meeting frontmatter and `people: ["Alice"]`
- Inserts a corresponding row into the `notes` SQLite table at session scope

## Verification

```
uv run pytest tests/test_gui.py::test_meetings_tab tests/test_gui.py::test_meetings_page_row_click -x -q
# Result: .. (2 passed)

uv run pytest tests/ --ignore=tests/test_gui.py -q
# Result: all pass (0 new failures)
```

Pre-existing failures (unrelated to this plan):
- `test_markdown_renders_as_html` — pre-existing
- `test_collapsible_sections` — pre-existing

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed page.goto() URL to use /ui instead of bare live_server_url**
- **Found during:** Task 1 verification
- **Issue:** Plan specified `page.goto(live_server_url)` which navigates to `/` (404) — app is served under `/ui`
- **Fix:** Changed both new tests to use `page.goto("/ui")` matching the pattern used by all 11 existing tests
- **Files modified:** tests/test_gui.py
- **Commit:** b23b212

## Checkpoint

Task 2 is `checkpoint:human-verify` — human sign-off required before plan can be marked complete.

**What to verify:**
1. Restart sb-api: `launchctl kickstart -k gui/$(id -u)/com.secondbrain.api` (or `cd ~/second-brain && uv run sb-gui`)
2. Open GUI — confirm "Meetings" tab appears in tab bar alongside Notes / Action Items / People
3. Click Meetings tab — confirm chronological list of meeting notes appears on the left
4. Click any meeting row — confirm right panel shows Note body, Participants, Action Items, Backlinks sections
5. Click "Open in Notes" — confirm it navigates to Notes tab with that note selected
6. Run: `uv run pytest tests/ -q` — confirm the two new tests pass

## Self-Check: PASSED

- tests/conftest.py: FOUND
- tests/test_gui.py: FOUND (2 new test functions)
- conftest.py meeting seed: FOUND (3 occurrences of q1-kickoff/Q1 Kickoff)
- Commit b23b212: FOUND
