---
phase: 27.5
slug: meetings-page
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-18
---

# Phase 27.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest + Playwright |
| **Config file** | pyproject.toml |
| **Quick run command** | `uv run pytest tests/test_meetings.py -x -q` |
| **Full suite command** | `uv run pytest tests/ -q` |
| **Estimated runtime** | ~15 seconds |

---

## Sampling Rate

- **After every task commit:** Run `uv run pytest tests/test_meetings.py -x -q`
- **After Wave 2 (frontend):** Run `uv run pytest tests/test_gui.py::test_meetings_page tests/test_gui.py::test_meetings_tab -x`
- **Before sign-off:** Run `uv run pytest tests/ -q`

---

## Requirement → Test Map

| Req ID | Requirement | Test Type | Test Command | Wave 0 |
|--------|-------------|-----------|--------------|--------|
| 27.5-MP-01 | `GET /meetings` returns list with date, participant_count, open_actions | unit/integration | `uv run pytest tests/test_meetings.py::test_list_meetings -x` | ❌ |
| 27.5-MP-02 | Meeting detail: body, participants, actions, backlinks | integration | `uv run pytest tests/test_meetings.py::test_meeting_detail -x` | ❌ |
| 27.5-MP-03 | MeetingsPage renders list and detail panel in browser | e2e Playwright | `uv run pytest tests/test_gui.py::test_meetings_page -x` | ❌ |
| 27.5-MP-04 | "Meetings" tab visible; clicking switches view | e2e Playwright | `uv run pytest tests/test_gui.py::test_meetings_tab -x` | ❌ |

---

## Wave 0 Gaps

- [ ] `tests/test_meetings.py` — xfail stubs for MP-01 through MP-04
