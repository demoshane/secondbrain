---
phase: 21-live-refresh-sse
plan: 04
type: execute
wave: 4
depends_on:
  - 21-03
files_modified: []
autonomous: false
requirements:
  - GUIX-01
must_haves:
  truths:
    - "User confirms sidebar updates within 2 seconds after sb-capture"
    - "User confirms status dot is visible and green"
    - "User confirms conflict banner appears when editor is open and note modified externally"
    - "User confirms deleted note shows 'Note was deleted' message"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete SSE live-refresh feature. Confirms all four success criteria from the roadmap are met from the user's perspective before phase is marked complete.

Purpose: SSE behavior requires real end-to-end interaction that automated tests cannot cover (pywebview window, actual file events, 2-second timing).
Output: User approval or list of issues to fix.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/21-live-refresh-sse/21-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    SSE live refresh:
    - /events endpoint on Flask sidecar with per-client queue, heartbeat, auto-unsubscribe
    - NoteChangeHandler watching ~/SecondBrain for .md changes (debounced 300ms)
    - EventSource client in app.js with status dot, conflict banner, dirty state tracking
  </what-built>
  <how-to-verify>
    Start the GUI:
      cd ~/second-brain && uv run sb-gui

    Check 1 — Status dot:
      Look at the topbar. A small dot should be visible. It should be GREEN within 1-2 seconds of launch.

    Check 2 — New note appears (GUIX-01 criterion 1):
      In a terminal: uv run sb-capture --title "SSE test note" --body "testing live refresh"
      Confirm: The note "SSE test note" appears in the sidebar within 2 seconds WITHOUT refreshing the GUI.

    Check 3 — Edited note reloads (GUIX-01 criterion 2):
      Open "SSE test note" in the viewer (click it).
      In a terminal: edit the file directly with any editor, add a line, save.
      Confirm: The viewer updates within 2 seconds without user action.

    Check 4 — Conflict banner (GUIX-01 implicit):
      Open "SSE test note", click Edit, type a few characters (do NOT save).
      In a terminal: edit the same file externally and save.
      Confirm: A banner appears with "Note was updated externally" and Keep/Load buttons.
      Click "Load new version" — confirm viewer shows the externally saved content.

    Check 5 — Deleted note (GUIX-01 criterion implied by SSE design):
      Open any note in the viewer.
      In a terminal: delete the file (rm ~/SecondBrain/path/to/note.md).
      Confirm: Viewer shows "Note was deleted" within 2 seconds.

    Check 6 — Reconnect after drop (GUIX-01 criterion 4):
      Stop and restart sb-api (or wait for a drop).
      Confirm: Status dot goes grey, then returns to green automatically.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe which checks failed</resume-signal>
</task>

</tasks>

<verification>
User approval via resume-signal above.
</verification>

<success_criteria>
All 6 manual checks pass. User types "approved".
</success_criteria>

<output>
After completion, create `.planning/phases/21-live-refresh-sse/21-04-SUMMARY.md`
</output>
