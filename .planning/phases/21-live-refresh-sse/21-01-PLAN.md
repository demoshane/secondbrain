---
phase: 21-live-refresh-sse
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_api_sse.py
  - tests/test_note_watcher.py
autonomous: true
requirements:
  - GUIX-01
must_haves:
  truths:
    - "Test file tests/test_api_sse.py exists with failing stubs for endpoint, broadcast, and unsubscribe tests"
    - "Test file tests/test_note_watcher.py exists with failing stubs for NoteChangeHandler filter, debounce, event types, path, and files/ exclusion tests"
  artifacts:
    - path: "tests/test_api_sse.py"
      provides: "SSE endpoint + subscriber registry test coverage"
      exports: ["test_events_endpoint_returns_stream", "test_broadcast_delivers_to_all_subscribers", "test_unsubscribe_removes_queue"]
    - path: "tests/test_note_watcher.py"
      provides: "NoteChangeHandler unit test coverage"
      exports: ["test_non_md_ignored", "test_debounce_suppresses_rapid_events", "test_created_modified_deleted_events", "test_path_is_relative", "test_files_dir_excluded"]
  key_links:
    - from: "tests/test_api_sse.py"
      to: "engine.api._broadcast, engine.api._subscribe, engine.api._unsubscribe"
      via: "direct import of private helpers"
      pattern: "from engine.api import"
    - from: "tests/test_note_watcher.py"
      to: "engine.watcher.NoteChangeHandler"
      via: "direct import"
      pattern: "from engine.watcher import NoteChangeHandler"
---

<objective>
Create failing test scaffolds for the SSE backend and NoteChangeHandler. All tests must import the symbols they test (which do not exist yet) and fail with ImportError or AttributeError at this stage. Wave 2 (Plan 02) implements the code that makes them pass.

Purpose: Establish the test contracts before writing implementation so Plan 02 executor has a clear RED target.
Output: tests/test_api_sse.py, tests/test_note_watcher.py — both failing, both with correct test structure.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-live-refresh-sse/21-RESEARCH.md
</context>

<feature>
  <name>SSE subscriber registry tests</name>
  <files>tests/test_api_sse.py, tests/test_note_watcher.py</files>
  <behavior>
    test_api_sse.py — expected behaviors:
    - test_events_endpoint_returns_stream: GET /events returns 200 with Content-Type text/event-stream
    - test_broadcast_delivers_to_all_subscribers: after _subscribe() x2, _broadcast({type:modified,path:a.md}) puts payload on both queues
    - test_unsubscribe_removes_queue: after _subscribe() + _unsubscribe(q), _broadcast() does NOT put on removed queue

    test_note_watcher.py — expected behaviors:
    - test_non_md_ignored: on_modified event with .json path → _broadcast never called
    - test_debounce_suppresses_rapid_events: 5 rapid on_modified events for same path → _broadcast called exactly once (after debounce settles)
    - test_created_modified_deleted_events: on_created/on_modified/on_deleted each fire → _broadcast called with correct type field
    - test_path_is_relative: on_modified for absolute path under brain root → emitted path is relative to brain root
    - test_files_dir_excluded: on_modified for path containing "files/" segment → _broadcast never called
  </behavior>
  <implementation>
    Write stub test files that import from engine.api and engine.watcher.
    Use pytest. For debounce test, patch threading.Timer to avoid real waits.
    Do NOT implement the production code — tests must fail (RED) after this plan.
    Run: uv run pytest tests/test_api_sse.py tests/test_note_watcher.py -q
    Expected result at end of this plan: collection errors or ImportError (RED confirmed).
  </implementation>
</feature>

<verification>
<automated>uv run pytest tests/test_api_sse.py tests/test_note_watcher.py --collect-only -q 2>&1 | head -40</automated>
</verification>

<success_criteria>
Both test files exist. pytest collects all 8 test functions (even if they error on import). Files are syntactically valid Python.
</success_criteria>

<output>
After completion, create `.planning/phases/21-live-refresh-sse/21-01-SUMMARY.md`
</output>
