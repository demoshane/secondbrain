---
phase: 04.1-native-macos-ux-global-cli-launchd-watcher-autostart-git-hook-installer
plan: "00"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_install_native.py
  - scripts/install_native.py
autonomous: true
requirements:
  - 4.1-CLI-01
  - 4.1-LAUNCHD-01
  - 4.1-LAUNCHD-02
  - 4.1-LAUNCHD-03
  - 4.1-HOOK-01
  - 4.1-HOOK-02

must_haves:
  truths:
    - "pytest can collect tests/test_install_native.py without import errors"
    - "scripts/install_native.py can be imported without side effects"
  artifacts:
    - path: "tests/test_install_native.py"
      provides: "7 test stubs covering all 4.1-* requirements"
      min_lines: 60
    - path: "scripts/install_native.py"
      provides: "Stub with function signatures, no real subprocess calls"
      min_lines: 40
  key_links:
    - from: "tests/test_install_native.py"
      to: "scripts/install_native.py"
      via: "import install_native"
      pattern: "import install_native|from scripts"
---

<objective>
Create Wave 0: test stubs for all 7 install_native requirements + importable script stub.

Purpose: Unblocks TDD cycle for Wave 1 implementation. Tests fail (RED) until Wave 1 fills the function bodies.
Output: tests/test_install_native.py (7 failing tests), scripts/install_native.py (stub bodies that raise NotImplementedError).
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.1-native-macos-ux-global-cli-launchd-watcher-autostart-git-hook-installer/04.1-RESEARCH.md
@.planning/phases/04.1-native-macos-ux-global-cli-launchd-watcher-autostart-git-hook-installer/04.1-VALIDATION.md
@tests/conftest.py
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/install_native.py stub</name>
  <files>scripts/install_native.py</files>
  <action>
Create `scripts/install_native.py` with all public function signatures but `raise NotImplementedError` bodies. No real subprocess calls at import time.

Module-level constants (top of file):
```python
REPO_ROOT = Path(__file__).resolve().parent.parent
HOOKS_DIR = REPO_ROOT / ".githooks"
PLIST_LABEL = "com.secondbrain.watch"
PLIST_PATH = Path.home() / "Library" / "LaunchAgents" / f"{PLIST_LABEL}.plist"
```

Required functions (all raise NotImplementedError):
- `find_uv() -> Path` — locates uv binary via shutil.which then fallback to ~/.local/bin/uv
- `install_global_cli(repo_root: Path) -> None` — runs `uv tool install --editable --force`
- `write_plist(sb_watch_bin: Path, repo_root: Path) -> Path` — generates plist dict, writes via plistlib.dump, returns plist_path
- `load_launchd_agent(plist_path: Path) -> None` — idempotent: bootout then bootstrap
- `install_hook(repo_path: Path, hooks_dir: Path) -> None` — validates git repo, runs `git config core.hooksPath`
- `main() -> None` — argparse dispatcher: --cli, --launchd, --hooks REPOS..., default=all three

Imports needed: `argparse, os, plistlib, shutil, subprocess, sys` from stdlib, `pathlib.Path`.

Do NOT add `scripts/install_native.py` as a pytest test path — it is a script, not a test module.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && python -c "import sys; sys.path.insert(0, 'scripts'); import install_native; print('import ok')"</automated>
  </verify>
  <done>Script imports without side effects; all 6 function names exist as attributes of the module.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tests/test_install_native.py stubs</name>
  <files>tests/test_install_native.py</files>
  <behavior>
    - test_global_cli_install: patches subprocess.run, calls install_global_cli(REPO_ROOT), asserts called with ["uv", "tool", "install", "--editable", "--force", str(REPO_ROOT)]
    - test_update_shell_if_needed: patches subprocess.run + shutil.which to return None for PATH check, asserts uv tool update-shell called
    - test_plist_keys: calls write_plist(Path("/fake/sb-watch"), Path("/fake/repo")), reads written plist via plistlib.load, asserts Label=="com.secondbrain.watch", ProgramArguments[0]=="/fake/sb-watch", KeepAlive==True, "StandardOutPath" key present
    - test_launchctl_bootstrap: patches subprocess.run, calls load_launchd_agent(Path("/fake/plist")), asserts "bootstrap" appears in one of the calls
    - test_idempotent_load: patches subprocess.run, calls load_launchd_agent twice, asserts "bootout" called before "bootstrap" each time
    - test_hook_install: patches subprocess.run to simulate valid git repo (returncode=0 for rev-parse), calls install_hook(Path("/fake/repo"), Path("/fake/.githooks")), asserts "core.hooksPath" in one call's args
    - test_hook_install_invalid_repo: patches subprocess.run to return returncode=1 for rev-parse, asserts install_hook raises ValueError
  </behavior>
  <action>
Write all 7 tests. Use `unittest.mock.patch` for subprocess.run. Use `tmp_path` pytest fixture for plist write test (pass tmp_path as plist write destination override or monkeypatch PLIST_PATH). Each test should currently FAIL with NotImplementedError — that is expected (RED phase).

Import path: `sys.path.insert(0, str(Path(__file__).parent.parent / "scripts"))` at top of test file so `import install_native` resolves without package install.

Note: `test_update_shell_if_needed` is a stretch goal — include it as `pytest.mark.skip(reason="implemented in Wave 1")` if the function signature doesn't support it cleanly at stub stage.
  </action>
  <verify>
    <automated>cd /Users/tuomasleppanen/second-brain && uv run --no-project --with pytest pytest tests/test_install_native.py --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <done>pytest collects 7 test items from test_install_native.py with no import errors. Tests may fail (NotImplementedError) — that is correct at Wave 0.</done>
</task>

</tasks>

<verification>
uv run --no-project --with pytest pytest tests/test_install_native.py --collect-only -q
</verification>

<success_criteria>
pytest collects exactly 7 test items; no import errors; script imports cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-native-macos-ux-global-cli-launchd-watcher-autostart-git-hook-installer/04.1-00-SUMMARY.md`
</output>
