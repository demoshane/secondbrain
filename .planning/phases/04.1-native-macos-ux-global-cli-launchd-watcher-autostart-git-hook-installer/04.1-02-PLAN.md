---
phase: 04.1-native-macos-ux-global-cli-launchd-watcher-autostart-git-hook-installer
plan: "02"
type: execute
wave: 3
depends_on:
  - "04.1-01"
files_modified:
  - pyproject.toml
autonomous: false
requirements:
  - 4.1-CLI-01
  - 4.1-LAUNCHD-01
  - 4.1-HOOK-01

must_haves:
  truths:
    - "sb-install entry point is callable from any directory after uv tool install"
    - "User confirms sb-capture and sb-search work from /tmp (any directory)"
    - "User confirms launchd agent is loaded (launchctl list | grep secondbrain)"
  artifacts:
    - path: "pyproject.toml"
      provides: "sb-install entry point pointing to scripts/install_native.py:main"
      contains: "sb-install"
  key_links:
    - from: "pyproject.toml sb-install"
      to: "scripts/install_native.py main()"
      via: "hatchling entry point"
      pattern: "sb-install.*install_native"
---

<objective>
Wire sb-install entry point into pyproject.toml and run the actual installer on this machine. Verify global CLI, launchd agent, and full test suite.

Purpose: Makes the installer a first-class CLI tool. Confirms the entire Phase 4.1 works end-to-end on the host machine.
Output: pyproject.toml updated; installer runs successfully; launchd agent running.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@pyproject.toml
@scripts/install_native.py
@.planning/phases/04.1-native-macos-ux-global-cli-launchd-watcher-autostart-git-hook-installer/04.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sb-install entry point to pyproject.toml</name>
  <files>pyproject.toml</files>
  <action>
Add one line to the `[project.scripts]` section in pyproject.toml:

```toml
sb-install = "install_native:main"
```

Note: `scripts/` is already in hatchling `packages` list (`packages = ["engine", "scripts"]`). However `install_native` lives directly under `scripts/` as a module, not a package. The entry point module path must be `install_native:main` (not `scripts.install_native:main`) since hatchling packages `scripts/` as a namespace.

If the entry point cannot resolve due to module path issues, use `scripts.install_native:main` — test both and use whichever resolves correctly after `uv tool install --editable --force .`.

After editing, run:
```bash
uv tool install --editable --force /Users/tuomasleppanen/second-brain
```
Verify the shim exists:
```bash
ls ~/.local/bin/sb-install
```
  </action>
  <verify>
    <automated>ls /Users/tuomasleppanen/.local/bin/sb-install && echo "shim exists"</automated>
  </verify>
  <done>sb-install shim exists in ~/.local/bin/; `sb-install --help` prints usage without error.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - scripts/install_native.py: 3-in-1 installer (global CLI, launchd, git hooks)
    - sb-install entry point in pyproject.toml
    - All 7 unit tests GREEN
  </what-built>
  <how-to-verify>
1. Run full test suite — must be green:
   `uv run --no-project --with pytest pytest tests/ -q`

2. Run the installer (global CLI + launchd only, no hook repos yet):
   `python /Users/tuomasleppanen/second-brain/scripts/install_native.py --cli --launchd`
   Expected: prints "Global CLI installed" and "Loaded launchd agent: com.secondbrain.watch"

3. Verify global CLI accessible from any directory:
   `cd /tmp && sb-search --help`
   Expected: usage text, not "command not found"

4. Verify launchd agent is loaded:
   `launchctl list | grep secondbrain`
   Expected: one line with "com.secondbrain.watch"

5. Check launchd agent log for errors:
   `cat ~/Library/Logs/second-brain-watch-error.log`
   Expected: empty or only startup lines (no crash loops)

6. Verify sb-install entry point works:
   `sb-install --help`
   Expected: usage text
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 checks pass, or describe any failures.</resume-signal>
</task>

</tasks>

<verification>
uv run --no-project --with pytest pytest tests/ -q
launchctl list | grep secondbrain
cd /tmp && sb-search --help
</verification>

<success_criteria>
All tests green; sb-* commands accessible from /tmp; launchd agent listed; no crash loop in error log.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-native-macos-ux-global-cli-launchd-watcher-autostart-git-hook-installer/04.1-02-SUMMARY.md`
</output>
