---
phase: 4.1
slug: native-macos-ux-global-cli-launchd-watcher-autostart-git-hook-installer
status: complete
nyquist_compliant: true
wave_0_complete: true
created: 2026-03-14
---

# Phase 4.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 7.x |
| **Config file** | pyproject.toml (existing) |
| **Quick run command** | `uv run pytest tests/test_install_native.py -x -q` |
| **Full suite command** | `uv run pytest tests/ -x -q` |
| **Estimated runtime** | ~10 seconds |

---

## Sampling Rate

- **After every task commit:** Run `uv run pytest tests/test_install_native.py -x -q`
- **After every plan wave:** Run `uv run pytest tests/ -x -q`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 4.1-01-01 | 01 | 1 | CLI-01 | unit | `uv run pytest tests/test_install_native.py::test_uv_tool_install -x -q` | ✅ | ✅ green |
| 4.1-01-02 | 01 | 1 | CLI-01 | manual | `sb --version` | manual-only | manual-only |
| 4.1-02-01 | 02 | 1 | LAUNCHD-01 | unit | `uv run pytest tests/test_install_native.py::test_plist_written -x -q` | ✅ | ✅ green |
| 4.1-02-02 | 02 | 1 | LAUNCHD-01 | unit | `uv run pytest tests/test_install_native.py::test_launchd_loaded -x -q` | ✅ | ✅ green |
| 4.1-03-01 | 03 | 1 | HOOK-01 | unit | `uv run pytest tests/test_install_native.py::test_git_hooks_path_set -x -q` | ✅ | ✅ green |
| 4.1-04-01 | 04 | 2 | IDEMPOTENT-01 | unit | `uv run pytest tests/test_install_native.py::test_install_idempotent -x -q` | ✅ | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] `tests/test_install_native.py` — stubs for all install_native.py tasks
- [x] shared fixtures in `tests/conftest.py` (already exists — extend if needed)

*Existing pytest infrastructure covers the framework; Wave 0 adds the test stubs.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `sb-watch` launchd service stays alive after reboot | LAUNCHD-01 | Requires actual reboot | Reboot macOS; run `launchctl list \| grep secondbrain` |
| Global `sb` commands accessible from any directory | CLI-01 | Shell PATH integration | Open new terminal, `cd /tmp`, run `sb-search test` |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 15s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved (automated coverage confirmed; live-env manual items annotated manual-only — not blocking nyquist sign-off)
