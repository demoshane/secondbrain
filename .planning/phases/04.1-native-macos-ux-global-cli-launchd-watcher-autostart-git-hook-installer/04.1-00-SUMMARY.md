---
phase: 04.1-native-macos-ux-global-cli-launchd-watcher-autostart-git-hook-installer
plan: "00"
subsystem: testing
tags: [pytest, tdd, install-native, uv, launchd, git-hooks, plistlib]

requires: []
provides:
  - "scripts/install_native.py stub with 6 function signatures raising NotImplementedError"
  - "tests/test_install_native.py with 7 test stubs covering all 4.1-* requirements (RED phase)"
affects:
  - 04.1-native-macos-ux-global-cli-launchd-watcher-autostart-git-hook-installer

tech-stack:
  added: []
  patterns:
    - "TDD Wave 0: stubs + failing tests before Wave 1 implementation"
    - "sys.path.insert(0, scripts/) for importable scripts without package install"
    - "patch.object(install_native, 'PLIST_PATH', ...) for hermetic plist write tests"

key-files:
  created:
    - scripts/install_native.py
    - tests/test_install_native.py
  modified: []

key-decisions:
  - "Use patch.object(install_native, 'PLIST_PATH', tmp_path/...) so test_plist_keys writes to tmp_path without touching ~/Library/LaunchAgents"
  - "test_update_shell_if_needed marked pytest.mark.skip(reason='implemented in Wave 1') — function signature unclear at stub stage"
  - "Use /usr/bin/git in install_hook docstring per project CLAUDE.md scm_breeze constraint"

patterns-established:
  - "Pattern 1: Wave 0 TDD — create importable stub with NotImplementedError bodies before writing any implementation"
  - "Pattern 2: mock subprocess.run per-call with side_effect=lambda to distinguish rev-parse from config calls in hook tests"

requirements-completed:
  - 4.1-CLI-01
  - 4.1-LAUNCHD-01
  - 4.1-LAUNCHD-02
  - 4.1-LAUNCHD-03
  - 4.1-HOOK-01
  - 4.1-HOOK-02

duration: 2min
completed: 2026-03-14
---

# Phase 04.1 Plan 00: install_native Wave 0 Stubs Summary

**TDD Wave 0: scripts/install_native.py stub + 7 failing pytest stubs covering uv tool install, launchd plist, and git hook requirements**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-14T20:17:40Z
- **Completed:** 2026-03-14T20:19:07Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Created `scripts/install_native.py` stub with all 6 public function signatures (`find_uv`, `install_global_cli`, `write_plist`, `load_launchd_agent`, `install_hook`, `main`) plus module constants (`REPO_ROOT`, `HOOKS_DIR`, `PLIST_LABEL`, `PLIST_PATH`) — no subprocess calls at import time
- Created `tests/test_install_native.py` with 7 test stubs (6 active RED + 1 skipped) covering all 4.1-* requirements; pytest collects exactly 7 items with no import errors
- All 6 active tests fail with NotImplementedError as expected (RED phase) — Wave 1 implementation will fill the bodies

## Task Commits

Each task was committed atomically:

1. **Task 1: Create scripts/install_native.py stub** — `a72ccc1` (feat)
2. **Task 2: Create tests/test_install_native.py stubs** — `b5ea858` (test)

## Files Created/Modified

- `scripts/install_native.py` — Stub with 6 function signatures, all raising NotImplementedError; module constants at top; no side effects on import
- `tests/test_install_native.py` — 7 test stubs: test_global_cli_install, test_update_shell_if_needed (skipped), test_plist_keys, test_launchctl_bootstrap, test_idempotent_load, test_hook_install, test_hook_install_invalid_repo

## Decisions Made

- Used `patch.object(install_native, "PLIST_PATH", fake_plist_path)` in `test_plist_keys` so the test writes into `tmp_path` without touching `~/Library/LaunchAgents/` — keeps tests hermetic
- `test_update_shell_if_needed` marked `pytest.mark.skip(reason="implemented in Wave 1")` because the function signature for `install_global_cli` doesn't expose PATH-check branching at stub stage
- Documented `/usr/bin/git -C <path>` requirement in `install_hook` docstring per project CLAUDE.md (scm_breeze breaks bare `git` on this machine)

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Wave 0 complete: both stub files exist, pytest collects 7 tests, RED phase confirmed
- Wave 1 (plan 04.1-01) can implement `scripts/install_native.py` function bodies to turn all tests GREEN
- All 4.1-* requirements have corresponding test stubs ready for Wave 1 to satisfy

---
*Phase: 04.1-native-macos-ux-global-cli-launchd-watcher-autostart-git-hook-installer*
*Completed: 2026-03-14*
