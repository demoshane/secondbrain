---
phase: 19-mcp-server
plan: 04
type: execute
wave: 2
depends_on:
  - 19-02
  - 19-03
files_modified: []
autonomous: false
requirements:
  - MCP-01
  - MCP-02

must_haves:
  truths:
    - "Claude Desktop shows second-brain MCP tools in its sidebar after running sb-init"
    - "User can invoke sb_search from a Claude Desktop session and receive real results"
    - "Running sb-init produces a Claude Desktop config with the correct absolute binary path"
  artifacts:
    - path: "~/Library/Application Support/Claude/claude_desktop_config.json"
      provides: "MCP server registration for Claude Desktop"
      contains: "second-brain"
  key_links:
    - from: "Claude Desktop"
      to: "sb-mcp-server binary"
      via: "claude_desktop_config.json command field (absolute path)"
      pattern: "second-brain"
---

<objective>
Human verification: confirm Claude Desktop discovers and exposes the second-brain MCP tools after running sb-init.

Purpose: MCP-01 and MCP-02 cannot be verified automatically — requires a live Claude Desktop session.
Output: Human sign-off that MCP tools appear and are callable from Claude Desktop.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-mcp-server/19-02-SUMMARY.md
@.planning/phases/19-mcp-server/19-03-SUMMARY.md
</context>

<tasks>

<!-- files is empty by design: this task reads and verifies an external config file (claude_desktop_config.json)
     that is written by engine/init_brain.py (Plan 03). No source files are modified here. -->
<task type="auto">
  <name>Task 1: Run sb-init and verify config file written</name>
  <files><!-- no source files modified — verifies ~/Library/Application Support/Claude/claude_desktop_config.json written by Plan 03 --></files>
  <action>
    1. Run `sb-init` (or `uv run sb-init` if not installed globally) and check that it completes without error.
    2. Verify the config was written:
       `cat "~/Library/Application Support/Claude/claude_desktop_config.json"`
    3. Confirm the JSON contains:
       - `"mcpServers"` key
       - `"second-brain"` entry
       - `"command"` field with an absolute path to sb-mcp-server
    4. Confirm the binary path resolves: run the path from the config directly to check it starts the server (it should hang waiting for stdin — Ctrl+C to exit).
    5. If the config is missing or the binary path is wrong, diagnose and fix in engine/init_brain.py write_mcp_config() before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>python -c "
import json, pathlib, sys
cfg = pathlib.Path.home() / 'Library' / 'Application Support' / 'Claude' / 'claude_desktop_config.json'
if not cfg.exists():
    print('MISSING'); sys.exit(1)
data = json.loads(cfg.read_text())
entry = data.get('mcpServers', {}).get('second-brain', {})
cmd = entry.get('command', '')
if not cmd:
    print('NO COMMAND'); sys.exit(1)
import pathlib as p
if not p.Path(cmd).exists():
    print(f'BINARY NOT FOUND: {cmd}'); sys.exit(1)
print(f'OK: {cmd}')
"</automated>
  </verify>
  <done>claude_desktop_config.json exists with second-brain entry and a valid absolute binary path</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full MCP server with 12 tools: sb_search, sb_capture, sb_read, sb_edit, sb_recap, sb_digest, sb_connections, sb_actions, sb_actions_done, sb_files, sb_forget (two-step), sb_anonymize (two-step). sb-init auto-writes Claude Desktop config.
  </what-built>
  <how-to-verify>
    1. Restart Claude Desktop (required after config change).
    2. Open a new Claude Desktop conversation.
    3. Look for the hammer/tools icon in the chat input area — click it to expand available MCP tools.
    4. Verify "second-brain" appears as a connected MCP server with tools listed.
    5. Try invoking sb_search: type "Use the sb_search tool to search for 'meeting'" in the chat.
    6. Confirm results come back (even empty list is OK — means the tool ran without error).
    7. Optional: try sb_capture with a test note to verify write tools work end-to-end.

    If tools do NOT appear:
    - Check Claude Desktop logs: ~/Library/Logs/Claude/
    - Verify config path: ~/Library/Application Support/Claude/claude_desktop_config.json
    - Verify binary is executable: ls -la $(cat "~/Library/Application Support/Claude/claude_desktop_config.json" | python3 -c "import json,sys; print(json.load(sys.stdin)['mcpServers']['second-brain']['command'])")
    - Re-run sb-init and restart Claude Desktop again
  </how-to-verify>
  <resume-signal>Type "approved" if MCP tools appear and sb_search runs. Describe any issues if not working.</resume-signal>
</task>

</tasks>

<verification>
Human confirms:
- Claude Desktop shows second-brain MCP server
- sb_search tool is invocable
- No fatal errors on tool call
</verification>

<success_criteria>
Human verification approved. Phase 19 complete — all 10 MCP requirements (MCP-01 through MCP-10) delivered.
</success_criteria>

<output>
After completion, create `.planning/phases/19-mcp-server/19-04-SUMMARY.md`
</output>
