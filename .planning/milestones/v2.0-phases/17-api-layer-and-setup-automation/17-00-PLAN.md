---
phase: 17-api-layer-and-setup-automation
plan: "00"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_api.py
  - tests/test_init_brain.py
autonomous: true
requirements:
  - SETUP-01
  - SETUP-02
  - SETUP-03
  - SETUP-04

must_haves:
  truths:
    - "tests/test_api.py exists with failing stubs for all API behaviors"
    - "tests/test_init_brain.py extended with failing stubs for Drive detection and Ollama automation"
    - "All scaffold tests fail (ImportError or NotImplementedError) confirming RED state"
    - "Flask + Waitress + flask-cors added to pyproject.toml dependencies"
  artifacts:
    - path: "tests/test_api.py"
      provides: "API test scaffold — health, notes, search, read, actions stubs"
    - path: "tests/test_init_brain.py"
      provides: "Extended with TestDriveDetection, TestDriveExitOnMissing, TestOllamaEnsure, TestOllamaModelSizeWarning"
    - path: "pyproject.toml"
      provides: "flask, waitress, flask-cors in [project.dependencies]"
  key_links:
    - from: "tests/test_api.py"
      to: "engine.api"
      via: "from engine.api import app (will raise ImportError until 17-01)"
      pattern: "from engine\\.api import app"
    - from: "tests/test_init_brain.py"
      to: "engine.init_brain"
      via: "detect_drive_macos, ollama_ensure symbols (raise ImportError until 17-02)"
      pattern: "detect_drive_macos|ollama_ensure"
---

<objective>
Create Wave 0 RED scaffold: failing test stubs for the API layer and setup automation, plus add Flask/Waitress deps to pyproject.toml.

Purpose: Establish failing tests before implementation so GREEN state in plans 17-01 and 17-02 is meaningful.
Output: tests/test_api.py (new), tests/test_init_brain.py (extended), pyproject.toml (flask+waitress+flask-cors added).
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-api-layer-and-setup-automation/17-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Flask deps and create test/test_api.py scaffold</name>
  <files>pyproject.toml, tests/test_api.py</files>
  <behavior>
    - TestHealthEndpoint: GET /health returns 200, body contains {"status": "ok"}
    - TestNotesList: GET /notes returns 200 JSON with "notes" key (list)
    - TestSearch: POST /search with {"query": "test"} returns 200 JSON with "results" key
    - TestReadNote: GET /notes/&lt;encoded_path&gt; returns 200 JSON with "content" key (or 404 on missing)
    - TestActionItems: GET /actions returns 200 JSON with "actions" key (list)
    All tests import from engine.api — raise ImportError until 17-01 creates the module (RED state).
  </behavior>
  <action>
    1. In pyproject.toml, add to [project.dependencies]: "flask>=3.0", "waitress>=3.0", "flask-cors>=4.0"
       Also add to [project.scripts]: sb-api = "engine.api:main"

    2. Create tests/test_api.py. Use Flask test client pattern (app.test_client()).
       Import: `from engine.api import app`
       Add a shared fixture `client` using `@pytest.fixture` + `app.test_client()`.

       TestHealthEndpoint:
         - test_health_returns_200: assert response.status_code == 200
         - test_health_body: assert response.get_json()["status"] == "ok"

       TestNotesList:
         - test_notes_returns_200: GET /notes, assert 200
         - test_notes_has_notes_key: assert "notes" in response.get_json()

       TestSearch:
         - test_search_returns_200: POST /search json={"query":"hello"}, assert 200
         - test_search_has_results_key: assert "results" in response.get_json()

       TestReadNote:
         - test_read_missing_note_404: GET /notes/nonexistent%2Fpath.md, assert 404

       TestActionItems:
         - test_actions_returns_200: GET /actions, assert 200
         - test_actions_has_actions_key: assert "actions" in response.get_json()

    3. Run `uv run pytest tests/test_api.py -q` — all tests must FAIL (ImportError from engine.api).
       This confirms RED state. Do NOT fix the ImportError — that is Wave 1's job.
  </action>
  <verify>
    <automated>uv run pytest tests/test_api.py -q 2>&amp;1 | head -5; echo "EXIT:$?"</automated>
  </verify>
  <done>tests/test_api.py exists with 9+ test stubs all failing with ImportError or similar. pyproject.toml includes flask, waitress, flask-cors, and sb-api entry point.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend tests/test_init_brain.py with Drive and Ollama stubs</name>
  <files>tests/test_init_brain.py</files>
  <behavior>
    - TestDriveDetection: detect_drive_macos() returns a Path when a GoogleDrive-* dir with My Drive subdirectory exists (tmp_path fixture); returns None when directory absent
    - TestDriveDetectionWindows: detect_drive_windows() returns a Path when GFS/My Drive exists (tmp_path fixture); returns None when absent
    - TestDriveExitOnMissing: assert_drive_or_exit() calls sys.exit(1) when no Drive found; accepts a base_path parameter for testability
    - TestOllamaEnsure: ollama_ensure() returns False and prints error message when shutil.which("ollama") returns None and no brew/winget; no subprocess is called
    - TestOllamaModelSizeWarning: ollama_model_size_warning() prints warning containing "800 MB" or "nomic-embed-text" before pull when model not in ollama list
    Import new symbols from engine.init_brain — these will raise ImportError/AttributeError until 17-02 (RED state).
  </behavior>
  <action>
    Read the existing tests/test_init_brain.py first to understand structure and append new classes at the bottom.

    Append to tests/test_init_brain.py:

    class TestDriveDetection:
      - test_macos_found: monkeypatch Path.home() to tmp_path; create
        tmp_path / "Library/CloudStorage/GoogleDrive-test@example.com/My Drive"
        assert detect_drive_macos() is not None
      - test_macos_not_found: empty CloudStorage dir → returns None
      - test_windows_found: create tmp_path / "GFS" / "My Drive"; patch Path.home
        assert detect_drive_windows() is not None
      - test_windows_not_found: empty home → returns None

    class TestDriveExitOnMissing:
      - test_exits_nonzero: monkeypatch detect_drive_path to return None;
        use pytest.raises(SystemExit) as exc; assert exc.value.code != 0
      - test_prints_error_message: capsys, assert "Google Drive not found" in stderr output

    class TestOllamaEnsure:
      - test_no_binary_no_brew_returns_false: monkeypatch shutil.which to return None;
        result = ollama_ensure(); assert result is False
      - test_prints_download_url: capsys; assert "ollama.com/download" in output

    class TestOllamaModelSizeWarning:
      - test_warning_printed: monkeypatch ollama.list to return empty models list;
        capsys; call ollama_model_size_warning(); assert "800 MB" in output or "nomic-embed-text" in output

    Run `uv run pytest tests/test_init_brain.py -q` — new classes must fail (ImportError for new symbols).
  </action>
  <verify>
    <automated>uv run pytest tests/test_init_brain.py -q 2>&amp;1 | tail -5; echo "EXIT:$?"</automated>
  </verify>
  <done>tests/test_init_brain.py has 4 new test classes with 9+ stubs, all failing. Existing test_init_brain tests still pass (no regression).</done>
</task>

</tasks>

<verification>
uv run pytest tests/test_api.py tests/test_init_brain.py -q
Expected: existing test_init_brain tests PASS; new TestDrive*/TestOllama* classes FAIL; all test_api.py tests FAIL.
</verification>

<success_criteria>
- pyproject.toml has flask, waitress, flask-cors in dependencies and sb-api entry point
- tests/test_api.py exists and fails (engine.api not yet created)
- tests/test_init_brain.py extended with Drive and Ollama stubs (fail on missing symbols)
- No existing tests broken
</success_criteria>

<output>
After completion, create `.planning/phases/17-api-layer-and-setup-automation/17-00-SUMMARY.md`
</output>
