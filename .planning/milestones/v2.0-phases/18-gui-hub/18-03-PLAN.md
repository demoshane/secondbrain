---
phase: 18-gui-hub
plan: "03"
type: execute
wave: 3
depends_on:
  - "18-02"
files_modified: []
autonomous: false
requirements:
  - GUI-01
  - GUI-02
  - GUI-03
  - GUI-04
  - GUI-05
  - GUI-06
  - GUI-07
  - GUI-08
  - GUI-09
  - GUI-10
  - GUI-11

must_haves:
  truths:
    - "sb-gui opens a native desktop window on macOS without error"
    - "Sidebar shows notes grouped by type"
    - "Clicking a note renders Markdown content in center panel"
    - "Keyword and semantic search update the sidebar results"
    - "Edit mode activates EasyMDE editor; Ctrl+S saves the note atomically"
    - "Create new note modal works end-to-end; new file appears in sidebar"
    - "Right panel shows backlinks and related notes for the open note"
    - "Action items panel lists open items; checking one marks it done"
    - "Intelligence panel shows recap and stale nudges"
    - "Open in Editor button opens the note in the system default editor"
    - "Binary files are browsable under Files (if files/ subdirectory exists in brain)"
  artifacts:
    - path: "engine/gui.py"
      provides: "Running sb-gui entry point"
      contains: "webview.start"
    - path: "engine/gui/static/app.js"
      provides: "Full SPA frontend"
      contains: "loadNotes"
  key_links:
    - from: "sb-gui command"
      to: "native window on screen"
      via: "pywebview WKWebView/WebView2"
      pattern: "manual"
---

<objective>
Human verification checkpoint: user runs sb-gui and manually confirms all 11 GUI requirements work end-to-end in the live desktop window.

Purpose: GUI rendering, interactivity, and OS integration cannot be headlessly automated. This checkpoint surfaces issues before phase sign-off so they can be fixed in the same phase.
Output: Approved or issues list for gap closure.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-gui-hub/18-RESEARCH.md
@.planning/phases/18-gui-hub/18-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — run full test suite and confirm sb-gui is registered</name>
  <files></files>
  <action>
    Run the full test suite and verify the entry point is installed:

    ```bash
    uv run pytest tests/ -q --ignore=tests/test_gui_smoke.py
    uv run sb-gui --help 2>&1 || uv run python -c "import engine.gui; print('entry point importable')"
    ```

    If any tests are RED, fix regressions before proceeding to the human verification step.
    If sb-gui is not registered, run `uv pip install -e .` to reinstall entry points.
  </action>
  <verify>
    <automated>uv run pytest tests/ -q --ignore=tests/test_gui_smoke.py 2>&1 | tail -3</automated>
  </verify>
  <done>Full test suite GREEN; sb-gui entry point importable</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full Phase 18 GUI Hub:
    - sb-gui entry point opens native pywebview window
    - Three-panel layout: sidebar (notes by type) | center (viewer + EasyMDE editor) | right (backlinks, actions, intelligence)
    - All 7 new API endpoints: PUT /notes, POST /notes, GET /notes/meta, GET /files, POST /files/move, POST /actions/done, GET /intelligence
    - EasyMDE vendored (no CDN required at runtime)
    - Port-reuse check prevents conflict with standalone sb-api
    - open_in_editor() exposed via pywebview bridge (system default editor)
  </what-built>
  <how-to-verify>
    Run the GUI:
    ```
    uv run sb-gui
    ```

    Work through each requirement in order:

    1. **GUI-01** — Window opens without error. Sidebar shows notes grouped by folder/type. (If window is blank or shows error, check terminal for stderr output.)

    2. **GUI-02** — Browse the sidebar. Confirm notes are grouped under type labels (meeting, project, person, etc.). Click a note — Markdown renders in the center panel.

    3. **GUI-03** — Type a keyword in the search bar. Confirm sidebar updates with matching results. Switch mode to "Semantic" and repeat — confirm different results based on meaning.

    4. **GUI-04** — Open a note, click Edit. Confirm EasyMDE editor activates with toolbar. Modify text, press Cmd+S (macOS) or Ctrl+S. Confirm note is saved (re-open to verify content).

    5. **GUI-05** — Open a note that links to another note. Confirm right panel shows backlinks. Confirm related notes appear. Click a backlink — confirms navigation works.

    6. **GUI-06** — Click "+ New Note". Fill in title, type, body. Click Create. Confirm note appears in sidebar and is openable.

    7. **GUI-07** — Confirm no error when the GUI starts (files panel in right area should not crash even if ~/SecondBrain/files/ doesn't exist).

    8. **GUI-08** — If files/ subdirectory exists in brain: confirm GET /files returns a non-error response. (Full file-move UI is API-only — verify via: `curl -s http://127.0.0.1:37491/files | python3 -m json.tool`)

    9. **GUI-09** — Scroll to Action Items in right panel. Confirm open actions are listed. Check one off — confirm it disappears after ~1 second.

    10. **GUI-10** — Scroll to Intelligence section in right panel. Confirm recap text and stale nudges appear (or "No recent recap" / "No stale notes" if brain is fresh).

    11. **GUI-11** — Open a note, click "Open in Editor". Confirm the note opens in the system default editor (Typora, TextEdit, VS Code, etc.).

    Close the window. Confirm process exits cleanly (no orphaned port).
  </how-to-verify>
  <resume-signal>Type "approved" if all 11 items pass, or describe which items failed (e.g., "GUI-04 editor did not activate") so gap closure plans can be created.</resume-signal>
</task>

</tasks>

<verification>
Human verification — see checkpoint above.
All 11 GUI requirements confirmed working in live desktop window.
</verification>

<success_criteria>
- All 11 GUI requirements verified as working by the user in the live window
- No uncaught exceptions or blank window on startup
- Process exits cleanly on window close
- User has typed "approved"
</success_criteria>

<output>
After completion, create `.planning/phases/18-gui-hub/18-03-SUMMARY.md`
</output>
