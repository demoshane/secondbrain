---
phase: 14-embedding-infrastructure
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - tests/test_embeddings.py
autonomous: true
requirements: [EMBED-01, EMBED-02, EMBED-03, EMBED-04]

must_haves:
  truths:
    - "`sentence-transformers` and `sqlite-vec` are installed and importable"
    - "All six embedding test functions exist in `tests/test_embeddings.py` and fail (RED state)"
    - "Running `pytest tests/test_embeddings.py -q` completes in < 10 seconds (mocked model, no download)"
  artifacts:
    - path: "tests/test_embeddings.py"
      provides: "Test scaffold covering EMBED-01 through EMBED-04"
      min_lines: 80
    - path: "pyproject.toml"
      provides: "sentence-transformers and sqlite-vec in dependencies"
      contains: "sentence-transformers"
  key_links:
    - from: "tests/test_embeddings.py"
      to: "engine.embeddings"
      via: "import (module created in Plan 02)"
      pattern: "from engine.embeddings import"
    - from: "tests/test_embeddings.py"
      to: "engine.embeddings._get_model"
      via: "unittest.mock.patch"
      pattern: "patch.*engine.embeddings._get_model"
---

<objective>
Add `sentence-transformers` and `sqlite-vec` as project dependencies and create the test scaffold for all Phase 14 embedding behaviors in RED state.

Purpose: Wave 0 establishes the test contract before implementation. All subsequent plans implement against these tests. Mocking the model prevents the 90MB download from running in CI.
Output: Updated `pyproject.toml`, `tests/test_embeddings.py` with 6 failing tests.
</objective>

<execution_context>
@/Users/tuomasleppanen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuomasleppanen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-embedding-infrastructure/14-CONTEXT.md
@.planning/phases/14-embedding-infrastructure/14-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add embedding dependencies to pyproject.toml</name>
  <files>pyproject.toml, uv.lock</files>
  <action>
Run `uv add sentence-transformers sqlite-vec` from the project root. This adds both packages to `pyproject.toml` dependencies and resolves `uv.lock`.

After the command completes, verify both packages appear in `[project]` dependencies in `pyproject.toml`.

Do NOT manually edit pyproject.toml — use `uv add` so lock file stays consistent.
  </action>
  <verify>
    <automated>python -c "import sentence_transformers; import sqlite_vec; print('ok')"</automated>
  </verify>
  <done>Both `sentence-transformers` and `sqlite-vec` import without error in the project's Python environment.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing test scaffold for EMBED-01 through EMBED-04</name>
  <files>tests/test_embeddings.py</files>
  <behavior>
    - test_reindex_generates_embeddings: after reindex, `note_embeddings` table has one row per indexed note, embedding is non-null BLOB
    - test_reindex_full_flag: `reindex_brain(..., full=True)` re-embeds all notes regardless of whether hash changed
    - test_embed_no_network_call: calling `embed_texts(["hello"], provider="sentence-transformers")` never makes an outbound HTTP request; mock `_get_model` to confirm
    - test_reindex_incremental_skips_unchanged: second `reindex_brain()` call on same notes does NOT update `updated_at` for unchanged rows
    - test_reindex_incremental_reembeds_changed: after modifying a note's body, `reindex_brain()` updates the row's `content_hash` and `updated_at`
    - test_forget_cascades_to_embeddings: after `forget_person()`, rows for deleted note paths are absent from `note_embeddings`
  </behavior>
  <action>
Create `tests/test_embeddings.py` with all six test functions. All tests must FAIL at this stage since `engine/embeddings.py` does not exist yet.

Implementation guidelines:
- Import: `from unittest.mock import patch, MagicMock` and `import numpy as np`
- Mock `engine.embeddings._get_model` to return a mock whose `.encode(texts, **kwargs)` returns `np.zeros((len(texts), 384), dtype=np.float32)` — avoids 90MB model download
- Use an in-memory SQLite DB fixture (`sqlite3.connect(":memory:")`) — call `init_schema(conn)` to create tables before each test
- For tests that call `reindex_brain()`, pass `conn=` parameter to use the in-memory DB
- For `test_forget_cascades_to_embeddings`: create a person note, run reindex, run `forget_person()`, assert `note_embeddings` has no rows for the deleted paths
- For `test_embed_no_network_call`: assert that `requests` or `httpx` are NOT called (use `patch("requests.get")` asserting it is not called, or simply verify mock `.encode()` was called instead of a real encode)
- For `test_reindex_incremental_skips_unchanged`: run reindex twice; capture `updated_at` from first run; assert second run's `updated_at` is identical for unchanged notes
- All tests import `from engine.embeddings import embed_texts` — this import will fail until Plan 02; that is intentional (RED state)

Fixture pattern:
```python
import sqlite3
import numpy as np
from unittest.mock import patch, MagicMock
import pytest

from engine.db import init_schema
from engine.reindex import reindex_brain
from engine.forget import forget_person
from engine.paths import BRAIN_ROOT


@pytest.fixture
def mem_conn():
    conn = sqlite3.connect(":memory:")
    init_schema(conn)
    yield conn
    conn.close()


def _mock_model():
    m = MagicMock()
    m.encode.side_effect = lambda texts, **kw: np.zeros((len(texts), 384), dtype=np.float32)
    return m
```

Write all 6 tests using this fixture. Keep each test focused on one behavior.
  </action>
  <verify>
    <automated>pytest tests/test_embeddings.py -q 2>&1 | grep -E "ERROR|ImportError|ModuleNotFoundError|FAILED" | head -5</automated>
  </verify>
  <done>
All 6 tests exist in `tests/test_embeddings.py`. Running `pytest tests/test_embeddings.py -q` shows errors/failures (not passes) — RED state confirmed. The errors are `ImportError` for `engine.embeddings` (not syntax errors in the test file itself).
  </done>
</task>

</tasks>

<verification>
1. `python -c "import sentence_transformers; import sqlite_vec; print('ok')"` prints `ok`
2. `pytest tests/test_embeddings.py -q` shows 6 errors/failures — all from missing `engine.embeddings` module
3. `grep "sentence-transformers" pyproject.toml` returns a match
4. `grep "sqlite-vec" pyproject.toml` returns a match
</verification>

<success_criteria>
- Both new dependencies installable and importable
- 6 test functions present in `tests/test_embeddings.py`
- All 6 tests fail with `ImportError` (not syntax errors) — true RED state
- Existing test suite still passes: `pytest tests/ -q --ignore=tests/test_embeddings.py`
</success_criteria>

<output>
After completion, create `.planning/phases/14-embedding-infrastructure/14-01-SUMMARY.md`
</output>
