# Copy to ~/.config/second-brain/.env.host
# NEVER commit the real .env.host file
#
# Add any environment variables needed inside the devcontainer.
# Claude Code auth is handled automatically via Keychain extraction
# (see .devcontainer/extract-claude-auth.sh).
#
# Example:
# SOME_API_KEY=your-key-here
